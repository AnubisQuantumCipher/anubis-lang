fn main() {
    let secret: u32 = symbolic("secret");
    assume(secret > 10);
    assume(secret < 10000);
    let derived = secret + 42;
    let masked = derived & 255;
    let result = secret * 3 + masked;
    assert(result > 30);
}