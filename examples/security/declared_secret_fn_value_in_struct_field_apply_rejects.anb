// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// CORPUS-1 2026-07-27. Defect A's container-apply form, STRUCT FIELD face: a secret-returning
// function stored as a struct field (not a closure literal — the field holds the named function
// itself), applied via `b.f()`. The existing field-closure resolution
// (`closure_in_struct_field_secret_egress_rejects`) generalizes to a named callable stored the same
// way. TODAY: already REJECTS correctly (coverage guard, verified by direct run).
fn key() -> secret<i64> { return 42; }
struct Box { f: u64 }
fn main() { let b = Box { f: key }; print(b.f()); }
