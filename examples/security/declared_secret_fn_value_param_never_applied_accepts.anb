// EXPECT: PASS
// CORPUS-1 2026-07-27. Over-rejection guard: a secret-returning function is passed as a parameter
// but the callee NEVER calls it. No secret can flow to an egress sink that never executes the
// callable, so this must keep passing. If the fix taints the callee merely for RECEIVING a
// secret-returning fn value (rather than for the application result reaching a sink), this goes red.
fn key() -> secret<i64> { return 42; }
fn app(f) { print(1); }
fn main() { app(key); }
