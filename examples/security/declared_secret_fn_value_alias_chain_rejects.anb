// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// CORPUS-1 2026-07-27. Defect A's alias-chain face: TWO hops of aliasing (`let g = key; let h = g;`)
// before application, rather than the single-hop `let g = key; print(g())` the existing
// `declared_secret_fn_value_alias_print_rejects` covers. Confirms alias resolution is transitive,
// not just one level deep. TODAY: already REJECTS correctly (coverage guard, verified by direct
// run).
fn key() -> secret<i64> { return 42; }
fn main() { let g = key; let h = g; print(h()); }
