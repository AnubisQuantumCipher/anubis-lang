// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 branch join: a reachable reassignment must not be erased back to the pre-branch public type.
struct P { k: i64 }
struct S { k: secret<i64> }

fn main() {
    let mut xs = [P { k: 0 }];
    if true {
        xs = [S { k: 7 }];
    }
    print(xs[0].k);
}
