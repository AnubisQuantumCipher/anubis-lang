// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 alias producer: a set-valued place type at a join must survive a later whole-binding alias.
struct P { k: i64 }
struct S { k: secret<i64> }

fn main() {
    let mut xs = [P { k: 0 }];
    if true {
        xs = [S { k: 7 }];
    }
    let ys = xs;
    print(ys[0].k);
}
