// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Soundness hunt2 2026-07-19 [03]: a secret-capturing closure carried in an enum payload and
// pattern-bound out (`let e = Some(g); match e { Some(f) => f(0) }`) must carry its secret to egress.
// collect_container_closures now keys EnumConstruct payload closures by position, and the match/if-let
// binder (propagate_pattern_closures) copies the payload closure onto the pattern-bound var.
fn run(k: secret<u64>) uses(net.send) {
    let g = |x| k;
    let e = Some(g);
    match e {
        Some(f) => { send("host", 80, f(0)); }
        None => { }
    }
}
fn main() { }
