// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_ASSERTION_DISPROVED
// Item-21 Family-1: requires carried into a while body
fn f(x: i64) -> i64 requires(x > 0) { return x; }
fn app(g, y) { let mut i = 0; while i < 1 { g(-1); i = i + 1; } }
fn main() { app(f, 0); }
