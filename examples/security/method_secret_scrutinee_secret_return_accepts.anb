// EXPECT: PASS
// Walker-parity H17 over-rejection guard. Same method-secret scrutinee shape, but the
// function returns `secret<i64>` — the documented escape hatch for secret PC
// (implicit_flow_return_secret_accepts.anb). Must stay accepted after the H17 fix.
// TODAY: check PASSES (and must keep passing after the H17 fix).
struct V { k: u32 }
impl V {
    fn bal(self) -> secret<i64> {
        return 1;
    }
}
fn bit(v: V) -> secret<i64> {
    match v.bal() {
        1 => { return 1; }
        _ => { return 0; }
    }
}
fn main() {
    let v = V { k: 1 };
    let _x = bit(v);
}
