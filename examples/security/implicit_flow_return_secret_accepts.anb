// EXPECT: PASS
// Escape hatch: function return type is secret-labelled.
fn bit(bal: secret<i64>) -> secret<i64> {
    if bal > 0 {
        return 1;
    }
    return 0;
}
fn main() {
    let x = bit(1);
}
