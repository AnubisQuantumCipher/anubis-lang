// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_TO_PUBLIC
// Secret written through an index of a public-typed list binding.
fn main() {
    let bal: secret<i64> = 1;
    let mut a: list<i64> = [0, 0];
    a[0] = bal;
}
