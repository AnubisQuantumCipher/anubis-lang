// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// FABLE5 2026-07-26. `compute_secret_fns` derived its summary PURELY FROM THE BODY, so a function
// DECLARED `-> secret<i64>` whose body returns a bare literal computed "returns nothing secret" and
// the declaration was decorative: `print(key())` and `let s = key(); print(s)` both CHECKED CLEAN
// and printed the value at runtime. Every rejection the corpus produced came from an annotation the
// user wrote at a `let`/param site, never from the signature. FIX: `collect_declared_ret_qualified`
// seeds `secret_fns`/`method_secret_fns` from the declaration before the body fixpoint.
// Discriminator: `declared_secret_return_declassified_accepts` is the same program with an explicit
// release and must still PASS. The METHOD face is `method_secret_return_untyped_print_rejects`.
fn key() -> secret<i64> {
    return 42;
}
fn main() {
    let s = key();
    print(s);
}
