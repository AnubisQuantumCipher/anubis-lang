// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 container mutation: push must widen the tracked element alternatives before readback.
struct P { k: i64 }
struct S { k: secret<i64> }

fn main() {
    let xs = [P { k: 0 }];
    push(xs, S { k: 7 });
    print(xs[1].k);
}
