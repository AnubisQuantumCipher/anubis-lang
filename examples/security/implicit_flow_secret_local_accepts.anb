// EXPECT: PASS
// Escape hatch: declare the assigned local secret — label is preserved.
fn main() {
    let bal: secret<i64> = 250000;
    let mut b0: secret<i64> = 0;
    if bal > 200000 {
        b0 = 1;
    }
}
