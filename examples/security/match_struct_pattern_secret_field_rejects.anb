// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Same binder gap reached through `match` rather than `let`.
struct V { k: secret<i64> }
fn main() {
    let v = V { k: 42 };
    match v {
        V { k } => { print(k); }
    }
}
