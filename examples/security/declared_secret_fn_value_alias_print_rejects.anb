// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// GROK-SEKHMET round2 (2026-07-26). FABLE5's collect_declared_ret_qualified seeds
// secret_fns so direct `print(key())` rejects for `fn key() -> secret<i64> { return 42; }`.
// The seed does NOT reach Call sites where the callee is a function-value alias:
// `let g = key; print(g())` looks up raw name `g` in secret_fns (mod.rs expr_secret_source_m
// Call arm) and misses. Discriminator: direct print(key()) rejects; this form accepts and
// runtime prints 42. pre=0 on post-declared-ret binary.
fn key() -> secret<i64> {
    return 42;
}
fn main() {
    let g = key;
    print(g());
}
