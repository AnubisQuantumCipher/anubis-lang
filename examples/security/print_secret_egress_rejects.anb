// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Operator security fix 2026-07-20: print() is an egress sink — a secret printed to stdout is
// exfiltration (the binary-extraction attack prints one secret bit per line).
fn main() uses(io.print) { let k = secret_source("k"); print(k); }
