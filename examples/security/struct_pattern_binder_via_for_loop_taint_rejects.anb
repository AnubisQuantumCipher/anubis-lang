// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TAINTED_SINK_WITHOUT_DECLASSIFY
// CORPUS-1 2026-07-27. Integrity dual of `struct_pattern_binder_via_for_loop_rejects`: a
// `tainted<i64>` field destructured from a for-loop-bound struct inside the loop body, reaching a
// `shell` sink. TODAY: already REJECTS correctly (coverage guard, verified by direct run;
// check-side only).
struct V { k: tainted<i64> }
fn main() uses(shell) {
    let vs = [V { k: 42 }];
    for item in vs {
        let V { k } = item;
        shell(k);
    }
}
