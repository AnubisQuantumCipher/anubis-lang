// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// GROK-SEKHMET round 9. D4 through a `while let` binder. The enforcing arm inserted its binders
// INLINE instead of calling `seed_effect_pattern`, so it missed everything that shared seeder had
// since learned — including the declared enum-payload qualifier — while the `if let` and `match`
// twins correctly rejected.
// A second binder-seeding path is exactly how these lanes drift apart; there is now one.
enum E { Box(secret<i64>), Empty }
fn main() {
    let mut e = E::Box(42);
    while let E::Box(x) = e {
        print(x);
        e = E::Empty;
    }
}
