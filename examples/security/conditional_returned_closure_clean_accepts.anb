// EXPECT: PASS
// Guard: a helper returning only non-secret closures must still compile.
fn mk(c: i64) { if c > 0 { return |x| { 1 }; } return |x| { 0 }; }
fn main() uses(net.send) { let g = mk(1); send("host", 80, g(0)); }
