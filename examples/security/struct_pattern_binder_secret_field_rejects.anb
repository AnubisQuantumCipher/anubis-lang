// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// A struct PATTERN binds a value out of a declared `secret` field. The direct read `print(v.k)`
// was already rejected; the destructuring form laundered it because the pattern walker recursed
// without ever consulting the field types.
struct V { k: secret<i64> }
fn main() {
    let v = V { k: 42 };
    let V { k } = v;
    print(k);
}
