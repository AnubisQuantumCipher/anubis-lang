// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 value-block join: a branch-updated exact array alternative must survive to a later sink.
struct P { k: i64 }
struct S { k: secret<i64> }

fn main() {
    let _v = if true {
        let mut xs = [P { k: 0 }];
        if true {
            xs = [S { k: 7 }];
        }
        print(xs[0].k);
        0
    } else {
        0
    };
}
