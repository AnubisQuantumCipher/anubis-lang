// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TAINTED_SINK_WITHOUT_DECLASSIFY
// W2.1 value-block integrity: effect enforcement must infer unannotated array place types.
struct S { k: tainted<i64> }

fn main() uses(shell) {
    let _v = if true {
        let xs = [S { k: 7 }];
        shell(xs[0].k);
        0
    } else {
        0
    };
}
