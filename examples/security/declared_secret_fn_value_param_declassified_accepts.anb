// EXPECT: PASS
// CORPUS-1 2026-07-27. Over-rejection guard: a secret-returning function passed as a parameter,
// applied, and its result explicitly `declassify()`'d before the sink. Declassify is the sanctioned
// release path; the fix for `declared_secret_fn_value_as_param_rejects` must not make the qualifier
// un-releasable once the callable crosses a parameter boundary.
fn key() -> secret<i64> { return 42; }
fn app(f) { print(declassify(f(), "release-policy", "audited")); }
fn main() { app(key); }
