// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// #71 at TWO hops: outer -> inner -> method. The joint fixpoint must close over chains, not just
// a single level.
struct V { k: u32 }
impl V {
    fn key(self) { return secret_source("api_key"); }
}
fn inner(v) { return v.key(); }
fn outer(v) { return inner(v); }
fn main() uses(net.send) {
    let v = V { k: 1 };
    send("host", 80, outer(v));
}
