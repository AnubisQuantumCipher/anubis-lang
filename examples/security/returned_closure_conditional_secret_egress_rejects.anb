// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// [05]: a closure returned from inside a conditional (the function's sole return) is now detected via
// collect_returns_in_stmt (tail_values yields the if's fallback, not the return).
fn make(k: secret<u64>) { if true { return |x| k; } }
fn run(k: secret<u64>) uses(net.send) { let g = make(k); send("host", 80, g(0)); }
fn main() { }
