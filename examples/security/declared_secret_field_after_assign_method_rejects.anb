// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
struct S { k: secret<i64> }
struct V { id: u32 }
impl V {
    fn make(self) -> S { return S { k: 42 }; }
}
fn main() {
    let v = V { id: 1 };
    let mut s = S { k: 0 };
    s = v.make();
    print(s.k);
}
