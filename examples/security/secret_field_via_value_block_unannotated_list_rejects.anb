// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 value-block confidentiality: effect enforcement must infer unannotated array place types.
struct S { k: secret<i64> }

fn main() {
    let _v = if true {
        let xs = [S { k: 7 }];
        print(xs[0].k);
        0
    } else {
        0
    };
}
