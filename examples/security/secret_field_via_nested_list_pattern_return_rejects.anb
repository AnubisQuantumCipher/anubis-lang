// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 nested pattern projection: every numeric path segment must peel one list layer.
struct S { k: secret<i64> }

fn leak() {
    let xs = [[S { k: 7 }]];
    match xs {
        [[item]] => { return item.k; }
        _ => { return 0; }
    }
}

fn main() { print(leak()); }
