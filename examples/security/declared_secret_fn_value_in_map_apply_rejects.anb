// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// CORPUS-1 2026-07-27. Defect A's container-apply form, MAP face: a secret-returning function
// stored as a map value, applied via `m["k"]()`. The sibling LIST form
// (`declared_secret_fn_value_in_list_apply_rejects`) already rejects; this confirms the map-indexed
// apply path resolves the same way. TODAY: already REJECTS correctly (this fixture is a coverage
// guard, not an open-defect spec — verified by direct run before adding to the corpus).
fn key() -> secret<i64> { return 42; }
fn main() { let m = { "k": key }; print(m["k"]()); }
