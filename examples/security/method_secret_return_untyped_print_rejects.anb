// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Walker-parity H14 (GROK-SEKHMET 2026-07-26). A method declared `-> secret<i64>` returns a
// secret-labelled value, but an *untyped* `let s = v.key()` does not seed secret on `s`, so
// `print(s)` launders confidentiality past Safe mode. Twin control: a `secret<i64>` local
// print is already rejected (see print_secret_egress_rejects / typed binding of the same
// method return).
// TODAY (pre-fix binary): check PASSES and `anubis run` prints the secret — known false
// accept; fixture is red until OPUS5 lands method-return secret tracking for untyped lets.
struct V { k: u32 }
impl V {
    fn key(self) -> secret<i64> {
        return 42;
    }
}
fn main() {
    let v = V { k: 1 };
    let s = v.key();
    print(s);
}
