// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_ASSERTION_DISPROVED
// Item-21 Family-1: requires carried into a loop body
fn f(x: i64) -> i64 requires(x > 0) { return x; }
fn app(g, y) { loop { g(-1); break; } }
fn main() { app(f, 0); }
