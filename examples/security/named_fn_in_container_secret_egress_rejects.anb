// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INTERPROC_EXFILTRATION
// Soundness hunt2 2026-07-19: a NAMED egress function stored in a container (`Box { f: leak }`) and
// applied via `b.f(k)` must consult leak's interproc param_egress summary. collect_container_closures
// now records a bare named-fn ref, and the CallExpr arm resolves it (flatten_access_path) and applies
// param_egress/param_sinks to the call args. The direct `leak(k)` was already rejected.
struct Box { f: u64 }
fn leak(h: string, p: u32, x: u64) uses(net.send) { send(h, p, x); }
fn run(k: secret<u64>) uses(net.send) {
    let b = Box { f: leak };
    b.f("host", 80, k);
}
fn main() { }
