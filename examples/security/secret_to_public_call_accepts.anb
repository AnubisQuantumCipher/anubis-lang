// EXPECT: PASS
// Secret formal or declassify at call site is the escape.
fn take_secret(x: secret<i64>) {
}
fn take_public(x: i64) {
}
fn main() {
    let bal: secret<i64> = 1;
    take_secret(bal);
    take_public(declassify(bal, "policy", "reason"));
}
