// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 match join: every reachable arm's exact array place type must survive the merge.
struct P { k: i64 }
struct S { k: secret<i64> }

fn main() {
    let mut xs = [P { k: 0 }];
    match true { true => { xs = [S { k: 7 }]; } false => { xs = [P { k: 1 }]; } }
    print(xs[0].k);
}
