// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_TO_PUBLIC
// Secret argument into a public formal is label laundering at the call boundary.
fn take(x: i64) {
}
fn main() {
    let bal: secret<i64> = 1;
    take(bal);
}
