// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Soundness (hunt wf_5b2a1bcc): a higher-order builtin (each/map/filter/...) binds the collection ELEMENT
// to the closure param, but the #65 descent inserted the param UNLABELLED, laundering the element secret.
// Fixed: the param carries the union of the non-closure args (element) labels.
fn main() uses(net.send) { each([secret_source("api_key")], |x| send("h", 80, x)); }
