// EXPECT: PASS
// Over-rejection guard for `declared_secret_return_print_rejects`. Seeding the summary from a
// DECLARED `-> secret<T>` must not make the qualifier un-releasable: an explicit `declassify` with a
// policy and a reason is the sanctioned escape and still has to pass. If this fixture ever goes red,
// the seed has become a wall rather than a guardrail.
fn key() -> secret<i64> {
    return 42;
}
fn main() {
    let s = declassify(key(), "release-policy", "audited test vector");
    print(s);
}
