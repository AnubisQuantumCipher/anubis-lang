// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Independent-review RED: the indexed element is a generic struct instantiation, and the secret
// field is one hop deeper. place_struct_type must strip Box<i64> to Box while resolving `.inner`.
struct Inner { k: secret<i64> }
struct Box<T> { inner: Inner, pad: T }

fn main() {
    let xs: list<Box<i64>> = [Box { inner: Inner { k: 7 }, pad: 0 }];
    print(xs[0].inner.k);
}
