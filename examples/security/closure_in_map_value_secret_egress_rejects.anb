// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// hunt2: a closure stored as a MAP value, applied via m["key"](0), resolves to path "key".
fn run(k: secret<u64>) uses(net.send) {
    let g = |x| k;
    let m = { "key": g };
    send("host", 80, m["key"](0));
}
fn main() { }
