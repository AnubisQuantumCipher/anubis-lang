// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 producer join: an element variable's nominal alternatives must survive into array inference.
struct P { k: i64 }
struct S { k: secret<i64> }

fn main() {
    let mut item = P { k: 0 };
    if true {
        item = S { k: 7 };
    }
    let xs = [item];
    print(xs[0].k);
}
