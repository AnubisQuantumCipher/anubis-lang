// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_TO_PUBLIC
// Secret written through a field of a public-typed struct binding.
struct Box { n: i64 }
fn main() {
    let bal: secret<i64> = 1;
    let mut b: Box = Box { n: 0 };
    b.n = bal;
}
