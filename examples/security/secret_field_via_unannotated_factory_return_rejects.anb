// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Item-21 Family-3 (D10): a `secret` struct field read off the result of an UNANNOTATED factory
// (`fn mk() { return S {..}; } ... let a = mk(); a.k`) leaked because the factory's return type was
// the empty string, so the declared field qualifier had nothing to key on. `fn_ret_types` now
// recovers the return struct type for an unannotated fn whose every return is the same struct literal.
struct S { k: secret<i64> }
fn mk() { return S { k: 42 }; }
fn main() { let a = mk(); print(a.k); }
