// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 value-block while-let parity: pattern binders carry exact element places.
struct S { k: secret<i64> }

fn main() {
    let _v = if true {
        let xs = [S { k: 7 }];
        while let [item] = xs {
            print(item.k);
            break;
        }
        0
    } else { 0 };
}
