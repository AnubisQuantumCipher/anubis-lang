// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Completion Blueprint Phase 4 (2026-08-15), CLAIMS item 21 row 6: a secret-returning function
// reference stored into a struct field by PLACE-ASSIGNMENT (after construction), read back through
// a `let`-bound field projection, and applied at a public sink. Pre-fix `check` ACCEPTED and `run`
// printed the secret, because `fn_alias_of_d` (the single-candidate spine `expr_source`'s Call arm
// consults) had no `FieldAccess`/`Index` arm — so the correctly-tracked `field_fn_identities` never
// reached the sink check. The literal-construction twin `Box { f: key }` already rejected.
struct Box { f: u64 }
fn key() -> secret<i64> { return 42; }
fn main() {
    let b = Box { f: 0 };
    b.f = key;
    let g = b.f;
    print(g());
}
