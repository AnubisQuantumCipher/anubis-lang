// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 collection-for projection: the loop element inherits an unannotated list's exact place.
struct S { k: secret<i64> }

fn main() {
    let xs = [S { k: 7 }];
    for item in xs {
        print(item.k);
    }
}
