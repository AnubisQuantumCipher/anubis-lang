// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 reduced confidentiality summary: a whole-binding array update must refresh exact place state.
struct P { k: i64 }
struct S { k: secret<i64> }

fn leak() {
    let mut xs = [P { k: 0 }];
    xs = [S { k: 7 }];
    return xs[0].k;
}

fn main() {
    print(leak());
}
