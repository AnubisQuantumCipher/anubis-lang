// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 projection producer: indexing a joined array must transfer every possible element place type.
struct P { k: i64 }
struct S { k: secret<i64> }

fn main() {
    let mut xs = [P { k: 0 }];
    if true {
        xs = [S { k: 7 }];
    }
    let elem = xs[0];
    print(elem.k);
}
