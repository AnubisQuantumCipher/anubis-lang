// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 reduced summary mutation parity: method-form list push must widen exact element alternatives.
struct P { k: i64 }
struct S { k: secret<i64> }

fn leak() {
    let mut xs = [P { k: 0 }];
    xs.push(S { k: 7 });
    return xs[0].k;
}

fn main() {
    print(leak());
}
