// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TAINTED_SINK_WITHOUT_DECLASSIFY
// CORPUS-1 2026-07-27. Integrity dual of `declared_secret_fn_value_alias_chain_rejects`: TWO hops of
// aliasing a `-> tainted<i64>` function before application, reaching a `shell` sink. TODAY: already
// REJECTS correctly (coverage guard, verified by direct run; check-side only).
fn key() -> tainted<i64> { return 42; }
fn main() uses(shell) { let g = key; let h = g; shell(h()); }
