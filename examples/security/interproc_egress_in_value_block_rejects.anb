// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INTERPROC_EXFILTRATION
// Soundness hunt2 2026-07-19 [10]: a secret through a function param whose egress is buried in a
// value-position while-in-if block was missed by the param_egress summary walker (the value-block
// statement loop dropped nested control-flow statements). collect_param_sinks_in_expr now delegates a
// nested control-flow statement to body_param_sinks, which descends into if/while/for bodies.
fn go(cmd: u64) uses(net.send) {
    let z = if true { let mut i = 0; while i < 1 { send("host", 80, cmd); i = i + 1; } 0 } else { 0 };
}
fn run(k: secret<u64>) uses(net.send) { go(k); }
fn main() { }
