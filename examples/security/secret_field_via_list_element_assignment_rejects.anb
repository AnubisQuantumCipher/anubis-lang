// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 element write: replacing one array slot must widen the tracked element alternatives.
struct P { k: i64 }
struct S { k: secret<i64> }

fn main() {
    let xs = [P { k: 0 }];
    xs[0] = S { k: 7 };
    print(xs[0].k);
}
