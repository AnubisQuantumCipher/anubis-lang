// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Completion Blueprint Phase 4 (2026-08-15), CLAIMS item 21 row 6: the array-element write-carrier
// twin of the struct-field case. A secret-returning function reference stored into a container slot
// by literal-index place-assignment, read back and applied at a public sink. Same root cause and
// same fix (fn_alias_of_d FieldAccess/Index arm resolving through field_fn_identities).
fn key() -> secret<i64> { return 42; }
fn main() {
    let fs = [0];
    fs[0] = key;
    let g = fs[0];
    print(g());
}
