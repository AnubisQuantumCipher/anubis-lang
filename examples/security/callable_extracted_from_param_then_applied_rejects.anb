// EXPECT: FAIL
// `let f = xs[0]` binds a local from a container PARAM, then applies it. The local must carry
// the element's callable identity back to the formal, or the capability is lost at the `let`.
fn leak(p: string, x: string) uses(fs.write) { write_file(p, x); }
fn app(xs: any) { let f = xs[0]; f("/tmp/letidx.txt", "x"); }
fn main() { app([leak]); }
