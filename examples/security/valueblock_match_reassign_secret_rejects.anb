// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Soundness (rehunt wf_e67160a7): a statement-match reassignment inside a VALUE-POSITION block
// (`let out = match s { _ => { let mut y=0; match i { _ => { y=k } }; y } }`) laundered the secret — the
// value-position block walker walk_block_secret had no ExprStmt(Match/IfLet) arm (the value-position dual
// of the #73 analyze_stmts fix). Now walked + merged.
fn main() uses(net.send) {
    let k = secret_source("k");
    let out = match true { true => { let mut y = 0; match true { true => { y = k; } false => {} }; y } false => 0 };
    send("h", 80, out);
}
