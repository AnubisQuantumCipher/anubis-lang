// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INTERPROC_EXFILTRATION
// Soundness (wf_095bd48b): a HOF with a NAMED function as its closure arg (`each(xs, snd)`) applies snd to
// each element; the #65 descent resolved only inline/var-bound lambdas, so the element (secret) laundered
// through the named fn. Fixed: the descent applies the element to the named fn/builtin sink summary.
fn snd(x) uses(net.send) { send("h", 80, x); }
fn main() uses(net.send) { let k = secret_source("k"); each([k], snd); }
