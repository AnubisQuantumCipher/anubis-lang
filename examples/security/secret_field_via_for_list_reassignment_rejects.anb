// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 loop join: a possible body reassignment must survive the zero/body merge.
struct P { k: i64 }
struct S { k: secret<i64> }

fn main() {
    let mut xs = [P { k: 0 }];
    for i in 0..1 {
        xs = [S { k: 7 }];
    }
    print(xs[0].k);
}
