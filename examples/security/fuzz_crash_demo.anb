// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_RESEARCH_MISSING_AUTHORIZATION
// @fuzz without authorization metadata must fail closed under `anubis check`.
// (Process fuzz crashes are covered by `anubis fuzz --target` + poc_kit gate, not this check fixture.)

@fuzz(target: "crash_demo", runs: 1, sandbox: true, effects: ["fuzz_exec", "crash_capture"])
fn main(input: bytes) {
    let s = taint_source("crash");
    sink(s);
}
