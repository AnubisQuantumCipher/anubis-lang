// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_TO_PUBLIC
// Secret value assigned into a public-typed binding is label laundering.
fn main() {
    let bal: secret<i64> = 5;
    let mut x: i64 = 0;
    x = bal;
}
