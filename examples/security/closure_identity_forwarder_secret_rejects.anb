// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// #72. `fn_returns_lambda` only recognised a function returning a lambda LITERAL, so an IDENTITY
// FORWARDER (`fn fwd(f) { return f; }`) — whose returned expression is a Var — dropped the closure's
// capture label entirely. Direct apply and applied-param forwarding both rejected; this did not.
fn fwd(f) { return f; }
fn main() {
    let k = secret_source("k");
    let g = fwd(|x| print(k));
    g(0);
}
