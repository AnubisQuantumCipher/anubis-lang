// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TAINTED_SINK_WITHOUT_DECLASSIFY
// W2.1 integrity join dual: a tainted field on any exact branch alternative must reach the sink.
struct P { k: i64 }
struct S { k: tainted<i64> }

fn main() uses(shell) {
    let mut xs = [P { k: 0 }];
    if true {
        xs = [S { k: 7 }];
    }
    shell(xs[0].k);
}
