// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Item-21 Family-3 (D8): a `secret` struct field read off an element of an UNANNOTATED array
// literal (`let xs = [S {..}]; xs[0].k`) leaked because the literal inferred the bare type `list`,
// whose element type `container_element_type` cannot recover. The array-literal arm of
// `infer_expr_type_scoped` now infers `list<S>` for a unanimous struct-literal array — the same
// spelling a declared `let xs: list<S>` produces — so the element-field qualifier is looked up.
struct S { k: secret<i64> }
fn main() { let xs = [S { k: 42 }]; print(xs[0].k); }
