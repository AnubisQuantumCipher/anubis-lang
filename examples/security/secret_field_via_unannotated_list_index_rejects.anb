// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// CLAIMS 21 / W2.1 RED: the unannotated homogeneous array must retain S as its element place type.
// Before the fix, `let xs = [...]` records only bare `list`, so xs[0].k loses k's declared secret type.
struct S { k: secret<i64> }

fn main() {
    let xs = [S { k: 7 }];
    print(xs[0].k);
}
