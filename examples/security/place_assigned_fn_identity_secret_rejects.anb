// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Phase 2 slice 3 — `docs/CLAIMS.md` item 21 root cause 6, stated verbatim in the compiler.
//
// RUNTIME-PROVEN false accept before this fixture existed: `check` exited 0 and `run` PRINTED 42.
//
// Every other route to the same leak already rejected — the direct call `print(key())`, the local
// alias `let g = key; g()`, the higher-order `fn app(f){ print(f()); } app(key)`, and the struct
// LITERAL `S { f: key }`. Only the PLACE-ASSIGNMENT route laundered, because a named function
// assigned to a field is recorded in `field_fn_identities` while the `Expr::CallExpr` consumer
// read only `field_closures`. A producer wrote the label down and the consumer on that same path
// never looked at it.
fn key() -> secret<i64> { return 42; }

struct S { f: i64 }

fn main() {
    let b = S { f: 0 };
    b.f = key;
    print(b.f());
}
