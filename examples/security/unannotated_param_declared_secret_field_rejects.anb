// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Phase 2 slice 1 — `docs/CLAIMS.md` item 21, "unannotated parameter place types".
//
// RUNTIME-PROVEN false accept before this fixture existed: `check` exited 0 and `run` PRINTED 42.
// The DIRECT twin (`unannotated_param_direct_read_rejects.anb`) already rejected, so this was a
// pure laundering route, not a missing policy.
//
// Mechanism: an unannotated formal binds with `info.ty == Some("")` (middle/mod.rs, the param
// binding in `analyze_function`). `place_struct_type`'s `Expr::Var` arm returned that empty
// spelling, `struct_field_type_for_place` found no struct named "", and the DECLARED `secret<i64>`
// qualifier on `Box.k` was silently dropped — the project's standing disease: a label the user
// wrote down that the consumer never reads.
struct Box { k: secret<i64> }

fn leak(s) {
    print(s.k);
}

fn main() {
    let b = Box { k: 42 };
    leak(b);
}
