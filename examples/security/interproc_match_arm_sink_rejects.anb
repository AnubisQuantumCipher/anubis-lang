// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INTERPROC_EXFILTRATION
// Soundness (walker audit post-wf_e67160a7): body_param_sinks had no ExprStmt(Match/IfLet) arm, so a
// param reassigned in a match arm then sunk after the join was not summarized. Now handled (Block AND
// bare-expr arm bodies).
fn leak(p: u64) uses(net.send) { let mut a = 0; match true { true => { a = p; } false => {} } send("h", 80, a); }
fn main() uses(net.send) { let k = secret_source("k"); leak(k); }
