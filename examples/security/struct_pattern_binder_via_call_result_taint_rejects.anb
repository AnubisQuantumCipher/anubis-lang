// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TAINTED_SINK_WITHOUT_DECLASSIFY
// CORPUS-1 2026-07-27. Integrity dual of `struct_pattern_binder_via_call_result_rejects`: a
// `tainted<i64>` field destructured straight off a function's return, reaching a `shell` sink.
// TODAY: already REJECTS correctly (coverage guard, verified by direct run; check-side only).
struct V { k: tainted<i64> }
fn get_v() -> V { return V { k: 42 }; }
fn main() uses(shell) { let V { k } = get_v(); shell(k); }
