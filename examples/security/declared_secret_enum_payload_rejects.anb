// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Lead agent 2026-07-27, census item D4. `match` arm binders were seeded from ONE flag derived from
// the SCRUTINEE — and here the scrutinee `e` is not secret. The secrecy is declared on the PAYLOAD,
// which nothing consulted because `enum_variants` stored only variant NAMES and discarded the
// declared payload types the parser had already captured.
// Control: `fn f(k: secret<i64>) { match k { x => print(x) } }` was already rejected, so the binder
// path worked — it just had no way to learn about a qualifier declared one level in.
enum E { A(secret<i64>), B }
fn main() {
    let e = E::A(42);
    match e {
        E::A(x) => { print(x); }
        E::B => { print(0); }
    }
}
