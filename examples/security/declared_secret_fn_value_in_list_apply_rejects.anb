// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// GROK-SEKHMET round2. Container of secret-returning function values then apply:
// `let xs = [key]; print(xs[0]())` — same secret_fns raw-name miss as bare alias.
// pre=0; runtime prints 42.
fn key() -> secret<i64> {
    return 42;
}
fn main() {
    let xs = [key];
    print(xs[0]());
}
