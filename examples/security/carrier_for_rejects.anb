// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_ASSERTION_DISPROVED
// Item-21 Family-1: requires carried into a for body
fn f(x: i64) -> i64 requires(x > 0) { return x; }
fn app(g, y) { for _k in 0..1 { g(-1); } }
fn main() { app(f, 0); }
