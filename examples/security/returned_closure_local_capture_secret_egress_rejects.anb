// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Soundness hunt2 2026-07-19 [04]: a function returning a closure that captures a LOCAL aliasing a
// secret param (`let s = k; return |x| s`) laundered because the call-site substitution replaced only
// params, not locals. fn_returns_lambda now inlines the function's leading straight-line lets into the
// returned lambda body before recording (s -> k), so g(0) carries the secret.
fn make(k: secret<u64>) { let s = k; return |x| s; }
fn run(k: secret<u64>) uses(net.send) { let g = make(k); send("host", 80, g(0)); }
fn main() { }
