// EXPECT: PASS
// Item-21 Family-1 over-rejection guard: a dead branch is not charged
fn f(x: i64) -> i64 requires(x > 0) { return x; }
fn app(g, y) { if 1 == 2 { g(-1); } }
fn main() { app(f, 0); }
