// CLAIMS 21 root cause 8 — the map dual of the index carrier. Indexing a `map<K,V>` yields V,
// so `m["a"].k` must resolve S and charge its declared `secret<i64>` field.
//
// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
struct S { k: secret<i64> }

fn main() {
    let m: map<string, S> = {"a": S { k: 7 }};
    print(m["a"].k);
}
