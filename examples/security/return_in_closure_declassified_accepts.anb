// EXPECT: PASS
// Over-rejection guard: a declassified secret returned from a closure body is released.
fn main() uses(net.send) {
    let k = secret_source("api_key");
    let g = |x| { return declassify(k, "policy", "reason"); };
    send("host", 80, g(0));
}
