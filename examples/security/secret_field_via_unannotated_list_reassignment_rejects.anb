// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 assignment parity: a whole-binding write of an exact homogeneous array must refresh place type.
struct S { k: secret<i64> }

fn main() {
    let mut xs = [];
    xs = [S { k: 7 }];
    print(xs[0].k);
}
