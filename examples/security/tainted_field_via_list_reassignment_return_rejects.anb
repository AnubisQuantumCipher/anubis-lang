// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TAINTED_SINK_WITHOUT_DECLASSIFY
// W2.1 reduced integrity summary: a whole-binding array update must refresh exact place state.
struct P { k: i64 }
struct S { k: tainted<i64> }

fn leak() {
    let mut xs = [P { k: 0 }];
    xs = [S { k: 7 }];
    return xs[0].k;
}

fn main() uses(shell) {
    shell(leak());
}
