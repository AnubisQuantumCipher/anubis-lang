// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 loop carry: a sink before the same-body reassignment observes that type on later iterations.
struct P { k: i64 }
struct S { k: secret<i64> }

fn main() {
    let mut xs = [P { k: 0 }];
    let mut i = 0;
    while i < 2 {
        print(xs[0].k);
        xs = [S { k: 7 }];
        i = i + 1;
    }
}
