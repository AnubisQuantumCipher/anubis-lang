// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Confidentiality face of `hof_forwarded_arg_taint_rejects`. Both lanes carried the identical hole
// and both are closed in the same commit — the integrity face is command injection and the
// confidentiality face is exfiltration, and fixing one while leaving the other is how a form the
// sibling lane already rejects becomes a working launder path.
fn apply(f, v) { f(v); }
fn main() {
    let k: secret<i64> = 42;
    apply(|v| { print(v); }, k);
}
