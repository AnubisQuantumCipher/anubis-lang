// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Soundness (hunt wf_5b2a1bcc): a secret reassigned inside a statement-position `match` arm was dropped
// at the join (the arm body was walked in a discarded clone), so `send(v)` read v as clean. Fixed by
// merging arm-body value labels like Stmt::If (analyze_value_block + merge_taint_over).
fn run(c: bool, k: secret<u64>) uses(net.send) {
    let mut v = 0;
    match c { true => { v = k; } false => { v = 0; } }
    send("h", 80, v);
}
fn main() {}
