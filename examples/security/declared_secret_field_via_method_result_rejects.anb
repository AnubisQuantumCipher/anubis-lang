// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// D2, the impl-method twin of `declared_secret_field_via_call_result_rejects`. It needed a
// `method_ret_types` registry, which did not exist: methods recorded their params, contracts and
// declared effects, but never their declared RETURN type.
// Bare-name keyed like its neighbours, and two impls whose same-named method returns DIFFERENT
// struct types collapse to unknown rather than guessing — attributing one type's field qualifiers
// to another would be a false reject, and the fail-open direction is the safe one for a registry
// that only ever adds labels.
struct V { k: secret<i64> }
struct F { z: i64 }
impl F { fn make(self) -> V { return V { k: 42 }; } }
fn main() { let f = F { z: 1 }; print(f.make().k); }
