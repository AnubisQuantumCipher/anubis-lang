// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_TO_PUBLIC
// Secret argument into a public method formal (method-call dual of free-fn formal).
struct S {
    n: i64,
}
impl S {
    fn take(self, x: i64) {
    }
}
fn main() {
    let bal: secret<i64> = 1;
    let s = S { n: 0 };
    s.take(bal);
}
