// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_
// Second control: the violating argument reached through a local alias is still caught, so argument
// provenance is not the gap — the callee's IDENTITY at the call site is.
fn f(x: i64) -> i64 requires(x > 0) { return x; }
fn main() { let n = -1; print(f(n)); }
