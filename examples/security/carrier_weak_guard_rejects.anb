// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_ASSERTION_DISPROVED
// Item-21 Family-1: a guard too weak to establish the precondition
fn f(x: i64) -> i64 requires(x > 0) { return x; }
fn app(g, y) { if y > -3 { g(y); } }
fn main() { app(f, -1); }
