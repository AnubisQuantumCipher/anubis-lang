// EXPECT: PASS
// CORPUS-1 2026-07-27. Over-rejection guard for the lead's Defect A fix: a PLAIN (non-secret) named
// function passed as a parameter and applied inside the callee must keep compiling. If the fix for
// `declared_secret_fn_value_as_param_rejects` makes ANY fn-value-through-a-param apply suspect
// regardless of the callee's declared return type, this goes red and the fix has become a wall.
fn plain() -> i64 { return 1; }
fn app(f) { print(f()); }
fn main() { app(plain); }
