// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// GROK-SEKHMET round 9. D1 residual: `get().k` read DIRECTLY was rejected, but BINDING the result
// first dropped the type, because a let's inferred type does not resolve a call's return type — so
// the declared field qualifier had nothing to key on.
// The bound form is what real code actually writes, which made this residual matter more than the
// direct form it followed. The let's type now falls back to `place_struct_type`, the same resolver
// D1/D2/D3 taught.
struct S { k: secret<i64> }
fn get() -> S { return S { k: 42 }; }
fn main() {
    let s = get();
    print(s.k);
}
