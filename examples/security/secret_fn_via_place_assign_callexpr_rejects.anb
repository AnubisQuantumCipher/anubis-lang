// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Item-21 Family-2 slice: a secret-returning fn placed into a struct field via PLACE-ASSIGNMENT
// (`b.f = key`) and applied DIRECTLY as `b.f()` (an `Expr::CallExpr` on a `FieldAccess`) leaked —
// the `CallExpr` arm of `expr_source` resolved a stored callable only through `field_closures`
// (populated by struct-literal construction), while the place-assign write handler records the
// identity in `field_fn_identities`. The arm now consults `field_fn_identities` on a
// `field_closures` miss, so `b.f()` rejects like `let g = b.f; g()` already did (row 6).
struct Box { f: u64 }
fn key() -> secret<i64> { return 42; }
fn main() { let b = Box { f: 0 }; b.f = key; print(b.f()); }
