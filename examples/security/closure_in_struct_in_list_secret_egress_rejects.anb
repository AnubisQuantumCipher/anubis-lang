// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// hunt2: struct-in-a-list — arr[0].f(0) (Index base + FieldAccess) resolves to path "0.f".
struct Box { f: u64 }
fn run(k: secret<u64>) uses(net.send) {
    let g = |x| k;
    let arr = [Box { f: g }];
    send("host", 80, arr[0].f(0));
}
fn main() { }
