// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INTERPROC_EXFILTRATION
// Soundness (rehunt wf_e67160a7): the SINK-side dual of the loop-carried return fixpoint — a value
// migrating across iterations reaches a post-loop SINK; body_param_sinks ran the body once. Now a
// monotone bounded fixpoint.
fn leak(x: u64) uses(net.send) { let mut a = 0; let mut b = 0; let mut c = x; let mut i = 0; while i < 2 { a = b; b = c; c = 0; i = i + 1; } send("h", 80, a); }
fn main() uses(net.send) { let k = secret_source("k"); leak(k); }
