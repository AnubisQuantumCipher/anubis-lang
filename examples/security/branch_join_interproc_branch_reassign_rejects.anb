// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INTERPROC_EXFILTRATION
// Soundness (hunt wf_5b2a1bcc): the interproc param->sink summary (body_param_sinks) restored *flow=saved
// after a branch, dropping a param reassigned to a local inside the branch. Fixed to may-merge (union),
// mirroring the already-correct body_param_returns twin.
fn leak(x: u64) uses(net.send) { let mut a = 0; if true { a = x; } send("h", 80, a); }
fn main() uses(net.send) { let k = secret_source("k"); leak(k); }
