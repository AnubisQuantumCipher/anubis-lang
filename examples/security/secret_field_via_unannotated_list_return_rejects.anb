// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// W2.1 reduced-summary parity: an unannotated array place type must survive a callee return walk.
struct S { k: secret<i64> }

fn key() {
    let xs = [S { k: 7 }];
    return xs[0].k;
}

fn main() {
    print(key());
}
