// EXPECT: PASS
// Secret formal or declassify at method call site.
struct S {
    n: i64,
}
impl S {
    fn take_secret(self, x: secret<i64>) {
    }
    fn take_public(self, x: i64) {
    }
}
fn main() {
    let bal: secret<i64> = 1;
    let s = S { n: 0 };
    s.take_secret(bal);
    s.take_public(declassify(bal, "policy", "reason"));
}
