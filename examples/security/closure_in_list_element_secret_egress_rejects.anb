// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Soundness hunt 2026-07-19 (#48-h): the list-element sibling of the struct-field case — a secret-
// capturing closure stored in a LIST and applied via `arr[0](0)` carries its captured secret to egress.
fn run(k: secret<u64>) uses(net.send) {
    let g = |x| k;
    let arr = [g];
    send("host", 80, arr[0](0));
}
fn main() { }
