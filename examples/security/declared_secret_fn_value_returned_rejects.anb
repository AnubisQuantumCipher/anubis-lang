// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// CORPUS-1 2026-07-27. Defect A through the RETURN path: a secret-returning function is itself
// returned (as a value, not a closure) by a second function, then applied at the call site. Same
// family as the param-passed form (`declared_secret_fn_value_as_param_rejects`) — the callable
// crosses a function boundary, this time via a `return` rather than a parameter. TODAY: check
// PASSES and `anubis run` PRINTS 42 — runtime-proven leak.
fn key() -> secret<i64> { return 42; }
fn get_key() { return key; }
fn main() { let g = get_key(); print(g()); }
