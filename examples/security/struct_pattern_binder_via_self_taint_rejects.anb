// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TAINTED_SINK_WITHOUT_DECLASSIFY
// CORPUS-1 2026-07-27. Integrity dual of `struct_pattern_binder_via_self_rejects`: a `tainted<i64>`
// field destructured out of `self` inside a method body, reaching a `shell` sink. TODAY: already
// REJECTS correctly (coverage guard, verified by direct run; check-side only) — same two-fixes
// composition as the confidentiality face.
struct V { k: tainted<i64> }
impl V {
    fn leak(self) uses(shell) {
        let V { k } = self;
        shell(k);
    }
}
fn main() uses(shell) {
    let v = V { k: 42 };
    v.leak();
}
