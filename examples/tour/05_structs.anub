// EXPECT: Point { x: 3, y: 4 }|25|Point { x: 30, y: 4 }
struct Point { x: u32, y: u32 }
fn dist_sq(p) { return p.x * p.x + p.y * p.y; }
fn main() {
    let p = Point { x: 3, y: 4 };
    print(p);
    print(dist_sq(p));
    p.x = 30;
    print(p);
}
