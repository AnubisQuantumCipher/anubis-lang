// CHECK_EXPECT: FAIL
// CHECK_ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Checker-only control: release credentials may not reach public output.
fn main() uses(io.print) {
    let release_token: secret<string> = "seshat-demo-token";
    print(release_token);
}
