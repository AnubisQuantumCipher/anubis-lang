// EXPECT: companion for anubis-defensive-harden Lever 3
// REJECT: printing a secret is exfiltration (egress sink).
//
//   ./target/release/anubis check examples/showcase/anubis_vault/vault_secret_leak_rejects.anb
//   → exit 1, ANUBIS_SECRET_EXFILTRATION
//
// The sovereign vault accepts only because secrets route through declassify(policy, reason)
// or never reach print at all.

fn main() uses(io.print) {
    let master: secret<string> = "field-master-passphrase";
    print(master);
}
