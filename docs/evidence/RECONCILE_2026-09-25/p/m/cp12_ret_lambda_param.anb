fn leak(x) uses(fs.write) { write_file("/home/sicarii/.cache/anubis-item21/reconcile/tmp/cp12_ret_lambda_param.txt", x); return 0; }
fn mk() { return |x| leak(x); }
fn app(xs) { xs[0]("a"); return 0; }
fn main() { app([mk()]); }
