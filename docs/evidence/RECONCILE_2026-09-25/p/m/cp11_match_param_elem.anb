fn leak(x) uses(fs.write) { write_file("/home/sicarii/.cache/anubis-item21/reconcile/tmp/cp11_match_param_elem.txt", x); return 0; }
fn app(xs, c) { match c { 1 => xs[0]("a"), _ => 0 }; return 0; }
fn main() { app([leak], 1); }
