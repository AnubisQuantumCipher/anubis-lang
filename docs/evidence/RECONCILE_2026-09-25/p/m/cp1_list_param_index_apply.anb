fn leak(x) uses(fs.write) { write_file("/home/sicarii/.cache/anubis-item21/reconcile/tmp/cp1_list_param_index_apply.txt", x); return 0; }
fn app(xs) { xs[0]("a"); return 0; }
fn main() { app([leak]); }
