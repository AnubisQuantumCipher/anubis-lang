fn leak(x) uses(fs.write) { write_file("/home/sicarii/.cache/anubis-item21/reconcile/tmp/cp2_map_param_key_apply.txt", x); return 0; }
fn app(m) { m["k"]("a"); return 0; }
fn main() { app({ "k": leak }); }
