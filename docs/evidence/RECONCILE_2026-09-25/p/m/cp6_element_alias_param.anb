fn leak(x) uses(fs.write) { write_file("/home/sicarii/.cache/anubis-item21/reconcile/tmp/cp6_element_alias_param.txt", x); return 0; }
fn app(xs) { let ys = xs; let f = ys[0]; f("a"); return 0; }
fn main() { app([leak]); }
