struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    let v = if true {
        let a = 0;
        let i = 0;
        while i < 2 { print(a); a = q.k; i = i + 1; }
        0
    } else { 0 };
    return v;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    f(p);
}
