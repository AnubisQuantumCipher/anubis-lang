fn strict(x: i64) -> i64 requires(x > 0) { print(x); return x; }
fn lax(x: i64) -> i64 { return x; }
fn main() {
    let n = len([1, 2, 3]);
    let g = lax;
    if n > 1 { g = strict; }
    print(g(0 - 1));
}
