fn leak(x) uses(fs.write) { write_file("/home/sicarii/.cache/anubis-item21/reconcile/tmp/cp9_if_bind_param.txt", x); return 0; }
fn app(xs, c) { let f = if c > 0 { xs[0] } else { xs[0] }; f("a"); return 0; }
fn main() { app([leak], 1); }
