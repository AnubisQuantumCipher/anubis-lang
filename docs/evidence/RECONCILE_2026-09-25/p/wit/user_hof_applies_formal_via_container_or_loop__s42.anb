struct S { k: secret<i64>, pub_n: i64 }
struct B { g: i64 }
fn pr(x) {
    print(x);
    return 0;
}
fn zero(x) {
    return 0;
}
fn app(g, x) {
    let fs = [g];
    let h = fs[0];
    return h(x);
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let z = app(pr, p.k);
}
