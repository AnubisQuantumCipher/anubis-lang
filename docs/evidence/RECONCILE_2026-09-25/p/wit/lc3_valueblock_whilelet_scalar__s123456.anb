struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 123456, pub_n: 3 };
    let v = if true {
        let a = 0;
        let i = 0;
        while let Some(w) = (if i < 2 { Some(a) } else { None }) { print(w); a = p.k; i = i + 1; }
        0
    } else { 0 };
    print(v);
}
