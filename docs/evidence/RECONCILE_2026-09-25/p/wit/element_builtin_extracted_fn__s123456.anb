struct S { k: secret<i64>, pub_n: i64 }
fn pr(x) {
    print(x);
    return 0;
}
fn zero(x) {
    return 0;
}
fn main() {
    let p = S { k: 123456, pub_n: 3 };
    let fs = [pr];
    let g = first(fs);
    let z = g(p.k);
}
