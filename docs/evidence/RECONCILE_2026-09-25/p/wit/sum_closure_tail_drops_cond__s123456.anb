struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    let s = q.k;
    let g = || { if s > 100 { 1 } else { 0 } };
    return g();
}
fn main() {
    let p = S { k: 123456, pub_n: 3 };
    print(f(p));
}