struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 123456, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    let m = { "a": p };
    let n = 0
    if len(str(p)) > 22 { n = 1; }
    print(n)
}
