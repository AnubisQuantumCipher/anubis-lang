struct S { k: secret<i64>, pub_n: i64 }
struct B { g: i64 }
fn pr(x) {
    print(x);
    return 0;
}
fn zero(x) {
    return 0;
}
fn main() {
    let p = S { k: 123456, pub_n: 3 };
    let fs = [|x| { print(x); 0 }];
    let z = each([p.k], fs[0]);
}
