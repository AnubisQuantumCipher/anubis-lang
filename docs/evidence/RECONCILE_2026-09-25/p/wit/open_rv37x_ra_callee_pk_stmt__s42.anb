struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) { return 0; }
fn leak(x) { print(x); return 0; }
fn f(q, n) {
    let g = zero;
    g = leak;
    g(q);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    f(p.k, 1);
}
