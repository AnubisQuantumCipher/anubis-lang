struct S { k: secret<i64>, pub_n: i64 }
fn f(p) {
    let q = S { k: 40 + 2, pub_n: 3 }
    loop {
        if p == q { break }
        return 1
    }
    return 0
}
fn main() { let p = S { k: 123456, pub_n: 3 }; print(f(p)); }
