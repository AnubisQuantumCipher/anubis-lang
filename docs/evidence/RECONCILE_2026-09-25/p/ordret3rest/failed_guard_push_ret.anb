struct S { k: secret<i64>, pub_n: i64 }
fn nope(v) { return false; }
fn f(q: S) {
    let xs = [0];
    match q.pub_n {
        3 if nope(push(xs, q.k)) => { return [7]; }
        _ => { return xs; }
    }
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p));
}
