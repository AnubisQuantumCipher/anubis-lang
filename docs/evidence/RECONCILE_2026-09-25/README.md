# Reconciliation of the open DEFECTS/CLAIMS items, 2026-09-25

A read-only investigator re-measured every open S1/S2 row of `docs/mission/DEFECTS.md`, every
assurance-chain, self-host, frontend and roadmap row, and every open entry of `docs/CLAIMS.md`
"Known open issues" against the head checker of that day: pin `anubis-ord3b` (source e7b56507,
sha256 90a21f28c49bd044202698ba889f72bb4d9757b721671b194bf4c7a3b786c855). Runtime witnesses used
the older pin `anubis-whole10` (`run --no-verify`, secret 42 vs 123456).

- `RECONCILE.md`, `RECONCILE.tsv`: one row per item (76): classification FIXED / OPEN /
  NOT-REPRODUCIBLE / NON-CODE, the evidence, whether a matrix case exists, duplicates across the two
  files, and what closing it requires. Generated by `gen_reconcile.py` from one row list.
- `p/`: the probe programs it wrote or extracted (`p/m/` its own; `p/ordret3rest/` the leak
  programs of the review of 1696925b; `p/fres*/` multi-file F-RESOLVE-1 probes). Logs and bundles are
  not versioned.

Measured on e7b56507's checker: later units change some verdicts (the alias unit d61c33d8 closes
several alias findings). What it found witnessed and unregistered was registered in the matrix by
d052f817, measured on the pin of a05b6100.
