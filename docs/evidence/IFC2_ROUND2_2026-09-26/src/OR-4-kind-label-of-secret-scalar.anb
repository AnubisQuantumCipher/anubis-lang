// Input validation report: the PIN must be an integer; the report states the PIN's runtime type
// (never its value).
fn main() {
    let pin: secret<i64> = 42;
    let kind = type(pin);
    if kind == "int" {
        print("pin format ok");
    } else {
        print("pin format bad");
    }
}
