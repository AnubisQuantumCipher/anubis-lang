fn pick(c) {
    let v = if c { 7 } else { 3 };
    v
}
fn main() {
    let s: secret<i64> = 42;
    let readings = [pick(s > 100), 1, 2];
    if len(args()) > 50 {
        readings = [];
    }
    println(bool(readings));
    println(int(readings));
}
