(generated; 64 locals) fn main() {
    let mut d1 = 0;
    let mut d2 = 0;
    ... one `let mut dK = 0;` for every K = 1..64 ...
    let key: secret<i64> = 7;
    for r in 0..128 {
        let t = d64;
        d64 = d63;
        d63 = d62;
        ... one `dK = dK-1;` line for every K = 64 down to 2 ...
        d1 = t + key * r;
    }
    println(declassify(d1 + d64, "digest", "published"));
}
(Full file with every line written out: ~/.cache/anubis-review-esc/ifc2-r2/control/p/r08_delay64.anb, generated by the python snippet in that directory. The same program with 59 stages is accepted; with 64 it hits the limit.)