{
 "findings": [
  {
   "id": "r2v_01_select_map_insertion_order",
   "kind": "leak",
   "program": "fn pick(c) {\n    if c { {\"a\": [], \"b\": []} } else { {\"b\": [], \"a\": []} }\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let m = pick(k > 100);\n    for key in m {\n        println(key);\n    }\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none (no information flow found)",
   "output_k42": "b\na",
   "output_other_k": "a\nb",
   "runner": "NEW (anubis-ifc2-v8e run --no-verify); NEW's full check accepts",
   "timing": "ifc2-report 0.02s",
   "root_cause": "value.rs V::select, map arm (current tree ~line 870: same_keys = ma.known.keys().eq(mb.known.keys()) && maybe equal && other bottom && klab PUB). MapV.known is a BTreeMap, so the check ignores insertion order. The runtime map is an insertion-ordered Vec<(String, AnubisValue)> (run.rs enum AnubisValue::Map, anubis_map_lit, map_keys, anubis_iter, display_string).",
   "explanation": "Two map literals with the same key set but different literal order pass select's same-shape test. The map's lab stays PUB and every element is an empty list, so nothing is raised. For-in iterates keys in insertion order, and keys(), values(), entries(), str(), print(m), == and sha256 behave the same way. The iteration order therefore reveals which branch the secret chose. Without the function wrapper (let m = if k > 100 {...} else {...}; println(keys(m))) IFC v2 still accepts, but the lanes then refuse it; a public twin prints [b, a] vs [a, b]. A fix must either track key order or refuse same-shape select when the literal orders differ."
  },
  {
   "id": "r2v_02_select_struct_field_order",
   "kind": "leak",
   "program": "struct P { a: list<i64>, b: list<list<i64>> }\nfn pick(c) {\n    if c { P { a: [], b: [[]] } } else { P { b: [[]], a: [] } }\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let p = pick(k > 100);\n    println(p);\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none",
   "output_k42": "P { b: [[]], a: [] }",
   "output_other_k": "P { a: [], b: [[]] }",
   "runner": "NEW",
   "timing": "ifc2-report 0.02s",
   "root_cause": "value.rs V::select, struct arm (current tree ~line 901: ta == tb && fa.keys().eq(fb.keys()) && maybe_fields equal). Struct fields are a BTreeMap<String, V>, so literal field order is lost. The runtime struct is Vec<(String, AnubisValue)> in literal order: display_string prints in that order, and index_get with an integer index reads by position.",
   "explanation": "Two literals of one struct type with the same field set but different field order keep shape PUB under a secret select. The print differs. The positional read also leaks: with P { a: list<i64>, b: list<i64> } and literals P { a: [], b: [1] } vs P { b: [1], a: [] }, println(len(p[0])) prints 1 vs 0 and IFC v2 accepts (probe v06). This is a separate code arm from finding 01."
  },
  {
   "id": "r2v_03_select_maybe_keys_map",
   "kind": "leak",
   "program": "fn pick(c, x, y) {\n    if c { x } else { y }\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let c = len(args()) > 100;\n    let m1 = if c { {\"a\": []} } else { {\"a\": [], \"b\": []} };\n    let m2 = if c { {\"a\": [], \"b\": []} } else { {\"a\": []} };\n    let m = pick(k > 100, m1, m2);\n    println(len(m));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none",
   "output_k42": "1",
   "output_other_k": "2",
   "runner": "NEW",
   "timing": "ifc2-report ~0.8s under load average 33 (0.02s idle)",
   "root_cause": "value.rs V::select map arm: `ma.maybe == mb.maybe` is taken to mean the same key set. A key in `maybe` is present or absent according to a decision that differs per alternative. m1 has b (the public c is false) and m2 lacks it, but both abstract to known {a, b} with maybe {b}, so the selected map keeps lab PUB.",
   "explanation": "Presence tracking is used as shape equality. Two maps whose optional keys were decided independently are treated as having the same shape, so the secret choice between them changes len, has_key, keys and get defaults without raising anything. Select should raise the shape whenever either side has a non-empty maybe set (or compare what decided the presence)."
  },
  {
   "id": "r2v_04_select_maybe_fields_struct",
   "kind": "leak",
   "program": "struct P { a: list<i64>, b: list<i64> }\nfn pick(c, x, y) {\n    if c { x } else { y }\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let c = len(args()) > 100;\n    let p1 = if c { P { a: [] } } else { P { a: [], b: [] } };\n    let p2 = if c { P { a: [], b: [] } } else { P { a: [] } };\n    let p = pick(k > 100, p1, p2);\n    println(len(p));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none",
   "output_k42": "1",
   "output_other_k": "2",
   "runner": "NEW",
   "timing": "ifc2-report 0.03s",
   "root_cause": "value.rs V::select struct arm: `a.maybe_fields == b.maybe_fields` is taken as the same field set. Both sides have fields {a, b} with maybe {b}, but b's presence is decided differently in each alternative.",
   "explanation": "This is the struct twin of finding 03, in a different select arm (the fix must cover both). len(p) counts struct fields at runtime (len_val on a Struct), and IFC gives it p.lab, which stays PUB."
  },
  {
   "id": "r2v_05_nested_write_into_missing_key_makes_it_present",
   "kind": "leak",
   "program": "fn addb(m, c) {\n    if c {\n        m[\"b\"] = 7;\n    }\n    m\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let mut m = {\"a\": 1};\n    m[\"b\"][\"c\"] = 5;\n    let r = addb(m, k > 100);\n    println(len(r));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none",
   "output_k42": "1",
   "output_other_k": "2",
   "runner": "NEW",
   "timing": "ifc2-report 0.03s",
   "root_cause": "eval.rs Interp::set_path, Seg::Index map arm with a literal key (and the Seg::Field map arm): it always does m2.known.insert(key, set_path(old, rest, ..)) and m2.maybe.remove(key). The runtime set_at (run.rs ~1981) adds a missing map key only when `rest.is_empty()`; a nested write m[\"b\"][\"c\"] = 5 into a missing key is a no-op.",
   "explanation": "After the no-op nested write, IFC records \"b\" as definitely present, with the value bottom because old = other = bottom. The later write under a secret pc then sees a present, non-maybe key and does not raise the map's shape. At runtime that write adds the key, so len (and has_key, keys) reveals the secret. The same happens when the key was maybe (maybe.remove runs regardless of rest), for m.b.c = 5 on a map, and for structs (finding 06). The bottom-valued slot also makes map_leq accept a map that lacks the key, so a loop can converge early."
  },
  {
   "id": "r2v_06_nested_write_into_missing_field_makes_it_present",
   "kind": "leak",
   "program": "struct Q { c: i64 }\nstruct P { a: i64, b: Q }\nfn addb(p, c) {\n    if c {\n        p.b = Q { c: 1 };\n    }\n    p\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let mut p = P { a: 1 };\n    p.b.c = 5;\n    let r = addb(p, k > 100);\n    println(len(r));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none",
   "output_k42": "1",
   "output_other_k": "2",
   "runner": "NEW",
   "timing": "ifc2-report 0.02s",
   "root_cause": "eval.rs Interp::set_path, Seg::Field struct arm: for a missing field it computes new = set_path(scalar PUB, rest, ..), then fields.insert(f, new) and removes f from maybe_fields, even when rest is non-empty. The runtime set_at pushes a missing struct field only when rest.is_empty().",
   "explanation": "The struct arm of finding 05. p.b.c = 5 on a struct literal that omitted b does nothing at runtime, but IFC now believes field b exists. The later p.b = ... under a secret condition adds the field at runtime without raising p.lab, so len(p) reveals the secret."
  },
  {
   "id": "r2v_07_remove_computed_key_keeps_known_keys_present",
   "kind": "leak",
   "program": "fn seta(m, c) {\n    if c {\n        m[\"a\"] = [];\n    }\n    m\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let mut m = {\"a\": [], \"b\": []};\n    let key = str(\"a\");\n    let v = remove(m, key);\n    let r = seta(m, k > 100);\n    println(len(r));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none",
   "output_k42": "1",
   "output_other_k": "2",
   "runner": "NEW",
   "timing": "ifc2-report ~0.8s under load (0.02s idle)",
   "root_cause": "builtins.rs mutate, remove on a map (current tree ~1000-1010): with a non-literal key it only joins pc and k.deep() into klab and pc into out.lab. The known keys stay in `known` and are not added to `maybe`, although a computed key may equal any of them and remove deletes it at runtime (anubis_remove).",
   "explanation": "After remove(m, str(\"a\")), IFC still has \"a\" as definitely present, so the secret-pc write m[\"a\"] = [] is treated as an overwrite and does not raise the map's shape. At runtime \"a\" was removed, so the write re-adds it and len changes with the secret. Every known key should become maybe (as a computed-key write joins into every known slot)."
  },
  {
   "id": "r2v_08_merge_drops_first_map_value_when_second_key_maybe",
   "kind": "leak",
   "program": "enum Vault {\n    Entry { pin: secret<i64> },\n}\nfn main() {\n    let pv = 42;\n    let e = Vault::Entry { pin: pv };\n    let p = e.pin;\n    let a = {\"k\": p};\n    let c = len(args()) > 100;\n    let b = if c { {\"k\": 1} } else { {\"z\": 1} };\n    let r = merge(a, b);\n    println(r[\"k\"]);\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none",
   "output_k42": "42",
   "output_other_k": "123456",
   "runner": "NEW",
   "timing": "ifc2-report 0.02s",
   "root_cause": "builtins.rs merge (current tree ~787): known = b.known.clone(), and a's entry for a key is added only when b.known lacks that key. When b.known has k only as maybe, the runtime result at k is a[k] whenever b lacks k (anubis_merge keeps a's entry), but IFC's value is b[k] alone.",
   "explanation": "This is an explicit flow of the secret value itself. The control (r = a instead of merge) is refused by IFC v2 (\"secret data reaches println\"), and with merge it is accepted. The fix is known[k] = b[k] \u2294 a[k] (or a.other) when k is in b.maybe. The secret here comes from a declared variant field type, which the lanes do not track, so every lane accepts it. With let s: secret<i64> = 42; let a = {\"k\": s} (probe v12), IFC v2 also accepts, the lanes refuse, and a public twin prints 42 vs 123456."
  },
  {
   "id": "r2v_09_pattern_decision_top_skips_concrete_parts",
   "kind": "leak",
   "program": "fn walk(a, inner, n) {\n    if n > 0 {\n        return walk(a, inner, n - 1);\n    }\n    let c = len(args()) > 100;\n    let v = if c { a[0][0][0][0][0] } else { Some(inner) };\n    match v {\n        Some(None) => println(\"none\"),\n        _ => println(\"other\"),\n    }\n    0\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let a = [[[[[[1]]]]]];\n    walk(a, Some(2), 1);\n    let inner = if k > 100 { Some(1) } else { None };\n    walk(a, inner, 0);\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none",
   "output_k42": "other\nnone",
   "output_other_k": "other\nother",
   "runner": "NEW",
   "timing": "ifc2-report ~1.2s under load average 33",
   "root_cause": "eval.rs pattern_decision (current tree ~3009): `if v.top { return if pattern_reads_content(pat) { v.deep() } else { v.lab } }` returns before looking at the value's concrete parts. A value can be top joined with concrete kinds: here the part folded at SUMMARY_DEPTH in the recursive summary context is joined with Some(inner) under a public condition. The sub-pattern None on the concrete Some payload reads inner.lab (SEC), and that label is never joined.",
   "explanation": "The first call makes walk recursive, so the second call goes to a summary context whose arguments are widened at depth 4. a[0][0][0][0][0] becomes top, and v = top \u2294 Some(inner) with v.lab PUB. The arm decision is PUB, so println in the arm is accepted, although which arm runs is decided by the secret tag of inner. Control: with `let v = Some(inner);` IFC v2 reports ANUBIS_IMPLICIT_FLOW. List and struct sub-patterns on top \u2294 concrete are skipped the same way. The top case should join v.lab (or deep) with the normal per-kind decision instead of returning early."
  },
  {
   "id": "r2v_10_join_enum_different_field_sets_drops_names",
   "kind": "leak",
   "program": "enum E {\n    V { a: i64, b: secret<i64> },\n}\nfn main() {\n    let sv = 42;\n    let c = len(args()) > 100;\n    let e = if c { E::V { a: 1 } } else { E::V { b: sv } };\n    println(e.b);\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none",
   "output_k42": "42",
   "output_other_k": "123456",
   "runner": "NEW",
   "timing": "ifc2-report 0.02s",
   "root_cause": "value.rs join_enum (current tree ~1601): when names differ it keeps `names` of whichever side has more names (a's on a tie) and makes every position `any`. A name that only the other side has is dropped. field_read (eval.rs) then finds no position for b and returns Int(0) as scalar PUB, and pattern named bindings and named sub-pattern decisions skip it too.",
   "explanation": "This is an explicit leak of a declared-secret variant field. Control: `let e = E::V { b: sv }; println(e.b);` is refused by IFC v2, and the join launders it. With `let s: secret<i64> = 42` and `E::V { b: s }` (probe v18) IFC v2 also accepts, the lanes refuse, and a public twin prints 42 vs 123456. The task notes say struct-variant payloads join by name. The pin does not: it neither joins by name nor keeps the union of names. The same code causes over-refusal 16."
  },
  {
   "id": "r2v_11_remove_trap_message_prints_key_and_index",
   "kind": "leak",
   "program": "fn main() {\n    let k: secret<i64> = 42;\n    let mut m = {\"a\": 1};\n    println(\"start\");\n    let v = remove(m, k);\n    println(\"done\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "new_code": "none",
   "output_k42": "start\nthread '<unnamed>' panicked at src/main.rs:952:25:\nANUBIS_MISSING_KEY: key `42` is not present in the map (use get(m, k, default) for optional access)\nError: run failed: exit_code=Some(101)",
   "output_other_k": "start\nthread '<unnamed>' panicked at src/main.rs:952:25:\nANUBIS_MISSING_KEY: key `123456` is not present in the map (use get(m, k, default) for optional access)\nError: run failed: exit_code=Some(101)",
   "runner": "NEW (its runtime has the round-1 trap redaction)",
   "timing": "ifc2-report 0.02s",
   "root_cause": "run.rs anubis_remove (~2385-2405) was missed by the round-1 redaction of trap messages. The map arm panics with \"key `{}` is not present\" (the key's display text). The list arm panics with \"index {} is out of bounds for a list of length {}\". IFC v2 models remove only as a value operation, not as an egress of its key, index or length.",
   "explanation": "This is the r1_032/r1_059 family: the redaction covered index_get but not remove. With `let mut xs = [1, 2, 3]; let v = remove(xs, k);` (probe v11) NEW prints \"index 42 is out of bounds for a list of length 3\" vs \"index 123456 ...\", and IFC v2 and the lanes accept. The list length is printed too. Whether the trap happens is a termination channel (out of scope), but the message content is explicit data. A scan of run.rs found no other operand-bearing trap messages outside egress builtins (send, connect and http print addr/url, but those are already egress)."
  },
  {
   "id": "r2v_12_state_merge_push_on_both_branches",
   "kind": "overrefusal",
   "program": "fn audit(log, ok) {\n    if ok {\n        push(log, \"granted\");\n    } else {\n        push(log, \"denied\");\n    }\n    log\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let mut log = [];\n    log = audit(log, k > 100);\n    println(\"entries: \" + str(len(log)));\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "new_code": "ANUBIS_SECRET_EXFILTRATION: [ifc2] in `main`: secret data reaches `println`",
   "output_k42": "entries: 1",
   "output_other_k": "entries: 1",
   "runner": "LANES (anubis-ord3x4l run --no-verify); NEW refuses",
   "timing": "ifc2-report 0.01s",
   "root_cause": "builtins.rs mutate push (out.lab \u2294 pc) raises the list's length label at the write, and eval.rs merge/St::join then joins the two branch states. The same-shape rule of V::select (which fixed r1_035/r1_064 for if-expressions) is never applied to state merges, so equal shape changes on both sides of a secret branch still leave the shape secret.",
   "explanation": "This is a realistic audit-log pattern: the length is 1 on every path, and IFC v2 newly refuses a program the lanes accept. Findings 13 and 14 are the same root cause through a map key added on both branches and a whole-variable reassignment. A fix would compute the merged value of a variable written under the branch condition with select-like logic from the unraised per-branch values, or raise only the parts whose shape differs."
  },
  {
   "id": "r2v_13_state_merge_same_key_added_on_both_branches",
   "kind": "overrefusal",
   "program": "fn status(c) {\n    let mut out = {\"user\": \"bob\"};\n    if c {\n        out[\"status\"] = \"ok\";\n    } else {\n        out[\"status\"] = \"denied\";\n    }\n    out\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let r = status(k > 100);\n    println(keys(r));\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "new_code": "ANUBIS_SECRET_EXFILTRATION: [ifc2] in `main`: secret data reaches `println`",
   "output_k42": "[user, status]",
   "output_other_k": "[user, status]",
   "runner": "LANES",
   "timing": "ifc2-report 0.02s",
   "root_cause": "eval.rs set_path, map literal-key arm: a key not yet known raises out.lab by pc. Both branches add \"status\", each raising lab by the secret pc, and the merge joins them into a SEC shape. keys() then carries SEC.",
   "explanation": "The same root cause as finding 12 (state merge after a secret if), reached through presence tracking. The key set is {user, status} in insertion order on every path."
  },
  {
   "id": "r2v_14_state_merge_reassign_same_length",
   "kind": "overrefusal",
   "program": "fn pick(c) {\n    let mut xs = [0, 0];\n    if c {\n        xs = [1, 2];\n    } else {\n        xs = [2, 1];\n    }\n    xs\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let xs = pick(k > 100);\n    for x in xs {\n        println(\"tick\");\n    }\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "new_code": "ANUBIS_IMPLICIT_FLOW: [ifc2] in `main`: `println` runs under a condition a secret decides",
   "output_k42": "tick\ntick",
   "output_other_k": "tick\ntick",
   "runner": "LANES",
   "timing": "ifc2-report 0.03s",
   "root_cause": "eval.rs Interp::assign, Expr::Var arm: v.raise(st.pc()) raises the whole value, including the list's length label, then St::join merges. This is the statement-assignment variant of r1_035 (`let xs = if k > 100 { [1, 2] } else { [2, 1] }`), which select now accepts.",
   "explanation": "The if-expression form is accepted, but the equivalent if-statement with an assignment on both branches is refused. The loop runs twice on every path."
  },
  {
   "id": "r2v_15_early_return_same_shape_raised",
   "kind": "overrefusal",
   "program": "fn tags(ok) {\n    if ok {\n        return [\"granted\", \"full\"];\n    }\n    [\"denied\", \"none\"]\n}\nfn main() {\n    let k: secret<i64> = 42;\n    let t = tags(k > 100);\n    for x in t {\n        println(\"tag slot\");\n    }\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "new_code": "ANUBIS_IMPLICIT_FLOW: [ifc2] in `main`: `println` runs under a condition a secret decides",
   "output_k42": "tag slot\ntag slot",
   "output_other_k": "tag slot\ntag slot",
   "runner": "LANES",
   "timing": "ifc2-report 0.03s",
   "root_cause": "eval.rs do_return (current tree ~2096): f.ret \u2294 v.raise(pc) raises the returned list's length label by the secret pc. analyze_body then joins frame.ret with the tail value (a join, not a select), so the result's lab is SEC although both exits return a two-element list.",
   "explanation": "Early return is a very common style. It is the return-path member of the r1_064 family: the same function written as `if ok { [..] } else { [..] }` is accepted."
  },
  {
   "id": "r2v_16_enum_variant_literal_order_join_over_refusal",
   "kind": "overrefusal",
   "program": "enum Msg {\n    Login { user: str, pin: secret<i64> },\n}\nfn make(c, u, p) {\n    if c {\n        Msg::Login { user: u, pin: p }\n    } else {\n        Msg::Login { pin: p, user: u }\n    }\n}\nfn main() {\n    let k = 42;\n    let c = len(args()) > 100;\n    let m = make(c, \"bob\", k);\n    match m {\n        Msg::Login { user, pin } => println(user),\n    }\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "new_code": "ANUBIS_SECRET_EXFILTRATION: [ifc2] in `main`: secret data reaches `println`",
   "output_k42": "bob",
   "output_other_k": "bob",
   "runner": "LANES",
   "timing": "ifc2-report 0.02s",
   "root_cause": "value.rs join_enum: for struct variants whose literal field orders differ, every position becomes the join of all fields (`any`). The public field user therefore carries pin's SEC label after a join decided publicly. The round-2 notes say these payloads join by name, but pin v8e does not.",
   "explanation": "Writing a struct-variant literal's fields in different orders in two branches is ordinary code. A by-name join (union of names, each joined by name) fixes this and leak 10 together."
  },
  {
   "id": "r2v_17_computed_key_rewrite_of_existing_keys",
   "kind": "overrefusal",
   "program": "fn bump(m, c) {\n    for k in keys(m) {\n        if c {\n            m[k] = 1;\n        }\n    }\n    m\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let m = {\"a\": 0, \"b\": 0};\n    let r = bump(m, s > 100);\n    println(keys(r));\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "new_code": "ANUBIS_SECRET_EXFILTRATION: [ifc2] in `main`: secret data reaches `println`",
   "output_k42": "[a, b]",
   "output_other_k": "[a, b]",
   "runner": "LANES",
   "timing": "ifc2-report 0.02s",
   "root_cause": "eval.rs set_path, computed-key map arm: any write through a computed key joins pc into out.lab, because the key may be new. IFC does not know that k is drawn from keys(m) of the same map, so it cannot tell this is an overwrite.",
   "explanation": "This is a precision limit, not a soundness bug: updating existing entries while iterating the map's own keys under a secret condition. The key set never changes. It needs a fact such as \"the key is drawn from this map's keys\" (for-in over keys(m) or over m itself) to avoid raising the shape."
  },
  {
   "id": "r2v_18_shared_list_dag_exponential_memory",
   "kind": "robustness",
   "program": "fn main() {\n    let x0 = [1];\n    let x1 = [x0, x0];\n    let x2 = [x1, x1];\n    let x3 = [x2, x2];\n    let x4 = [x3, x3];\n    let x5 = [x4, x4];\n    let x6 = [x5, x5];\n    let x7 = [x6, x6];\n    let x8 = [x7, x7];\n    let x9 = [x8, x8];\n    let x10 = [x9, x9];\n    let x11 = [x10, x10];\n    let x12 = [x11, x11];\n    let x13 = [x12, x12];\n    let x14 = [x13, x13];\n    let x15 = [x14, x14];\n    let x16 = [x15, x15];\n    let x17 = [x16, x16];\n    let x18 = [x17, x17];\n    let x19 = [x18, x18];\n    let x20 = [x19, x19];\n    let x21 = [x20, x20];\n    let x22 = [x21, x21];\n    let mut acc = [];\n    let mut i = 0;\n    while i < 3 {\n        acc = [acc, len(x22)];\n        i = i + 1;\n    }\n    println(len(acc));\n}\n",
   "new_rc": 137,
   "lanes_rc": 0,
   "new_code": "killed by the 1500M memory cap (no ANUBIS_IFC2_LIMIT)",
   "output_k42": "2 (LANES runner; lanes check passes in 0.04s)",
   "output_other_k": "2 (no secret in the program; runtime values share Rc nodes, so it is O(n))",
   "runner": "LANES run; NEW's own `run` stops with ANUBIS_ANALYSIS_LIMIT (memory budget)",
   "timing": "ifc2-report OOM-killed at 1.5G after 6.6s. Call-boundary variant (fn size_of(x) { len(x) } ... println(size_of(xN))): n=16 0.14s, n=18 0.62s, n=20 2.69s, n=22 OOM-killed at 3.5s. Without a call or widening loop: 0.03s.",
   "root_cause": "value.rs widen_from / widen_at_limit, size, collapse_items, deep, shape_all and the CallKey hash/eq recurse over the value as a tree. Nothing is memoized on Rc identity, so a DAG of shared nodes (each x_i holds x_(i-1) twice) costs 2^n. ptr_eq short-circuits only join and eq. Widening at a loop head (after WIDEN_AFTER rounds) or of call arguments walks every path. The work budget is checked only every 64 ticks, and a single widen call allocates without charging enough to stop before memory runs out.",
   "explanation": "This is the r1_104/r1_105 family: those fixes removed the items+all doubling, but sharing through Rc still expands exponentially. The program is small and runs instantly. A value merely in scope of a loop that needs widening triggers it (a function call taking the value does too). It fails by OOM kill, not by ANUBIS_IFC2_LIMIT, so the result is a crash, not a fail-closed refusal. A perfect tree built bottom-up by reusing subtrees has exactly this shape."
  },
  {
   "id": "r2ctl_L01_int_literal_patterns_collide_in_f64",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    let x = 9007199254740992.0;\n    match x {\n        9007199254740992 if s > 100 => {\n            let z = 0;\n        }\n        9007199254740993 => {\n            println(\"second arm\");\n        }\n        _ => {\n            let z = 1;\n        }\n    }\n    println(\"done\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "second arm\ndone",
   "output_other_k": "done   (secret 123456)",
   "new_code": "none (no information flow found)",
   "runner": "~/.cache/anubis-item21/pins/anubis-ifc2-v8e run --no-verify",
   "root_cause": "eval.rs:2941 patterns_disjoint (P::Literal(a), P::Literal(b)): two integer literals that parse to different i64 values are declared disjoint. The runtime (run.rs pattern_test_and_binds, Pattern::Literal) matches an integer literal i against a Float scrutinee with `*x == i as f64`, so two distinct integers above 2^53 that round to the same f64 (9007199254740992 and 9007199254740993) both match Float(9007199254740992.0). Arm 2's pc then omits arm 1's secret guard.",
   "explanation": "At runtime the second arm runs exactly when the guard `s > 100` of the first arm fails, because both integer patterns match the float scrutinee. IFC v2 treats the two literal patterns as disjoint, so arm 2's println runs under a public pc and nothing is reported. The same holds for nested sub-patterns (Some(9007199254740992) vs Some(9007199254740993)) and for any pair of i64 literals whose f64 casts are equal. Integer-vs-integer disjointness is only sound when the scrutinee cannot be a Float, or when the two values stay distinct after the `as f64` cast."
  },
  {
   "id": "r2ctl_L02_select_ignores_struct_field_order",
   "kind": "leak",
   "program": "struct Cfg {\n    a: list<i64>,\n    b: list<i64>,\n}\n\nfn main() {\n    let s: secret<i64> = 42;\n    let c = match s > 100 {\n        true => Cfg { a: [], b: [0] },\n        _ => Cfg { b: [0], a: [] },\n    };\n    let first = c[0];\n    println(len(first));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "1",
   "output_other_k": "0   (secret 123456)",
   "new_code": "none",
   "runner": "anubis-ifc2-v8e run --no-verify",
   "root_cause": "value.rs:747 V::select (struct branch) treats two structs as the same shape when they have the same type, the same field-name set (`fa.keys().eq(fb.keys())` on a BTreeMap, so order is lost) and equal maybe_fields. It keeps lab public and selects field by field. At runtime a struct is an ordered Vec<(name, value)> in literal (or insertion) order (run.rs:5234-5274 StructLiteral, set_at pushes new fields). The order is observable through a positional index (`c[0]` gives the first field, eval.rs index_read joins all fields and raises only by b.lab), through display (println of the struct), and through ordering comparisons (anubis_value_cmp compares display strings).",
   "explanation": "The secret chooses between two Cfg literals that list the same fields in different orders. The select keeps the struct's shape public, and the list fields `[]`/`[0]` keep public length labels, so `len(c[0])` is public for IFC v2. The runtime returns the first field in literal order, so the printed length reveals the secret. The r1 fix for enum struct-variants (join by name when orders differ) was not carried over to structs in select."
  },
  {
   "id": "r2ctl_L03_select_ignores_map_key_order",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    let x = {\"a\": [], \"b\": []};\n    let y = {\"b\": [], \"a\": []};\n    let m = match s > 100 {\n        true => x,\n        _ => y,\n    };\n    let ks = keys(m);\n    println(ks[0]);\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "b",
   "output_other_k": "a   (secret 123456)",
   "new_code": "none",
   "runner": "anubis-ifc2-v8e run --no-verify",
   "root_cause": "value.rs:716-717 V::select (map branch) treats maps as the same shape when `ma.known.keys().eq(mb.known.keys())` (BTreeMap, sorted) and maybe/other/klab agree, so lab and klab stay public. The runtime map is a Vec of (key, value) pairs in insertion order (anubis_map_lit, index_set/set_at push new keys, anubis_merge appends). keys(), values(), entries(), `for k in m` and display all follow that order. builtins.rs keys() returns list_of(scalar(s), s) with s = lab \u2294 klab, which is public here.",
   "explanation": "Two maps with the same key set in different insertion orders are chosen by a secret. IFC v2's select says the shape is identical, but `keys(m)[0]` (or iterating or printing the map) returns the first-inserted key, which differs. This is the same root cause as L02, in the map branch of select."
  },
  {
   "id": "r2ctl_L04_select_equal_maybe_fields_not_same_shape",
   "kind": "leak",
   "program": "struct P {\n    a: list<i64>,\n    b: list<i64>,\n}\n\nfn main() {\n    let s: secret<i64> = 42;\n    let k = len(args()) < 100;\n    let mut p1 = P { a: [] };\n    let mut p2 = P { a: [] };\n    if k {\n        p1.b = [];\n    } else {\n        p2.b = [];\n    }\n    let c = match s > 100 {\n        true => p1,\n        _ => p2,\n    };\n    println(len(c));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "1",
   "output_other_k": "2   (secret 123456)",
   "new_code": "none",
   "runner": "anubis-ifc2-v8e run --no-verify",
   "root_cause": "value.rs:747 V::select requires `a.maybe_fields == b.maybe_fields` and then treats the structs as the same shape. A maybe field means only 'present or absent, decided by whatever lab says'. Two alternatives that both have b as maybe can differ in whether b is present: here a public condition put b in p1 and not in p2. Selecting between them under a secret makes the field count secret, but lab stays public, and len_label reads only lab.",
   "explanation": "The public condition k makes b present in exactly one of p1 and p2, so both carry maybe_fields {b} with a public lab. The secret selects p1 or p2, and len(c) (field count) prints 2 or 1. IFC v2 keeps the shape public because the maybe sets compare equal. A sound select must raise the shape by `by` whenever either side has a maybe field or key."
  },
  {
   "id": "r2ctl_L05_select_equal_maybe_keys_map",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    let k = len(args()) < 100;\n    let mut m1 = {\"id\": []};\n    let mut m2 = {\"id\": []};\n    if k {\n        m1[\"extra\"] = [];\n    } else {\n        m2[\"extra\"] = [];\n    }\n    let m = match s > 100 {\n        true => m1,\n        _ => m2,\n    };\n    println(len(m));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "1",
   "output_other_k": "2   (secret 123456)",
   "new_code": "none",
   "runner": "anubis-ifc2-v8e run --no-verify",
   "root_cause": "value.rs:716-717 V::select map branch: `ma.maybe == mb.maybe` is taken as 'same key set', but a maybe key may be present in one alternative and absent in the other. The selected map keeps lab and klab public, and len, truthiness, has_key, keys and iteration count all read only lab \u2294 klab.",
   "explanation": "This is the map counterpart of L04, in a separate branch of V::select. A public condition inserts \"extra\" into exactly one of m1 and m2, and the secret picks which map survives, so len(m) reveals the secret. `if m`, has_key(m, \"extra\") and `for k in m` leak it the same way."
  },
  {
   "id": "r2ctl_L06_method_narrowing_drops_maybe_fields",
   "kind": "leak",
   "program": "struct P {\n    a: i64,\n    b: i64,\n}\n\nimpl P {\n    fn upd(self) -> P {\n        let s: secret<i64> = 42;\n        let mut q = self;\n        for i in [1] {\n            if s > 100 {\n                continue;\n            }\n            q.b = 7;\n        }\n        q\n    }\n}\n\nfn main() {\n    let mut p = P { a: 1 };\n    if false {\n        p.b = 2;\n    }\n    let r = p.upd();\n    println(len(r));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "2",
   "output_other_k": "1   (secret 123456)",
   "new_code": "none",
   "runner": "anubis-ifc2-v8e run --no-verify",
   "root_cause": "eval.rs:2023-2038 method_call builds the per-type `narrowed` receiver by copying `recv.structs.get(ty)` (and the enums) into a fresh V::bottom(). recv.maybe_fields is not copied. Inside the method, set_path (Field) then sees field b as definitely present (`!fields.contains_key(f) || maybe` is false), so a write under a secret pc does not raise the struct's shape, even though at runtime the write appends a new field.",
   "explanation": "`p` carries maybe_fields {b} with a public lab, because b was added only under a public condition (false at runtime). The method writes q.b under a pc lifted by a secret continue; at runtime this adds the field or not, so len(r) is 2 or 1. Control: the same body as a free function `fn upd(q0: P) -> P` is refused (ANUBIS_SECRET_EXFILTRATION), so the method dispatch's narrowing is what loses the information."
  },
  {
   "id": "r2ctl_L07_whilelet_scrutinee_continue_does_not_lift_next_header",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    let mut n = 0;\n    while let Some(x) = if true { println(\"poll\"); n = n + 1; if s > 100 && n < 3 { continue } ; None } else { None } {\n    }\n    println(\"done\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "poll\ndone",
   "output_other_k": "poll\npoll\npoll\ndone   (secret 123456)",
   "new_code": "none",
   "runner": "anubis-ifc2-v8e run --no-verify",
   "root_cause": "eval.rs:1583 and 1610 exec_loop_solve: `let mut lift = hx.brk_at...` takes only a break in the header, and `next.lift_loop = head.lift_loop.join(lift).join(clab)` adds only that and the pattern decision. A while-let is lowered as `loop { let t = scrutinee; if test { body } else { break } }` (run.rs Stmt::WhileLet), so a continue inside the scrutinee skips the pattern test, which is the loop's only exit. Whether another header runs therefore depends on the continue's condition. hx.cnt_at is never added to the next iteration's lift_loop. The pattern decision on the scrutinee value (`None`, a public literal) does not carry it either.",
   "explanation": "The number of header evaluations (and so of 'poll' prints, and the final value of n) is 1 or 3 depending on the secret. IFC v2 checks only the first header under a public pc, and later headers under head.lift_loop \u2294 brk \u2294 clab, all public. Control: replacing `continue` with `break` in the same position is refused (ANUBIS_IMPLICIT_FLOW). The new rule 'a continue does not lift later iterations' is right for bodies but wrong for a continue in a while-let scrutinee."
  },
  {
   "id": "r2ctl_L08_panic_under_secret_pc_not_egress",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    if s > 100 {\n        panic(\"limit exceeded\");\n    }\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "(no output, rc 0)",
   "output_other_k": "stderr: thread '<unnamed>' panicked at src/main.rs:1095:52: / ANUBIS_PANIC: limit exceeded / note: run with `RUST_BACKTRACE=1` ... / Error: run failed: exit_code=Some(101)   (secret 123456)",
   "new_code": "none",
   "runner": "anubis-ifc2-v8e run --no-verify",
   "root_cause": "builtins.rs:139-148 \"panic\": the message is reported only when it is secret (data.secret()). Then end_program lifts the continuation. The program counter at the panic is never checked, so panic is not treated as an egress under D1, although IFC v2 declares the panic message to be egress and the message is written to stderr.",
   "explanation": "A public panic message written under a secret condition discloses the condition (stderr and exit status 101 versus a clean exit). IFC v2 only lifts code after the panic, and with nothing after it, it reports nothing. D1 refuses an egress run under a secret condition; panic is explicit and modelled (not a trap), so it should get the same p.pc().secret() check as the other egress points."
  },
  {
   "id": "r2ctl_L09_exit_status_under_secret_pc_not_egress",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    if s > 100 {\n        exit(3);\n    }\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "(no output, rc 0)",
   "output_other_k": "Error: run failed: exit_code=Some(3)   (process exit status 3; secret 123456)",
   "new_code": "none",
   "runner": "anubis-ifc2-v8e run --no-verify",
   "root_cause": "builtins.rs:150-161 \"exit\": only a secret exit code is reported. exit(code) under a secret pc only ends the path (end_program lifts the continuation); the pc is not checked. IFC v2 names the exit status as an egress, so an exit with a public but non-default status under a secret condition is an egress under a secret condition (D1).",
   "explanation": "This has the same root cause as L08, for exit. The exit status (3 or 0), and the runner's stderr line, reveal the secret. It works through callees as well: `fn fail() { exit(3) }` called under a secret condition only lifts the caller's continuation and is otherwise accepted."
  },
  {
   "id": "r2ctl_O01_same_shape_assignment_under_secret_raises_tag",
   "kind": "overrefusal",
   "program": "struct Reading {\n    level: i64,\n}\n\nimpl Reading {\n    fn describe(self) {\n        println(\"reading\");\n    }\n}\n\nfn main() {\n    let s: secret<i64> = 42;\n    let mut last = Some(0);\n    let mut r = Reading { level: 0 };\n    for i in [1, 2, 3] {\n        if s > 100 {\n            continue;\n        }\n        last = Some(i);\n        r = Reading { level: i };\n    }\n    match last {\n        Some(_) => println(\"have a reading\"),\n        None => println(\"no reading\"),\n    }\n    r.describe();\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "have a reading\nreading",
   "output_other_k": "have a reading\nreading   (secret 123456; run on LANES runner anubis-ord3x4l, since NEW refuses)",
   "new_code": "ANUBIS_IMPLICIT_FLOW (`println` runs under a condition a secret decides)",
   "runner": "~/.cache/anubis-item21/pins/anubis-ord3x4l run --no-verify",
   "root_cause": "eval.rs:1298 (Stmt::Assign to a variable: `v.raise(st.pc())`) and 1182 (Let) raise every label of the value by the pc, including shape labels (enum tag, struct type, list length). The state merge (St::join / join_env) then keeps the raised tag, although the overwritten value and the new one have the same variant. Round 1 (r1_064) made V::select raise only the nodes whose shape differs. Assignment, and the merge after it, still raise everything.",
   "explanation": "`last` is Some on every path, but it is reassigned (to another Some) after a secret-dependent continue, so its tag label becomes secret and the match arms (which only test the tag) are treated as secret-decided. LANES accept the program. Its output is identical for both secrets, so IFC v2 alone would newly refuse it. The same pattern affects dispatch_label, truthiness and for-loop counts of reassigned same-shape values."
  },
  {
   "id": "r2ctl_O02_distinct_float_literal_patterns_not_disjoint",
   "kind": "overrefusal",
   "program": "fn main() {\n    let pin: secret<i64> = 42;\n    let mut unlocked: secret<bool> = false;\n    let rates = [0.5, 1.5, 2.5];\n    for rate in rates {\n        match rate {\n            0.5 if pin == 1234 => {\n                unlocked = true;\n            }\n            1.5 => println(\"standard tier\"),\n            2.5 => println(\"premium tier\"),\n            _ => 0,\n        }\n    }\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "standard tier\npremium tier",
   "output_other_k": "standard tier\npremium tier   (secret 1234, which makes the guard true; run on LANES runner)",
   "new_code": "ANUBIS_IMPLICIT_FLOW",
   "runner": "anubis-ord3x4l run --no-verify",
   "root_cause": "eval.rs:2941 patterns_disjoint (Literal, Literal) reports disjoint only when both texts parse as i64; any float literal falls to the bool check and is 'not disjoint'. At runtime (run.rs Pattern::Literal) two float literals with different f64 values cannot both match one value, so arm `1.5` never depends on arm `0.5`'s guard. Control: the same program with integer literals 5/15/25 is accepted.",
   "explanation": "The arms after the secret-guarded `0.5` arm are decided only by the public rate, but IFC v2 folds the guard into their pc because float literals are never treated as disjoint. LANES accept and the output is identical. Mixed-kind literal pairs (a string literal vs a numeric literal, bool vs int) are also disjoint at runtime (type-exact matching) but are treated as overlapping, with the same effect."
  },
  {
   "id": "r2ctl_O03_early_return_same_shape_raises_tag",
   "kind": "overrefusal",
   "program": "enum Tier {\n    Basic(i64),\n    Gold(i64),\n}\n\nfn discount(level: secret<i64>) -> Option<secret<i64>> {\n    if level > 100 {\n        return Some(20);\n    }\n    Some(5)\n}\n\nfn main() {\n    let level: secret<i64> = 42;\n    match discount(level) {\n        Some(_) => println(\"discount applied\"),\n        None => println(\"no discount\"),\n    }\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "discount applied",
   "output_other_k": "discount applied   (secret 123456; run on LANES runner)",
   "new_code": "ANUBIS_IMPLICIT_FLOW",
   "runner": "anubis-ord3x4l run --no-verify",
   "root_cause": "eval.rs:1994-1999 do_return: `f.ret = f.ret.join(&v.raise(pc.control()))` raises the whole returned value, the Option tag included, by the pc of the return. It is then joined with the tail value (analyze_body `frame.ret.join(&tail)`), so a function that returns Some on every path yields a secret tag. This is the return-site counterpart of O01; round 1 fixed only V::select (if/match values).",
   "explanation": "Both exits of discount return Some(_), so the caller's match on the tag always takes the Some arm. IFC v2 treats the tag as secret and refuses the println. LANES accept and the output is identical."
  },
  {
   "id": "r2ctl_O04_select_of_compositions_raises_every_stage",
   "kind": "overrefusal",
   "program": "fn audit(x: i64) -> i64 {\n    println(\"audit step\");\n    x\n}\n\nfn cheap(x: i64) -> i64 {\n    x + 1\n}\n\nfn costly(x: i64) -> i64 {\n    x * 2\n}\n\nfn main() {\n    let tier: secret<i64> = 42;\n    let pipeline = match tier > 100 {\n        true => compose(audit, costly),\n        _ => compose(audit, cheap),\n    };\n    let r = pipeline(10);\n    println(\"pipeline done\");\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "audit step\npipeline done",
   "output_other_k": "audit step\npipeline done   (secret 123456; run on LANES runner)",
   "new_code": "ANUBIS_IMPLICIT_FLOW (in `audit`)",
   "runner": "anubis-ord3x4l run --no-verify",
   "root_cause": "value.rs:796-797 V::select handles function values only when both sides are `plain` (no composed pairs, no chain). Two compositions fall through to `a.join(b).raise(by)`, so the composition's own lab becomes secret, and eval.rs apply runs every composed function, audit included, under flab = secret. The choice only decides the inner stage; the outer stage `audit` is the same function on both sides and runs either way.",
   "explanation": "The secret selects which inner stage runs, but `audit` (which prints a constant) runs in both pipelines. IFC v2 raises the whole composition and reports audit's println. LANES accept and the output is identical. Selecting pair-wise (compose(select(g1,g2), select(h1,h2))) when both sides are single compositions would keep audit public."
  },
  {
   "id": "r2ctl_R01_shift_register_hits_loop_limit",
   "kind": "robustness",
   "program": "(generated; 64 locals) fn main() {\n    let mut d1 = 0;\n    let mut d2 = 0;\n    ... one `let mut dK = 0;` for every K = 1..64 ...\n    let key: secret<i64> = 7;\n    for r in 0..128 {\n        let t = d64;\n        d64 = d63;\n        d63 = d62;\n        ... one `dK = dK-1;` line for every K = 64 down to 2 ...\n        d1 = t + key * r;\n    }\n    println(declassify(d1 + d64, \"digest\", \"published\"));\n}\n(Full file with every line written out: ~/.cache/anubis-review-esc/ifc2-r2/control/p/r08_delay64.anb, generated by the python snippet in that directory. The same program with 59 stages is accepted; with 64 it hits the limit.)",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "1778 (secret 7)",
   "output_other_k": "2032 (secret 8); the output differs only through the intended declassify",
   "new_code": "ANUBIS_IFC2_LIMIT (a loop did not settle)",
   "runner": "anubis-ord3x4l run --no-verify (NEW refuses)",
   "timing": "about 26 ms; the limit is the round count, not time",
   "root_cause": "eval.rs:34 LOOP_LIMIT = 60 and 1542 exec_loop_solve. In a shift register the secret label moves one variable per iteration, so the loop needs n+1 rounds to settle. Neither widening (widen after 2 rounds, coarse SUMMARY_DEPTH widening after COARSE_AFTER = 12) joins labels across variables, so the fixpoint is not accelerated, and with 60 or more chained variables it gives up. The threshold measured: 59 stages settle, 64 fail.",
   "explanation": "This is a straightforward delay line or FIR-style loop (the kind unrolled or generated DSP and crypto code produces) with nothing unusual. LANES accept it and it runs, but IFC v2 refuses it with ANUBIS_IFC2_LIMIT. Past COARSE_AFTER rounds, the widening could join labels, for example raising every variable of the loop state to the join of the state's labels, so that label propagation along long chains settles."
  },
  {
   "id": "r2p_01_select_ignores_runtime_order",
   "kind": "leak",
   "program": "fn pick(c) {\n    let m = if c { {\"a\": 1, \"b\": 2} } else { {\"b\": 2, \"a\": 1} };\n    m\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let m = pick(s > 100);\n    println(keys(m));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "[b, a]",
   "output_other_k": "[a, b]   (k=123456)",
   "root_cause": "value.rs:678 V::select (v8e, git show d6801d4d). Map arm value.rs:715-735: same_keys compares ma.known.keys() of a BTreeMap and returns lab = a.lab \u2294 b.lab without raising. Struct arm value.rs:745-760: fa.keys().eq(fb.keys()) over a BTreeMap. The runtime map is an insertion-ordered Vec (run.rs:2883 anubis_map_lit; display_string 1622; map_keys; anubis_iter 2896; values/entries/first/last), and a runtime struct keeps its literal field order (run.rs:5234 StructLiteral; display_string 1616; index_get by position). Two maps with the same keys in a different order, or two structs with the same fields in a different literal order, are different values; select treats them as the same shape.",
   "explanation": "The secret picks between two maps that differ only in key order; keys(), print, str, values, entries, for-in, first/last all expose the order. Struct variant (same root cause), also rc=0 on NEW and LANES, run on NEW: struct P { a: list<i64>, b: list<i64> }\nfn pick(c) {\n    let p = if c { P { a: [], b: [] } } else { P { b: [], a: [] } };\n    p\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let p = pick(s > 100);\n    println(p);\n}\nprints \"P { b: [], a: [] }\" (42) vs \"P { a: [], b: [] }\" (123456); p[0] also differs. The non-laundered form (if-value in main) is also rc=0 on IFC v2 but the lanes refuse it.",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_02_select_equal_maybe_sets_same_shape",
   "kind": "leak",
   "program": "fn mk(present) {\n    let m = {\"x\": 0};\n    if present {\n        m[\"a\"] = 1;\n    }\n    m\n}\nfn pick(c, m1, m2) {\n    let m = if c { m1 } else { m2 };\n    m\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let n = len(args());\n    let m1 = mk(n == 0);\n    let m2 = mk(n == 5);\n    let m = pick(s > 100, m1, m2);\n    println(has_key(m, \"a\"));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "false",
   "output_other_k": "true   (k=123456)",
   "root_cause": "value.rs:716-721 (v8e) select's same_keys accepts two maps whose maybe sets are equal (ma.maybe == mb.maybe) as the same key set; value.rs:747 accepts two structs with equal maybe_fields. A key or field that is 'maybe' on both sides can be present in one alternative and absent in the other at runtime (here m1 has \"a\", m2 lacks it; both are the same abstract value from one call context), so which alternative the secret picks decides has_key/len/truth/keys/iteration count, but the result's lab stays public.",
   "explanation": "Presence tracking made maybe-keys part of the 'same shape' test; equality of the maybe sets is not equality of presence. Select must raise the container's lab when either side has a non-empty maybe set (or maybe_fields). Struct variant, rc=0 on NEW and LANES: struct P { x: i64 }\nfn mk(present) {\n    let p = P { x: 0 };\n    if present {\n        p.a = 1;\n    }\n    p\n}\nfn pick(c, p1, p2) {\n    let p = if c { p1 } else { p2 };\n    p\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let n = len(args());\n    let p1 = mk(n == 0);\n    let p2 = mk(n == 5);\n    let p = pick(s > 100, p1, p2);\n    println(len(p));\n}\nprints 1 (42) vs 2 (123456).",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_03_set_path_nested_write_marks_missing_key_present",
   "kind": "leak",
   "program": "fn upd(m, c) {\n    if c {\n        m.f = 7;\n    }\n    m\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let mut m = {\"a\": 1};\n    m.f.g = 5;\n    let r = upd(m, s > 100);\n    println(len(r));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "1",
   "output_other_k": "2   (k=123456)",
   "root_cause": "eval.rs:1345 set_path (v8e). Seg::Field on a struct (1352-1375) inserts field f and clears maybe_fields, and on a map (1377-1385) inserts known[f] and clears maybe (1383-1384), whatever `rest` is; the Seg::Index literal-key map branch (about 1433-1438) does the same. The runtime set_at (run.rs:1981-2020) adds a missing field/key only when rest.is_empty(); `m.f.g = 5` on a map without f does nothing. IFC then believes f is definitely present, so the later `m.f = 7` under the secret pc does not raise the map's shape label (the presence test at 1378 sees a known, non-maybe key).",
   "explanation": "A nested place write through a missing key is a runtime no-op but makes the key 'present' in IFC v2; any later secret-conditioned first write of that key then changes len/keys/has_key without a label. Variants verified (all NEW rc=0, LANES rc=0, outputs differ): struct field `struct P { a: i64 } ... let mut p = P { a: 1 }; p.f.g = 5; let r = upd(p, s > 100); println(len(r));` with upd doing `p.f = 7` prints 1 vs 2; index form `m[\"f\"][\"g\"] = 5;` then `m[\"f\"] = 7` under the secret prints [a] vs [a, f]. Fix: when rest is non-empty and the key/field is missing or maybe, recurse without adding it (or keep it maybe).",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_04_merge_drops_first_map_value_for_maybe_key",
   "kind": "leak",
   "program": "fn pick(c) {\n    let v = if c { 7 } else { 3 };\n    v\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let a = {\"k\": pick(s > 100)};\n    let mut b = {\"x\": 0};\n    if len(args()) > 5 {\n        b[\"k\"] = 1;\n    }\n    let m = merge(a, b);\n    println(m[\"k\"]);\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "3",
   "output_other_k": "7   (k=123456)",
   "root_cause": "builtins.rs:673-703 merge (v8e): `let mut known = b.known.clone()` (677) takes b's value for every key b knows, including keys in b.maybe. When b lacks such a key at runtime, anubis_merge (run.rs:3184) keeps a's value. IFC never joins a.known[k] (or a.other) into a key that b only may have, so m[\"k\"] is b's public 1 while the runtime returns a's secret-derived value.",
   "explanation": "b has key k only on a public path (len(args()) > 5 is false at run), so b.maybe = {k}. The runtime merge result at k is a[k] = pick(s > 100). IFC v2 reads the public 1. With the direct secret (a = {\"k\": s}) IFC v2 is also rc=0 but the lanes refuse the explicit flow; pick() is the usual lanes-blind encoding. Fix: for k in b.known \u2229 b.maybe, join a.known[k] (or a.other when a does not know k).",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_05_remove_by_computed_key_keeps_keys_present",
   "kind": "leak",
   "program": "fn upd(m, c) {\n    if c {\n        m[\"a\"] = 5;\n    }\n    m\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let mut m = {\"a\": 1, \"b\": 2};\n    let ks = [\"a\"];\n    let k = ks[0];\n    let x = remove(m, k);\n    let r = upd(m, s > 100);\n    println(len(r));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "1",
   "output_other_k": "2   (k=123456)",
   "root_cause": "builtins.rs:866-893 mutate \"remove\" (v8e), map arm 878-889: known keys are removed only for a literal public key (880-885); for a computed key only klab/lab are joined (886-887) and every known key stays definitely present (not added to maybe). anubis_remove (run.rs:2385) deletes the key equal to k's display text, which can be any known key. A later write of that key under a secret pc then passes set_path's presence test (known and not maybe) and the key-set label stays public.",
   "explanation": "remove(m, k) with a public but computed k removes \"a\" at runtime; IFC still believes \"a\" is present, so re-adding it under the secret changes len(m) without a label. Fix: a computed-key remove must put every known key into maybe.",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_06_min_by_max_by_callback_args_decided_by_results",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    let xs = [1, 2, 3];\n    let m = min_by(xs, |x| {\n        println(x);\n        if x == 2 { s } else { 100 }\n    });\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "1\n2\n2\n3",
   "output_other_k": "1\n2\n1\n3   (k=123456)",
   "root_cause": "builtins.rs:565-571 (v8e) applies the min_by/max_by key callback once via cb(it, &f, [elems], s, p), under the collection's shape only (comment: 'called a number of times fixed by the length'). anubis_min_by/anubis_max_by (run.rs:3133-3142) use Iterator::min_by/max_by = reduce(|acc, x| cmp(f(acc), f(x))): the count is fixed, but the element passed to f at every later comparison is the running min/max, decided by the earlier key results.",
   "explanation": "This is the sort_by family (r1_022/r1_074) that the round-1 fix did not extend to min_by/max_by: a logging key function prints the running minimum, which the secret key decided. Fix: run the callback with cb_short(..., deep) as sort_by does.",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_07_stdin_cursor_is_an_unmodelled_store",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    if s > 100 {\n        let skipped = input();\n    }\n    let line = input();\n    if line == \"x\" {\n        println(\"first line\");\n    }\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "first line",
   "output_other_k": "(no output)   (k=123456)",
   "root_cause": "builtins.rs:191 (v8e) models input/read_line as a pure TNT source. anubis_input (run.rs:2545) consumes a line of process stdin: stdin's position is runtime state that input() writes and every later input() reads back. An input() under a secret pc changes what later input() calls return; the later value is only TNT, and the TNT part of a condition is dropped by control(), so the branch on it is public.",
   "explanation": "Run with stdin containing the two lines \"x\" and \"y\" (both runs: `... run --no-verify f.anb < stdin.txt`). Fix: treat stdin as a store (like Store::Files): input()/read_line() write it with the pc and read it back into their result's confidentiality label.",
   "runner": "anubis-ifc2-v8e run --no-verify (stdin = \"x\\ny\\n\")"
  },
  {
   "id": "r2p_08_write_file_to_device_path_is_egress",
   "kind": "leak",
   "program": "fn main() uses(fs.write) {\n    let s: secret<i64> = 42;\n    let r = write_file(\"/dev/stderr\", str(s));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "42",
   "output_other_k": "123456   (k=123456)",
   "root_cause": "builtins.rs:176-180 (v8e): write/write_file/append_file/delete_file/remove_file are an integrity sink plus a Store::Files write, never egress. anubis_write_file (run.rs:2563) writes to any path, including /dev/stdout, /dev/stderr and /proc/self/fd/N, which are the program's stdout/stderr.",
   "explanation": "The lanes' design keeps a secret written to a LOCAL file inside the trust boundary; a device path is not local storage but the print channel, so write_file(\"/dev/stdout\"|\"/dev/stderr\", secret) is print(secret) under another name (and under a secret pc it is a D1 egress). Fix: treat a file write as egress unless its path is a literal that is not a device/proc path (or treat every file write as egress).",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_09_panic_exit_under_secret_pc_not_refused",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    if s > 100 {\n        panic(\"over the limit\");\n    }\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "(no output, rc 0)",
   "output_other_k": "thread '<unnamed>' panicked at src/main.rs:1095:52:\nANUBIS_PANIC: over the limit\nError: run failed: exit_code=Some(101)   (k=123456)",
   "root_cause": "builtins.rs:139-164 (v8e): panic and exit report ANUBIS_SECRET_EXFILTRATION only when their DATA is secret; neither checks p.pc(). Every other egress goes through Interp::egress (eval.rs ~593-612), which also refuses a secret pc (D1). IFC v2 declares the panic message (stderr) and the exit status as egress, so a panic/exit with a public message/status under a secret condition is an implicit flow: whether stderr gets the message, or the process status becomes 3, is the secret's bit.",
   "explanation": "Exit variant (NEW rc 0, LANES rc 0): `fn main() { let s: secret<i64> = 42; if s > 100 { exit(3); } }` gives rc 0 with no output for 42 and `Error: run failed: exit_code=Some(3)` for 123456. The round-1 fix (r1_078) lifts the continuation after exit/panic, but the exit/panic itself is still unchecked. proof_commit_* (builtins.rs:165-174) has the same gap for the public journal (not observable in native run, so not verified). Fix: route panic/exit/proof_commit through egress() (data and pc).",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_10_try_on_empty_payload_gives_bottom",
   "kind": "leak",
   "program": "struct T { v: i64 }\nimpl T {\n    fn foo(self, a) {\n        0\n    }\n}\nfn pick(c) {\n    let v = if c { 7 } else { 3 };\n    v\n}\nfn get() {\n    let r = Some();\n    let x = r?;\n    x\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let x = get();\n    let y = x.foo(println(pick(s > 100)));\n    println(\"end\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "3\nend",
   "output_other_k": "7\nend   (k=123456)",
   "root_cause": "eval.rs:2238 (v8e) try_value: `cont.join(&e.fields.first().cloned().unwrap_or_default())` yields BOTTOM for Some()/Ok() with no payload; the runtime Try lowering yields `fields.first().cloned().unwrap_or(AnubisValue::Int(0))` (run.rs ~5085). method_call (eval.rs:2009-2074) on a bottom receiver finds no arm and no fallback and returns bottom at 2070-2071 WITHOUT evaluating the arguments; the runtime's Int(0) receiver takes the closure fallback, which evaluates every argument.",
   "explanation": "IFC v2 does not even see the println (with `println(s)` directly IFC v2 is still rc=0; the lanes then refuse, hence pick()). Since round 1, method arguments are evaluated only in the arms of the receiver kinds IFC believes, so any receiver-kind under-approximation drops argument side effects. Fix: Int(0) (V::scalar(PUB)) for a missing payload; and method_call should treat a bottom receiver as unreachable only when the path is.",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_11_join_enum_drops_runtime_default_for_short_payload",
   "kind": "leak",
   "program": "struct T { v: i64 }\nstruct U { v: i64 }\nimpl T {\n    fn foo(self) {\n        0\n    }\n}\nimpl U {\n    fn foo(self, a) {\n        0\n    }\n}\nfn pick(c) {\n    let v = if c { 7 } else { 3 };\n    v\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let n = len(args());\n    let o = if n == 0 { Some() } else { Some(T { v: 1 }) };\n    if let Some(x) = o {\n        let y = x.foo(println(pick(s > 100)));\n    }\n    println(\"end\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "3\nend",
   "output_other_k": "7\nend   (k=123456)",
   "root_cause": "value.rs:1370-1385 (v8e) join_enum, same names but different payload lengths: `(Some(p), None) | (None, Some(p)) => p.clone()` keeps only the longer side at a position the other variant value lacks. The runtime reads a missing position as Int(0) (payload_expr in pattern_test_and_binds, run.rs ~5500). bind_pattern (eval.rs, positional) then finds position 0 in the joined payload (all_have = true) and binds x = T only; method_call dispatches only to T's arm (arity 1, no argument evaluated), while at runtime (n == 0) x = Int(0) takes the fallback that evaluates println(...).",
   "explanation": "Same consequence as r2p_10 through a different site: a public join of Some() and Some(T{..}). Fix: join the one-sided position with V::scalar(PUB).",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_12_struct_string_index_ignores_maybe_field",
   "kind": "leak",
   "program": "struct P { x: i64 }\nstruct T { v: i64 }\nstruct U { v: i64 }\nimpl T {\n    fn foo(self) {\n        0\n    }\n}\nimpl U {\n    fn foo(self, a) {\n        0\n    }\n}\nfn pick(c) {\n    let v = if c { 7 } else { 3 };\n    v\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let n = len(args());\n    let p = P { x: 0 };\n    if n == 5 {\n        p.a = T { v: 1 };\n    }\n    let y = p[\"a\"].foo(println(pick(s > 100)));\n    println(\"end\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "3\nend",
   "output_other_k": "7\nend   (k=123456)",
   "root_cause": "eval.rs:2766-2768 (v8e) index_read on a struct with a literal string key returns f.get(k) and joins Int(0) only when the field is missing, not when it is in maybe_fields; the runtime index_get on a Struct with a Str key returns Int(0) when the field is absent (run.rs ~1684-1690). field_read (eval.rs:2788) does join the default for maybe fields; index_read does not. The receiver is then believed to be only T and the argument is never evaluated (method_call, as in r2p_10).",
   "explanation": "p.a is added only on a public path that is not taken at run; p[\"a\"] is Int(0) at runtime, so the call goes to the fallback and prints. Fix: join V::scalar(PUB) when the field is in maybe_fields.",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_13_proof_commit_bool_result_modelled_as_argument",
   "kind": "leak",
   "program": "struct P { v: i64 }\nstruct Q { v: i64 }\nimpl P {\n    fn m(self) {\n        0\n    }\n}\nimpl Q {\n    fn m(self, x) {\n        0\n    }\n}\nfn pick(c) {\n    let v = if c { 7 } else { 3 };\n    v\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let r = proof_commit_bool(\"flag\", P { v: 1 });\n    let y = r.m(println(pick(s > 100)));\n    println(\"end\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "3\nend",
   "output_other_k": "7\nend   (k=123456)",
   "root_cause": "builtins.rs:165-174 (v8e) returns arg(a, 1) for proof_commit_u32/bool/u64. anubis_proof_commit_bool returns Int(1|0) (v.as_bool()) in both the native stubs (run.rs NATIVE_PROOF_STUBS_RS) and the guest runtime. IFC believes r is the struct P, dispatches only to P::m (arity 1, the argument is dropped unevaluated), while the runtime's Int receiver takes the fallback, which evaluates the argument.",
   "explanation": "A builtin-table kind mismatch that matters because method-call argument evaluation depends on the receiver's kinds (see r2p_10). Fix: proof_commit_bool returns V::scalar(truth of arg 1).",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_14_remove_trap_message_prints_index_length_key",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    let mut xs = [1, 2, 3];\n    println(\"start\");\n    let y = remove(xs, s);\n    println(\"done\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "start\nthread '<unnamed>' panicked at src/main.rs:942:25:\nANUBIS_INDEX_OUT_OF_BOUNDS: index 42 is out of bounds for a list of length 3 (use get(xs, i, default) for optional access)\nError: run failed: exit_code=Some(101)",
   "output_other_k": "start\nthread '<unnamed>' panicked at src/main.rs:942:25:\nANUBIS_INDEX_OUT_OF_BOUNDS: index 123456 is out of bounds for a list of length 3 (use get(xs, i, default) for optional access)\nError: run failed: exit_code=Some(101)",
   "root_cause": "run.rs:2385-2407 (v8e) anubis_remove still formats key.as_i64() and l.len() into ANUBIS_INDEX_OUT_OF_BOUNDS (2391-2393) and the missing key's text into ANUBIS_MISSING_KEY (2401-2403). The round-1 redaction covered index_get (1643-1675) but not remove().",
   "explanation": "Same family as r1_032/r1_059; the fix missed this helper. The map form remove(m, str(s)) prints the key the same way.",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_15_crypto_trap_messages_print_operands",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    println(\"start\");\n    let h = sha256(s);\n    println(\"done\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "start\nthread '<unnamed>' panicked at src/main.rs:2287:13:\nANUBIS_CRYPTO_BYTES_KIND: bare integer 42 is not accepted as crypto input; use a byte list [0..255, ...] or a string\nError: run failed: exit_code=Some(101)",
   "output_other_k": "start\nthread '<unnamed>' panicked at src/main.rs:2287:13:\nANUBIS_CRYPTO_BYTES_KIND: bare integer 123456 is not accepted as crypto input; use a byte list [0..255, ...] or a string\nError: run failed: exit_code=Some(101)",
   "root_cause": "audited_crypto_runtime.inc.rs (v8e, the native `run` crypto runtime) anubis_crypto_bytes prints a bare integer operand's value (line 50) and a list element's index and value (37); length traps print (possibly secret) lengths: HKDF L (166, 173), AEAD key/nonce length (265, 271), PBKDF2 dkLen (373), Ed25519 secret-key length (605, 619), X25519 sk/pk length (660, 672), hybrid nonce length (783). pure_crypto_runtime.inc.rs (guest) has the same value-printing messages (104, 115).",
   "explanation": "Verified variants (NEW rc 0, LANES rc 0, run on NEW): `let key = [1, 2, s + 250]; let h = sha256_bytes(key);` traps with `list element [2] = 292` (42) vs `= 123706` (123456); `let sk = take(random_bytes(40), s % 7 + 20); let pk = ed25519_public_key(sk);` traps with `secret key must be 32 bytes, got 20` vs `got 24`. Hashing/keying a secret is exactly where these arguments are secret.",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_16_builtin_value_arity_trap_prints_argument_count",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    let xs = range(0, s % 5 + 2);\n    println(\"start\");\n    let y = apply(abs, xs);\n    println(\"done\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "start\nthread '<unnamed>' panicked at src/main.rs:3287:194:\nANUBIS_ARITY: builtin `abs` cannot take 4 argument(s)\nError: run failed: exit_code=Some(101)",
   "output_other_k": "start\nthread '<unnamed>' panicked at src/main.rs:3287:194:\nANUBIS_ARITY: builtin `abs` cannot take 3 argument(s)\nError: run failed: exit_code=Some(101)",
   "root_cause": "run.rs:4624 (v8e) var_as_value: a builtin used as a value is a closure dispatching on __args.len() whose default arm panics with \"ANUBIS_ARITY: builtin `{name}` cannot take {} argument(s)\", n. With apply(f, xs) the count is len(xs), a secret length here.",
   "explanation": "A trap message that still prints an operand count (the round-1 redaction list mentions counts). Fix: drop n from the message.",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_17_poc_kit_trap_messages_print_operands",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    println(\"start\");\n    let pat = cyclic(0 - s);\n    println(\"done\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "start\nthread '<unnamed>' panicked at src/main.rs:3149:9:\nANUBIS_POC_CYCLIC_LENGTH: cyclic length must be >= 0, got -42\nError: run failed: exit_code=Some(101)",
   "output_other_k": "start\nthread '<unnamed>' panicked at src/main.rs:3149:9:\nANUBIS_POC_CYCLIC_LENGTH: cyclic length must be >= 0, got -123456\nError: run failed: exit_code=Some(101)",
   "root_cause": "run.rs:3484-3496 (v8e, POC_KIT_RUNTIME_RS, always lowered without --allow-research) anubis_cyclic prints the negative length n_raw. anubis_pack_require_numeric (about run.rs:3436-3462, used by p8/p16/p32/p64/cyclic) prints an enum operand's variant as `{ty}::{tag}`: the tag is the value's data, not its type name.",
   "explanation": "Another trap site the round-1 redaction missed (cyclic/p8 are pure helpers available in plain `run`).",
   "runner": "anubis-ifc2-v8e run --no-verify"
  },
  {
   "id": "r2p_18_bool_int_of_collection_use_content",
   "kind": "overrefusal",
   "program": "fn pick(c) {\n    let v = if c { 7 } else { 3 };\n    v\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let readings = [pick(s > 100), 1, 2];\n    if len(args()) > 50 {\n        readings = [];\n    }\n    println(bool(readings));\n    println(int(readings));\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "true\n3",
   "output_other_k": "true\n3   (k=123456)",
   "root_cause": "builtins.rs:252-264 (v8e) put bool, int and float in the 'pure functions of content' group: scalar(deep_all(args)). The runtime's bool is as_bool (run.rs:1557: a list's/map's/string's non-emptiness; a struct, enum or closure is always true) and int/float are as_i64/as_f64 (a list's or map's length, a struct's field count, a closure's 0). IFC v2 already models truth() for if/while/&&/||/!, but not for the bool() builtin, and never models int()/float() of a collection as its length.",
   "explanation": "NEW ifc2-report: ANUBIS_SECRET_EXFILTRATION (secret data reaches println). NEW's run refuses because its checker runs IFC v2; output shown from the LANES pin (anubis-ord3x4l run --no-verify), identical for both secrets. The direct form ([s, 1, 2]) is refused by the lanes too, so this matters only where the lanes do not see the element as secret. Fix: bool -> V::truth; int/float -> len_label for list/map/struct/enum/closure parts, deep for scalars.",
   "runner": "anubis-ord3x4l run --no-verify (NEW run refuses)"
  },
  {
   "id": "r2p_19_files_store_is_global_and_flow_insensitive",
   "kind": "overrefusal",
   "program": "fn main() uses(fs.read, fs.write) {\n    let s: secret<i64> = 42;\n    let mode = read_file(\"cfg.txt\");\n    if mode == \"on\" {\n        println(\"feature enabled\");\n    }\n    let r = write_file(\"vault.txt\", sha256(str(s)));\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "feature enabled",
   "output_other_k": "feature enabled   (k=123456)",
   "root_cause": "builtins.rs:176-195 with eval.rs store_read/store_write (about 574-591, v8e): every file is one Store::Files label, joined over the whole program and every round, so a read of any path at any time (here BEFORE any write, and of a different literal path) returns the label of every write anywhere. The config read becomes SEC and the branch on it is an ANUBIS_IMPLICIT_FLOW.",
   "explanation": "Run in a directory with cfg.txt containing \"on\". A program that stores a secret digest in a vault file and reads an unrelated config file is refused although its output is identical for both secrets and the lanes accept it; NEW's run refuses, output from the LANES pin. Fix: key the store by literal path (one slot for computed paths) and do not let a read see writes that cannot precede it.",
   "runner": "anubis-ord3x4l run --no-verify (NEW run refuses)"
  },
  {
   "id": "r2_fn_L1_min_by_max_by_callback_argument",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    let xs = [1, 2, 3];\n    let m = min_by(xs, |x| {\n        println(x);\n        if x == 2 { s } else { x * 10 }\n    });\n    println(\"done\");\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "1\n2\n1\n3\ndone",
   "output_other_k": "k=5: 1\n2\n2\n3\ndone",
   "root_cause": "builtins.rs:566-571 (v8e) models min_by/max_by as a single `cb(it, &f, vec![elems(&c)], s, p)`. The pc is only the collection shape, and the argument is just `elems(c)`. The comment says the key function is called a number of times fixed by the length, which is true. But the runtime (run.rs:3133-3141, Iterator::min_by/max_by with `anubis_value_cmp(&f(x), &f(y))`) calls f on the running minimum or maximum, and which element that is depends on the earlier results of f. sort_by gets this right with `cb_short(.., deep)`; min_by/max_by do not.",
   "explanation": "The key function prints its argument. With s=42 the running minimum stays 1, so the calls are f(1), f(2), f(1), f(3). With s=5 the element 2 becomes the minimum, so the calls are f(1), f(2), f(2), f(3). stdout therefore reveals the secret. max_by is the same arm and leaks the same way (probe p01b: 1 2 2 3 vs 1 2 1 3). Fix: run the callback under cb_short with `deep`, as sort_by does, or raise the callback's argument and pc by r.deep().",
   "runner": "NEW (anubis-ifc2-v8e run --no-verify); its full checker accepts",
   "timing": "ifc2-report 0.02 s"
  },
  {
   "id": "r2_fn_L2_map_insertion_order_not_modelled_in_select",
   "kind": "leak",
   "program": "fn main() {\n    let s: secret<i64> = 42;\n    let ms = map([1], |v| if s > 100 { {\"a\": 1, \"b\": 2} } else { {\"b\": 2, \"a\": 1} });\n    let m = ms[0];\n    println(keys(m));\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "[b, a]",
   "output_other_k": "k=500: [a, b]",
   "root_cause": "value.rs:716 (V::select, map case). Two maps count as the same shape when `ma.known.keys().eq(mb.known.keys())`, and MapV.known is a BTreeMap (value.rs:218), so the comparison uses sorted keys. Their lab is then not raised by the condition. Runtime maps are insertion-ordered (`Map(Rc<Vec<(String, AnubisValue)>>)`; keys(), iteration, display and display-based `<` comparisons all follow insertion order), and MapV does not model order at all.",
   "explanation": "The if-value chooses between two maps with the same key set in different insertion orders. select keeps m.lab PUB, so keys(m) (label = lab \u2294 klab) is public, yet the printed key order reveals s. The direct form without map() (`let m = if s > 100 {..} else {..}; println(keys(m));`, probe p06) is also accepted by IFC v2, but the lanes refuse it, so the lambda form is given here to show a program that actually runs. The same order reaches each/map/filter callbacks over the map (keys in secret order), str()/println of a map whose values do not raise (for example [] values), and `<` between maps. Fix: treat different insertion orders as a shape difference (keep order in MapV, or raise lab whenever both sides have more than one key).",
   "runner": "NEW (anubis-ifc2-v8e); its full checker accepts",
   "timing": "ifc2-report < 0.4 s"
  },
  {
   "id": "r2_fn_L3_struct_literal_field_order_not_modelled_in_select",
   "kind": "leak",
   "program": "enum Tag {\n    Plain,\n}\nstruct P {\n    a: list<i64>,\n    b: Tag,\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let ps = map([1], |v| if s > 100 { P { a: [], b: Tag::Plain } } else { P { b: Tag::Plain, a: [] } });\n    let p = ps[0];\n    println(p[0]);\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "Tag::Plain",
   "output_other_k": "k=500: []",
   "root_cause": "value.rs:747 (V::select, struct case) treats two values as the same shape when `ta == tb && fa.keys().eq(fb.keys())`. Struct fields are a BTreeMap, so literal order is lost, and lab is not raised. The runtime stores struct fields in literal order (run.rs:5234 StructLiteral; the Struct{fields: Vec<(String, V)>} value), and index_get with an integer index reads fields[idx] positionally (run.rs:1682-1692). The field values ([] and a unit variant) are nodes that select does not raise, and index_read with a non-string key joins every field raised only by b.lab, which is PUB.",
   "explanation": "The two literals of the same struct type write the fields in different orders. p[0] returns whichever field was written first, so the output reveals s. Enums already get this right: select requires `ea.names == eb.names`. Structs, and maps (L2), are missing the equivalent check. Display and display-based `<` comparisons of such structs are affected in the same way.",
   "runner": "NEW (anubis-ifc2-v8e); its full checker accepts",
   "timing": "ifc2-report 0.01 s"
  },
  {
   "id": "r2_fn_L4_panic_and_exit_under_secret_pc_not_refused",
   "kind": "leak",
   "program": "fn check(xs, pred) {\n    println(\"checking\");\n    each(xs, |x| if pred(x) { panic(\"rejected entry\") } else { 0 });\n    return len(xs);\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let n = check([1, 2, 3], |x| x == s);\n}\n",
   "new_rc": 0,
   "lanes_rc": 0,
   "output_k42": "checking   (exit 0)",
   "output_other_k": "k=2: checking\n\nthread '<unnamed>' panicked at src/main.rs:...\nANUBIS_PANIC: rejected entry\n... Error: run failed: exit_code=Some(101)",
   "root_cause": "The builtins.rs:139 (\"panic\") and :150 (\"exit\") arms report only when the message or status data is secret (`deep_all(a).secret()`). They never check `p.pc()`, although the code treats the panic message and the exit status as egress. end_program then lifts only the continuation. Under decision D1, an egress that runs under a secret program counter should be ANUBIS_IMPLICIT_FLOW.",
   "explanation": "The panic runs inside a callback whose condition depends on s. Its constant message reaches stderr only when s is in the list, and nothing runs after it, so the continuation lift never fires. The same happens with exit (probe p17c): `if len(filter([10, 20, 42], |x| x == s)) > 0 { exit(3); }` after println(\"start\") is accepted (rc 0). The exit status is 3 for s=42 and 0 for s=2, and `anubis run` adds `Error: run failed: exit_code=Some(3)` to stderr. `exit(if s > 10 { 3 } else { 0 })` is refused as a secret exit status, so this variant, which reveals the same bit, is inconsistent with it. Round 1 (r1_078) fixed only the lifting of the continuation after exit. If the team counts a panic or exit under a secret condition as a termination channel, this becomes a policy decision; as the analysis stands (panic and exit modelled as egress), it is a D1 gap.",
   "runner": "NEW (anubis-ifc2-v8e); its full checker accepts",
   "timing": "ifc2-report 0.02 s"
  },
  {
   "id": "r2_fn_O1_recursive_hof_summary_merges_closure_captures",
   "kind": "overrefusal",
   "program": "fn apply_all(xs, f) {\n    if len(xs) == 0 {\n        return [];\n    }\n    let rest = apply_all(drop(xs, 1), f);\n    return [f(xs[0])] + rest;\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let hidden = apply_all([1, 2, 3], |x| x + s);\n    let shown = apply_all([1, 2, 3], |x| x * 2);\n    println(shown);\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "[2, 4, 6]",
   "output_other_k": "k=123456: [2, 4, 6]",
   "root_cause": "eval.rs:696-700 plus 706 (v8e). Once apply_all has recursed, it is in recursive_callees for the rest of the round, so even the second top-level call goes to a summary context. The summary key is `sig = join of args' deep()`, and V::deep() deliberately excludes closure captures (value.rs deep). Both closures therefore have sig PUB and share one context whose joined argument f = {lambda0 (captures s), lambda1}. The trace confirms it: `apply_all (summary [0])` is called with <lambda 0> and <lambda 1>, and the result has lab=1.",
   "explanation": "IFC v2 reports ANUBIS_SECRET_EXFILTRATION for println(shown), but shown is [2, 4, 6] for every secret, and the lanes accept the program. The same root cause refuses a visitor pattern (probe p19: `fn walk(n, visit) { return visit(n, |c| walk(c, visit)); }` called with a salted visitor and then a summing visitor, printing only the sum 6). It is order-dependent: if the secret call comes after the public one, the public result is computed before the summary grows and is accepted. Fix: include the reach label of closure arguments (V::reach) in the summary signature.",
   "runner": "anubis-whole10 (NEW's own checker refuses)",
   "timing": "ifc2-report ~1 s"
  },
  {
   "id": "r2_fn_O2_context_budget_summary_merges_closure_captures",
   "kind": "overrefusal",
   "program": "fn transform(xs, f) {\n    return map(xs, f);\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let a1 = transform([1], |x| x + 1);\n    let a2 = transform([1, 2], |x| x + 2);\n    let a3 = transform([1, 2, 3], |x| x + 3);\n    let a4 = transform([4], |x| x * 4);\n    let a5 = transform([5, 5], |x| x * 5);\n    let a6 = transform([6], |x| x - 6);\n    let a7 = transform([7, 7], |x| x - 7);\n    let a8 = transform([8], |x| x / 2);\n    let a9 = transform([9, 9], |x| x / 3);\n    let a10 = transform([10], |x| x * x);\n    let a11 = transform([11, 11], |x| x + x);\n    let a12 = transform([12], |x| 0 - x);\n    let masked = transform([1, 2], |x| x + s);\n    let doubled = transform([3, 4], |x| x * 2);\n    println(doubled);\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "[6, 8]",
   "output_other_k": "k=123456: [6, 8]",
   "root_cause": "eval.rs:706 (v8e): the summary signature is the join of the arguments' deep(), which ignores closure captures. Past CONTEXTS_PER_CALLEE (eval.rs:36, 12), the secret-capturing closure call (masked) and the public one (doubled) both have sig PUB at pc PUB. They share one summary context with joined f, so doubled's result is secret.",
   "explanation": "This is a variant of the round-1 context-budget family (r1_061, r1_086, r1_098) that still fails. The fix keyed summaries by argument label, but the label ignores what closures capture, so a generic helper called 13+ times with distinct lambdas, one of which captures a secret, taints every later public call. The output is identical for every secret and the lanes accept the program. It has the same root as O1 but a different trigger (the count limit rather than recursion).",
   "runner": "anubis-whole10",
   "timing": "ifc2-report 0.02 s"
  },
  {
   "id": "r2_fn_O3_closure_summary_reach_label_merges_secret_reaching_pipelines",
   "kind": "overrefusal",
   "program": "fn wrap(next, name) {\n    return |req| next(req + 1);\n}\nfn pipeline(base) {\n    return wrap(wrap(wrap(wrap(wrap(base, \"log\"), \"trace\"), \"limit\"), \"cors\"), \"gzip\");\n}\nfn main() {\n    let key: secret<i64> = 50;\n    let check = pipeline(|req| declassify(req == key, \"auth\", \"only whether the request is authorized\"));\n    let issue = pipeline(|req| req * key);\n    let token = issue(3);\n    let ok = check(37);\n    println(ok);\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "k=50 (secret literal in this program): false",
   "output_other_k": "k=60: false",
   "root_cause": "value.rs:1181-1183 (fns_widened) plus eval.rs:961 (analyze_lambda on Clo::Summary). Wrap closures nested deeper than CLOSURE_DEPTH (3) are summarized per (lambda, reach label). Both pipelines reach `key` (one only to declassify it), so both summarize into (wrap-lambda, SEC). The joined env's `next` includes issue's base (req * key), so check(37) is analyzed as possibly returning req * key.",
   "explanation": "Two middleware pipelines are built from one wrap helper. The auth pipeline releases only a declassified bit, and the token pipeline's result is never printed. The printed bit is identical for both secret values (neither equals 42), and the lanes accept the program. Keying the round-1 fix (r1_011, r1_099) by reach label separates public-reaching from secret-reaching pipelines, but any two secret-reaching pipelines built from the same helper still merge. The secret literal is 50 rather than 42 so that the declassified bit is the same for both runs.",
   "runner": "anubis-whole10",
   "timing": "ifc2-report 0.03 s"
  },
  {
   "id": "r2_fn_O4_nested_compose_becomes_unordered_chain_at_call_widening",
   "kind": "overrefusal",
   "program": "fn run_on(xs, f) {\n    return map(xs, f);\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let add_s = |x| x + s;\n    let show = |x| {\n        println(x);\n        x\n    };\n    let pipeline = compose(add_s, compose(show, |x| x * 2));\n    let sealed = run_on([5, 6], pipeline);\n    println(\"ok\");\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "10\n12\nok",
   "output_other_k": "k=123456: 10\n12\nok",
   "root_cause": "value.rs:1204 (fns_widened): any composed pair whose component is itself a composition (`nested(g) || nested(h)`) becomes a chain at the first widening, even at closure depth 1. The first widening here is call()'s argument widening (eval.rs:675) when the pipeline is passed to run_on. chain_value (value.rs:1288) flattens the components into an unordered set, and apply's chain loop (eval.rs:1064) applies every member to every other member's results, so `show` is analyzed on add_s's secret output.",
   "explanation": "At runtime the pipeline is add_s(show(x*2)), so show prints only the public 10 and 12. IFC v2 reports ANUBIS_SECRET_EXFILTRATION in <closure>. The same pipeline passed directly to map (probe p03b) is accepted, so the refusal comes only from passing the composition through a user function. The lanes accept the program. When run() is itself a user function taking f (probe p03), the lanes also refuse. Fix: keep composition order (for example a bounded sequence) instead of an any-order chain until real nesting depth demands it.",
   "runner": "anubis-whole10",
   "timing": "ifc2-report 0.02 s"
  },
  {
   "id": "r2_fn_O5_chain_joins_component_labels_into_every_stage_pc",
   "kind": "overrefusal",
   "program": "fn run_on(xs, f) {\n    return map(xs, f);\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let log_stage = |x| {\n        println(\"stage\");\n        x\n    };\n    let pick = if s > 100 { |x| x * 3 } else { |x| x * 5 };\n    let pipeline = compose(log_stage, compose(pick, |x| x + 1));\n    let sealed = run_on([1, 2], pipeline);\n    println(\"ok\");\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "stage\nstage\nok",
   "output_other_k": "k=123456: stage\nstage\nok",
   "root_cause": "value.rs:1288-1300 (chain_value) sets the chain's lab to the join of every component's lab. apply (eval.rs:1064-1080) then runs every chain member under that joined label (flab of the chain value). The secret-chosen `pick` (lab SEC) therefore puts the unconditional log_stage under a secret pc. The conversion happens at the call-argument widening, as in O4 (value.rs:1204).",
   "explanation": "log_stage always runs once per element, and only which multiplier runs depends on s. IFC v2 reports ANUBIS_IMPLICIT_FLOW for println in log_stage, the output is identical, and the lanes accept the program. A direct `map([1, 2], pipeline)` keeps the composed pair and runs log_stage under the pair's own label (PUB). This is a second symptom of the chain flattening in O4, listed separately because a fix to ordering alone would not fix the label conflation.",
   "runner": "anubis-whole10",
   "timing": "ifc2-report 0.02 s"
  },
  {
   "id": "r2_fn_O6_apply_non_list_argument_runs_callback_under_scalar_content",
   "kind": "overrefusal",
   "program": "fn main() {\n    let pw: secret<str> = \"hunter2\";\n    let check = |x| {\n        println(\"checking password strength\");\n        len(x) > 6\n    };\n    let strong = apply(check, pw);\n    println(declassify(strong, \"policy\", \"strength bit only\"));\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "secret \"hunter2\": checking password strength\ntrue",
   "output_other_k": "secret \"swordfish\": checking password strength\ntrue",
   "root_cause": "builtins.rs:649 (apply, non-list part) calls `cb(it, &f, vec![other], xs.kind_label(), p)`. V::kind_label returns a pure scalar's lab, which is its content label, because it answers \"which scalar kind\". apply only has to decide list or not list (anubis_apply, run.rs:2874), and a pure scalar is never a list, so the decision is public.",
   "explanation": "The callback runs exactly once whatever the string holds, and IFC v2 refuses its println as ANUBIS_IMPLICIT_FLOW. The output is identical for both secrets (both have more than 6 characters) and the lanes accept the program. A calling pattern like this is less common than the others here, but it is a cheap precision fix: use a list-or-not label, which is PUB for a value that cannot be a list.",
   "runner": "anubis-whole10",
   "timing": "ifc2-report 0.03 s"
  },
  {
   "id": "r2_fn_O7_recursive_walk_folds_records_at_summary_depth_4",
   "kind": "overrefusal",
   "program": "struct Acct {\n    id: i64,\n    bal: secret<i64>,\n}\nenum Tree {\n    Leaf,\n    Node(Tree, Acct, Tree),\n}\nfn build(lo, hi) {\n    if lo > hi {\n        return Tree::Leaf;\n    }\n    let mid = (lo + hi) / 2;\n    return Tree::Node(build(lo, mid - 1), Acct { id: mid, bal: mid * 100 }, build(mid + 1, hi));\n}\nfn ids(t) {\n    return match t {\n        Tree::Leaf => [],\n        Tree::Node(l, a, r) => ids(l) + [a.id] + ids(r),\n    };\n}\nfn main() {\n    let t = build(1, 7);\n    println(ids(t));\n}\n",
   "new_rc": 1,
   "lanes_rc": 1,
   "output_k42": "[1, 2, 3, 4, 5, 6, 7]  (secret field values mid*100)",
   "output_other_k": "secret field values mid*1000: [1, 2, 3, 4, 5, 6, 7]",
   "root_cause": "eval.rs:719 (the summary context's joined arguments widened with widen_summary, SUMMARY_DEPTH=4, eval.rs:40). The recursive walk ids() goes to a summary context (sig SEC because the tree holds bal), and its tree argument is folded below depth 4 into `top`, whose tlab joins the secret bal. Reading a.id then yields part_of_top (SEC). The trace shows `ids (summary [1])` with result lab=1, while build's own result keeps the fields separate (lab=0, top=false).",
   "explanation": "A recursive walk over a tree of records with one secret field cannot print the records' public ids. The output is identical for different secret balances. The lanes also refuse this program (the whole-struct lane flags any value computed from a struct holding a secret field), so it matters less than the others. The lanes' refusal of other variants of it (map records, records built with map()) likewise comes from their tracking the secret through the data. It is the same precision loss for any recursion over data nested deeper than 4 that mixes public and secret leaves.",
   "runner": "anubis-whole10",
   "timing": "ifc2-report ~0.7 s"
  },
  {
   "id": "r2_fn_R1_parser_combinators_on_secret_input_hit_step_budget",
   "kind": "robustness",
   "program": "fn lit(c) {\n    return |s, i| if i < len(s) && s[i] == c { [1, i + 1] } else { [0, i] };\n}\nfn digit() {\n    return |s, i| if i < len(s) && contains(\"0123456789\", s[i]) { [1, i + 1] } else { [0, i] };\n}\nfn seq(p, q) {\n    return |s, i| {\n        let r = p(s, i);\n        if r[0] == 1 { q(s, r[1]) } else { [0, i] }\n    };\n}\nfn alt(p, q) {\n    return |s, i| {\n        let r = p(s, i);\n        if r[0] == 1 { r } else { q(s, i) }\n    };\n}\nfn many1(p, s, i) {\n    let r = p(s, i);\n    if r[0] == 0 {\n        return [0, i];\n    }\n    let more = many1(p, s, r[1]);\n    if more[0] == 1 {\n        return more;\n    }\n    return r;\n}\nfn number(s, i) {\n    return many1(digit(), s, i);\n}\nfn factor(s, i) {\n    let paren = seq(lit(\"(\"), seq(|s2, j| expr(s2, j), lit(\")\")));\n    return alt(paren, |s2, j| number(s2, j))(s, i);\n}\nfn term(s, i) {\n    let op = alt(lit(\"*\"), lit(\"/\"));\n    return alt(seq(|s2, j| factor(s2, j), seq(op, |s2, j| term(s2, j))), |s2, j| factor(s2, j))(s, i);\n}\nfn expr(s, i) {\n    let op = alt(lit(\"+\"), lit(\"-\"));\n    return alt(seq(|s2, j| term(s2, j), seq(op, |s2, j| expr(s2, j))), |s2, j| term(s2, j))(s, i);\n}\nfn parses(text) {\n    let r = expr(text, 0);\n    return r[0] == 1 && r[1] == len(text);\n}\nfn main() {\n    let pin: secret<i64> = 42;\n    println(parses(\"12+(3*4)-5\"));\n    println(parses(\"(1+2\"));\n    let hidden = parses(str(pin) + \"*2\");\n    println(declassify(hidden, \"demo\", \"validity bit only\"));\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "true\nfalse\ntrue",
   "output_other_k": "k=123456: true\nfalse\ntrue",
   "root_cause": "This is inferred from trace counts; the exact mechanism was not isolated. The grammar is a recursive component (expr, term and factor through combinator closures), and eval.rs:744-810 (mark_provisional, finish, prov_hit) never memoize a provisional result; it is reused only while every approximation it read is unchanged. With the input secret, every condition is secret (pc=1), the approximations keep changing, and the nested fixpoints re-solve the same tiny summaries over and over. The ANUBIS_IFC2_TRACE run shows the summary of <lambda 4> (seq's closure, 2-node arguments, 3-node result) solved about 30,009 times and factor's summary about 2,351 times. The trace has 79,963 summary rounds in total before the 1.5M step budget (eval.rs:26) is spent.",
   "explanation": "IFC v2 reports ANUBIS_IFC2_LIMIT (\"its step budget was spent\") after about 1.5 s. The lanes accept the program and it runs correctly. Without the one line that parses the secret, the same program is accepted in 0.1 s (probe p27b; 6,662 summary rounds). Validating a secret with a parser and declassifying only the validity bit is a realistic use of a security-oriented language.",
   "runner": "anubis-whole10 (run); NEW for ifc2-report",
   "timing": "ifc2-report ~1.5 s wall, then ANUBIS_IFC2_LIMIT"
  },
  {
   "id": "r2_fn_R2_mutual_recursion_with_closure_args_42s_then_work_budget",
   "kind": "robustness",
   "program": "fn f0(xs, d, f) {\n    if d <= 0 || len(xs) == 0 {\n        return [f(len(xs))];\n    }\n    let a = f1(drop(xs, 1), d - 1, |v| f(v) + 0);\n    let b = f3(xs + [d], d - 2, f);\n    return a + b + [xs[0]];\n}\nfn f1(xs, d, f) {\n    if d <= 0 || len(xs) == 0 {\n        return [f(len(xs))];\n    }\n    let a = f2(drop(xs, 1), d - 1, |v| f(v) + 1);\n    let b = f4(xs + [d], d - 2, f);\n    return a + b + [xs[0]];\n}\nfn f2(xs, d, f) {\n    if d <= 0 || len(xs) == 0 {\n        return [f(len(xs))];\n    }\n    let a = f3(drop(xs, 1), d - 1, |v| f(v) + 2);\n    let b = f5(xs + [d], d - 2, f);\n    return a + b + [xs[0]];\n}\nfn f3(xs, d, f) {\n    if d <= 0 || len(xs) == 0 {\n        return [f(len(xs))];\n    }\n    let a = f4(drop(xs, 1), d - 1, |v| f(v) + 3);\n    let b = f6(xs + [d], d - 2, f);\n    return a + b + [xs[0]];\n}\nfn f4(xs, d, f) {\n    if d <= 0 || len(xs) == 0 {\n        return [f(len(xs))];\n    }\n    let a = f5(drop(xs, 1), d - 1, |v| f(v) + 4);\n    let b = f7(xs + [d], d - 2, f);\n    return a + b + [xs[0]];\n}\nfn f5(xs, d, f) {\n    if d <= 0 || len(xs) == 0 {\n        return [f(len(xs))];\n    }\n    let a = f6(drop(xs, 1), d - 1, |v| f(v) + 5);\n    let b = f0(xs + [d], d - 2, f);\n    return a + b + [xs[0]];\n}\nfn f6(xs, d, f) {\n    if d <= 0 || len(xs) == 0 {\n        return [f(len(xs))];\n    }\n    let a = f7(drop(xs, 1), d - 1, |v| f(v) + 6);\n    let b = f1(xs + [d], d - 2, f);\n    return a + b + [xs[0]];\n}\nfn f7(xs, d, f) {\n    if d <= 0 || len(xs) == 0 {\n        return [f(len(xs))];\n    }\n    let a = f0(drop(xs, 1), d - 1, |v| f(v) + 7);\n    let b = f2(xs + [d], d - 2, f);\n    return a + b + [xs[0]];\n}\nfn main() {\n    let s: secret<i64> = 42;\n    let r = f0([1, 2, 3], 6, |v| v * 2);\n    println(len(r));\n    println(r);\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "33\n[3, 12, 10, 4, 7, 3, 3, 15, 13, 5, 10, 3, 13, 10, 2, 2, 2, 18, 16, 6, 13, 3, 16, 13, 2, 2, 19, 16, 2, 12, 1, 1, 1]",
   "output_other_k": "identical (the secret is unused; program is public)",
   "root_cause": "This is a hypothesis from traces. Eight mutually recursive functions pass growing closure chains (|v| f(v) + i). Each summary's joined closure argument holds unions of the 8 lambdas at each of the CLOSURE_DEPTH levels; traced summary argument sizes reach 147+ nodes. The step budget is not reached (1.32M steps), and the work budget (charge() on join/leq/widen/raise, value.rs) takes about 42 s of wall time to reach 40M. Much of the per-call cost appears to be uncharged: V::size() in call() (eval.rs:677) and in widen_from, plus hashing and equality of full V memo keys.",
   "explanation": "ifc2-report takes 44.8 s and then fails closed with ANUBIS_IFC2_LIMIT (\"its work budget was spent\"). That is over the 10 s threshold. The lanes accept the program and it runs instantly. The program has no secret flow at all. It scales badly with size: with 4 or 6 functions the same shape stops on the step budget in about 2 s; with 8 it takes about 42 s. It is more synthetic than R1, but it shows that the budgets do not bound wall time.",
   "runner": "anubis-whole10 (run); NEW for ifc2-report",
   "timing": "ifc2-report 44.78 s (42.2 s on a second run), then ANUBIS_IFC2_LIMIT"
  },
  {
   "id": "ROB-1-nested-summary-fixpoint-precedence-parser",
   "kind": "robustness",
   "program": "// A parser with one function per precedence level: level i parses level i+1 operands\n// joined by operator ops[i]; the last level parses a number or a parenthesized expression.\nenum Ast {\n    Num(i64),\n    Bin(str, Ast, Ast),\n}\n\nfn peek(toks, p) {\n    if p < len(toks) {\n        return toks[p];\n    }\n    return \"\";\n}\n\nfn level1(toks, p) {\n    let r = level2(toks, p);\n    let mut node = r[0];\n    let mut q = r[1];\n    while peek(toks, q) == \"+\" {\n        let r2 = level2(toks, q + 1);\n        node = Ast::Bin(\"+\", node, r2[0]);\n        q = r2[1];\n    }\n    return [node, q];\n}\n\nfn level2(toks, p) {\n    let r = level3(toks, p);\n    let mut node = r[0];\n    let mut q = r[1];\n    while peek(toks, q) == \"-\" {\n        let r2 = level3(toks, q + 1);\n        node = Ast::Bin(\"-\", node, r2[0]);\n        q = r2[1];\n    }\n    return [node, q];\n}\n\nfn level3(toks, p) {\n    let r = level4(toks, p);\n    let mut node = r[0];\n    let mut q = r[1];\n    while peek(toks, q) == \"*\" {\n        let r2 = level4(toks, q + 1);\n        node = Ast::Bin(\"*\", node, r2[0]);\n        q = r2[1];\n    }\n    return [node, q];\n}\n\nfn level4(toks, p) {\n    let r = level5(toks, p);\n    let mut node = r[0];\n    let mut q = r[1];\n    while peek(toks, q) == \"/\" {\n        let r2 = level5(toks, q + 1);\n        node = Ast::Bin(\"/\", node, r2[0]);\n        q = r2[1];\n    }\n    return [node, q];\n}\n\nfn level5(toks, p) {\n    let r = level6(toks, p);\n    let mut node = r[0];\n    let mut q = r[1];\n    while peek(toks, q) == \"%\" {\n        let r2 = level6(toks, q + 1);\n        node = Ast::Bin(\"%\", node, r2[0]);\n        q = r2[1];\n    }\n    return [node, q];\n}\n\nfn level6(toks, p) {\n    let r = level7(toks, p);\n    let mut node = r[0];\n    let mut q = r[1];\n    while peek(toks, q) == \"&\" {\n        let r2 = level7(toks, q + 1);\n        node = Ast::Bin(\"&\", node, r2[0]);\n        q = r2[1];\n    }\n    return [node, q];\n}\n\nfn level7(toks, p) {\n    if peek(toks, p) == \"(\" {\n        let r = level1(toks, p + 1);\n        return [r[0], r[1] + 1];\n    }\n    return [Ast::Num(int(peek(toks, p))), p + 1];\n}\n\nfn size(a) {\n    return match a {\n        Ast::Num(x) => 1,\n        Ast::Bin(o, l, r) => 1 + size(l) + size(r),\n    };\n}\n\nfn main() {\n    let toks = [\"(\", \"1\", \"+\", \"2\", \")\", \"*\", \"3\"];\n    let r = level1(toks, 0);\n    print(\"nodes: \" + str(size(r[0])));\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "nodes: 5",
   "output_other_k": "nodes: 5 (the program has no secret; with a secret added, as in the realistic version below, both runs also print identical output)",
   "root_cause": "eval.rs (pinned d6801d4d) call() 664-740, solve_summary() 841-878, finish() 758-793, prov_hit() 795-810. A callee already on the solving stack is added to recursive_callees (683/697), and its calls go to summary contexts (706). Every summary context is solved to its own fixpoint, nested inside the fixpoint of the context that called it. A result that read an in-progress approximation is dropped from memo (779) and reused only while every approximation it read is unchanged (prov_hit). Each outer round changes the outer approximation, so every inner level of the cycle is solved again from the start. Each round also bumps `generation` (835/874), which invalidates every loop-cache entry (1477), so the while loop in each level is also solved again. The cost is roughly the product of the rounds at each level of the recursion cycle, which is exponential in the cycle length.",
   "explanation": "A 7-level precedence parser (one function per precedence level, each with a while loop, the last level recursing into the first for parentheses) is a standard program, and it has no secret at all. ifc2-report spends its 1.5M-step budget and reports ANUBIS_IFC2_LIMIT ('its step budget was spent'). `anubis-ifc2-v8e check` refuses it with ANUBIS_IFC2_LIMIT, and LANES accepts it in 20 ms. Steps grow about 6.5x per extra level (ANUBIS_IFC2_TRACE steps, generated by gen_chain.py): 3 levels 8,059; 4 levels 58,019; 5 levels 378,117; 6 levels 1,222,504; 7 and 8 levels hit the limit. Replacing each `while` with an `if` still grows about 3.4x per level (5/7/9 levels: 2,117 / 16,133 / 229,368 steps). A realistic 8-level precedence-climbing parser and evaluator that uses a secret also fails the same way: it prints 'tokens: 46' and 'value: 2' identically for 42 and 123456, LANES accepts it in 29 ms, and NEW takes 2.4 s to reach the limit (~/.cache/anubis-review-esc/ifc2-r2/precision/p/r02_prec_parser.anb). Parsers for real languages have 10 or more precedence levels, so every such program is refused. A 3-cycle JSON parser (p/r13_json.anb, 3,548 steps) and a 4-function tree-walking interpreter (p/r20_interp.anb, 90,162 steps) still pass. Probe files: ~/.cache/anubis-review-esc/ifc2-r2/precision/p/r03_chain{3..8}.anb and r03b_chainif{5,7,9}.anb.",
   "runner": "anubis-whole10 run --no-verify (rc 0 for both secret values; r02 also rc 0/0)",
   "timing": "NEW ifc2-report 1.0 s to ANUBIS_IFC2_LIMIT (1,500,016 steps); LANES check 20 ms. r02: NEW 2.4 s to the limit, LANES 29 ms (load average about 8)"
  },
  {
   "id": "ROB-2-oo-evaluator-method-dispatch-limit",
   "kind": "robustness",
   "program": "// An expression tree in object style: one struct per operator, each with an `eval` method that\n// evaluates its children through method calls, and a `show` method that renders it.\nstruct Num {\n    v: i64,\n}\n\nimpl Num {\n    fn eval(self) -> i64 {\n        return self.v;\n    }\n    fn show(self) -> str {\n        return str(self.v);\n    }\n}\n\nstruct Add {\n    l: Num,\n    r: Num,\n}\n\nimpl Add {\n    fn eval(self) -> i64 {\n        let a = self.l.eval();\n        let b = self.r.eval();\n        return a + b;\n    }\n    fn show(self) -> str {\n        return \"add(\" + self.l.show() + \", \" + self.r.show() + \")\";\n    }\n}\n\nstruct Sub {\n    l: Num,\n    r: Num,\n}\n\nimpl Sub {\n    fn eval(self) -> i64 {\n        let a = self.l.eval();\n        let b = self.r.eval();\n        return a - b;\n    }\n    fn show(self) -> str {\n        return \"sub(\" + self.l.show() + \", \" + self.r.show() + \")\";\n    }\n}\n\nstruct Mul {\n    l: Num,\n    r: Num,\n}\n\nimpl Mul {\n    fn eval(self) -> i64 {\n        let a = self.l.eval();\n        let b = self.r.eval();\n        return a * b;\n    }\n    fn show(self) -> str {\n        return \"mul(\" + self.l.show() + \", \" + self.r.show() + \")\";\n    }\n}\n\nstruct Div {\n    l: Num,\n    r: Num,\n}\n\nimpl Div {\n    fn eval(self) -> i64 {\n        let a = self.l.eval();\n        let b = self.r.eval();\n        return if b == 0 { 0 } else { a / b };\n    }\n    fn show(self) -> str {\n        return \"div(\" + self.l.show() + \", \" + self.r.show() + \")\";\n    }\n}\n\nstruct Mod {\n    l: Num,\n    r: Num,\n}\n\nimpl Mod {\n    fn eval(self) -> i64 {\n        let a = self.l.eval();\n        let b = self.r.eval();\n        return if b == 0 { 0 } else { a % b };\n    }\n    fn show(self) -> str {\n        return \"mod(\" + self.l.show() + \", \" + self.r.show() + \")\";\n    }\n}\n\nstruct Max {\n    l: Num,\n    r: Num,\n}\n\nimpl Max {\n    fn eval(self) -> i64 {\n        let a = self.l.eval();\n        let b = self.r.eval();\n        return if a > b { a } else { b };\n    }\n    fn show(self) -> str {\n        return \"max(\" + self.l.show() + \", \" + self.r.show() + \")\";\n    }\n}\n\nfn main() {\n    let k: secret<i64> = 42;\n    let e = Max { l: Max { l: Add { l: Div { l: Num { v: 17 }, r: Num { v: 18 } }, r: Mod { l: Num { v: 19 }, r: Num { v: 20 } } }, r: Sub { l: Max { l: Num { v: 21 }, r: Num { v: 22 } }, r: Add { l: Num { v: 23 }, r: Num { v: 24 } } } }, r: Add { l: Mul { l: Sub { l: Num { v: 25 }, r: Num { v: 26 } }, r: Mul { l: Num { v: 27 }, r: Num { v: 28 } } }, r: Div { l: Div { l: Num { v: 29 }, r: Num { v: 30 } }, r: Mod { l: Num { v: 31 }, r: Num { v: 32 } } } } };\n    print(e.show() + \" = \" + str(e.eval()));\n    let h = Add { l: Num { v: k }, r: e };\n    assert(h.eval() > -1000000000);\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "max(max(add(div(17, 18), mod(19, 20)), sub(max(21, 22), add(23, 24))), add(mul(sub(25, 26), mul(27, 28)), div(div(29, 30), mod(31, 32)))) = 19",
   "output_other_k": "max(max(add(div(17, 18), mod(19, 20)), sub(max(21, 22), add(23, 24))), add(mul(sub(25, 26), mul(27, 28)), div(div(29, 30), mod(31, 32)))) = 19",
   "root_cause": "This is the same nested summary-fixpoint machinery as ROB-1, reached through method dispatch: eval.rs call() 683-722, solve_summary() 841, method_call() 2009-2033. Max::eval, Div::eval and Mul::eval are marked recursive during e.eval(). The second evaluation h.eval() calls Max::eval(e) with an argument larger than the summary's joined arguments (`grew`, 709), so the summary's memo is dropped and it is solved again with joined arguments widened at SUMMARY_DEPTH 4. Its receivers `self.l` and `self.r` then become joins of several struct types or `top`. method_call dispatches a `top` receiver to every impl (`narrowed = recv.clone()`, 2033), so all seven `eval` summary contexts call one another and are solved as nested fixpoints. That is about 100k calls of each method and 25k summary rounds before the step budget is spent.",
   "explanation": "This is the classic Interpreter/Composite pattern: one struct per operator, each with an eval method that evaluates its children through method calls, and a 31-node tree. `show()` and the first `eval()` are cheap: without the last two lines the program takes 609 steps (p/r09b_nosecret.anb). Evaluating the tree a second time, wrapped in another node, spends the whole 1.5M-step budget, so NEW reports ANUBIS_IFC2_LIMIT and its check refuses the program. LANES accepts it in 16 ms. The secret is irrelevant: replacing `Num { v: k }` with `Num { v: 5 }` (p/r09c_pub2.anb, no secret at all) also reaches the limit. With 4 operator types the same generator takes 13,005 steps. Tree shapes built from 8 or 10 types happened to stay small (about 780 steps), so whether this happens depends on how the types nest in each other. Generator: ~/.cache/anubis-review-esc/ifc2-r2/precision/gen_methods.py.",
   "runner": "anubis-whole10 run --no-verify (rc 0/0)",
   "timing": "NEW ifc2-report 2.3 s to ANUBIS_IFC2_LIMIT (1,500,018 steps, 2.87M work); LANES check 16 ms"
  },
  {
   "id": "OR-1-file-store-single-label",
   "kind": "overrefusal",
   "program": "// A login gate: a failed PIN check appends a line to the audit log on disk. Afterwards the\n// program loads its public banner setting from a different file and prints the banner style.\nfn main() uses(io.print, fs.read, fs.write) {\n    let pin: secret<i64> = 42;\n    let attempt = 1234;\n    write_file(\"banner.cfg\", \"wide\");\n    if attempt != pin {\n        append_file(\"audit.log\", \"failed attempt\\n\");\n    }\n    let style = read_file(\"banner.cfg\");\n    if style == \"wide\" {\n        print(\"==================== WELCOME ====================\");\n    } else {\n        print(\"== WELCOME ==\");\n    }\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "==================== WELCOME ====================",
   "output_other_k": "==================== WELCOME ====================",
   "root_cause": "builtins.rs 176-178 and 193-194 with eval.rs 574-586 (Store::Files). Every write_file, append_file, delete_file or remove_file joins deep(args) and the pc into one label, Store::Files, which covers every file. Every read_file or open returns that label whatever path it reads. The append under the secret-decided `if` makes the whole file store secret, so read_file(\"banner.cfg\") is secret even though that file was written only with public data, under a public pc, before the secret branch.",
   "explanation": "NEW refuses with ANUBIS_IMPLICIT_FLOW ('print runs under a condition a secret decides'), both in ifc2-report and in `anubis-ifc2-v8e check`. LANES accepts. The banner file's content never depends on the PIN; only whether audit.log gets a line does. The output is the same for every PIN, including 1234, because the branch writes a different file. A variant that writes the secret itself to vault.dat and then reads a public settings.txt behaves the same way (p/h05_file_store_conflation.anb: NEW rc 1, LANES rc 0, prints 'logging: verbose' for both values). Keying the store by literal path, with a computed path meaning any path, would remove this over-refusal. Persisting a secret or an audit trail next to public configuration is a common vault pattern.",
   "runner": "anubis-whole10 run --no-verify (rc 0/0), run from ~/.cache/anubis-review-esc/ifc2-r2/precision/run",
   "timing": "NEW 22 ms; LANES 17 ms"
  },
  {
   "id": "OR-2-loop-built-tree-fold-mixes-payload",
   "kind": "overrefusal",
   "program": "// A binary search tree of accounts: public ids as keys, secret balances as values, built in a\n// loop. The report prints only how many accounts the tree holds.\nenum Tree {\n    Leaf,\n    Node(i64, secret<i64>, Tree, Tree),\n}\n\nfn insert(t, k, v) {\n    return match t {\n        Tree::Leaf => Tree::Node(k, v, Tree::Leaf, Tree::Leaf),\n        Tree::Node(k2, v2, l, r) => if k < k2 {\n            Tree::Node(k2, v2, insert(l, k, v), r)\n        } else {\n            Tree::Node(k2, v2, l, insert(r, k, v))\n        },\n    };\n}\n\nfn size(t) {\n    return match t {\n        Tree::Leaf => 0,\n        Tree::Node(k, v, l, r) => size(l) + 1 + size(r),\n    };\n}\n\nfn main() {\n    let base: secret<i64> = 42;\n    let ids = [50, 20, 80];\n    let mut t = Tree::Leaf;\n    for id in ids {\n        t = insert(t, id, base + id);\n    }\n    print(\"accounts: \" + str(size(t)));\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "accounts: 3",
   "output_other_k": "accounts: 3",
   "root_cause": "value.rs fold() 293-298 (called from widen_at_limit 1109) and eval.rs 1617-1619, the loop widening after COARSE_AFTER=12 rounds at SUMMARY_DEPTH=4. In the abstract, a tree built in a loop grows every round whatever the runtime count, so it is always folded at depth 4. fold() makes the subtree `top` with lab = shape_all (public) and ONE content label tlab = deep() of everything inside it, so the public keys k2 and the secret balances v2 share a label. insert's `if k < k2` reads k2 from part_of_top (tlab joined with lab), which is secret. V::select then raises the tree's shape (the ANUBIS_IFC2_TRACE result for insert shows shape=1), and size(t) becomes secret.",
   "explanation": "The runtime shape of a BST depends only on its keys. The balances ride along and are never compared or printed. Three inserts are enough for the refusal (ANUBIS_SECRET_EXFILTRATION 'secret data reaches print'), and `anubis-ifc2-v8e check` refuses too. LANES accepts. Two controls: the same tree with public balances is accepted (twin), and three straight-line inserts without the loop are accepted (p/r04f_avl_straight.anb). This is the family of r1_106 (a BST built in a loop), which passes now only because it wraps its prints in declassify. A realistic AVL tree whose rotations are decided by the heights stored in the nodes fails the same way (p/r04e_avl15_sizeonly.anb: 15 ids inserted in a loop, prints 'accounts: 15', NEW rc 1, LANES rc 0). Tracking content labels per payload position inside a folded value, or folding each enum payload field separately, would keep the public keys public.",
   "runner": "anubis-whole10 run --no-verify (rc 0/0)",
   "timing": "NEW 1.3 s under load (under 50 ms idle); LANES 0.8 s"
  },
  {
   "id": "OR-3-recursion-summary-fold-depth4",
   "kind": "overrefusal",
   "program": "// A binary search tree of accounts: public ids as keys, secret balances as values, inserted\n// one by one. The report prints only how many accounts the tree holds.\nenum Tree {\n    Leaf,\n    Node(i64, secret<i64>, Tree, Tree),\n}\n\nfn insert(t, k, v) {\n    return match t {\n        Tree::Leaf => Tree::Node(k, v, Tree::Leaf, Tree::Leaf),\n        Tree::Node(k2, v2, l, r) => if k < k2 {\n            Tree::Node(k2, v2, insert(l, k, v), r)\n        } else {\n            Tree::Node(k2, v2, l, insert(r, k, v))\n        },\n    };\n}\n\nfn size(t) {\n    return match t {\n        Tree::Leaf => 0,\n        Tree::Node(k, v, l, r) => size(l) + 1 + size(r),\n    };\n}\n\nfn main() {\n    let base: secret<i64> = 42;\n    let t0 = insert(Tree::Leaf, 50, base + 50);\n    let t1 = insert(t0, 20, base + 20);\n    let t2 = insert(t1, 80, base + 80);\n    let t3 = insert(t2, 10, base + 10);\n    let t4 = insert(t3, 30, base + 30);\n    let t5 = insert(t4, 70, base + 70);\n    let t6 = insert(t5, 90, base + 90);\n    let t7 = insert(t6, 5, base + 5);\n    let t8 = insert(t7, 15, base + 15);\n    print(\"accounts: \" + str(size(t8)));\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "accounts: 9",
   "output_other_k": "accounts: 9",
   "root_cause": "eval.rs call() 695-722 and widen_summary() 520 (SUMMARY_DEPTH=4), plus solve() 824, which widens recursive results with widen_summary from round 1 on. Once insert recurses it is in recursive_callees, and its calls go to summary contexts whose joined arguments are widened at depth 4. A tree at least 4 levels deep has its lower subtrees folded (value.rs fold() 293), which gives them a single content label that mixes the public keys with the secret balances. The key comparison `k < k2` against a folded key is then secret, so the rebuilt tree's shape, and size(), are secret.",
   "explanation": "This has the same effect as OR-2 without any loop: nine straight-line inserts that reach depth 4 (50\u219220\u219210\u21925). The recursion-summary widening, not the loop, triggers the fold here, so the fix for OR-2 would not cover it. The threshold is exactly the depth: with the same program printing keys, depth 3 (h03_d3) is accepted and depth 4, 5 and 6 are refused. LANES accepts this program. Variants: p/r04g_avl_straight15.anb (an AVL tree with 15 straight-line inserts, prints 'accounts: 15', LANES rc 0, NEW rc 1); p/h03_bst_keys.anb (prints in-order ids, LANES refuses too); p/h04b_orgchart_untyped.anb (a recursive walk over an org chart of structs {name, secret salary, reports}, printing names, deeper than 4; LANES refuses too). Recursive walks over any tree deeper than 4 that stores public and secret data together lose the public data.",
   "runner": "anubis-whole10 run --no-verify (rc 0/0)",
   "timing": "NEW 23 ms; LANES 102 ms"
  },
  {
   "id": "OR-4-kind-label-of-secret-scalar",
   "kind": "overrefusal",
   "program": "// Input validation report: the PIN must be an integer; the report states the PIN's runtime type\n// (never its value).\nfn main() {\n    let pin: secret<i64> = 42;\n    let kind = type(pin);\n    if kind == \"int\" {\n        print(\"pin format ok\");\n    } else {\n        print(\"pin format bad\");\n    }\n}\n",
   "new_rc": 1,
   "lanes_rc": 0,
   "output_k42": "pin format ok",
   "output_other_k": "pin format ok",
   "root_cause": "value.rs kind_label() 444-463: `if self.scalar || kinds > 1 { self.lab }`, so for a pure scalar the 'kind' label is the scalar's content label. The abstract domain has no scalar-kind component, so builtins.rs 219 (`\"type\" => V::scalar(arg(a,0).kind_label())`) makes type(pin) secret for any secret scalar, even when its kind is fixed by the operations that produced it (an int literal, int arithmetic). The same function is used by plus() at eval.rs 2390 (`lab: l.lab.join(r.kind_label())`) for `list + x`. There only list versus non-list matters, and a pure scalar is never a list, yet the list's length label takes the scalar's content label.",
   "explanation": "type() of an i64 is 'int' for every value. NEW refuses with ANUBIS_IMPLICIT_FLOW and LANES accepts. A second manifestation, which LANES also refuses: `digests = digests + sha256_hex(str(pin))` makes len(digests) secret, although appending a scalar always adds exactly one element (p/h17_list_append_scalar_kind.anb prints 'digests logged: 2' for both values; `digests + [sha256_hex(...)]` is accepted, p/h17c_list_concat.anb). A kind component for scalars, or labelling plus()'s list-versus-non-list decision separately from scalar content, would fix both.",
   "runner": "anubis-whole10 run --no-verify (rc 0/0)",
   "timing": "NEW 24 ms; LANES 26 ms"
  },
  {
   "id": "OR-5-container-shape-label-taints-present-key-reads",
   "kind": "overrefusal",
   "program": "// A login session held as a map. A successful password check adds a token key; the audit line\n// prints the user name, which is in the map from the start either way.\nfn main() {\n    let stored: secret<i64> = 42;\n    let guess = 7;\n    let mut session = {\"user\": \"ana\", \"role\": \"guest\"};\n    if guess == stored {\n        session[\"token\"] = \"t-1\";\n    }\n    print(\"audit: session for \" + session[\"user\"]);\n    print(\"role: \" + session.role);\n}\n",
   "new_rc": 1,
   "lanes_rc": 1,
   "output_k42": "audit: session for ana\nrole: guest",
   "output_other_k": "audit: session for ana\nrole: guest",
   "root_cause": "eval.rs set_path() 1378/1433 (and 1359 for struct fields) raise the container's own `lab` (its key or field set) by the pc when a place write may add a key or field, which is correct for len and keys. index_read() then ends with `out.raise(b.lab.join(il))` (2783), and field_read() with `out.raise(b.lab)` (2816). So a read of a literal key that is known and not `maybe` (certainly present, with the same value on both paths) is raised by the key-set label. Whether 'token' was added does not change session[\"user\"].",
   "explanation": "Adding an optional key or field under a secret condition makes every later read of the map or struct secret, even reads of keys that were there from the start. The struct form behaves the same way (p/h01c_struct_field_added.anb: a literal omitting `token`, `s.token = ...` under a secret condition, then print(s.user) is refused; output 'audit: session for ana' for both values). An insert() under the secret condition behaves the same way (p/h01b_session_insert.anb). LANES refuses all three (IMPLICIT_FLOW on the assignment), so this is IFC v2 imprecision, not a new refusal. Raising reads of certainly-present keys only by klab, and by lab only when the key may be absent or the kind is mixed, would fix it.",
   "runner": "label-erased twin (secret<\u2026> annotations removed) on anubis-ifc2-v8e run --no-verify, rc 0/0; every runner refuses the labelled program (LANES implicit flow)",
   "timing": "NEW 75 ms (loaded); LANES 52 ms"
  },
  {
   "id": "OR-6-closure-summary-reach-label-merges-pipelines",
   "kind": "overrefusal",
   "program": "// Composable transforms: `then(f, g)` chains two stages. The billing pipeline mixes in a secret\n// salt (its result is only range-checked). The display pipeline reads the public offset of a\n// settings record that also holds an API key; its result is printed.\nstruct Settings {\n    offset: i64,\n    api_key: secret<i64>,\n}\n\nfn then(f, g) {\n    return |x| g(f(x));\n}\n\nfn main() {\n    let salt: secret<i64> = 42;\n    let cfg = Settings { offset: 7, api_key: salt + 1 };\n    let salted = then(then(then(then(|x| x + salt, |x| x * 2), |x| x + 1), |x| x % 1000), |x| x - 3);\n    let h = salted(5);\n    assert(h > -1000000);\n    let shown = then(then(then(then(|x| x + cfg.offset, |x| x * 2), |x| x + 1), |x| x % 1000), |x| x - 3);\n    print(shown(5));\n}\n",
   "new_rc": 1,
   "lanes_rc": 1,
   "output_k42": "22",
   "output_other_k": "22",
   "root_cause": "value.rs fns_widened() 1181 and env_reach() 1261, with eval.rs add_summary() 537 and analyze_lambda(). Closures nested deeper than CLOSURE_DEPTH=3 are summarized per (lambda, reach label). The reach label is the join of everything the captures can reach. The display pipeline's first stage captures `cfg`, whose api_key field is secret, so its reach is SEC even though the stage reads only cfg.offset. It therefore shares the SEC summary of the `then` lambda with the salted pipeline. The joined environment makes f and g in `|x| g(f(x))` any stage of either pipeline, including `|x| x + salt`.",
   "explanation": "This is a variant of r1_099 that its fix does not cover. The reach-label key separates a public pipeline from a secret one, but not two pipelines that both reach a secret. Control: the display pipeline alone is accepted by NEW (p/h06c_ctrl.anb, rc 0); adding the unrelated salted pipeline makes NEW refuse print(shown(5)). The printed value is 22 for every salt. LANES refuses both the program and the control ('captured secret in closure shown'), so LANES would not accept it either way.",
   "runner": "anubis-whole10 run --no-verify (rc 0/0)",
   "timing": "NEW 20 ms; LANES 72 ms"
  },
  {
   "id": "OR-7-summary-context-after-12-merges-all-secret-rows",
   "kind": "overrefusal",
   "program": "// Timesheets: each row is an employee name followed by the hours logged per day. Hours are\n// confidential; the summary line shows the name and how many days were logged.\nfn summary(row) {\n    return row[0] + \": \" + str(len(row) - 1) + \" days\";\n}\n\nfn main() {\n    let h: secret<i64> = 42;\n    print(summary([\"ana\", h + 0]));\n    print(summary([\"ben\", h + 0, h + 1]));\n    print(summary([\"cho\", h + 0, h + 1, h + 2]));\n    print(summary([\"dev\", h + 0, h + 1, h + 2, h + 3]));\n    print(summary([\"eli\", h + 0, h + 1, h + 2, h + 3, h + 4]));\n    print(summary([\"fay\", h + 0, h + 1, h + 2, h + 3, h + 4, h + 5]));\n    print(summary([\"gus\", h + 0, h + 1, h + 2, h + 3, h + 4, h + 5, h + 6]));\n    print(summary([\"hal\", h + 0, h + 1, h + 2, h + 3, h + 4, h + 5, h + 6, h + 7]));\n    print(summary([\"ivy\", h + 0, h + 1, h + 2, h + 3, h + 4, h + 5, h + 6, h + 7, h + 8]));\n    print(summary([\"jon\", h + 0, h + 1, h + 2, h + 3, h + 4, h + 5, h + 6, h + 7, h + 8, h + 9]));\n    print(summary([\"kai\", h + 0, h + 1, h + 2, h + 3, h + 4, h + 5, h + 6, h + 7, h + 8, h + 9, h + 10]));\n    print(summary([\"lea\", h + 0, h + 1, h + 2, h + 3, h + 4, h + 5, h + 6, h + 7, h + 8, h + 9, h + 10, h + 11]));\n    print(summary([\"max\", h + 0, h + 1, h + 2, h + 3, h + 4, h + 5, h + 6, h + 7, h + 8, h + 9, h + 10, h + 11, h + 12]));\n    print(summary([\"nia\", h + 0, h + 1, h + 2, h + 3, h + 4, h + 5, h + 6, h + 7, h + 8, h + 9, h + 10, h + 11, h + 12, h + 13]));\n}\n",
   "new_rc": 1,
   "lanes_rc": 1,
   "output_k42": "ana: 1 days\nben: 2 days\ncho: 3 days\ndev: 4 days\neli: 5 days\nfay: 6 days\ngus: 7 days\nhal: 8 days\nivy: 9 days\njon: 10 days\nkai: 11 days\nlea: 12 days\nmax: 13 days\nnia: 14 days",
   "output_other_k": "ana: 1 days\nben: 2 days\ncho: 3 days\ndev: 4 days\neli: 5 days\nfay: 6 days\ngus: 7 days\nhal: 8 days\nivy: 9 days\njon: 10 days\nkai: 11 days\nlea: 12 days\nmax: 13 days\nnia: 14 days",
   "root_cause": "eval.rs call() 695-722. After CONTEXTS_PER_CALLEE=12 precise contexts, a call goes to a summary context keyed by (callee, pc, the join of every argument's deep label), and its arguments are the join of all calls merged there. All 13th and later rows have deep label SEC, so they merge. The join of lists of different lengths forgets positions (value.rs join: `items: None, all: a.all \u2294 b.all`), so row[0] reads li.all, the join of names and hours, which is SEC.",
   "explanation": "This is a variant of r1_061, r1_086 and r1_098, which the deep-label key fixed only when the merged calls have different labels. Here every row carries a secret, but no call ever prints one, and each precise context is accepted. Control: the same program with 12 rows is accepted by NEW (p/h16c_12rows.anb). The 13th distinct context flips the verdict. LANES refuses both, since a list with a secret passed to a function that prints is refused.",
   "runner": "label-erased twin on anubis-ifc2-v8e run --no-verify, rc 0/0; every runner refuses the labelled program",
   "timing": "NEW 17 ms; LANES 42 ms"
  },
  {
   "id": "OR-8-max-nodes-collapse-merges-map-literal-keys",
   "kind": "overrefusal",
   "program": "// A settings table: a host, a secret API key, and the allowed choices for a few dozen options.\n// A helper prints the public host setting.\nfn show_host(settings) {\n    print(\"host = \" + settings[\"host\"]);\n}\n\nfn main() {\n    let key: secret<i64> = 42;\n    let settings = {\n        \"host\": \"db.internal\",\n        \"api_key\": str(key),\n        \"choices_00\": [\"v0_0\", \"v0_1\", \"v0_2\", \"v0_3\", \"v0_4\", \"v0_5\", \"v0_6\", \"v0_7\", \"v0_8\"],\n        \"choices_01\": [\"v1_0\", \"v1_1\", \"v1_2\", \"v1_3\", \"v1_4\", \"v1_5\", \"v1_6\", \"v1_7\", \"v1_8\"],\n        \"choices_02\": [\"v2_0\", \"v2_1\", \"v2_2\", \"v2_3\", \"v2_4\", \"v2_5\", \"v2_6\", \"v2_7\", \"v2_8\"],\n        \"choices_03\": [\"v3_0\", \"v3_1\", \"v3_2\", \"v3_3\", \"v3_4\", \"v3_5\", \"v3_6\", \"v3_7\", \"v3_8\"],\n        \"choices_04\": [\"v4_0\", \"v4_1\", \"v4_2\", \"v4_3\", \"v4_4\", \"v4_5\", \"v4_6\", \"v4_7\", \"v4_8\"],\n        \"choices_05\": [\"v5_0\", \"v5_1\", \"v5_2\", \"v5_3\", \"v5_4\", \"v5_5\", \"v5_6\", \"v5_7\", \"v5_8\"],\n        \"choices_06\": [\"v6_0\", \"v6_1\", \"v6_2\", \"v6_3\", \"v6_4\", \"v6_5\", \"v6_6\", \"v6_7\", \"v6_8\"],\n        \"choices_07\": [\"v7_0\", \"v7_1\", \"v7_2\", \"v7_3\", \"v7_4\", \"v7_5\", \"v7_6\", \"v7_7\", \"v7_8\"],\n        \"choices_08\": [\"v8_0\", \"v8_1\", \"v8_2\", \"v8_3\", \"v8_4\", \"v8_5\", \"v8_6\", \"v8_7\", \"v8_8\"],\n        \"choices_09\": [\"v9_0\", \"v9_1\", \"v9_2\", \"v9_3\", \"v9_4\", \"v9_5\", \"v9_6\", \"v9_7\", \"v9_8\"],\n        \"choices_10\": [\"v10_0\", \"v10_1\", \"v10_2\", \"v10_3\", \"v10_4\", \"v10_5\", \"v10_6\", \"v10_7\", \"v10_8\"],\n        \"choices_11\": [\"v11_0\", \"v11_1\", \"v11_2\", \"v11_3\", \"v11_4\", \"v11_5\", \"v11_6\", \"v11_7\", \"v11_8\"],\n        \"choices_12\": [\"v12_0\", \"v12_1\", \"v12_2\", \"v12_3\", \"v12_4\", \"v12_5\", \"v12_6\", \"v12_7\", \"v12_8\"],\n        \"choices_13\": [\"v13_0\", \"v13_1\", \"v13_2\", \"v13_3\", \"v13_4\", \"v13_5\", \"v13_6\", \"v13_7\", \"v13_8\"],\n        \"choices_14\": [\"v14_0\", \"v14_1\", \"v14_2\", \"v14_3\", \"v14_4\", \"v14_5\", \"v14_6\", \"v14_7\", \"v14_8\"],\n        \"choices_15\": [\"v15_0\", \"v15_1\", \"v15_2\", \"v15_3\", \"v15_4\", \"v15_5\", \"v15_6\", \"v15_7\", \"v15_8\"],\n        \"choices_16\": [\"v16_0\", \"v16_1\", \"v16_2\", \"v16_3\", \"v16_4\", \"v16_5\", \"v16_6\", \"v16_7\", \"v16_8\"],\n        \"choices_17\": [\"v17_0\", \"v17_1\", \"v17_2\", \"v17_3\", \"v17_4\", \"v17_5\", \"v17_6\", \"v17_7\", \"v17_8\"],\n        \"choices_18\": [\"v18_0\", \"v18_1\", \"v18_2\", \"v18_3\", \"v18_4\", \"v18_5\", \"v18_6\", \"v18_7\", \"v18_8\"],\n        \"choices_19\": [\"v19_0\", \"v19_1\", \"v19_2\", \"v19_3\", \"v19_4\", \"v19_5\", \"v19_6\", \"v19_7\", \"v19_8\"],\n        \"choices_20\": [\"v20_0\", \"v20_1\", \"v20_2\", \"v20_3\", \"v20_4\", \"v20_5\", \"v20_6\", \"v20_7\", \"v20_8\"],\n        \"choices_21\": [\"v21_0\", \"v21_1\", \"v21_2\", \"v21_3\", \"v21_4\", \"v21_5\", \"v21_6\", \"v21_7\", \"v21_8\"],\n        \"choices_22\": [\"v22_0\", \"v22_1\", \"v22_2\", \"v22_3\", \"v22_4\", \"v22_5\", \"v22_6\", \"v22_7\", \"v22_8\"],\n        \"choices_23\": [\"v23_0\", \"v23_1\", \"v23_2\", \"v23_3\", \"v23_4\", \"v23_5\", \"v23_6\", \"v23_7\", \"v23_8\"],\n        \"choices_24\": [\"v24_0\", \"v24_1\", \"v24_2\", \"v24_3\", \"v24_4\", \"v24_5\", \"v24_6\", \"v24_7\", \"v24_8\"],\n        \"choices_25\": [\"v25_0\", \"v25_1\", \"v25_2\", \"v25_3\", \"v25_4\", \"v25_5\", \"v25_6\", \"v25_7\", \"v25_8\"],\n        \"choices_26\": [\"v26_0\", \"v26_1\", \"v26_2\", \"v26_3\", \"v26_4\", \"v26_5\", \"v26_6\", \"v26_7\", \"v26_8\"],\n        \"choices_27\": [\"v27_0\", \"v27_1\", \"v27_2\", \"v27_3\", \"v27_4\", \"v27_5\", \"v27_6\", \"v27_7\", \"v27_8\"]\n    };\n    print(\"host = \" + settings[\"host\"]);\n    show_host(settings);\n}\n",
   "new_rc": 1,
   "lanes_rc": 1,
   "output_k42": "host = db.internal\nhost = db.internal",
   "output_other_k": "host = db.internal\nhost = db.internal",
   "root_cause": "value.rs widen_from() 848-866 and collapse_items() 869-901, applied to every call argument (eval.rs 675). A value over MAX_NODES=256 first has its lists' positions forgotten and its maps' literal keys merged into `other`. The map therefore loses its per-key labels: settings[\"host\"] inside show_host reads the join of every value, including api_key, which is SEC. In main, where no widening happened, the same read is accepted, and the report names only `show_host`.",
   "explanation": "This is a variant of r1_100 (a MAX_NODES fold merging record fields) that its fix does not cover. The fix collapses list positions before folding, but collapse itself merges map literal keys, so a wide map with mixed labels becomes secret as soon as it is passed to any function. Control: the same program with 10 option lists (p/h12e_map_lists10.anb, below 256 nodes) is accepted by NEW. The same happens with 260 plain string options (p/h12_wide_map_collapse.anb). LANES refuses both, because a map holding a secret passed to a function is refused. Suggested fix: when collapsing, keep literal keys whose values are small (or keep per-key labels) and forget only list positions and nested sizes.",
   "runner": "label-erased twin on anubis-ifc2-v8e run --no-verify, rc 0/0; every runner refuses the labelled program",
   "timing": "NEW 17 ms; LANES 34 ms"
  },
  {
   "id": "OR-9-struct-variant-field-order-join-mixes-all-fields",
   "kind": "overrefusal",
   "program": "// An event log: login events carry the user name and the secret PIN that was entered. Two call\n// sites build the event with the fields written in different orders. The report prints users.\nenum Event {\n    Login { user: str, pin: secret<i64> },\n    Logout { user: str },\n}\n\nfn main() {\n    let pin: secret<i64> = 42;\n    let events = [\n        Event::Login { user: \"ana\", pin: pin },\n        Event::Login { pin: pin + 1, user: \"ben\" },\n        Event::Logout { user: \"ana\" },\n    ];\n    for e in events {\n        match e {\n            Event::Login { user, pin } => print(\"login \" + user),\n            Event::Logout { user } => print(\"logout \" + user),\n        };\n    }\n}\n",
   "new_rc": 1,
   "lanes_rc": 1,
   "output_k42": "login ana\nlogin ben\nlogout ana",
   "output_other_k": "login ana\nlogin ben\nlogout ana",
   "root_cause": "value.rs join_enum() 1370-1400: when two payloads of one struct variant have different `names` orders, the result is `fields: vec![any; n]`, so every position holds the join of every field. A named sub-pattern (bind_pattern `named`) or field_read by name then reads `user` as the join of user and pin, which is SEC. The change description says struct-variant payloads 'join by name when literal orders differ'. The code does not do that; it joins everything together.",
   "explanation": "The list element of `for e in events` is the join of the two Login literals, whose fields are written in different orders. Writing `user` first in both literals makes NEW accept the program (p/h15c_same_order.anb, rc 0). At runtime a name-based read gives the user in both orders, and the output is the same for every PIN. Joining by name (reordering b's fields to a's names) would give a precise result for name-based reads, and positional reads could keep the conservative join if they must. LANES refuses both orders, because the list contains a secret.",
   "runner": "label-erased twin on anubis-ifc2-v8e run --no-verify, rc 0/0; every runner refuses the labelled program",
   "timing": "NEW 16 ms; LANES 45 ms"
  }
 ],
 "verdicts": [
  {
   "id": "r2ctl_L01_int_literal_patterns_collide_in_f64",
   "kind": "leak",
   "lens": "control",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "second arm\\ndone",
   "output_other_k": "done (123456)",
   "reason": "Reproduced on NEW run. ifc2-report says 'no information flow found' and LANES check passes. With the secret at 42 the guard fails and arm 2 (literal 9007199254740993, which matches the f64 value 9007199254740992.0) prints, so the output depends on the secret. This is the same family as r1_085, but the program is new."
  },
  {
   "id": "r2ctl_L02_select_ignores_struct_field_order",
   "kind": "leak",
   "lens": "control",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1",
   "output_other_k": "0 (123456)",
   "reason": "Reproduced on NEW run: len(c[0]) prints 1 or 0 because the struct's field order is chosen by the secret. Same root as r2v_02, r2_fn_L3 and the struct part of r2p_01 (V::select struct arm ignores literal field order)."
  },
  {
   "id": "r2ctl_L03_select_ignores_map_key_order",
   "kind": "leak",
   "lens": "control",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "b",
   "output_other_k": "a (123456)",
   "reason": "Reproduced on NEW run: keys(m)[0] is b or a. Same root as r2v_01, r2_fn_L2 and r2p_01 (V::select map arm compares sorted BTreeMap keys and ignores insertion order)."
  },
  {
   "id": "r2ctl_L04_select_equal_maybe_fields_not_same_shape",
   "kind": "leak",
   "lens": "control",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1",
   "output_other_k": "2 (123456)",
   "reason": "Reproduced on NEW run: len(c) prints 1 or 2 depending on the secret, and IFC v2 and LANES both accept. Same root as r2v_04 and the struct variant in r2p_02 (select treats equal maybe_fields as the same shape)."
  },
  {
   "id": "r2ctl_L05_select_equal_maybe_keys_map",
   "kind": "leak",
   "lens": "control",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1",
   "output_other_k": "2 (123456)",
   "reason": "Reproduced on NEW run: len(m) prints 1 or 2. This is the map twin of L04 and has the same root as r2v_03 and r2p_02."
  },
  {
   "id": "r2ctl_L06_method_narrowing_drops_maybe_fields",
   "kind": "leak",
   "lens": "control",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "2",
   "output_other_k": "1 (123456)",
   "reason": "Reproduced on NEW run: len(r) prints 2 or 1 depending on the secret continue inside the method, and IFC v2 and LANES both accept. Related to r1_102 and r1_043 (a field added under a secret pc), but this path goes through method-receiver narrowing, which is new."
  },
  {
   "id": "r2ctl_L07_whilelet_scrutinee_continue_does_not_lift_next_header",
   "kind": "leak",
   "lens": "control",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "poll\\ndone",
   "output_other_k": "poll\\npoll\\npoll\\ndone (123456)",
   "reason": "Reproduced on NEW run: the number of 'poll' lines is 1 or 3. This is the continue counterpart of r1_080 (a break in a while-let scrutinee), which the round-1 fix does not cover."
  },
  {
   "id": "r2ctl_L08_panic_under_secret_pc_not_egress",
   "kind": "leak",
   "lens": "control",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "(no output, rc 0)",
   "output_other_k": "stderr 'ANUBIS_PANIC: limit exceeded' + 'Error: run failed: exit_code=Some(101)', rc 1 (123456)",
   "reason": "Reproduced on NEW run. A public panic under a secret condition changes stderr and the exit status, and IFC v2 reports nothing. It is in scope because the task treats panic as explicit and modelled egress. Same root as L09, r2p_09 and r2_fn_L4. r1_078 covered only the continuation after exit."
  },
  {
   "id": "r2ctl_L09_exit_status_under_secret_pc_not_egress",
   "kind": "leak",
   "lens": "control",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "(no output, rc 0)",
   "output_other_k": "'Error: run failed: exit_code=Some(3)', rc 1 (123456)",
   "reason": "Reproduced on NEW run. exit(3) under a secret condition is accepted, and the exit status and stderr differ. It is close to r1_078 (exit(0) followed by println) but is a different program: there is no continuation, and the status is non-zero. Same root as L08, r2p_09 and r2_fn_L4."
  },
  {
   "id": "r2ctl_O01_same_shape_assignment_under_secret_raises_tag",
   "kind": "overrefusal",
   "lens": "control",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "have a reading\\nreading",
   "output_other_k": "have a reading\\nreading (123456)",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW, and LANES check passes. The program runs on the whole10 runner (NEW's run refuses it) with identical output for 42 and 123456, so IFC v2 alone would newly refuse it. Same root as r2v_14 (an assignment raises the shape) and r2v_12 and r2v_13 (the state merge)."
  },
  {
   "id": "r2ctl_O02_distinct_float_literal_patterns_not_disjoint",
   "kind": "overrefusal",
   "lens": "control",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "standard tier\\npremium tier",
   "output_other_k": "standard tier\\npremium tier (pin 1234, guard true)",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW, and LANES passes. On whole10 the output is identical for pin 42 and pin 1234 (1234 makes the guard true)."
  },
  {
   "id": "r2ctl_O03_early_return_same_shape_raises_tag",
   "kind": "overrefusal",
   "lens": "control",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "discount applied",
   "output_other_k": "discount applied (123456)",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW, and LANES passes. On whole10 the output is identical. Same root as r2v_15 (do_return raises the shape of the returned value)."
  },
  {
   "id": "r2ctl_O04_select_of_compositions_raises_every_stage",
   "kind": "overrefusal",
   "lens": "control",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "audit step\\npipeline done",
   "output_other_k": "audit step\\npipeline done (123456)",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW in `audit`, and LANES passes. On whole10 the output is identical."
  },
  {
   "id": "r2ctl_R01_shift_register_hits_loop_limit",
   "kind": "robustness",
   "lens": "control",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1778 (key 7)",
   "output_other_k": "2032 (key 8); the difference is only through the intended declassify",
   "reason": "I generated the program from its description, and it is byte-identical to control/p/r08_delay64.anb. NEW ifc2-report reports ANUBIS_IFC2_LIMIT ('a loop did not settle') in 0.02 s. LANES passes, and whole10 runs it. This meets the robustness criterion: an IFC2_LIMIT on a plausible generated delay-line or DSP loop."
  },
  {
   "id": "r2v_01_select_map_insertion_order",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "b\\na",
   "output_other_k": "a\\nb (123456)",
   "reason": "Reproduced on NEW run: for-in iterates in the insertion order the secret chose. Same root as r2ctl_L03, r2_fn_L2 and r2p_01."
  },
  {
   "id": "r2v_02_select_struct_field_order",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "P { b: [[]], a: [] }",
   "output_other_k": "P { a: [], b: [[]] } (123456)",
   "reason": "Reproduced on NEW run: the printed field order reveals the secret. Same root as r2ctl_L02 and r2_fn_L3."
  },
  {
   "id": "r2v_03_select_maybe_keys_map",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1",
   "output_other_k": "2 (123456)",
   "reason": "Reproduced on NEW run. Same root as r2ctl_L05 and r2p_02 (equal maybe sets are treated as the same shape)."
  },
  {
   "id": "r2v_04_select_maybe_fields_struct",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1",
   "output_other_k": "2 (123456)",
   "reason": "Reproduced on NEW run. Same root as r2ctl_L04."
  },
  {
   "id": "r2v_05_nested_write_into_missing_key_makes_it_present",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1",
   "output_other_k": "2 (123456)",
   "reason": "Reproduced on NEW run: len(r) is 1 or 2. This is a variant of r1_042 (0.90 text similarity) that adds the nested no-op write m[\"b\"][\"c\"] = 5, which defeats the round-1 fix. It is not the round-1 program. Same root as r2p_03."
  },
  {
   "id": "r2v_06_nested_write_into_missing_field_makes_it_present",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1",
   "output_other_k": "2 (123456)",
   "reason": "Reproduced on NEW run. This is the struct variant of r2v_05 and a variant of r1_043 (it adds the nested no-op write p.b.c = 5). It is not the round-1 program."
  },
  {
   "id": "r2v_07_remove_computed_key_keeps_known_keys_present",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1",
   "output_other_k": "2 (123456)",
   "reason": "Reproduced on NEW run. A remove with a computed key does not make the known keys maybe. Same root as r2p_05."
  },
  {
   "id": "r2v_08_merge_drops_first_map_value_when_second_key_maybe",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "42",
   "output_other_k": "123456",
   "reason": "Reproduced on NEW run: the variant-field secret is printed. My control (the same program with r = a instead of merge) is refused by IFC v2 with ANUBIS_SECRET_EXFILTRATION, which confirms that IFC v2 treats pv as secret through the variant field type. Same root as r2p_04."
  },
  {
   "id": "r2v_09_pattern_decision_top_skips_concrete_parts",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "other\\nnone",
   "output_other_k": "other\\nother (123456)",
   "reason": "Reproduced on NEW run: which arm prints on the second call depends on the secret tag of inner, and IFC v2 and LANES both accept."
  },
  {
   "id": "r2v_10_join_enum_different_field_sets_drops_names",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "42",
   "output_other_k": "123456",
   "reason": "Reproduced on NEW run: the declared-secret variant field b is printed. My control (let e = E::V { b: sv }; println(e.b)) is refused by IFC v2, so the join launders the secret. Related to r1_046 (a positional join of an enum struct variant) but a different program and case (different field sets). Same code as r2v_16 and OR-9."
  },
  {
   "id": "r2v_11_remove_trap_message_prints_key_and_index",
   "kind": "leak",
   "lens": "values",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "start + ANUBIS_MISSING_KEY: key `42` is not present in the map ... exit 101",
   "output_other_k": "start + ANUBIS_MISSING_KEY: key `123456` is not present ... exit 101",
   "reason": "Reproduced on NEW's own runtime, which has the round-1 redaction: the remove() trap message still prints the secret key. Both runs trap, so only the message content differs. Same family as r1_032 and r1_059, through the missed anubis_remove helper. Same root as r2p_14."
  },
  {
   "id": "r2v_12_state_merge_push_on_both_branches",
   "kind": "overrefusal",
   "lens": "values",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "entries: 1",
   "output_other_k": "entries: 1 (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES passes. On whole10 the output is identical."
  },
  {
   "id": "r2v_13_state_merge_same_key_added_on_both_branches",
   "kind": "overrefusal",
   "lens": "values",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "[user, status]",
   "output_other_k": "[user, status] (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES passes. On whole10 the output is identical. Same root as r2v_12."
  },
  {
   "id": "r2v_14_state_merge_reassign_same_length",
   "kind": "overrefusal",
   "lens": "values",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "tick\\ntick",
   "output_other_k": "tick\\ntick (123456)",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW, and LANES passes. On whole10 the output is identical. This is the statement-assignment member of the r1_035 family, a different program. Same root as r2ctl_O01."
  },
  {
   "id": "r2v_15_early_return_same_shape_raised",
   "kind": "overrefusal",
   "lens": "values",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "tag slot\\ntag slot",
   "output_other_k": "tag slot\\ntag slot (123456)",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW, and LANES passes. On whole10 the output is identical. Same root as r2ctl_O03."
  },
  {
   "id": "r2v_16_enum_variant_literal_order_join_over_refusal",
   "kind": "overrefusal",
   "lens": "values",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "bob",
   "output_other_k": "bob (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES passes. On whole10 the output is identical. The join_enum code is the same as in r2v_10 and OR-9."
  },
  {
   "id": "r2v_17_computed_key_rewrite_of_existing_keys",
   "kind": "overrefusal",
   "lens": "values",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "[a, b]",
   "output_other_k": "[a, b] (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES passes. On whole10 the output is identical. The finder rightly calls this a precision limit rather than a soundness bug."
  },
  {
   "id": "r2v_18_shared_list_dag_exponential_memory",
   "kind": "robustness",
   "lens": "values",
   "confirmed": true,
   "new_rc": 137,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "(no secret) NEW ifc2-report OOM-killed; NEW run: ANUBIS_ANALYSIS_LIMIT (memory budget)",
   "output_other_k": "n/a (no secret)",
   "reason": "Reproduced twice. ifc2-report under CAP=1500M was killed after about 6.1 to 6.8 s: shell rc 137, and the journal shows 'The kernel OOM killer killed some processes' with a 1.4G memory peak. There is no ANUBIS_IFC2_LIMIT. LANES check passes in 0.03 s. NEW's own run fails closed with ANUBIS_ANALYSIS_LIMIT (the memory budget), so a valid program is refused because of IFC v2. The program is synthetic (a DAG of shared lists), but the crash reproduces."
  },
  {
   "id": "r2_fn_L1_min_by_max_by_callback_argument",
   "kind": "leak",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1\\n2\\n1\\n3\\ndone",
   "output_other_k": "1\\n2\\n2\\n3\\ndone (s=5)",
   "reason": "Reproduced on NEW run: the key callback's arguments (the running minimum) depend on the secret. This is the min_by counterpart of the sort_by and short-circuit HOF family (r1_021, r1_022, r1_074), a different program. Same root as r2p_06."
  },
  {
   "id": "r2_fn_L2_map_insertion_order_not_modelled_in_select",
   "kind": "leak",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "[b, a]",
   "output_other_k": "[a, b] (s=500)",
   "reason": "Reproduced on NEW run, and NEW's full checker accepts it. Same root as r2ctl_L03, r2v_01 and r2p_01."
  },
  {
   "id": "r2_fn_L3_struct_literal_field_order_not_modelled_in_select",
   "kind": "leak",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "Tag::Plain",
   "output_other_k": "[] (s=500)",
   "reason": "Reproduced on NEW run: p[0] reads the first field in literal order. Same root as r2ctl_L02 and r2v_02."
  },
  {
   "id": "r2_fn_L4_panic_and_exit_under_secret_pc_not_refused",
   "kind": "leak",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "checking (rc 0)",
   "output_other_k": "checking + ANUBIS_PANIC: rejected entry + exit_code=Some(101) (s=2)",
   "reason": "Reproduced on NEW run: the panic inside a callback under a secret condition is accepted. Same root as r2ctl_L08, r2ctl_L09 and r2p_09."
  },
  {
   "id": "r2_fn_O1_recursive_hof_summary_merges_closure_captures",
   "kind": "overrefusal",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "[2, 4, 6]",
   "output_other_k": "[2, 4, 6] (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES passes. On whole10 the output is identical."
  },
  {
   "id": "r2_fn_O2_context_budget_summary_merges_closure_captures",
   "kind": "overrefusal",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "[6, 8]",
   "output_other_k": "[6, 8] (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES passes. On whole10 the output is identical. The round-1 context-budget fix (r1_061, r1_086, r1_098) does not cover closure captures."
  },
  {
   "id": "r2_fn_O3_closure_summary_reach_label_merges_secret_reaching_pipelines",
   "kind": "overrefusal",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "false (key 50)",
   "output_other_k": "false (key 60)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES passes. On whole10 the output is identical. The round-1 reach-label keying (r1_011, r1_099) does not cover this case. Same root as OR-6, which LANES refuses."
  },
  {
   "id": "r2_fn_O4_nested_compose_becomes_unordered_chain_at_call_widening",
   "kind": "overrefusal",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "10\\n12\\nok",
   "output_other_k": "10\\n12\\nok (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION in `<closure>`, and LANES passes. On whole10 the output is identical."
  },
  {
   "id": "r2_fn_O5_chain_joins_component_labels_into_every_stage_pc",
   "kind": "overrefusal",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "stage\\nstage\\nok",
   "output_other_k": "stage\\nstage\\nok (123456)",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW in `<closure>`, and LANES passes. On whole10 the output is identical. It is related to O4 (chain flattening) and to r2ctl_O04 (select of compositions)."
  },
  {
   "id": "r2_fn_O6_apply_non_list_argument_runs_callback_under_scalar_content",
   "kind": "overrefusal",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "checking password strength\\ntrue",
   "output_other_k": "checking password strength\\ntrue (\"swordfish\")",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW in `<closure>`, and LANES passes. On whole10 the output is identical for \"hunter2\" and \"swordfish\". The kind_label root is the same as OR-4."
  },
  {
   "id": "r2_fn_O7_recursive_walk_folds_records_at_summary_depth_4",
   "kind": "overrefusal",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 1,
   "duplicate_of_round1": "none",
   "output_k42": "[1, 2, 3, 4, 5, 6, 7] (bal mid*100)",
   "output_other_k": "[1, 2, 3, 4, 5, 6, 7] (bal mid*1000)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION. LANES also refuses (whole-struct secret lane), as the finder states, so IFC v2 would not newly refuse it and it matters less. whole10 runs it with identical output. Related to OR-3 (a summary fold at depth 4)."
  },
  {
   "id": "r2_fn_R1_parser_combinators_on_secret_input_hit_step_budget",
   "kind": "robustness",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "true\\nfalse\\ntrue",
   "output_other_k": "true\\nfalse\\ntrue (123456)",
   "reason": "NEW ifc2-report reports ANUBIS_IFC2_LIMIT (step budget spent) after 1.36 s. LANES passes, and whole10 runs the program correctly with identical output. This is an IFC2_LIMIT on a realistic parser-combinator program."
  },
  {
   "id": "r2_fn_R2_mutual_recursion_with_closure_args_42s_then_work_budget",
   "kind": "robustness",
   "lens": "functions",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "33\\n[3, 12, 10, 4, 7, 3, 3, 15, 13, 5, 10, 3, 13, 10, 2, 2, 2, 18, 16, 6, 13, 3, 16, 13, 2, 2, 19, 16, 2, 12, 1, 1, 1]",
   "output_other_k": "identical (123456; the secret is unused)",
   "reason": "NEW ifc2-report took 42.75 s wall time and then reported ANUBIS_IFC2_LIMIT (work budget spent), well over the 10 s threshold. LANES passes, and whole10 runs it instantly. The program is synthetic, but the time bound is reproduced."
  },
  {
   "id": "r2p_01_select_ignores_runtime_order",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "[b, a]",
   "output_other_k": "[a, b] (123456)",
   "reason": "Reproduced on NEW run. Same root as r2ctl_L03, r2v_01 and r2_fn_L2. The struct variant in the text is the same root as r2v_02."
  },
  {
   "id": "r2p_02_select_equal_maybe_sets_same_shape",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "false",
   "output_other_k": "true (123456)",
   "reason": "Reproduced on NEW run: has_key reveals the secret choice. Same root as r2ctl_L04, r2ctl_L05, r2v_03 and r2v_04."
  },
  {
   "id": "r2p_03_set_path_nested_write_marks_missing_key_present",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1",
   "output_other_k": "2 (123456)",
   "reason": "Reproduced on NEW run. This is the dotted-field form of r2v_05 (same root) and a variant of r1_042 with the nested no-op write m.f.g = 5, not the round-1 program."
  },
  {
   "id": "r2p_04_merge_drops_first_map_value_for_maybe_key",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "3",
   "output_other_k": "7 (123456)",
   "reason": "Reproduced on NEW run: the secret-derived value from pick(s > 100) is printed. Same root as r2v_08."
  },
  {
   "id": "r2p_05_remove_by_computed_key_keeps_keys_present",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1",
   "output_other_k": "2 (123456)",
   "reason": "Reproduced on NEW run. Same root as r2v_07."
  },
  {
   "id": "r2p_06_min_by_max_by_callback_args_decided_by_results",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "1\\n2\\n2\\n3",
   "output_other_k": "1\\n2\\n1\\n3 (123456)",
   "reason": "Reproduced on NEW run. Same root as r2_fn_L1."
  },
  {
   "id": "r2p_07_stdin_cursor_is_an_unmodelled_store",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "first line",
   "output_other_k": "(no output) (123456)",
   "reason": "Reproduced on NEW run with the same stdin, \"x\\ny\\n\", for both runs: an input() under the secret condition moves the stdin cursor, so the later branch reveals the secret. IFC v2 and LANES both accept."
  },
  {
   "id": "r2p_08_write_file_to_device_path_is_egress",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "stderr: 42",
   "output_other_k": "stderr: 123456",
   "reason": "Reproduced on NEW run: write_file(\"/dev/stderr\", str(s)) prints the secret, and IFC v2 and LANES both accept. It meets the operational LEAK definition. Whether file writes count as egress is a design choice, but a device path is the program's own output channel."
  },
  {
   "id": "r2p_09_panic_exit_under_secret_pc_not_refused",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "(no output, rc 0)",
   "output_other_k": "ANUBIS_PANIC: over the limit + exit_code=Some(101) (123456)",
   "reason": "Reproduced on NEW run. It is essentially the same program and root as r2ctl_L08 (only the message text differs), and it is also the same root as L09 and r2_fn_L4."
  },
  {
   "id": "r2p_10_try_on_empty_payload_gives_bottom",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "3\\nend",
   "output_other_k": "7\\nend (123456)",
   "reason": "Reproduced on NEW run: the println in the unevaluated method argument prints the secret-derived value."
  },
  {
   "id": "r2p_11_join_enum_drops_runtime_default_for_short_payload",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "3\\nend",
   "output_other_k": "7\\nend (123456)",
   "reason": "Reproduced on NEW run. The mechanism is the same as r2p_10 (an argument dropped because the receiver kinds are under-approximated), but the site is different."
  },
  {
   "id": "r2p_12_struct_string_index_ignores_maybe_field",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "3\\nend",
   "output_other_k": "7\\nend (123456)",
   "reason": "Reproduced on NEW run. p[\"a\"] is Int(0) at runtime, so the call takes the fallback and prints."
  },
  {
   "id": "r2p_13_proof_commit_bool_result_modelled_as_argument",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "3\\nend",
   "output_other_k": "7\\nend (123456)",
   "reason": "Reproduced on NEW run."
  },
  {
   "id": "r2p_14_remove_trap_message_prints_index_length_key",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "start + ANUBIS_INDEX_OUT_OF_BOUNDS: index 42 is out of bounds for a list of length 3 ... exit 101",
   "output_other_k": "start + ... index 123456 is out of bounds for a list of length 3 ... exit 101",
   "reason": "Reproduced on NEW's redacted runtime: both runs trap, and the message carries the secret index. Same root as r2v_11 (anubis_remove was missed by the r1_032 and r1_059 redaction)."
  },
  {
   "id": "r2p_15_crypto_trap_messages_print_operands",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "start + ANUBIS_CRYPTO_BYTES_KIND: bare integer 42 is not accepted ... exit 101",
   "output_other_k": "start + ... bare integer 123456 ... exit 101",
   "reason": "Reproduced on NEW run: the crypto runtime's trap message prints the secret operand. I did not run the other variants listed in the text."
  },
  {
   "id": "r2p_16_builtin_value_arity_trap_prints_argument_count",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "start + ANUBIS_ARITY: builtin `abs` cannot take 4 argument(s) ... exit 101",
   "output_other_k": "start + ... cannot take 3 argument(s) ... exit 101",
   "reason": "Reproduced on NEW run: both runs trap, and the argument count (from a secret-derived length) differs in the message."
  },
  {
   "id": "r2p_17_poc_kit_trap_messages_print_operands",
   "kind": "leak",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 0,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "start + ANUBIS_POC_CYCLIC_LENGTH: cyclic length must be >= 0, got -42 ... exit 101",
   "output_other_k": "start + ... got -123456 ... exit 101",
   "reason": "Reproduced on NEW run."
  },
  {
   "id": "r2p_18_bool_int_of_collection_use_content",
   "kind": "overrefusal",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "true\\n3",
   "output_other_k": "true\\n3 (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES passes. On whole10 the output is identical."
  },
  {
   "id": "r2p_19_files_store_is_global_and_flow_insensitive",
   "kind": "overrefusal",
   "lens": "parity",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "feature enabled",
   "output_other_k": "feature enabled (123456)",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW, and LANES passes. On whole10 the output is identical; each run used a fresh directory containing cfg.txt = \"on\". Same root as OR-1 (a single Store::Files label)."
  },
  {
   "id": "ROB-1-nested-summary-fixpoint-precedence-parser",
   "kind": "robustness",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "nodes: 5",
   "output_other_k": "n/a (no secret)",
   "reason": "NEW ifc2-report reports ANUBIS_IFC2_LIMIT (step budget spent) after 1.0 s, and NEW's run is refused with the same code. LANES passes, and whole10 prints 'nodes: 5'. This is an IFC2_LIMIT on a realistic 7-level precedence parser with no secret. I did not rerun the 3-to-8-level scaling claims."
  },
  {
   "id": "ROB-2-oo-evaluator-method-dispatch-limit",
   "kind": "robustness",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "max(max(add(div(17, 18), mod(19, 20)), sub(max(21, 22), add(23, 24))), add(mul(sub(25, 26), mul(27, 28)), div(div(29, 30), mod(31, 32)))) = 19",
   "output_other_k": "identical (123456)",
   "reason": "NEW ifc2-report reports ANUBIS_IFC2_LIMIT (step budget spent) after 2.19 s. LANES passes, and whole10 gives identical output for both secrets. This is an IFC2_LIMIT on a realistic Interpreter/Composite program."
  },
  {
   "id": "OR-1-file-store-single-label",
   "kind": "overrefusal",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "==================== WELCOME ====================",
   "output_other_k": "==================== WELCOME ==================== (123456)",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW (print), and LANES passes. On whole10 the output is identical, with each run in a fresh directory. Same root as r2p_19."
  },
  {
   "id": "OR-2-loop-built-tree-fold-mixes-payload",
   "kind": "overrefusal",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "accounts: 3",
   "output_other_k": "accounts: 3 (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES passes. On whole10 the output is identical. It belongs to the r1_106 family (a BST built in a loop) but is not the round-1 program: r1_106 had 10 ids and declassified its prints, while this one has 3 ids and no declassify."
  },
  {
   "id": "OR-3-recursion-summary-fold-depth4",
   "kind": "overrefusal",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "accounts: 9",
   "output_other_k": "accounts: 9 (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES passes. On whole10 the output is identical."
  },
  {
   "id": "OR-4-kind-label-of-secret-scalar",
   "kind": "overrefusal",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 0,
   "duplicate_of_round1": "none",
   "output_k42": "pin format ok",
   "output_other_k": "pin format ok (123456)",
   "reason": "NEW refuses with ANUBIS_IMPLICIT_FLOW, and LANES passes. On whole10 the output is identical. The kind_label root is the same as r2_fn_O6."
  },
  {
   "id": "OR-5-container-shape-label-taints-present-key-reads",
   "kind": "overrefusal",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 1,
   "duplicate_of_round1": "none",
   "output_k42": "audit: session for ana\\nrole: guest (label-erased twin)",
   "output_other_k": "audit: session for ana\\nrole: guest (twin, 123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION. LANES also refuses (IMPLICIT_FLOW on the assignment), and every runner (NEW, whole10, ordret2a, LANES) refuses the labelled program. So I confirmed it through the label-erased twin, as the finding describes: the twin is accepted by NEW and gives identical output. This is imprecision only; it is not a new refusal, because the lanes already refuse it."
  },
  {
   "id": "OR-6-closure-summary-reach-label-merges-pipelines",
   "kind": "overrefusal",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 1,
   "duplicate_of_round1": "none",
   "output_k42": "22",
   "output_other_k": "22 (123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION. LANES also refuses (captured secret in closure `shown`). whole10 runs the program with identical output. It matters less because the lanes refuse it too. Same root as r2_fn_O3, which LANES accepts."
  },
  {
   "id": "OR-7-summary-context-after-12-merges-all-secret-rows",
   "kind": "overrefusal",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 1,
   "duplicate_of_round1": "none",
   "output_k42": "ana: 1 days ... nia: 14 days (label-erased twin)",
   "output_other_k": "identical (twin, 123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES also refuses. Every runner refuses the labelled program, so I confirmed it through the label-erased twin (NEW accepts it; 14 identical lines for both values). This is imprecision only; the lanes already refuse it."
  },
  {
   "id": "OR-8-max-nodes-collapse-merges-map-literal-keys",
   "kind": "overrefusal",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 1,
   "duplicate_of_round1": "none",
   "output_k42": "host = db.internal\\nhost = db.internal (label-erased twin)",
   "output_other_k": "identical (twin, 123456)",
   "reason": "I generated the program from the finding's regular 28-row pattern. NEW refuses with ANUBIS_SECRET_EXFILTRATION in `show_host`, and LANES also refuses. Every runner refuses the labelled program, so I confirmed it through the label-erased twin (NEW accepts it; identical output). This is a variant of r1_100 and imprecision only; the lanes already refuse it."
  },
  {
   "id": "OR-9-struct-variant-field-order-join-mixes-all-fields",
   "kind": "overrefusal",
   "lens": "precision",
   "confirmed": true,
   "new_rc": 1,
   "lanes_rc": 1,
   "duplicate_of_round1": "none",
   "output_k42": "login ana\\nlogin ben\\nlogout ana (label-erased twin)",
   "output_other_k": "identical (twin, 123456)",
   "reason": "NEW refuses with ANUBIS_SECRET_EXFILTRATION, and LANES also refuses. Every runner refuses the labelled program, so I confirmed it through the label-erased twin (both secret<i64> annotations removed; NEW accepts it; identical output). The join_enum root is the same as r2v_10 and r2v_16."
  }
 ]
}