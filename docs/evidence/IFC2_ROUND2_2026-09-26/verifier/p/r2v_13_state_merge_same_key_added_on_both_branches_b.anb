fn status(c) {
    let mut out = {"user": "bob"};
    if c {
        out["status"] = "ok";
    } else {
        out["status"] = "denied";
    }
    out
}
fn main() {
    let k: secret<i64> = 123456;
    let r = status(k > 100);
    println(keys(r));
}
