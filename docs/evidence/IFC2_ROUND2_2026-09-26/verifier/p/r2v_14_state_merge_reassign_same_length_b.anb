fn pick(c) {
    let mut xs = [0, 0];
    if c {
        xs = [1, 2];
    } else {
        xs = [2, 1];
    }
    xs
}
fn main() {
    let k: secret<i64> = 123456;
    let xs = pick(k > 100);
    for x in xs {
        println("tick");
    }
}
