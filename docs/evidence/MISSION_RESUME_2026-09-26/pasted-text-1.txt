You are the appointed lead engineer for completing Anubis: https://github.com/AnubisQuantumCipher/anubis-lang.
I want a complete, usable, dependable language and toolchain. Carry this mission through implementation, verification, independent review, integration, documentation, and preparation of a releasable product. Do not stop after producing a plan, fixing one example, finishing one slice, or publishing another list of residuals. Do not reduce the ambition because you assume time, computation, or engineering effort is scarce.
Preserve Anubis’s identity: a general-purpose language with approachable syntax, useful inference and dynamic ergonomics, native execution, contracts, effects, capabilities, information-flow enforcement, machine-readable diagnostics, independently checkable evidence, and a credible self-hosting path. It must be excellent for humans and AI agents. Its distinctive advantage should be that an agent can build useful software, understand a precise failure, repair it, and provide checkable evidence about the resulting program and its permitted actions.
Do not turn this into a narrow demonstration DSL, a permanently incomplete verifier, or a language that obtains a green board by refusing ordinary valid programs. Improve the actual language and the experience of using it.
1. Authority, continuity, and working discipline
I authorize the reversible engineering work needed for this mission: repository inspection, isolated branches/worktrees, implementation, tests in permitted environments, local evidence generation, independent agent reviews, and separate local commits. You are the active lead. Delegate mapping, implementation with explicit ownership, and independent review when useful; retain one accountable integrator. Follow the repository’s lead-only build and pin-publication discipline unless a reviewed workflow explicitly supplies isolated worker build lanes.
This mandate authorizes continuation across routine slice and phase boundaries. Produce the required receipts and continue when their criteria are met; do not repeatedly ask whether to proceed. Preserve actual permission boundaries. Prepare reviewable PR bodies, patches, artifacts, and release material locally. Reuse any existing authorization for pushing or opening PRs; otherwise request it once when that concrete stack is ready. Merging protected branches, publishing releases, changing account security, destructive operations, and weakening isolation or security policy require their applicable authorization. Never change instructions or gates to manufacture it.
Read applicable AGENTS.md files and existing ownership rules first. Resolve conflicting branch instructions explicitly. Preserve unrelated work. No destructive reset, broad process killing, silent rewrite of published history, or incidental OS reconfiguration.
I am working on SIA separately. Do not modify, migrate, restart, or make this mission depend on SIA. Use its memory interface only if already available and appropriate. Maintain a durable, versioned project record independently. Temporary directories and caches may hold disposable build products; neither is the only copy of essential evidence.
Across context resets, resume from the recorded commit, evidence manifest, and next dependency. Do not replay completed work blindly. If an external blocker prevents a lane, record the exact missing prerequisite and continue independent authorized work. Never label a blocked mandatory gate complete.
2. Reconcile the real starting point
First identify the actual worktree, branch, commit, dirty state, worktrees, remotes, staged changes, open PRs where accessible, running jobs, and immutable binaries. Detect the actual OS, architecture, host versus guest relationship, toolchains, available isolation backends, and permitted hardware. Record facts, not guesses.
The owner reports this history; verify its existence, ancestry, content, and current integration state:
Reported revision	Reported purpose
e34d0c89	Older public-main review baseline
7220c4b1	Later evidence-branch baseline used for the original item-21 reproduction
13373e31	Place-assigned callable return-carrier fix in the secret and taint lanes
029d5538	Unannotated array-element and factory-return type inference fixes
84296cef	Family-1 contract-carrier implementation and fixtures
c87ad1cf	Family-1 receipt, claim updates, and documentation-stamp reconciliation


The reported branch is item21/soundness-slices, based on the newer evidence work. Do not recreate commits already present. Do not merge that entire ancestry accidentally into a supposedly isolated PR. Establish its dependency stack and use reviewable integration units.
Read the newer JSON diagnostic implementation and docs/language/DIAGNOSTICS_JSON.md if present. Extend the existing protocol; do not propose it as missing merely because an older review missed it.
Recover the Family-1 matrix, source-matched binary manifests, reviews, receipts, and logs. The reported receipt is docs/evidence/ITEM21_FAMILY1_CONTRACT_CARRIER_2026-09-23.md. Resolve abbreviated hashes to full identities. Rebuild when evidence is missing or no longer source-bound. Never claim a matching short hash proves complete provenance.
The reported “9 of 11” means nine failures in the original eleven-case reproduction. It does not mean nine of every defect subsequently discovered. Keep the expanded matrix, SHARED cases, precision regressions, and newly discovered failures visible.
3. Read the whole project and reconcile its roadmap
Inventory every tracked file and relevant untracked project artifact. Read all first-party source, tests, specifications, documentation, CI, build scripts, examples, formal developments, package tooling, runtime, native and proof backends, and self-hosted compiler code. Organize the reading by subsystem so it produces an architectural understanding.
For generated files, vendored dependencies, binary artifacts, and very large datasets, record their origins, generators, versions, integrity checks, and relevant trust assumptions. Distinguish a file inventory from a source review. Do not claim to have read every word when only search hits or summaries were inspected. Complete the first-party review over the mission without using it as an indefinite excuse to postpone verified work.
Reconcile at least README.md, AGENTS.md, docs/CLAIMS.md, docs/COMPLETION_BLUEPRINT.md, docs/language/ROADMAP.md, the language specification and freeze policy, capabilities, proof correspondence, proof scaling, self-host plans, installation documents, and CI trust-boundary documentation. Use current paths if names changed.
Keep one authoritative defect registry and one authoritative execution roadmap. Extend the established authorities; a mission index may reference them but must not become a competing status database. Map every existing roadmap requirement to implementation, tests, evidence, and remaining work. Record a precise reason for any proposed roadmap change. A phase marked complete because its residuals were published has not thereby fixed those residuals.
4. Freeze a meaningful completion contract
Establish two named finish lines without confusing them:
1. Anubis 1.0 product completion: the frozen language surface, supported platforms, developer tools, required assurance profiles, packages, production examples, and release process satisfy the acceptance contract below.
2. Full trust-chain completion: the production-linked formal correspondence and full self-hosting work specified below are complete for the declared semantic surface, with an explicit remaining external trusted computing base.
The second is part of this mission, not a research paragraph to forget after 1.0. A releasable product checkpoint may occur before it, but do not report the entire mission finished until both are satisfied. Formalizing arbitrary future features or automatically deciding every possible program is not the completion criterion.
Publish a versioned coverage map from language constructs and program boundaries to each claimed property. Freeze required valid workloads and invalid witnesses before grading implementations. A bounded proof must state its bounds. Partial correctness must not imply termination. Proof of an abstract model must not imply proof of production code without correspondence evidence.
The release contract must distinguish:
Outcome	Required meaning
Verified property	All obligations required for that named property and profile are accounted for and discharged under recorded assumptions
Disproved	A checked counterexample to the precise encoded claim; label any gap between that model and executable source behavior
Undecided	The property is unresolved; provide a typed reason such as unsupported encoding, analysis imprecision, budget exhaustion, or solver unknown
Runtime enforced	The stated property is enforced by a specified runtime mechanism; this is not a static proof
Invalid input / tool error	Syntax, typing, infrastructure, or internal failure; never counted as a contract disproof
External / not run	A required witness was not obtained; never silently substituted by another platform or a unit test


Ordinary dynamic programming and a proof-required deployment policy may have different acceptance conditions. Make the distinction explicit and compositional. Preserve the existing Safe/Research authorization distinction separately; “Research” is not a switch that converts missing proofs into valid evidence.
A security-required build must not receive a verified seal through an unmodeled boundary. A development execution path, if supported, must expose unresolved properties and obey the independent runtime capability policy. Do not use a new profile to silently reclassify formerly promised guarantees away.
5. Close the complete known soundness surface
Finish item 21, then every other known false-accept family affecting the frozen assurance claims. Work through mechanisms, not just specimen names.
The starting inventory must include:
- D9: unannotated formal parameters and interprocedural type/qualifier information.
- The place-assigned taint sink-argument carrier.
- Function values held in local containers, nested containers, fields, returned values, and joins.
- Direct-call requires paths that emit no obligation when modeling fails, including parameter arguments.
- Mutable and shadowed variables, stale branch facts, loop-carried assignments, and mutable callable identities.
- All application forms: direct names, aliases, methods, indexed calls, returned closures, lambdas, builtin higher-order functions, and cross-package calls.
- Missing expression positions: conditions, bounds, scrutinees, arms, value blocks, short-circuit expressions, initializers, and early-exit paths.
- Wrong counterexamples caused by capture, unintended caller facts, incorrect coercions, or fresh symbols that do not denote the source value.
- Unreachable code and zero-iteration loops being incorrectly rejected, without skipping condition or range-bound evaluation.
- Evidence consumers that omit, misclassify, duplicate, or collapse obligations.
“Unknown identity is accepted” is not a sound rule for a claimed contract guarantee. Fix the information loss or emit an explicit unresolved obligation at the assurance boundary. Equally, treating every data value as a dangerous callable is not an acceptable final implementation. Develop typed callable summaries and precise value flow so valid data processing remains usable.
Maintain an immutable history for each matrix case: unique ID, source, intended semantics, baseline outcome, current outcome, diagnostic class, runtime witness where applicable, category, and any recategorization rationale. Moving a case from SLICE to SHARED does not remove it from mission totals or release blockers.
Require both safety and precision evidence. A newly refused violated program can close a false accept while remaining analytically unresolved. A newly refused valid program remains a precision defect. In particular, preserve the valid apply(f, positive_value) twin; refusing it alongside the invalid twin is not complete higher-order contract support.
Do not weaken user contracts, discard labels, whitelist fixture names, suppress obligations, or change expected outcomes to make the implementation pass.
6. Establish one semantic foundation for analysis
Replace fragile string-based and name-based reasoning where it causes semantic ambiguity. Develop explicit representations for types, callable identity sets, places, lexical bindings, source spans, effects, security labels, contracts, and obligation provenance.
Use stable binding identities so shadowing and global-name collisions cannot change resolution. Represent control flow explicitly enough to account for branches, joins, loops, exceptions or error propagation where supported, and exits. Use SSA or an equivalently justified state model; select the architecture through a reviewed design, not fashion.
Conditions must refer to the right value version. Invalidate or transform facts after writes and effectful calls. Account for indirect mutation, collection operations, captures, and aliases according to the actual value/reference semantics. A loop invariant must hold initially and be preserved; a guard observed before mutation cannot justify a later call.
Make interprocedural summaries compositional. Include callable targets, parameter-to-result and parameter-to-sink flow, writes, effects, contract requirements, and relevant exceptional behavior. Resolve recursion and mutually recursive components with a sound fixed-point or invariant strategy. Budget exhaustion must become an honest unknown, not an empty successful summary. Cache keys must include all semantic dependencies.
Use a shared traversal and dataflow foundation where appropriate while retaining distinct lattices and policies for effects, capabilities, confidentiality, integrity, and contracts. Do not merge different security meanings merely to reduce line counts. Specify joins and transfer functions and test their algebraic laws.
Create a declarative builtin registry covering signatures, callable argument positions, evaluation behavior, mutability, effects, labels, runtime implementation, and proof modeling. Generate consistency checks and appropriate consumers. Every builtin must have an explicit account, including new builtins; unknown entries must not inherit a permissive default.
Migrate incrementally with old/new verdict comparisons and independent semantic tests. The existing direct lane is a comparison target, not an oracle. Extract coherent modules from the large middle layer as contracts between subsystems become clear. Do not replace the compiler wholesale before earning equivalence.
7. Finish and specify the language itself
Write a normative specification and executable conformance suite for the entire 1.0 surface, including lexical grammar, parsing precedence, evaluation order, bindings, inference, conversions, functions and closures, recursion, collections, structs, enums, pattern matching, generic facilities, traits/interfaces where promised, modules, visibility, errors, and resource behavior.
Resolve integer-width and runtime-representation inconsistencies explicitly. Specify overflow, signedness, shifts, division, remainder, casts, and literal ranges. Specify floating-point behavior, NaN, infinities, signed zero, comparisons, and float-to-integer/range coercions. Verification must describe the executable semantics exactly.
Preserve existing valid behavior by default. Where a sound, coherent design requires an intentional semantic change, introduce an explicit edition or migration mechanism, diagnostic, migration tool, compatibility fixtures, and reviewable rationale. Do not silently change the meaning of u32 or any other established spelling. Do not call a deliberate compatibility break “zero regressions.”
Keep inference ergonomic while separating lack of type precision from absence of a security label. Dynamic values need defined checked boundaries. Complete structural typing and generic behavior promised by the roadmap rather than carrying ad hoc type strings through security analysis.
Define string/byte distinctions, Unicode behavior, indexing, map ordering, equality, serialization, numeric conversions, and resource exhaustion. Define what determinism means under each execution mode, including time, randomness, network input, and concurrency. External nondeterminism must be explicit; it cannot be wished away by a deterministic compiler.
Complete the error model and diagnostics for invalid programs. Malformed source must never panic the compiler or look like a successful verification. Version the language, schemas, standard library contracts, and compatibility policy together.
8. Complete the production proof chain
Build a dependency map for the actual assurance chain:
source semantics → resolved/typed representation → control/dataflow analysis → verification conditions → solver encoding → checked solver result → compiled/runtime behavior → evidence verification.
For each arrow, identify its trusted code, test evidence, mechanized theorem, or independently checked translation certificate. State which is present. Close gaps for the declared verified core; a proof about a disconnected miniature model does not finish this work.
Extend the Lean development to connect production representations and algorithms to their specifications. Prefer generated/extracted verified components or checked translation witnesses where practical. If maintaining independent implementations, mechanize the correspondence that the guarantee actually needs. Finite cross-check tables are valuable evidence but not a universal implementation theorem.
Cover name resolution, substitution/capture avoidance, value updates, label propagation, callable summaries, control-flow joins, contract generation, arithmetic encodings, and evidence outcome preservation in dependency order. State the exact semantic assumptions for confidentiality, including whether implicit flows, termination, timing, and other side channels are covered. Never advertise a stronger noninterference theorem than the model establishes.
Keep solver authority restricted to supported, justified fragments. Validate SAT models and UNSAT certificates independently where the format permits. Test malformed, truncated, malicious, and resource-exhausting certificates. Validate parser and encoder agreement. A solver proving the wrong formula is a compiler defect, not a proof success.
Complete needed arithmetic and data-theory coverage, loop invariants, inductive contracts, and recursion support for the frozen workload set. Give inference and proof search explicit budgets and cancellation without converting timeout into proof. A user may supply additional invariants or lemmas; independently check them.
Use independent reference solvers for differential evidence where available, without making agreement equivalent to a theorem. Record versions and fragment boundaries. Prevent proof results from being reused after source, assumptions, contracts, environment, or encoding changes.
For the native backend, either establish a production-linked lowering result for the verified core or use a verified/checkable lowering route with a clearly stated external backend/runtime trust boundary. Do not claim to have verified Rust, LLVM, the OS, or the hardware merely because Anubis emits Rust.
9. Make evidence an independently usable product
Define stable obligation IDs independently of human-readable display text. Preserve exact source locations, call chains, property kind, assumptions, scope, outcome, reason, and provenance. Sanitizing internal names must not merge two obligations. Counterexamples should map to source bindings and values rather than unexplained synthetic symbols.
Use typed outcomes throughout the compiler and evidence pipeline. Do not infer proof status from English diagnostic wording. Explicitly unencoded obligations must record that no solver ran. Fix misleading replay labels through versioned schema changes and migration support; preserving an old digest is not a reason to keep a false claim.
Bind bundles to source closure, compiler/runtime identities, build configuration, dependency lockfiles, semantic profile, proof inputs, capability policy, target, and relevant toolchain. Separate reproducible payloads from timestamps, signatures, and machine-local provenance. Specify canonical serialization and independently verify it.
Deliver an offline verifier with a small, documented trust boundary. It must reject forged status fields, missing obligations, altered source, altered dependencies, substituted binaries, stale cached results, malformed certificates, and inappropriate cross-version reuse.
Refactor evidence honesty tests so they do not require a real vulnerability to remain unfixed. Use controlled negative fixtures and explicit test doubles at the correct boundary; preserve real end-to-end forgery tests. Never represent synthetic test evidence as a genuine production proof.
Audit the documentation-stamp reconciliation, including the reported floor change from 53 to 38. Verify each historical exemption against its actual dated receipt. Preserve historical measurements, keep every current claim enforced, and replace brittle count-only coverage with a structured claim inventory where feasible. Do not lower a floor merely because a gate is inconvenient.
10. Finish the runtime, memory, concurrency, and system boundaries
Specify memory ownership, lifetime, allocation, aliasing, collection copying, closure capture, cycles, destruction, and resource limits. Implement the chosen safe model consistently. An Rc-backed runtime and value semantics do not automatically give predictable memory usage or freedom from every resource leak.
Make system resources explicit: files, sockets, processes, secrets, timers, tasks, and device handles. Their creation, transfer, borrowing, cancellation, and disposal must preserve capability policy. If affine/linear capabilities are promised, enforce their identity and non-duplication across closures, containers, packages, serialization, and FFI.
Complete concurrency using a coherent ownership/message-passing and synchronization model. Provide structured task lifetimes, cancellation, timeout, bounded queues, deterministic replay where supported, and defined error propagation. Cover race freedom and resource cleanup under failure. Do not claim a single-threaded proof establishes concurrent correctness.
Implement a stable C interoperability boundary and a documented Rust integration route where required. Specify ABI, layouts, calling conventions, ownership, callbacks, unwinding, threading, and error conversion. Foreign code requires explicit summaries and trust declarations; an unchecked annotation cannot confer verified status on arbitrary native behavior.
Make compile-time effect declarations connect to actual runtime permissions. Deny undeclared sensitive effects even through a native adapter or dynamic call. Address filesystem path resolution and symlinks, process arguments, inherited environment/handles, network destinations, DNS changes, secret access, and time-of-check/time-of-use behavior in the enforcement design.
Use reviewed cryptographic implementations and platform facilities through explicit boundaries. Do not invent cryptographic primitives to make the stack appear self-contained. Keep private inputs and credentials out of logs, diagnostics, counterexamples, caches, and public proof journals under the stated confidentiality policy.
11. Complete packages, standard library, and reproducible builds
Finish package resolution, deterministic lockfiles, dependency identity and integrity checks, signatures where promised, offline builds, workspace/module behavior, version conflicts, and meaningful diagnostics. Specify build-script and plugin authority; importing a package must not silently execute unrestricted host code.
Cross-package contracts, labels, effects, callable summaries, and capability requirements must be checked against the actual dependency contents. Reject stale or forged summaries. Incremental rebuilds must invalidate every affected result, including negative dependencies and semantic configuration.
Complete a coherent, documented standard library: text and bytes, collections and iteration, checked numeric operations, serialization, files, streams, paths, clocks, controlled randomness, networking/TLS, processes, concurrency, testing, and system integration. Use audited dependencies where appropriate and expose their trust assumptions. Match the exact frozen API inventory to executable examples and conformance tests.
Provide useful profiling, tracing, logging, debugger integration, crash reporting under user control, and reproducible stack traces with source maps. Avoid making a developer inspect emitted Rust to understand an ordinary Anubis error.
Document all required build dependencies and provide a reproducible toolchain setup. Local clean builds, CI builds, release artifacts, and proof builds must have a defined relationship. Do not claim bit-for-bit reproducibility where signatures or external nondeterminism are deliberately variable; compare the specified reproducible payload instead.
12. Build the AI development interface around verified changes
Extend the existing anubis-diagnostics/1 implementation with deliberate versioning. Preserve distinctions between disproved and undecided in every adapter, including editor integrations. A presentation format’s severity field must never be the only storage for proof status.
Finish source ranges, related locations, counterexample decoding, inferred effects, obligation explanations, proof assumptions, structured reason codes, and documentation links. Stable identities must survive benign formatting changes where the protocol promises that property.
Complete LSP navigation, completion, hover, rename, diagnostics, code actions, formatting, and tests for multi-file projects. Add machine interfaces for capability inspection, dependency summaries, proof status, and checking a proposed patch. Reuse existing commands or add reviewed ones; do not invent a second unrelated toolchain.
Implement a re-verification gate for suggested repairs. A repair may be marked validated only after applying to the intended source revision, rechecking the affected dependency closure, confirming unchanged required contracts/policies, and passing applicable behavioral tests. If that process did not run, call it a suggestion.
Distinguish fixes that change implementation from proposals that change the specification. Never silently weaken requires, remove an assertion, declassify data, broaden a capability, edit a test oracle, or change the assurance profile to make an AI patch pass.
Treat retrieved text, model output, tool output, and package metadata as data with appropriate provenance. Enforce actions through capabilities and a runtime policy independent of model instructions. The language must not rely on the model obeying a prompt to protect secrets or user files.
Measure the actual developer loop: time to a useful diagnosis, successful repairs, invalid repairs rejected, proof latency, editor responsiveness, and ability to explain a refusal. Use held-out tasks and ordinary human-written programs as well as generated code.
13. Make the actual operating system a supported target
Detect the current environment before choosing a backend. If it is Omarchy/Arch Linux, provide a first-class Linux workflow without requiring an unrelated OS migration. If it is a Linux guest on Apple hardware, distinguish guest capabilities from host capabilities. Do not assume an ARM CPU implies access to Apple Virtualization, Metal, Keychain, or Secure Enclave APIs.
The completion target includes a reproducible native Linux CLI and toolchain on x86-64 and AArch64, with Omarchy integration tested on an actually supported Omarchy installation. Retain the existing Apple Silicon support and its specialized evidence lanes. Verify support claims against actual installations; emulator, guest, and physical-host witnesses must be labeled accurately.
Deliver install, upgrade, uninstall, shell completion, editor setup, headless use, and environment diagnostics. Use the platform’s normal packaging conventions. System service examples should use least authority and explicit paths. Do not alter the user’s desktop, boot configuration, package manager, or unrelated services to make a demo pass.
Define a portable isolation interface and implement platform-specific backends with capability discovery and an explicit threat model. Linux process restrictions, namespaces, syscall filters, resource controls, and virtualization have different guarantees. Choose and validate mechanisms for the required threat model; do not relabel a process sandbox as a hypervisor boundary.
Preserve mandatory disposable-guest rules for research, crash, fuzz, and exploit execution. If existing rules require Tart/VZ and the current host cannot provide it, that lane remains blocked until a properly authorized equivalent backend and revised policy are reviewed. Continue ordinary permitted work. Never substitute host execution quietly.
Keep authorization and engagement scope for existing Research features. Improve their correctness, isolation, and evidence integrity without expanding this mission into new stealth, persistence, or unauthorized offensive operations. A missing specialized platform witness prevents that claim, not unrelated development.
SIA integration, if useful, is a bounded adapter with a documented protocol and test substitute. Language compilation, verification, installation, and core examples must work when SIA is offline.
14. Complete self-hosting, rather than its demonstration subset
Inventory which lexer/parser, resolution, type, effect, information-flow, contract, code-generation, packaging, and diagnostic components are Anubis-authored and which remain in the Rust implementation.
Grow the self-hosted compiler to the frozen production language, including constructs the current self-host grammar cannot parse. Maintain per-function and per-obligation semantic comparisons, not only aggregate verdict agreement. Two implementations silently skipping the same feature are not equivalent to the specification.
Use staged promotion: shadow comparisons, explicit opt-in, then authority transfer after the gates pass. Keep the Rust compiler as a reference/bootstrap asset during migration. Self-hosted code must obey the same security and correctness requirements as user code.
Demonstrate a clean bootstrap and a reproducible compiler fixed point from pinned sources and toolchains. Exercise diverse double compilation using a meaningfully independent route where feasible, with documented assumptions. Do not claim that self-reproduction alone solves trusting-trust attacks.
The finish line is a self-hosted production compiler capable of building itself, the required tools, and the reference applications with equivalent documented semantics. A parser/code-generator spine or a limited grammar remains an intermediate milestone. External backend, solver-kernel, OS, and hardware dependencies must be listed; self-hosting does not erase them.
15. Finish the proof-execution and performance story
Complete the existing proof-execution backend for its declared supported programs. Bind a proof receipt to the executable image, source/build closure, entrypoint, inputs, public outputs, and relevant assumptions. State precisely what executing a program inside a proof system establishes and what it does not establish about external effects.
Scale proofs through dependency-aware entrypoint slicing, sound specialization, caching, incremental verification, and appropriate composition. Validate that slicing retains all dependencies and security-relevant behavior. Reducing the proof workload by omitting a relevant path is a correctness bug.
Benchmark compile/check/prove latency, peak memory, binary size, runtime throughput, allocation, startup, and incremental changes. Include small scripts, container-heavy programs, contract-heavy code, the compiler itself, long-lived services, and adversarial proof workloads. Publish hardware, toolchains, flags, distributions, and variance.
Improve representation and code generation where measurements justify it: unboxed/specialized values, efficient collection operations, fewer redundant copies, sensible allocation, summary reuse, and deterministic parallel work. Re-verify optimizations against observable semantics and the assurance model.
Develop reproducible Rust, Ada/SPARK, and Zig comparison workloads using current official documentation and pinned versions when executing the benchmark work. Compare the same behavior, safety conditions, algorithms, and external dependencies. Report unsupported features and unfavorable results honestly.
Target demonstrable advantages in the combined workflow: usable contracts and information-flow policies, precise AI repair, evidence portability, runtime confinement, and reproducibility. Do not claim universal superiority based on a selected microbenchmark, theorem count, or language age. Include native performance, predictable resource use, and systems interoperability as engineering requirements to measure and improve.
16. Prove usefulness with complete applications
Deliver and maintain at least three substantial applications written primarily in Anubis. Extend strong existing examples when they can satisfy these criteria; avoid shallow duplicates.
1. A capability-constrained AI tool runner. It processes untrusted retrieved/model content, handles a protected secret, performs useful permitted file/network work, supports cancellation, and emits verifiable action evidence. Adversarial content must not broaden its authority. Supply positive controls proving the useful workflow still succeeds.
2. A production-style service. It has a documented API, persistence through a clearly identified dependency boundary, input validation, concurrent requests, errors, resource limits, configuration, deployment, recovery, and integration tests. Demonstrate confidentiality and integrity requirements on realistic data paths.
3. A systems/data-processing application. It exercises exact binary/text handling, checked arithmetic, large collections or streaming, parallel work where appropriate, and reproducible outputs. Include a real performance workload and an independent result oracle.
Each application needs installation instructions, tests, threat/assumption documentation, operational limits, performance evidence, and meaningful failure recovery. It must work from a clean supported environment. A README claiming “production ready” is not an acceptance test.
Use them continuously while changing the compiler. They are integration evidence, not a replacement for semantic coverage. Their success must not reduce the frozen language scope to only the constructs they happen to use.
17. Validation and independent review are permanent requirements
Build a reproducible acceptance system with these distinct layers:
- Unit/property tests for representations, joins, substitution, encodings, and transfer functions.
- Executable language conformance tests with independent expected semantics.
- Security negative cases paired with satisfied, clean, and unreachable positive controls.
- Generated composition tests across expressions, carrier shapes, nesting, mutation, control flow, and package boundaries.
- Differential tests between reference execution, native execution, supported proof execution, and self-hosted compilation.
- Certificate, evidence, package, cache, and diagnostic-schema tampering tests.
- Whole-corpus verdict and behavior comparisons, plus held-out applications and adversarial cases.
- Fuzzing, mutation testing, and crash testing in the mandated isolation environment.
- Formal proof checks, source-correspondence gates, bootstrap checks, platform witnesses, and independent clean-room reproduction.
For each security fix, establish the baseline failure and diagnostic class on a source-matched immutable pin, then the corrected result and acceptance controls on the candidate. Use a harmless executable witness when needed to show a semantic violation. A parser error is not a successful security rejection.
Track changes on the same original corpus independently from newly introduced witnesses. Classify every verdict flip. “Zero flips” proves neither complete semantic coverage nor absence of valid programs outside that corpus. The reported corpus had few contracted higher-order examples; strengthen the test distribution accordingly.
Run independent design review for trust-surface changes and independent review of the actual final diff. Give reviewers an explicit mandate to find stale facts, missing positions, aliasing errors, incorrect joins, invalid counterexamples, evidence laundering, and over-rejection. Review fixes after the review when they change semantics. Agents are useful reviewers but do not substitute for external expert audit in the final release process.
Make tests deterministic in setup and isolation. Capture exit codes and complete failure logs. Passing alone on retry establishes intermittency, not its cause. Investigate the reported native-build and stdin failures using actual error evidence, unique paths, controlled stdin, process ownership, and appropriate test scheduling. Fix the harness or affected code; do not retry until green and discard failures.
Run the final required workspace suite on the final candidate. An earlier whole-workspace result and a later compiler-library-only result are different witnesses. Likewise, “no new lint warnings” is not a pass for a gate requiring zero warnings. Satisfy the actual gate or report it unmet.
18. Execution order and durable deliverables
Use the following dependency order. Parallelize independent lanes when their interfaces and work ownership are explicit. Split work into as many reviewable units as required; the unit size is a safety mechanism, not a limit on the mission.
Stage	Required completed outcome	Dependency
A — Establish truth	Verified branch state, recovered evidence, complete baseline inventory, authoritative acceptance contract	First
B — Bank existing work	Final reviewed item-21 commits reconciled, honest test results, dependency-correct integration stack prepared	A
C — Close trust gaps	Known false accepts and evidence misstatements fixed; precision regressions accounted for and required valid cases restored	A/B; foundational IR work may run alongside
D — Semantic foundation	Normative semantics, binding/type/control-flow model, compositional analysis, builtin inventory, conformance suite	A; iteratively supports C
E — Complete language/toolchain	Runtime, modules, standard library, packages, concurrency, FFI, developer tools, AI repair, Linux/Apple support	D; ship verified increments
F — Complete trust chain	Production-linked formal correspondence, checked evidence, full self-hosting, proof-execution guarantees	Starts with D; integrate throughout E
G — Exercise real products	Required applications, performance program, operational hardening, external review	Starts early; final acceptance requires C–F as applicable
H — Produce final candidates	Clean-room reproduction, exact artifact gates, complete docs, migration/release package	All required product dependencies


The 1.0 checkpoint can be prepared when its explicitly frozen product requirements pass. Full mission completion additionally requires the declared F deliverables. Keep both states visible without using the earlier milestone to abandon the later one.
Maintain the following versioned deliverables in the project’s established documentation/evidence structure:
1. Mission index linking the canonical roadmap and defect registry.
2. Architecture map and complete first-party review inventory.
3. Normative semantics, compatibility decisions, and threat/assurance profiles.
4. Requirement-to-source-to-test-to-proof coverage map.
5. Stable-ID defect and acceptance matrix, including all historical recategorizations.
6. Reproducible binary/build manifest and machine-readable gate receipts.
7. Trust-boundary and correspondence map, including external assumptions.
8. Platform support matrix with actual execution witnesses.
9. Self-host bootstrap and equivalence evidence.
10. Developer/AI protocol documentation and repair-validation evidence.
11. Application acceptance and benchmark reports.
12. Release checklist, migration guide, SBOM/provenance, rollback instructions, and prepared PR/release material.
Generate counts and current status from structured evidence. Preserve dated receipts unchanged except for transparent corrections with provenance. Prefer links to primary evidence over repeatedly copying live numbers into prose.
Code and its verifying fixtures belong in the code slice. Keep documentation in separate commits where repository discipline requires it, and complete the associated documentation promptly. Run documentation-drift checks whenever fixture or inventory counts change. Use independent review for any change that reduces enforcement or coverage.
19. The actual definition of done
Do not declare product completion until all of the following are true on the same identified release candidate and compatible platform-specific artifacts:
- The frozen language/API surface is implemented, specified, and covered by conformance tests.
- Every required assurance claim has a precise scope, enumerated obligation coverage, and appropriate evidence. No known unresolved soundness violation remains inside a claim being shipped as verified.
- All original and expanded known-defect cases are accounted for. Required valid programs pass for the right reasons; refusals of valid required programs have been fixed rather than relabeled away.
- Unknown, unsupported, timeout, tool failure, runtime enforcement, counterexample, and proof remain distinguishable end to end.
- Runtime behavior, contract semantics, labels, effects, packages, and native/proof backends agree within their stated boundaries.
- The required applications run from clean supported installations and meet their behavioral, security, operational, and measured performance criteria.
- The current host/guest is supported honestly, and every mandatory platform/isolation witness for each advertised target exists. Missing hardware is an open gate, not an inferred pass.
- Build, check, run, test, package, prove where supported, and offline evidence verification form a documented end-to-end workflow.
- The actual final workspace, fixtures, formal, lint, format, drift, bootstrap, and platform gates pass under their documented conditions. No result is borrowed from an earlier incompatible source revision.
- Independent reviewers have evaluated the final trust-sensitive code, and an independent environment has reproduced the required release evidence.
- Installation, upgrades, migrations, stable schemas, debugging, reference docs, tutorials, and release/rollback procedures are complete.
- The release stack respects branch protection and required hosted witnesses; local success is not reported as merged or released.
Do not declare full mission completion until the production-linked correspondence and full self-hosting goals also satisfy their declared acceptance criteria. State the remaining external trusted computing base explicitly. Do not claim verification of arbitrary programs or freedom from every possible future bug.
20. Start executing and continue
Begin with the actual repository and environment reconciliation. Within that first pass, produce the canonical requirement/dependency map and select the first unblocked implementation unit. Then implement it. Do not spend the entire session producing another plan when useful work is possible.
The likely immediate sequence is: verify the reported Family-1 commits and final evidence; close any remaining review/test/integration gaps; restore the complete defect matrix; finish D9, the taint sink-argument mechanism, and the shared contract/identity gaps according to their actual dependencies. Reorder only when source evidence demonstrates a better dependency order, and record why.
Report progress concisely: what works now, the evidence, the remaining blockers, and the next dependency being resolved. Do not narrate every shell command or repeatedly tell me to wait. Track real job IDs and outputs; do not claim that a background task will notify you unless that mechanism exists.
Before every session ends, record the exact state and the next executable step. Continue autonomously through authorized work. When you genuinely need an external decision, finish all independent work and present the concrete decision, its reason, and its effect on completion. Do not ask me to choose between arbitrary slices.
The objective is a finished, useful, independently assessable Anubis—not a growing number of optimistic completion reports. Earn each claim, preserve the language’s strengths, and carry the whole mission forward.