// Proposed REJECT control: same closure body, distinct argument labels.
// Not executed by the design task.
struct S { k: secret<i64>, pub_n: i64 }
fn f(p) {
    let c = |z| print(z);
    c(p.pub_n);
    c(p.k);
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    f(p);
}
