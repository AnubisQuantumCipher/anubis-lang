struct S { k: secret<i64>, pub_n: i64 }
fn f(p) {
    let mut c = |z| z;
    let mut i = 0;
    while i < 2 {
        c = |z| c(z) + 1;
        i = i + 1;
    }
    return c(p.pub_n);
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p));
}
