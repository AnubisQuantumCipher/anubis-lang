# Anubis — Maturity Roadmap (the phases)

**This is the canonical source of truth for "what phase are we in / what's next."** The verbatim
11-phase arc (operator-authored, 2026-07-14) is preserved at the bottom, unchanged. The top of this
file is the living status layer — update the STATUS marks and the NEXT pointer as slices land.

The honest frame (from the arc): full maturity is *not* feature-parity with Rust/Dafny — it's
completing the one stack no other language holds at once: push-button proof, agent-readable
counterexamples, capability-and-effect enforcement that makes attack classes a compile error,
evidence bundles verifiable without trust, and a self-reproducing compiler witnessed by diverse
toolchains. Maturity is the *assembly*; the honesty about each boundary is what makes it believable.

---

## STATUS — as of 2026-07-20 (branch `a-plus-maturity/20260705-1649`)

> **ROADMAP COMPLETE PER ITS OWN DEFINITION (2026-07-20).** The arc's honest completion criterion is
> *"the distinctive stack is complete, every guarantee is either proven or precisely scoped, and the
> residuals are named rather than hidden"* — and that state now holds. The distinctive stack: **0** trust
> spine ✅, **1** type system 🟢100%, **2** capability+effect 🟢core-done (trifecta = a compile error;
> `http_get`/`http_post` classified net.send), **3** verified surface 🟢AT-DoD (float+string differential
> harness, 0 false-accepts), **5** mechanized soundness 🟢AT-DoD (all three theorems + 14 Lean modules),
> **6** proof-carrying packages 🟢AT-DoD (cross-package call-site discharge verified + locked), **8** DX
> 🟢AT-DoD (`run_dx_gate.sh` 15/15), **7** TCB-minimization 🟢advanced — the from-scratch native SMT solver
> (`solver/`, ZERO external dependency) decides the integer lane behind a **PROOF-BACKED FRAGMENT GATE**:
> its authority is bounded to bit-blasts machine-checked in Lean 4 (155 theorems across 14 modules), a
> TOTAL no-wildcard walker (a new op is a compile error, not a silent admission), drift-checked against
> the live theorem set. That fragment now covers **every operation a real integer contract emits except
> division** — all 8 comparators, equality, bitwise `& | ^ ~`, negate, add/sub, abs/min/max (ite), AND
> both shifts (`<<`/`>>` un-deferred via z3-PROVEN encoder identities built only from proven ops).
> Verdict-equivalent to z3 across the whole **445-file** corpus, 0 disagreements; z3 droppable for the
> integer lane (opt-in). Every guarantee is proven-or-scoped; the named residuals are: **4** self-host the
> checker's semantic engines (the largest remaining *engineering* effort; parse+codegen already
> dogfooded) — **slice 1 LANDED**: **effect inference** is now Anubis-authored in
> `selfhost/src/anubis_sh.anb` (the `effects` subcommand), reproducing the Rust engine's `caps_used ∪
> row` verdict via a TWO-set model — a propagating transitive **row** (`eff_compute_rows` fixpoint,
> all_fns/scope-first) plus a non-propagating **direct** name-charge (a call named `send`/`shell`/…
> charges the cap even when shadowed by a user fn/param, but locally, never to callers) — checked
> `(row ∪ direct) ⊆ declared`. Differential-gated at **0 disagreements** against the Rust engine —
> PER-FUNCTION (not a coarse cap-union) — over a curated corpus + a 457-file whole-corpus sweep
> (`scripts/run_effect_selfhost_gate.sh`). It is a PARALLEL pass whose authority transfers only on
> promotion (the shadow→opt-in→default-flip arc of the native solver), and its honest scope is the SH
> subset. **slice 2 LANDED**: the SH parser grew **lambda literals** (`|p| body`) and the effect
> engine ports the **HOF-closure descent** — `map(xs, |x| send(x))`'s closure body is charged to the
> calling function (`higher_order_closure_args` indices; effects.rs #65) — AND (#107) tracks VAR-BOUND
> closures (`let f = |x| send(x); apply(f,7)` / direct `f(3)`), closing the unsafe-direction gap; AND
> (#108) method/receiver calls `obj.f(args)` (→ open) + field access `obj.field`. With these the
> self-hosted EFFECT engine is PARSE- and DIFFERENTIAL-COMPLETE over the SH subset (0 disagreements
> per-function over the whole corpus; the only remaining subset boundary is struct literals,
> orthogonal to the effect lane). **slice 3 LANDED**: the WHOLE-PROGRAM
> capability set (`effects.rs program_capability_set` — the union of every function's transitive row
> caps + declared fold + OR of open) is now Anubis-authored too (the `capset` subcommand,
> `eff_program_capset`), so the input to the VZ hypervisor confinement grant is now derivable by the
> self-hosted analysis. Pinned EXACT against the Rust `anubis vz confine` derivation over a curated
> corpus (`scripts/run_capset_selfhost_gate.sh`) AND — after mirroring `is_builtin_name` into the
> self-hosted engine (`sh_is_known_builtin`, a completeness-verified 199-name registry, drift-checked
> in the battery) — over the WHOLE SH-parseable corpus: 150 check-passing programs, **0 disagreements,
> 0 over-grants, 0 conservative** (`scripts/run_capset_corpus_failclosed.sh STRICT=1`). **TYPE slice 1
> LANDED** (#109): the Anubis-authored **type engine** now checks LET-ANNOTATION ASSIGNABILITY —
> `ty_normalize`/`compatible`/`assignable` (ported byte-for-byte from `middle/ty.rs:149-408`) over a flat
> `sh_infer_type`, emitting the byte-identical `ANUBIS_TYPE_MISMATCH` on `let x: T = e` where `e`'s
> inferred type is not assignable to `T` (`types` subcommand). Differential-clean vs the Rust checker
> over `tests/fixtures/types_selfhost` (`scripts/run_type_selfhost_gate.sh`, in the battery) AND SH ⊆
> Rust (0 SPURIOUS — SH never rejects what Rust accepts) over the whole corpus (490 .anb: 281 exact-match, 208 SKIP=SH parse errors, 1 tolerated INCOMPLETE = the `let x: string = produce()` call-return lie Rust catches via its fn-return fallback). **TYPE slice 2 LANDED** (#112): the two remaining enforcing type-mismatch sites are now Anubis-authored and byte-identical to Rust — ARGUMENT-position (`type mismatch: argument N of \`F\` expects \`E\`, got \`G\``, ported from `mod.rs:5407`) and RETURN-position (`function declared \`-> R\` but returns a value of type \`A\``, `mod.rs:11693`; TOP-LEVEL + implicit-tail only, exactly mirroring Rust's `check_return_types:11670` which does NOT recurse into nested `if`/`while`/`for` returns — so a nested constant return-mismatch is spurious-safe in BOTH engines). The `fns` table carries a `{arity, ptys, ret}` record so a call site knows each callee's declared param + return types. Gated **20/20 AGREE** on `tests/fixtures/types_selfhost` (12 slice-1 + 8 arg/return fixtures incl. the nested-return spurious guard, Var-arg env-parity, and the implicit-tail), SH ⊆ Rust whole-corpus (**512 .anb swept, 12 emitters, ALL SH ⊆ Rust, 0 spurious** — the new arg/return surface produced 0 real-corpus emitters, as expected since real code has no literal-arg-into-mismatched-param or constant-return lie). Holds BY CONSTRUCTION: `sh_infer_type` under-approximates Rust's `infer_expr_type_scoped` arm-for-arm and `sh_is_constant` ⊆ `is_constant_expr`, so SH's mismatch set ⊆ Rust's; the Rust-only `check_mismatch_scoped` Call/Index/FieldAccess synthesis is the tolerated INCOMPLETE. Honest
> boundary [NEEDS-HUMAN parser-growth]: HM/union-find, generics/traits/typed-`?`, struct-field typing,
> and float→int narrowing are STRUCTURALLY UNREACHABLE in the SH grammar (single-token annotations, no
> `<T:Bound>`/trait/impl/`?`, no float literal), so `sh_infer_type` under-approximates Rust for
> Call/Index/FieldAccess/IfExpr/Match (=> accept) — a tolerated INCOMPLETE, never a spurious reject.
> Remaining slices:
> the **interprocedural depth** of the self-hosted type + taint engines (return-taint + param→sink
> summaries, closure/container granularity — the slice-2s) → **Z3/contract** self-host (capstone). **7**-default-flip — the encoding is now fully proven, so the SOLE remaining prerequisite to
> drop z3 by DEFAULT is a checkable CDCL `Unsat` certificate (LRAT/DRAT emit + verified replay; the SAT
> *search* is the only unmechanized piece, and z3 is retained as a fail-closed cross-check until then),
> **9** external reproduction and **10** production-hardening + 1.0-freeze (both
> `[NEEDS-HUMAN]` — independent reproducers and the operator's stability commitment; artifacts prepared,
> real systems already shipped in ≥2 domains). Plus the **VZ↔language confinement** lane (a second,
> hypervisor boundary derived from the same proof; native `objc2-virtualization` backend enforces the
> zero-NIC air-gap + per-hostname substrate, entitlement shown ad-hoc-signable — no longer `[NEEDS-HUMAN]`).
> **Soundness is adversarially stress-tested, not assumed:** whole-surface audits that BUILD+RUN candidate
> programs hunt for any green-check/run-violation across the whole checker surface. This session's audits
> closed the narrow-integer-CAST false-accepts inline (`x as u8..u32`/`i8..i32` now modeled exactly as
> zero/sign-extension, matching the runtime) and precisely SCOPED two delicate residuals for careful
> dedicated fixes rather than rushing them: the fail-open-vs-fail-closed stance for genuinely-unmodelable
> in-body asserts (a language-design decision), and a call-site `requires` bypass for calls inside
> block-bodied branches (`if c { let t=…; f(t) }` — the discharge walker's documented block/if-let residual).
> No guarantee is overstated; each boundary above is either machine-checked, gate-verified, or explicitly
> marked as scoped/human. That is the honest definition of done for a language of this class.

## Earlier status — 2026-07-20 (HEAD `636a41b`)

> **Operator `/goal` 2026-07-20 (4 items, all landed + VM-sealed) + hunt2 closeout + Phase-5 capstone:**
> **(hunt2)** the final 2 of 17 higher-order-taint leaks closed — a closure passed as a fn PARAMETER, applied+egressed in the callee (`fn run(cb){ send(cb(0)); } … run(|x| k)`), via `expr_param_flow` applied-param propagation + a closure-body check at the interproc call site (`bfd395c`). **(item 1)** a `return` inside a closure body laundered secret/taint (`let g=|x|{ return k }; send(g(0))`) — `expr_tail_values` dropped the trailing return; fixed + the inline `run(mk())` returned-closure compose (`1ec4362`). **(item 4)** the taint lane's `is_sink` omitted shell/system/connect, so `input() → shell(cmd)` COMMAND INJECTION `check passed` — fixed by making `is_sink ⊇ is_egress_sink` (integrity ⊇ confidentiality on sinks) (`708f7e8`). **(item 3)** `ANUBIS_CONTRACT_UNPROVABLE` now NAMES the untracked variable ("the variable `y` is no longer tracked …") instead of a generic bail (`6528b25`). **(item 2)** PATH-SENSITIVITY: a guard-clause early return now proves a postcondition that holds only on its path (`if x<0 { return 0; } return x;` ⟹ `result>=0`) — the branch-free-rewrite tax is gone; sound by a 12-case adversarial battery + 0 corpus flips + a mechanized Lean proof (`0f2f355`). **(Phase-5 capstone)** `formal/Anubis/PathCondition.lean` machine-checks the path-condition soundness (4 theorems, ZERO axioms) — the 13th proof module (`636a41b`). **(Phase-5 cont. 2026-07-20)** `formal/Anubis/DeclassifyWellFormed.lean` — the 14th module — mechanizes the operator item-2 security fix: a MALFORMED `declassify(secret, "", "")` is a no-op that KEEPS the label, and non-interference holds over the whole `NoAuthRelease` fragment (every declassify malformed), so an empty-policy declassify is NOT a hole in the guarantee; a well-formed declassify is proven to be the sole (authorized) downgrade (`wellformed_declassify_downgrades` tightness). Formal gate PASS (17 lake jobs, no sorry/admit/axiom); Lean-only, binary + fixpoint untouched. All VM-sealed, fixpoint dc680001 held throughout.

> **Ultracode soundness hunt + higher-order closeout (2026-07-20):** an adversarial Workflow (`wf_4bdce827`, 7 finders → verify, 26 confirmed) caught a false PROOF I had introduced with path-sensitivity (guard-clause escape unsound under reassignment — `4938361`) plus 21 higher-order-closure taint leaks. Closed 24: the path-sensitivity regression, the closure-local-let-alias + builtin-sink-alias batch (`9ef5741`), and the GENERAL fix — a capturing closure is a first-class labeled value the container/field/param/return flow tracks everywhere (`6d870f5`, closes struct-field-param/dict-param/fn-returns-closure/capture-through-callee-param). ~5 deep residuals (nested / conditionally-returned / symbolic-map-key closures) documented honestly in `docs/language/INFORMATION_FLOW.md` + tracked. LESSON reaffirmed: **run the hunt after every checker change** — it found a false proof my verdict-diff + hand battery missed.

> **Walker-completeness information-flow arc (2026-07-20, ~99 leaks closed):** an adversarial hunt (`wf_5b2a1bcc`) + two rehunts (`wf_e67160a7`, `wf_095bd48b`) + proactive walker×construct audits closed ~99 PRE-EXISTING compiling information-flow leaks (a secret/tainted value reaching egress/sink under a green `check`) across four VM-sealed commits: `988d338` (#73, 49 — branch/loop-join reassignment discard, HOF element→closure-param labelling, push/insert read-back, statement-`match`/`if let` arm merge, loop-carry fixpoint), `dccf0e1` (#74, 46 — the symmetric RETURN-side: return-taint/secret summary push/reassign, `body_param_returns` match-arm-binding return, CallExpr `b.f()`/`arr[0]()` closure-param labelling, sink-side loop-carry fixpoint), `89722d1` (#75, 4 — `body_param_sinks` Match/IfLet incl. bare-expr arms, `expr_param_flow`/`body_param_returns` value-block-nesting). ROOT CAUSE: ~7 PARALLEL value-flow walkers (`analyze_stmts`, `walk_block_taint`/`_secret`, `body_returns_taint`/`_secret`, `body_param_returns`, `body_param_sinks`, `expr_param_flow`/`_param_return_flow`) that must EACH handle EVERY construct (match/if-let/push/closure-apply/loop-carry) with nothing keeping them in sync. EVERY leak was pre-existing (ACCEPTed on the pre-arc baseline) so all three commits are net soundness gains with ZERO regressions; each landed only after full-corpus verdict-diff 0-flip + 531/531 unit tests + VM seal (fixpoint dc680001). Durable follow-up (queued): a single shared statement-effect helper all walkers route through + a completeness TEST asserting every walker handles every construct — to end the whack-a-mole. See [[anubis-walker-completeness-flow-arc-2026-07-20]].

> **Higher-order residuals CLOSED (2026-07-20, follow-on):** the deep residuals above are now closed and VM-sealed (fixpoint dc680001 held). (1) NESTED closure — a closure applied through another closure nested in a function body (`|y| { let inner = |x| k; inner(0) }`) laundered the capture because the outer body's return `inner(0)` found no binding for the body-local `inner`; `seed_body_local_lambdas` seeds a closure body's local lambda `let`s into the source-read scope, recursing to arbitrary depth (`e3f4eef`). (2) SYMBOLIC-map/index — verified already closed precisely by the `symbolic_index_capturing_closure` fail-closed backstop (secret rejects, identical clean shape accepts). (3) NEW leak found while verifying (1)/(2): a helper that RETURNS or SINKS a container (list/map/struct) holding a closure capturing a call-argument-bound secret (`fn build(k){ return [|x| k] }; send(build(secret)[0](0))`, + the param→sink dual) — both interproc param-flow summaries fell through `Expr::Lambda` to the empty set; added a `Lambda` arm to `expr_param_flow` and `expr_param_return_flow` that descends into the body with shadowed params dropped (`7b739c3`). Consistent with the fail-closed invariant (enlarges the summary — never the unsound `param_return`-aware reduction warned against below). 13 leak shapes reject / 8 clean shapes accept across both lanes; verdict-diff 0 flips / 397 corpus. `INFORMATION_FLOW.md` updated: all enumerated higher-order shapes covered, fail-closed on genuinely symbolic selection. Post-change adversarial fan-out hunt run to reconfirm.

> **Platform capability — Apple Virtualization.framework (`anubis vz`, new 2026-07-20):** the full VM lifecycle is now first-class in the toolchain (`tools/anubis/src/vz.rs`): `status/list/create/run/ip/exec/snapshot/stop/delete/sync`, plus a **disposable offensive lifecycle** — `vz exploit` / `vz fuzz` clone a CoW guest, boot it, sync the code in, run it `--allow-research` INSIDE, and discard, so the blast radius of dangerous code is a throwaway VM, never the host (research-gated, fail-closed). VZ-backed via `tart` (the same layer the seal battery uses); a native `objc2-virtualization` FFI backend is the documented next step (needs the `com.apple.security.virtualization` entitlement + signing — a human step). This is the language becoming a self-contained high-assurance *platform*: verify code, isolate its execution, prove the result. `docs/language/INFORMATION_FLOW.md` + `docs/CLI.md` updated.
>
> **VZ confinement DERIVED from the verified effect set (`anubis vz confine`, new 2026-07-20):** the checker already PROVES a whole-program capability set (the six canonical effects). This turns that same proof into a hypervisor CONFINEMENT MANIFEST — a real SECOND, independent boundary whose grant is consistent-BY-CONSTRUCTION with what `anubis check` proved. `compiler/src/middle/effects.rs::program_capability_set` exposes the proven caps + the `open` bit; `compiler/src/package/confinement.rs::derive_confinement` maps each capability to a concrete Apple-Virtualization (tart) grant, auto-sealed into every `anubis build --evidence` bundle (covered by MANIFEST.sha256 + pca.sig) and **re-derived + byte-compared on verify** (`verify_confinement_matches_source`, wired into `verify_pca`) so a forged/source-swapped grant fails closed. The grant lattice is fail-closed in the MISS direction: a proven-net-free program is confined to `--net-host` (host-only, no internet), and a MISSED `net.send` (e.g. a higher-order residual) yields a MORE restrictive grant, so a mis-analysed guest breaks rather than leaks — the hypervisor is precisely the backstop for undeclared flows. Honesty pillar (adversarially reviewed): the manifest never claims an isolation tart cannot deliver — tart has no zero-NIC air-gap, no per-hostname egress (IPv4 CIDR only via Softnet), and cannot gate an in-guest shell. **NATIVE backend closes the two tart cannot (`anubis vz native-preflight`, `tools/anubis/src/vz_native.rs`, new 2026-07-20):** a direct `objc2-virtualization` binding (no tart) derives the posture from the same proven effect set and FULLY ENFORCES a true zero-NIC air-gap for a proven-net-free program (a `VZVirtualMachineConfiguration` with zero network devices) + a per-hostname egress SUBSTRATE for a net-using one (a `VZFileHandleNetworkDeviceAttachment` host-held socket; the DNS-pinned frame filter is honestly labelled STAGED, not enforcing). **The `com.apple.security.virtualization` entitlement is NOT `[NEEDS-HUMAN]`** — a LOCAL ad-hoc signature (`scripts/build_signed_anubis.sh`) suffices; `native-preflight` proves it firsthand by `validateWithError:` + `VZVirtualMachine` instantiation (the negative control — an ad-hoc binary WITHOUT the entitlement — is rejected with the exact entitlement error). Only notarization-for-distribution stays human. Fail-closed lattice locked by `native_posture_is_fail_closed` (net-free/unbounded → air-gap, net-using → per-hostname, non-checking → refused). Slice-1 is the proof-carrying COMPUTE core (`vz confine` prints/seals, fail-closed); slice-2 (queued, #88) APPLIES the flags to a live BOOTED guest (kernel/initrd) with a Softnet-presence gate + fail-closed derivation. Extends Phase-2's effect pillar into a runtime enforcement surface through the Phase-6 PCA machinery. `compiler/src/package/confinement.rs` (4 unit tests incl. forged-grant re-derive) + `examples/showcase/vz_confine_demo.anb`.

> **Soundness hunt 2026-07-19 (`0fdf6a4`, VM-sealed):** a 15-surface adversarial hunt (discriminator-verified, 41 agents) found and closed **5 false accepts** — (A) a `break` embedded in a struct-literal field or map-literal entry evaded `expr_escapes_loop`, so the loop-invariant engine proved a false post-loop fact; (B) a secret/tainted-capturing closure stored in a struct field or list element, applied via `b.f(0)`/`arr[0](0)`, laundered its label past `ANUBIS_SECRET_EXFILTRATION`/`ANUBIS_TAINTED_SINK_WITHOUT_DECLASSIFY` (the source analyzers never marked the application *result*). Full-corpus verdict-diff 0 flips; fixpoint dc680001 held. 21 fail-open completeness gaps recorded (safe-direction, none soundness-critical).

| Phase | State | Evidence / residuals |
|---|---|---|
| **0 — Trust spine** | ✅ **DONE** | byte-identical source + binary fixpoint (`c640badd` host / `dc680001` in-VM), ablation gate, DDC C-native parser lane, hermetic repro. Residual → author-diversity (Phase 7). |
| **1 — Real type system** | 🟢 **100%** | bidirectional inference (`97c17f3`), floats, captured generics (`898cc3e`), real traits + coherence + missing-method (`fa88628`) — all enforcing, fixpoint held. **typed `?` propagation** for the inferable-inner case (`2b42d17` — `ty::try_unwrap_ok` + an `Expr::Try` arm in `infer_expr_type_scoped`: `let x: WrongT = r?` on an annotated `Option`/`Result` is caught; fixpoint-safe, verdict-diff zero flips). **typed `?` + call-return type-checking at LET bindings** (`3536581` — an `Expr::Try` arm in `ty::synth` (the InferEnv path, so it carries fn return types via `fn_ret_types`) + a let-init fallback to `check_mismatch_scoped` for `Call`/`Index`/`FieldAccess`/`?`-on-one: `let x: WrongT = f()?` AND the plainer `let x: WrongT = f()` are now caught — closing the CALL-return residual AND a broader call-return let-init hole where the annotation-vs-declared-return contract was unchecked; the return/arg positions already had this via the same `assignable`, so the semantics are consistent (empirically verified: `return g()`/`f(g())` with `g -> f64` bound to `u32` already rejected); does NOT touch the separate float-narrowing solver lint; diagnostic-only/fixpoint-safe, verdict-diff 0 flips across all 559 .anb (incl. arg-position `?` + double-`?`); design consistency + code both adversarially reviewed). **Trait BOUND checking DONE** (`2704226` — Phase 1 → 100%): `fn f<T: Comparable>(...)` rejects a call whose type argument is a KNOWN user type lacking `impl Comparable` (`ANUBIS_TRAIT_BOUND_UNSATISFIED`). The parser now CAPTURES inline `<T: A+B>` bounds (byte-identical token consumption) into a checker-only `Item::Fn.generic_bounds`; enforcement is accept-biased (a primitive / unpinnable type / foreign trait all accept). FIXPOINT UNMOVED — the operator authorized a reseal but none was needed (`generic_bounds` is not projected by `selfhost_schema::project_item`, exactly like the real-traits slice). Residuals (accept-biased, under-enforce): return-only/nested-position generics, a var bound to a struct literal synth can't pin, `where`/impl-level bounds. **Struct-literal field-value type-checking DONE** (`92abbbd`): a struct literal supplying a cross-category value for a concrete-primitive field (`name: string` given an int, `id: u32` given a string) is now rejected at construction via the same `types_assignable`/`check_mismatch_scoped` path call-args use. Gated to scalar-primitive declared AND got types (skips container values), so no valid program false-rejects; corpus scan 0 unexpected firings, fixpoint held. Exposed a latent parser bug (`collect_type_until` drops list `[]` so a `[int]` field type is stored as `"int"`) — filed as a follow-up (also mis-types `s.log` field access; struct-field-only fix is fixpoint-inert, the broader param/return fix risks the self-host fixpoint). |
| **2 — Capability & effect (the differentiator)** | 🟢 **CORE DONE** | transitive effect inference / Koka rows (`da28de4`); linear capability tokens / Austral (`9824807`); effect-capability composition (`7aa9f63`); **lethal trifecta as a verified-lane compile error** (`27554fb`) + shell egress (`c7cec93`) + leg-1 confidentiality label `secret_source` (`59585f0`). taint **reassignment fail-open CLOSED** (`2ac45ab` — control-flow-merge dataflow, `merge_taint_over`, span-identity). **leg-1 confidentiality FLOW REAL** (`c727b8d` — `ANUBIS_SECRET_EXFILTRATION`: a `secret_source` value that actually reaches net/shell egress without well-formed declassify is a Safe-mode compile error; flow-sensitive + may-secret branch merge, enforcing, corpus-inert). **INTERPROCEDURAL secret + leg-2 REAL** (`8624882` — `compute_secret_fns` (dual of `compute_tainting_fns`) so `send(get_key())` fires when the secret is minted in a helper; `compute_leg2_fns` (presence, `is_leg2_source`-built so file reads excluded) so the trifecta leg-2 sees a helper wrapping `input()`; discard-arg-precise via shared `param_return_taint`; declassify barrier holds across the call boundary. Design + landed code both adversarially reviewed). **COMPOSITE/aggregate flow REAL** (`a930e7e` — pure-aggregate arms (Array/Struct/Enum/Map/Try) on all 4 flow walkers so a tainted/secret value in a container no longer launders, intra + interproc; review caught + removed a shadowing false-positive from control-flow arms → deferred to a scope-aware walker). **SCOPE-AWARE control-flow value walkers REAL** (`fe44f35` — match/if/if-let/block arms on all 4 walkers build a local extended scope (clone + shadow-seed pattern vars/block-lets, straight-line block Assign set/CLEAR), so the shadowing false-positive is closed AND binding-passthrough is tracked, intra + interproc; retires the tail-if/match return-summary boundary; design + landed code both adversarially reviewed; fixpoint held). **BURIED-SINK enforcement REAL** (`984ff80` — `analyze_expr_effect` + `collect_param_sinks_in_expr` descend SCOPE-AWARE into match/if/if-let/block, so a sink/egress/privileged call buried in an arm body / branch / block statement is enforced; closes a Safe-mode capability-laundering bypass (`if true { shell(..) }` with no `uses`); validated by a full-corpus verdict-diff since shadow-diff is blind to enforcing diagnostics; design + code both adversarially reviewed; fixpoint held). **CONFIDENTIALITY param→egress-sink dual REAL** (`3d94861` — `compute_param_egress` (shares the `param_sinks` walk, parameterized by `is_egress_sink`) + `ANUBIS_INTERPROC_EXFILTRATION`: a secret into a param that reaches a net/shell egress inside a helper (`leak(secret)` where `fn leak(x){send(x)}`, incl. shell + transitive) is a Safe-mode compile error, the interproc twin of `ANUBIS_SECRET_EXFILTRATION`; egress-only (local writes excluded), declassify releases, taint→INTERPROC_SINK orthogonal; verdict-diff gated; design + code both adversarially reviewed; fixpoint held). **BURIED-CALL-IN-COMPOUND enforcement REAL** (`1dd3bd4` — `analyze_expr_effect` now recurses into every non-control-flow compound expr (aggregate/index/cast/unary/non-`==`-binary/field-access/`?`/declassify-inner) AND closure/method-application callee+args, so a sink/egress/privileged call buried in a value-shape wrapper (`shell("id") as u64`, `[shell()]`, `obj.method(shell())`) is enforced; closes the last capability-laundering wrapper class; verdict-diff gated (145/76, zero flips); only the Lambda BODY stays opaque; design pattern from `984ff80`, code adversarially reviewed; fixpoint held). **RESIDUALS:** higher-order taint summaries + field granularity; the `Lambda` literal body (effects deferred to the application site); `expr_param_flow` not `param_return`-aware (a secret into a discarding forwarding-call over-flags — param_sinks parity; the accept-biasing precision fix was found UNSOUND by design review — it would extend the `param_return_taint` false negatives below to the interproc summary — so deferred); **`param_return_taint` SOUNDNESS improved** (`b3b9bd1` — a param that reaches a helper's RETURN via a non-Var assign (`buf[0]=k`), a conditional local (`if c {r=x}`), a method/CallExpr (`x.clone()`), or a destructuring let was silently ACCEPTED at `send(fwd(secret))`; now closed via assign_target_root weak-union + branch may-union (replacing the flow restore) + LetPattern arm + a conservative CallExpr arm on both the summary and the DIRECT taint/secret walkers; monotone/convergent, verdict-diff 151/83 zero flips; NAMED fail-closed residuals: value-position nested control-flow, embedded-return-in-initializer, loop-carried, `Lambda`, shadow-over-rejection, buffer-never-clear; architectural follow-on = UNIFY the two drifted param→return walkers). **PARAM→RETURN WALKERS UNIFIED** (`719915b` — the `b3b9bd1` follow-on: `expr_param_return_flow`'s hand-rolled Block loop (whose `_ => {}` DROPPED nested control-flow — a value-position UNDER-approximation / missed leak: a secret assigned inside a branch nested in a value-position block then yielded through a helper summarized `{}`) now DELEGATES to `body_param_returns`, THE single stmt walker, so the two cannot re-diverge and the block inherits branch may-union; and `body_param_returns`' straight-line `Assign{Var}` becomes strong-overwrite (exact — old flow dead; confined to straight-line by the clone+may-union branch arms) adding precision. Both deltas monotone + fixpoint-stable (add-only accumulation never defeats strong-overwrite: a clearing RHS is intrinsically small from iteration 1). Vetted by a design workflow (3 grounding lenses + 2 designs + 2 adversarial critiques, both SOUND) + a read-only code review (CLEAR). Gold-standard verdict-diff: both binaries built + run BACK-TO-BACK under identical load over all 237 committed .anb → 0 isolated flips (corpus-inert; a 15-flip in-context artifact was solver-timeout flakiness under load, not this change). Two-directional fixtures: value-position nested-CF secret forwarder now REJECTS; straight-line reassign-to-clean forwarder now ACCEPTS. Pre-existing residuals unchanged (loop-carried, value-block-local return, Lambda, WhileLet-pattern-var); the twin taint/secret DIRECT walkers carry the SAME `_ => {}` residual on a different lattice → deferred to its own slice). **TWIN DIRECT-WALKER nested-CF fail-open CLOSED** (`d74c49b` — the direct-walker sibling of `719915b`: `expr_taint_source`/`expr_secret_source`'s value-position Block arm dropped nested control-flow (`_ => {}`), so a secret/tainted value assigned inside control-flow nested in a value-position block, then yielded and sent, laundered undetected (`send(if true { let r=0; if c { r=secret_source("k"); } r } else {0})` COMPILED — a real intraprocedural exfil leak). Two recursive per-walker stmt-walkers (`walk_block_taint`/`walk_block_secret`) thread the block flow at EXACT PARITY with the enforcing `analyze_stmts` merge pass (If=[then, else|snap], loops=[snap, body], hybrid/research/exploit unmerged, for-var seeded), so the walker can only reject what the statement pass already rejects — structurally bounded against over-rejection. Design workflow CAUGHT 3 would-be defects (LetPattern-shadow FP → patch real spans on Let+LetPattern-bound vars so merge_taint_over's span-identity skips shadows; CLEAR-BOTH FP → mirror the oracle's exact [then, else|snap] merge, which also REMOVES a pre-existing FP; HybridBlock over-reject → leave unmerged). Corpus-inert (exhaustive static verdict-diff 0/240 + oracle-parity guarantee; self-host declassified-at-let inert). VM 458/0, language 141/141 (7 fixtures), fixpoint dc680001 UNCHANGED, Ammit 0-contradicted; design + code both adversarially reviewed. RESIDUALS (shared with the oracle): non-Var lvalue assign, match/if-let-as-statement, cross-iteration loop carry). **LETHAL TRIFECTA is a Safe-mode COMPILE ERROR — the Phase-2 differentiator, DONE** (`ec69ab6` shadow-first → `ad86af2` promoted to enforcing: gate `mode==Safe || ctx.verified`, `emit(.., false)`; a function holding private read + a distinct untrusted-input channel + a net/shell egress with no well-formed declassify rejects by DEFAULT, `ANUBIS_LETHAL_TRIFECTA`; Research/Exploit bypass unless `@verified`; landed shadow-first (shadow UNEXPECTED=0) then promoted after a full-corpus verdict-diff (145/75, zero flips); accept-bias decided with the operator; relief = declassify the outbound value or split legs across functions with no common caller; design + code both adversarially reviewed; fixpoint held). **`secret<T>` QUALIFIER REAL** (`e6fd51a` — the confidentiality dual of `tainted<T>`: a param/`let` annotated `secret<T>` is auto-labelled secret without a `secret_source(..)` call; honored at all 5 sites the taint qualifier is (param/stmt-let/block-let/return-summary seeds + a `secret<T>` param wired as trifecta leg-1 private data); `ty::is_secret` a new sibling outside the frozen oracle; `getenv`/`env` deliberately NOT auto-secreted (untrusted input ≠ secret output); verdict-diff gated (149/80, zero flips — corpus-inert); design + code both adversarially reviewed; fixpoint held). **INTERPROC `secret<T>`/`tainted<T>` PARAM→RETURN CLOSED** (`11935e5` — the last named residual of the qualifier slice: a helper returning its `secret<T>`/`tainted<T>` param is now UNCONDITIONALLY secret-/taint-returning (the qualifier declares the param secret regardless of the arg), so `send(get(5))` where `fn get(x: secret<u64>){ return x; }` rejects; `seed_qualifier_params` seeds the qualifier params into both return-summary fixpoints (`compute_secret_fns`/`compute_tainting_fns`), a PLAIN param stays arg-flow (not over-marked), declassify-at-return releases; corpus-inert 0/247 static + controlled empirical; VM 458/0, language 144/144, fixpoint dc680001 UNCHANGED, Ammit 0-contradicted; code review CLEAR). **IMPL-METHOD ARGUMENT LAUNDERING CLOSED** (`c151bdc` — a method call `recv.m(arg)` parses as `Expr::CallExpr` (callee a `FieldAccess`), so the interproc sink/egress checks that lived only in the bare `Expr::Call` arm never ran on method args: `m.deliver(secret_source(..))` / `r.run(input())` (a method that sends/execs its arg) checked GREEN and leaked. Fix (checker-only, no parser/AST/projection change): `method_param_{sinks,egress}` summaries — bare-name keyed and UNIONED across impls (the receiver's static type is unrecoverable, so this is the fail-closed choice; runtime dispatch stays precise) — built by `collect_impl_method_params_bodies` + `compute_method_param_{sinks,egress}` monotone fixpoints, consulted in the `CallExpr` arm with the SELF-OFFSET verified against `run.rs` dispatch (`want=arity-1`, `call_args=[__anb_recv, args…]`): summary index 0 → receiver base, p≥1 → arg p-1; emits `ANUBIS_INTERPROC_SINK` / `ANUBIS_INTERPROC_EXFILTRATION`, Safe-gated, declassify-aware. Sound (fail-open) for ONE-HOP argument laundering with a DIRECTLY-supplied labelled value (+ a secret receiver via index 0). Blast-radius zero (no committed program has an impl method that sinks/egresses a param, nor passes a labelled source through a method call); 3 read-only review lenses (compile-correctness / soundness / precision) all clear; VM language 168/168, security 17/17, shadow unexpected=0, seal 9/9, fixpoint dc680001 UNCHANGED, Ammit 0-contradicted. **NAMED fail-open residuals (queued as follow-on slices):** (a) a secret/taint that ORIGINATES FROM a method return (`let k = v.key(); send(h,p,k)` — the getter/accessor exfil shape, HIGHEST reachability): no impl method ever enters `secret_fns`/`tainting_fns`/`param_return_taint` (their collectors skip `Item::Impl`); (b) any sink/egress reached THROUGH a method call on the flow path, free-fn→method or method→method (`fn f(m,x){ m.snd(x) }`): the summary walkers `collect_param_sinks_in_expr` / `expr_param_flow` lack an `Expr::CallExpr` arm, so a method call truncates an interprocedural flow path to ∅. No previously-working check regressed. **IMPL-METHOD-RETURN secret/taint exfil CLOSED** (`79705f1` — residual (a) of `c151bdc`, the highest-reachability getter/accessor exfil: `fn key(self){ return secret_source(); }` / `fn tag(self){ return input(); }` made `let k = v.key(); send(h,p,k)` and `send(h,p,v.key())` check GREEN and leak past even the DIRECT egress/sink check. Fix (checker-only, fixpoint dc680001 UNMOVED): ctx `method_secret_fns`/`method_tainting_fns` (bare method name → its return carries an internally-minted secret/taint) built by `collect_impl_method_typed_bodies` + `compute_method_{secret,tainting}_fns` (mirror `compute_{secret,tainting}_fns` over impl methods, self@0, reuse `fn_returns_{secret,taint}`, consult the FROZEN free-fn sets, wired after the free-fn computes); `expr_secret_source`/`expr_taint_source` split into 4-arg WRAPPERS (empty method set = pre-#67) over 5-arg `_m` bodies whose `CallExpr` arm fires on `v.key()` when `key ∈ method_secret_fns`, the set threaded through `walk_block_{secret,taint}` + `seed_one_let{,_secret}` + `seed_effect_let` so the value-block-local if/match-branch form is closed too; 19 single-line + 4 multi-line enforcing egress/sink + let/seed sites routed to `_m`; summary `body_returns_*` pass an empty set → free-fn fixpoints byte-identical → fixpoint unmoved. Sound within scope: bind/nested/value-block/container/for/assign/direct+interproc-arg forms all caught (secret+taint); declassify releases; non-secret returns accept. Design workflow (3 grounding + 2 adversarial critiques — the completeness critique caught the value-block blocker → folded in) + 3 read-only landed-code lenses (transform/soundness/precision) all clear. Blast-radius zero (no committed impl method returns an io/secret source). VM language 176/176 (8 fixtures), security 17/17, shadow unexpected=0, seal 9/9, cargo-test+clippy EXIT=0, fixpoint dc680001 UNCHANGED, Ammit 0-contradicted. **NAMED residuals:** method→method / free-fn→method RETURN chaining (#68 — summaries consult only the frozen free-fn set, pinned by `method_return_chain_residual_accepts.anb`); lambda-body returns (#65); **INTERPROC ARGUMENT-LAUNDERING-THROUGH-METHODS CLOSED** (`4f4433e` — residual (b) of `c151bdc`: a param laundered THROUGH a method that sinks/egresses it (`fn f(m,x){ m.snd(x) }` free-fn→method, `fn ship(self,p){ self.deliver(p) }` method→method) checked GREEN and leaked. The summary walkers were method-blind: `collect_param_sinks_in_expr` had no `Expr::CallExpr` arm (dropped the flow to ∅) and `expr_param_flow` returned ∅ for a method call (truncating value provenance); and the STAGED fixpoint (free-fn passes, then method passes with the method map frozen) could not express the free↔method mutual recursion. Fix (checker-only, fixpoint dc680001 UNMOVED): `expr_param_flow` gains a `CallExpr` value-carry arm; `collect_param_sinks_in_expr`+`body_param_sinks` thread `known_method_param_sinks` + a `CallExpr` arm consulting it with the #64 self-offset; the four staged compute fns are REPLACED by one `compute_sink_summaries_joint` run twice (is_sink / is_egress_sink) — a COMBINED add-only fixpoint snapshotting BOTH maps each iteration, monotone over a finite product lattice → terminates. Fail-closed, declassify-through-method releases, discard-arg accepts. Design workflow (ground + 2 critiques — caught the scope/numbering framing) + a landed lens (threading/convergence/offset/borrow/dangling/precision/scope all CLEAR). Blast-radius zero. VM language 189/189 (7 fixtures), security 17/17, shadow 0, seal 9/9, fixpoint dc680001 UNCHANGED, Ammit 0-contradicted. **Method-RETURN CHAINING then CLOSED** (`8faf9f0` #70 — `fn alias(self){ return self.key() }` where key mints a secret: `compute_method_{secret,tainting}_fns` became COMBINED fixpoints consulting the growing method set, so a method returning another method's minted secret chains; fixture flipped EXPECT:PASS→FAIL; the now-dead 4-arg `expr_secret_source` wrapper removed; VM 191/191, fixpoint dc680001 UNCHANGED). **The impl-method exfil cluster (arg-laundering #64/#68, return-exfil #67, return-chaining #70) + destructuring #69 are all CLOSED.** **CLOSURE-HIDDEN EGRESS CLOSED** (`573f955` #65 — a sink/egress inside a lambda applied by a higher-order builtin (each/map/times/apply/…) was charged nothing; now `effects::higher_order_closure_args` + a lambda-body descent in effects.rs (row) and analyze_expr_effect (Safe gate/taint/secret) at HO-builtin call sites, guarded by `!all_fns` so user `fn apply`/`fn compose` don't over-charge; leg-3 reads the now-populated row so the full trifecta fires; design workflow caught the omitted `times` builtin; 2-binary before/after verdict-diff over 77 examples/selfhost/stdlib programs = 0 flips; VM language 202/202, fixpoint dc680001 UNCHANGED, Ammit 0-contradicted). **With #65 the ENTIRE Phase-2 taint/secret/effect flow surface is hardened across free-fn / impl-method / destructuring / closure forms — no known fail-open remains.****DESTRUCTURING-LET seeding hole CLOSED** (`d91497c` — the main-analyzer `Stmt::LetPattern` arm never seeded taint/secret, so `let [a,b]=[secret_source(),0]; send(a)` and the direct `let [a,b]=[input(),0]; sink(a)` compiled GREEN; a PRE-EXISTING method-independent hole surfaced by the #67 lens. Fix: seed every `pattern.bound_names()` with the init's whole-value label via `seed_effect_pattern` (all array/tuple/struct forms), declassify releases, whole-value fail-closed over-approx; plus a span-patch so a statement-position destructure shadow doesn't over-reject — the landed-code lens caught the missing patch. VM language 182/182, fixpoint dc680001 UNCHANGED, corpus-inert).** coarse **presence-level** declassify hatch (not value-tied); closure application still opaque; `sql` egress; local `fs.write` not egress for the secrecy check. **BROAD SOUNDNESS HUNT — 3 Safe-mode/loop false-proofs CLOSED** (`0fa067a`): an 8-surface adversarial workflow hunt (`wf_bf84c047`, deferred-vs-proven discriminator baked into each agent) found 6 genuine false-PROOFS (composition/string/array/struct surfaces reported ZERO — deferrals self-discarded); 3 fixed here. FP0 `push(xs, tainted)` did NOT taint the container binding, so `xs[0]` reached a sink CLEAN (the `[t]` literal + `xs[0]=t` place-write WERE caught) — fixed by tainting the container root on a mutating `push`/`insert` of a tainted/secret arg (the CALL analog of the place-write taint). FP1 a `while let Some(v)=o` binder did NOT inherit the scrutinee's taint (the `if let`/`match` DID) — fixed by seeding the binder from `expr_taint_source_m`/`expr_secret_source_m` of the scrutinee. FP5 (loop) `for i in 0..e { e=e+1 }` proved a postcondition over the body-mutated bound while the runtime FREEZES the range at entry — rejected when a range bound references a body-mutated var. Checker-only, fixpoint dc680001 held, 518/518, verdict-diff 0/319. L64 a per-agent proven-vs-deferred triage self-suppresses hunt noise; L65 enforcement has ISOMORPHIC-FORM gaps (call vs assign, if-let vs while-let, inline vs var-bound closure) — audit every twin. RESIDUAL #47: closure-hidden egress (var-bound/returned closures launder taint AND effect via a HOF; #65 descends only INLINE lambdas). ([[anubis-broad-soundness-hunt-2026-07-18]].) **VAR-BOUND CLOSURE egress enforced** (task #47, `f7bc442`, broad-hunt FP3/FP4): a closure bound to a variable hid its body from Safe-mode taint/effect/capability enforcement — `let g=|x| write_file(t); each([1],g)` (HOF) and `g(0)` (direct) laundered the captured stdin to disk (#65 re-enters only an INLINE lambda). Fixed by storing the lambda in the scoped `ScopeBinding.closure_lambda` (let/assign/block-let, propagated through alias+reassign) and descending into it at the HOF-arg (resolve a `Var`), direct-application, and nested sites; the descent charges the effect + runs the taint/secret checks, a declared effect still accepts. Closes inline+var-bound+alias+reassign+nested. L66 a first-class value's analysis must be re-run at every APPLICATION site and CARRIED there through the scoped binding. Checker-only, fixpoint dc680001 held, 519/519, verdict-diff 0/319. RESIDUAL #48 (interproc leaks): returned closure, closure→user-fn-param, closure-in-struct-field. ([[anubis-closure-hidden-egress-2026-07-18]].) **RETURNED-closure egress enforced** (#48 FP2, `ee31768`, the last reported broad-hunt finding): `fn make(t){ return |x| write_file(t); } let g=make(t); g(0)` laundered the returned closure's egress (g's init is a Call, not a lambda literal, so the #47 descent never fired). Fixed by a pass-1 `fn_returns_lambda` summary + a beta-substitution of the callee's params by the call args INTO the returned lambda body at the `let g=make(args)` binding, stored as g's `closure_lambda` for the #47 descent. Edge-verified (multi-arg, param-shadowing, HOF-applied, effect-side). Checker-only, fixpoint held, 519/519, verdict-diff 0/319. RESIDUAL #48: closure→user-fn-param, closure-in-struct-field, nested-lambda-in-returned-body. **CLOSURE→USER-FN-PARAM egress CLOSED** (`364e111` #48-A — a closure passed to a user fn that APPLIES that param position (`fn apply(g){g(0);} … apply(|x| write_file(..))` / `apply(|x| sink(tainted))`) laundered its taint AND effect: the callee's isolated analysis sees `g` as an opaque param (no descent) and the caller never DIRECTLY applies the closure, so the #47 descent never fired. Fix (checker-only, fixpoint dc680001 UNMOVED): a pass-1 `fn_applies_param` map records the param positions a fn applies DIRECTLY at its own level (`p(...)`, NOT inside a deferred nested lambda) and never shadows/reassigns — two full Stmt+Expr scan walkers with `Pattern::bound_names` for precise shadow detection; the `Expr::Call` arm then descends into the closure ARG's body at those positions, in the CALLER's scope (a captured tainted/secret binding is seen), charging the egress to the caller (sound — the application happens in the caller's dynamic extent through the callee). UNDER-approximated (a shadowed/lambda-captured param is EXCLUDED) so a false reject is IMPOSSIBLE; honors `declassify(v, policy, reason)`; enforces through if-branches; inline + var-bound args both resolved. Over-rejection hunt 6/6 accept (stored-not-applied, double-charge-declared, fn-name-arg, clean-capture, recursion-terminates, conditional-declared) + shadow-skip + returned-closure non-interference accept. Full suite 524/524, clippy clean, corpus verdict-diff 0/324; VM language 222/222, security 17/17, stdlib 11/11, turing 13/13, shadow unexpected=0, seal 9/9, dogfood 3/3, fixpoint dc680001 UNCHANGED. RESIDUAL #48-B: closure-in-a-struct-field applied via `s.f(0)` (`Expr::CallExpr` on a `FieldAccess` callee — needs closure tracking through struct fields) + nested-lambda-in-returned-body. **HOF-FORWARD + N-HOP TRANSITIVE closure laundering CLOSED** (`a6d712c` #48, soundness-hunt `wf_7e946a56` confirmed 2 more false accepts): a closure passed to a user fn that FORWARDS it — to a HOF builtin (`fn runner(g){ each([1], g) }`) or through 2+ user fns (`fn a(g){ b(g) } fn b(g){ g(0) }`) — reached its sink/egress with no Safe-mode diagnostic (#48-A caught only DIRECT `g(0)`). Fix (checker-only, fixpoint dc680001 UNMOVED): `scan_applied_param_expr` marks a param at a HOF-closure-arg position as applied; `register_program_surface` collects per-fn the shadowed-param set + forward edges `(callee, arg_pos, param_idx)`, and `compute_applies_param_fixpoint` closes `fn_applies_param` under transitive forwarding (monotone over a finite param set → converges; gated by the shadowed set → a forwarded-but-shadowed param is never marked applied → no false reject). The existing call-site descent then fires at the OUTERMOST call, charging egress to the caller. Over-rejection battery 6/6 accept (clean chain, declared effect, non-applying callee, shadowed-in-chain, HOF-clean, self-forwarding recursion terminates); full suite 526/526, verdict-diff 0/324; VM language 222/222, security 17/17, stdlib 11/11, turing 13/13, shadow 0, seal 9/9, dogfood 3/3, fixpoint dc680001 UNCHANGED. **ENTIRE #48 CLOSURE/TAINT-LAUNDERING CLUSTER CLOSED — all 9 confirmed false accepts of soundness-hunt `wf_7e946a56` are fixed, each VM-sealed (fixpoint dc680001 UNMOVED), each corpus verdict-diff 0/324:** **struct-field closure** (`8bf0d7a` — `ScopeBinding.field_closures` populated at the `S{f:|x|…}` literal + alias, descended at `b.f(0)`); **list-element closure** (`005af5c` — reuses `field_closures` keyed by index, descended at `arr[i](args)`); **closure-return taint** (`65f3cb4` — a closure-call arm in BOTH `expr_taint_source_m` and `expr_secret_source_m`: the result carries the body's tail-value label via `expr_tail_values`, evaluated in the caller scope MINUS the lambda params so `|x| x` does not over-taint); **container-return taint** (`d6230db` — `body_param_returns` weak-unions a `push`/`insert` value's param-flow into the returned container, so `send(fill([0], secret)[0])` is caught; taint + secret share `param_return_taint`). Consolidated re-verification: all 9 original finding programs REJECT. RESIDUAL: an independent re-hunt is DEFERRED (weekly usage limit); deeper residuals (alias-of-alias / reassigned field-closures, symbolic-index list elements, deep-nested captures) are named for the next hunt. With #48 the interproc closure/aggregate surface — direct/HOF/transitive-forward through fn params, struct fields, list elements, closure returns, and container returns — is hardened; no known fail-open remains in the probed surface. |
| **3 — Broaden verified surface** | 🟢 **AT DoD** | **DoD MET (verified 2026-07-20):** the differential harness exercises BOTH floats (`phase3_float_proptest_solver_matches_oracle`) and strings (`phase3_string_proptest_solver_matches_oracle`) with zero false-accepts, plus the encoder↔runtime-oracle differential (`differential_solver_encoder_matches_runtime_oracle`), and every unmodeled construct defers fail-closed — exactly the Phase-3 definition of done. Adversarial hunts (`21f441a`/`824a19e`/`a43906b`) closed every latent false-accept; verdict-diffs corpus-inert throughout. Residuals are BROADEN-FURTHER items beyond the DoD (named, not hidden): the sequence-length lane (#54, reverted — a length predicate re-opens every composition path), large representable float literals (#42), and bounded-array-lane maturation. **QF_FP float-comparison contract lane REAL** (`c3960d5` — an `ensures`/`requires` that is a comparison over `+ - *` of finite f64 literals/params now DISCHARGES in Z3 QF_FP Float64/RNE, instead of blanket `ANUBIS_FLOAT_CONTRACT_UNMODELED`; solver-only, fixpoint dc680001 held). Within the admitted fragment the encoder is a BIT-EXACT runtime model — sound both ways; the LOAD-BEARING NaN fix (`<=`/`>=` → `(or (fp.leq a b) (fp.isNaN a) (fp.isNaN b))` matching run.rs `partial_cmp().unwrap_or(Equal)`) closes an assumption-side FALSE ACCEPT proven necessary against z3. Adversarial soundness review CLEAR on all 6 false-accept vectors; VM 459/0, language 147/147 (3 fixtures), verdict-diff 0/247 corpus-inert. **Float `/` DONE** (`8171b5b` — division joined the subset: `fp.div RNE` is bit-exact + total, so a BOUNDED division contract discharges (`2<x<4 ⇒ x/2<2`) while an UNBOUNDED one correctly rejects at the inf edge (inf/2=inf ≮ inf); z3-validated, verdict-diff 0/247). **Float DIFFERENTIAL HARNESS beyond i64 DONE** (`038f12b` — proptest float generator: a random-f64-arithmetic `ensures(result==<oracle>)` discharges IFF z3's QF_FP evaluation equals the Rust f64 oracle, catching any encoder/rounding mismatch; test-only, binary + fixpoint unchanged). **STRING DIFFERENTIAL HARNESS DONE** (`f8928bf` — the QF_S analog satisfying the Phase-3 DoD "harness exercising floats AND strings": string `==` is exact structural equality both ways so the property is ENCODER INJECTIVITY — a TRUE `result=="s"` contract discharges (reflexivity), a FALSE contract over a runtime-DISTINCT pair FAILS. The pool is loaded with the encoder's risk surface (a backslash, a `"`, a `\u{..}`-shaped literal); MUTATION-VERIFIED to go RED on the unescaped-`\u` encoder — so it auto-guards the entire `\u`-decode / quote-doubling false-accept class the review caught by hand. Test-only, fixpoint unchanged; printable domain (raw control chars a documented residual)). **Float `assert` DONE** (`33f4ba3` — a body-position `assert` over the modelable subset now discharges/rejects inside a contracted function via the same float_bool_to_smt encoder; `ANUBIS_ASSERTION_UNPROVEN` on a false one; kept OUT of the int `ctx.constraints` bv-context to avoid a sort-clash, every failure mode fail-closed; corpus-inert by construction — the only float-param-assert in all 582 .anb is the new EXPECT:FAIL fixture; verdict cannot flip). **Float `let` chaining DONE** (`06eb6c1` — a genuinely-float, NEVER-reassigned `let` becomes a Float64 defining-fact so `ensures`/`assert` prove through it, incl. depth-2 chains; `fact_is_float` sort-partitions the shared assumptions per obligation, closing the earlier "sort-clash" concern (which was misdiagnosed — the channel is the local assumptions vector, not ctx.constraints); adversarial review caught + fixed a FALSE ACCEPT (a `match`-arm write is invisible to the statement-level frame sweep → the `reassigned_roots` gate admits only never-reassigned lets, leak impossible by construction) and an over-reject (spurious bit-vector fact on a float-let-of-float-let → `genuinely_float` disjoint-lane gate); reassigned float lets stay UNMODELED fail-closed; verdict-diff 0/255). **Embedded-write FALSE ACCEPT closed (`21f441a` — INTEGER lane soundness fix).** The statement-level frame sweep (drop_written_after_scope) only visited Stmt::If/While/… bodies, so a write embedded in a `match`-arm/`if`-expression escaped it and its stale `let` fact certified a violable contract (`fn leaki(c)->i64 ensures(result==2){let y=2; match c {0=>{y=100;} _=>{}} return y;}` — `leaki(0)`=100 yet PASSED). Fix: `invalidate_embedded_writes` (`expr_assigned_roots` finds every embedded write; each gets the reassignment invalidation WITHOUT re-establish — path-dependent, fail-closed) wired into every statement that analyzes an expression (ExprStmt/Assume/Assert/Let/LetPattern/Assign + If/While/WhileLet conds). Straight-line reassign re-establish preserved; corpus-inert (verdict-diff 0/259 — the pattern occurs in no committed program). **Adversarial soundness hunt (`824a19e`) closed 3 MORE latent false accepts** — an early `return` over a reassigned parameter (discharged against the frozen call-entry precondition), a `break`/`continue` in expression position escaping the loop-invariant check (`let s = break;`), and a write embedded in a `for`-loop SOURCE (the un-wired residual of 21f441a) — each reproduced live, each fail-closed, corpus-inert 0/261. The one fail-OPEN the hunt also flagged is now CLOSED (`a43906b` — a z3 `unknown` on a loop-invariant preservation STEP is now converted to FAIL like an `ensures`, via a new `obligation_undecided_is_unsound` predicate that includes the step where the vacuity `is_contract` set deliberately excludes it; non-deterministic so unit-tested at the predicate; corpus-inert 0/264). **QF_S string-equality contract lane DONE** (`d0ed06d` — a `requires`/`ensures`/`assert` that is an `==`/`!=` between `string` params/literals WITH at least one literal side (plus `&&`/`||`/`!`) now discharges or is DISPROVED in Z3 QF_S instead of blanket `ANUBIS_STRING_CONTRACT_UNMODELED`; sound both ways by construction — runtime string `==` and SMT `(= a b)` are both exact structural equality, no NaN-like edge; theory selected per-obligation (`"` in body → QF_S). The adversarial review lens caught — and the commit fixes — a CRITICAL false-accept: z3's Unicode-strings theory decodes `\u{XXXX}` inside literals, so the 6-char runtime `\u{41}` re-decoded to the 1-char `A`, collapsing runtime-distinct literals into a provable false contract; fix escapes every backslash → `\u{5c}` (z3-validated: unescaped `sat`, escaped `unsat`; EXPECT:FAIL fixture + negative unit assertion guard it). Also completed the SYMMETRIC sort-partition across all 8 SolverObligation sites (int obligations filter float+string facts; the loop-invariant outer-assumptions filter and the B2 `requires@` call-site obligation were the last two leaks — both fail-closed, now filtered). VM 473/0, language 207/207, fixpoint dc680001 held, Ammit 0-contradicted.) **QF_S string-`let` chaining DONE** (`a8d4180` — the exact mirror of the float-`let` slice `06eb6c1` over QF_S: a genuinely-string, NEVER-REASSIGNED `let` becomes a String defining fact in the shared sort-partitioned assumptions channel, so `let s = "ok"; return s` proves `ensures(result == "ok")`, incl. depth-2 alias chains and a param-aliasing let carrying a `requires` into a body `assert`. The `reassigned_roots` gate is the soundness boundary (a match-arm/if-expression embedded write escapes the statement frame sweep → only never-reassigned lets carry an unconditional fact); genuinely-string lets are REMOVED from `symbolic_widths` so `expr_to_smt_value` can't mint a `bvadd` over String-sorted symbols for `let u = s + s`; the shared `string_expr_to_smt` carries the backslash escape so a `\u{..}` literal can't false-accept through a let fact. 3 read-only review lenses: soundness+regression CLEAR, completeness's items folded (embedded-branch, same-name-shadow, cross-sort-shadow regression guards added). VM 474/0, language 211/211 (4 fixtures), fixpoint dc680001 held, Ammit 0-contradicted. HONEST residual at the time: var-vs-var equality still needed a literal anchor — CLOSED by the follow-up slice below.) **QF_S VAR-VAR string equality DONE** (`0f9d903` — the literal-anchor requirement is dropped: a true identity contract `fn f(s: string) -> string ensures(result == s) { return s; }` (previously a SPURIOUS reject — theory selection keyed on a `"` in the SMT body, which a quoteless `(= anb_s anb_t)` lacks) now discharges, and an unconstrained var-var `ensures(result == t)` is DISPROVED with a genuine z3 String counterexample. Mechanism: a per-obligation `strings` sort tag on SolverObligation (`#[serde(default)]`, set at all 8 push sites) OR-ed into both theory-selection sites forces QF_S + String declarations; `is_bool_modelable_string` drops the literal-anchor conjunct — still membership-gated on `ctx.solver_string_vars` so no int/float var can enter, int→float→string gate order stands. SOUND BY CONSTRUCTION: runtime String==String and SMT `(= a b)` are BOTH exact structural equality — strictly simpler than the shipped literal case (no encoding surface touched; the backslash-escape and printable-ASCII guards are untouched). Adversarial review (3 read-only lenses: soundness/regression/test-validity) GO with zero blockers; the sort tag is verdict-inert for pure var-var (dis)equality (QF_BV64 coincides on quantifier-free equality) but is the honest sort and load-bearing for future `str.len`/concat surface. Red-baseline unit test phase3_qf_s_var_var_string_equality + PASS/FAIL fixture pair. VM 477/0, language 214/214, fixpoint dc680001 held, Ammit 0-contradicted.) **CALL-SITE `requires` DISCHARGE across all lanes + positions DONE** (`e2bff34` — the standing post-solver HUNT after var-var found a 3rd-class FALSE ACCEPT: a callee's `requires` is ASSUMED in its body (seeding its `assert`/`ensures`) but was NEVER discharged at the CALL SITE for the string/float lanes, and never at ALL for STATEMENT-position calls — so `f("b")` against `requires(s=="a")` certified a runtime-trapping `assert`; the var-var slice WIDENED it (`labeled("alpha","beta")` was correctly rejected before, falsely accepted after). Fix: `discharge_call_requires` (3-lane, mirrors each ensures-site encoding) wired into Let-init/ExprStmt/Assign-value. The INT lane discharges at ALL depths (preserving the pre-existing Let-init contract — gating it would turn a caught branch violation into a false accept); the NEW float/string lanes + all ExprStmt/Assign discharge only at UNCONDITIONAL depth via an `unconditional` flag threaded through `analyze_stmts` — because a branch has no path condition in scope, so an in-branch discharge would OVER-REJECT a guard-protected call (the FIRST implementation did exactly that; a 3-lens review returned NO-GO, the gating fixed it). Two adversarial reviews (soundness/regression/over-rejection): GO. Corpus verdict-diff = exactly 1 flip (the new EXPECT:FAIL fixture, buggy-accept→correct-reject). RESIDUAL (fail-OPEN, honest): a contracted call nested in an arg / an `if`/`match` branch (new lanes) / a short-circuit operand is not discharged — sound closure needs path-condition context (push the guard as a scoped assumption). VM 540/0 (compiler 478 + tool 62), language 216/216, fixpoint dc680001 held, Ammit 0-contradicted.) **CLOSED-ARG (constant) discharge at ALL depths DONE** (`310fb02` — a follow-up to the call-site slice that closes 3 MORE false accepts: a definitely-violating call with a CONSTANT arg inside an `if`/`match` branch (`g(0-5)` against `requires(x>0)`, and the string/float analogues) was fail-open because the statement-position + float/string discharge was gated to unconditional depth. A per-precondition `closed` flag — `collect_expr_vars(concrete).is_empty()` after arg substitution — now discharges a CLOSED precondition at ANY depth in every lane: a ground term in a decidable theory is decided ABSOLUTELY, independent of the branch path condition, so there is ZERO over-rejection risk (and a mis-classified predicate can only ADD a rejection, never a false accept — if z3 proves it, it holds for all values of any free var). A VARIABLE-arg branch call (whose provability depends on the out-of-scope guard) STAYS gated — that residual needs a scoped path condition (task #6). Adversarial review (2 Explore lenses + synthesis) initially NO-GO: `collect_expr_vars` MISSED `Expr::Index`/`FieldAccess` (it had a `_ => {}` catch-all) while `is_int_modelable` admits a bounded-seq `arr[0]`, so a modelable index was mis-classified CLOSED — a latent over-rejection (and a latent unsoundness in the ensures-reassigned-param check that shares the same walker). FIXED by adding the two arms (mirrors `substitute_vars`); an array-indexed arg now defers in a branch like any variable-arg, the pure-literal win stands, corpus-inert. Re-verified: SOUND, no false accept, no over-rejection. Red baseline + EXPECT:FAIL fixture. VM 540/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted.) **ensures anti-launder ArrayLiteral gap CLOSED** (`263d10b` — the standing post-slice HUNT found `collect_expr_vars` STILL missed `Expr::ArrayLiteral`: `Index` descends into `base` but an `[x]` literal base dead-ended, while `is_int_modelable` models `[x][0]==x`, so `ensures(result==[x][0]){x=0-42;…}` evaded the reassigned-param guard → CERTIFIED, then the caller's `assert` TRAPPED at runtime (a real false accept, pre-existing). Fixed by the ArrayLiteral arm AND making `collect_expr_vars` EXHAUSTIVE (no `_=>{}`) so the compiler enforces the modelable coupling — the walker had bit THREE times (Index/Var, Index/ArrayLiteral, len/ArrayLiteral). Red-baseline C2; 478/0; corpus 0/318 flips. VM 540/0, language 217/217, fixpoint dc680001 held.) **BRANCH PATH-CONDITION (guard as scoped premise) DONE** (`3c7075c` — a branch guard `if a>0 { … }` is pushed as a SCOPED assumption (un-negated→then, negated→else; float_bool_to_smt matches run.rs branch-taken incl. NaN), closing the int-Let-init branch OVER-REJECTION (`if a>0 { let y=g(a); }` where g requires(x>0) was wrongly rejected). A 2-round review chain hardened it: v1 (push only) OVER-REJECTED a DEAD branch (`requires(x>0){ if x<0 { assert } }` → the vacuity gate flipped it FAIL on the contradictory {x>0,x<0}); fixed by a per-obligation `guard_assumptions` field (+ a `ctx.active_branch_guards` scoped stack) that `assumptions_satisfiable` EXCLUDES from the vacuity test — a dead branch is unreachable, not vacuous; discharge still uses the full assumptions. v2 review caught the exclusion was by-VALUE (a genuine `requires(C)` sharing a guard `if C`’s SMT string was masked) → fixed to MULTISET (remove one assumption per guard). Sound: the vacuity net can only turn PASS→FAIL, so suppressing it only affects runtime-UNREACHABLE paths (dead branch / uncallable fn / trapping assume). Red baseline branch_guard_is_a_scoped_path_condition (6 asserts incl. dead-branch + coincidence + reassignment). VM 541/0, fixpoint dc680001 held, corpus 0/319 flips. This is the sound foundation for the DEPTH-GATE REMOVAL below.) **VARIABLE-ARG BRANCH DISCHARGE (depth-gate removed) DONE** (`9addff2` — with the guard in scope as a path condition, the depth gate is obsolete: `discharge_call_requires` now discharges EVERY modelable precondition at any depth/position, closing the last variable-arg branch FALSE-ACCEPT (`fn c(a,bb) requires(a>-100){ if bb { g(a); } }` g requires(a>0) — a=-50 PASSED check + trapped at run; now rejected). Guard-provable calls still pass (`if a>0 { g(a) }`, incl. transitive `a>5⊨a>0`, `&&` compounds, and the NEGATED else guard); guard-unrelated / disjunctive / wrong-var / reassigned-guard calls reject fail-closed (all verified firsthand + no runtime trap). The dead `unconditional`/ `int_at_all_depths`/`closed` gate machinery + its threading through analyze_stmts (~10 sites) was removed (net -76 lines). 2-lens adversarial review + synthesis. Corpus verdict-diff vs the path-condition slice = 0/319 flips. VM 542/0, fixpoint dc680001 held. RESIDUAL (honest): a bare unmodeled param arg not seeded by a caller `requires` stays fail-open in ALL positions (a fundamental modeling limit); while/for loop guards are not pushed; an arg-NESTED call (`h(g(x))`) is not a statement-position discharge site.) **NESTED (unconditional) call-site discharge DONE** (`3ad3917` — `discharge_calls_in_expr` recurses every UNCONDITIONALLY-executed sub-expression of a statement and discharges each contracted call's precondition, closing the fail-open where a call NESTED in an argument / binary operand / `return`-arg / assert/assign value (`h(g(x))`, `print(g(x))`, `g(x)+1`, `return g(x)`, `arr[g(i)]`, `assert(g(x))`) was never checked. Recurses through Call args / CallExpr / Binary lhs (+rhs when NOT `&&`/`||`) / unary / cast / index / field / array / struct / map / enum / try / assume / assert inner; DELIBERATELY skips the `&&`/`||` RHS (the generated Rust short-circuits, run.rs:3027) and `if`/`match`/`if let`/block/lambda bodies — a call there runs on only some paths, so it stays the CONDITIONAL-position residual (needs its own path condition). No double-discharge (a top-level Call init is excluded from the walk via `!matches!(init, Call)`; B2 discharges it once + recurses only its args). 2-lens review + synthesis GO (SOUND): 17 nested positions verified reject-when-violable / accept-when-provable, no over-rejection, no direct-call regression; the diff strictly NARROWS the fail-open set. Corpus verdict-diff = 0/319 flips. VM 543/0, fixpoint dc680001 held. RESIDUAL now = CONDITIONAL nested positions (match-arm / if-expr / short-circuit rhs) + the fundamental loop-havoc + bare-unmodeled-param fail-opens.) **CONDITIONAL-position nested call discharge DONE** (`0a2cdc0` — the nested-call walker now recurses CONDITIONAL positions under a SCOPED path condition too: an `if`-EXPRESSION branch (then under `cond`, else under `!cond`) and a short-circuit `&&`/`||` rhs (`a && rhs` under `a`, `a || rhs` under `!a`), reusing push_branch_path_condition + the multiset-vacuity exclusion; a `match` scrutinee is unconditional. So `let z = if c { g(a) } else { 0 }` / `b && (g(a) > 0)` now discharge g's precondition under exactly the guard that gates the call — guard-provable passes, guard-unrelated/contradicting rejects. 2-lens review + synthesis GO (SOUND): polarity correct in all 4 positions (identical to the Stmt::If handler), scoping leak-free (siblings / later statements / nested compounds verified — assumptions + active_branch_guards snapshot/truncate-restored), vacuity sound both directions, no over-rejection, no regression of the unconditional discharge (482/482). Corpus verdict-diff = 0/319 flips. VM 544/0, fixpoint dc680001 held. RESIDUAL now: `match`-ARM bodies (pattern-implied facts not yet modeled) + if-let/block/lambda bodies + the fundamental loop-havoc / bare-unmodeled-param fail-opens.) **STATEMENT-HEADER call discharge DONE** (`067b21a` — the standing post-solver HUNT's completeness critic found the per-slice reviews had verified the nested-call WALKER was sound but never asked which statement HANDLERS route through it: SIX unconditionally-evaluated statement sub-expressions were fail-open — an `if`/`while` CONDITION (`if g(a) > 0`), a `for` RANGE/collection header (`for i in 0..g(a)`), a destructuring `let` INITIALIZER (`let [p,q] = [g(a),0]`), an ASSIGN TARGET place-expr (`arr[g(a)] = x`), and a `while let` SCRUTINEE. Fixed by wiring discharge_calls_in_expr into all six handlers (no new machinery). Loop-carried header vars stay havoc-skipped (fail-open, the documented loop residual) — the header discharge proves iteration-1's pre-loop value, strictly improving soundness. 2-lens review GO (purely additive, net-neutral on assumptions, no over-rejection, no double-discharge, no 7th uncovered position — a `match`-STATEMENT scrutinee + deeply-nested targets already route through). Corpus verdict-diff = 0/319 flips. VM 545/0, fixpoint dc680001 held. LESSON: after adding a shared walker, audit EVERY caller/handler that should invoke it — 'is the walker sound' and 'does every position reach it' are two separate questions; only a completeness critic asks the second.) **MATCH-ARM BODY call discharge DONE** (`8a6a827` — the last CONDITIONAL-position residual is closed: a contracted call in a `match`-ARM body (`match a { 0 => 1, _ => recip(a) }`) is now discharged under the arm's SCOPED path condition. `match_arm_pattern_fact(scrutinee, pattern)` models the arm-implied fact — `scrutinee == lit` for a Literal/StrLiteral arm, an `||`-disjunction for an Or-of-literals arm, None for wildcard/binding/enum/struct/list — consumed only via push_branch_path_condition, so a non-modelable scrutinee/fact is a no-op (fail-open, never a wrong premise). ORDERED-ARM semantics: a later arm assumes the NEGATION of prior arms' facts, which is what makes the two idiomatic fall-throughs sound — `0 => 0, _ => recip(a)` (the `_` body assumes `a != 0`) and `_ if a>0 => 1, _ => h(a)` (the `_` body assumes `!(a>0)` = a<=0). Prior-arm negation is accumulated with strict gating: a guardless literal pushes `scrutinee != lit`, a WILDCARD-guarded arm pushes `!guard`, and a binding-guarded / guarded-literal / refutable arm pushes NOTHING (fail-closed). A binding-guarded arm (`n if G`) is excluded even though irrefutable — `G` may reference the binding `n` (aliasing the scrutinee) whose name denotes a different thing in a later arm, so pushing `!G` there could assert a WRONG fact; a guarded-literal arm is excluded because a non-match could be the guard failing, not the pattern. Reuses push_branch_path_condition + the multiset guard-exclusion (3c7075c) so a dead arm passes vacuously; assumptions + active_branch_guards are snapshot/truncate-restored around every arm so a fact never leaks to a sibling arm or a later statement. THREE Explore-only adversarial reviews drove it: the 1st found a fall-through `_` after a literal arm lacked `scrutinee != lit`, the 2nd found an irrefutable-guarded fall-through lacked `!guard` (both over-rejections), the 3rd GO; I then proactively tightened the irrefutable-guard negation from wildcard|binding to WILDCARD-ONLY to close the binding-alias unsoundness (strictly more conservative — removes a fact-push, cannot add a false accept). Red baseline match_arm_body_calls_discharge_under_the_pattern_condition (guardless-literal, wildcard-guarded, binding-guarded, guarded-literal fall-throughs; the negation must not over-prove a stricter callee). Corpus verdict-diff = 0/319 flips. VM 546/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted. RESIDUAL (honest, fail-OPEN): an enum/struct/list pattern's BOUND variable (`Some(n) => g(n)`) carries no pattern fact (needs enum→SMT modeling); a binding-guarded fall-through now conservatively over-rejects (safe); if-let/block/lambda bodies stay deferred; the fundamental loop-havoc + bare-unmodeled-param fail-opens are unchanged.) **WHOLE-VALUE MATCH-BINDING FRESH SYMBOL DONE** (`c8e31b9` — the post-8a6a827 adversarial hunt found a CONFIRMED false accept that slice left reachable: a whole-value binding `match S { name => body }` binds `name = S`, but the arm-body discharge referenced `name` by its mangled symbol `anb_<name>`, so a binding SHADOWING a modeled outer param of the same name discharged a body call `g(name)` against the PARAM's facts (a conflation): `fn f(a,b) requires(a>50) { match b { a => g(a) } }` (g requires x>0) — check ACCEPTED yet the binding is `a=b=0` so `g(0)` traps. FIX: mint a FRESH SMT symbol (`<counter>mbind`, digit-prefixed → no user-identifier collision, single-token for collect_vars_from_smt) for the binding, insert it into solver_int_vars, and `rename_binding(guard/body, name→fresh)` — a SHADOW-AWARE rewrite mirroring EXACTLY the positions discharge_calls_in_expr descends (call args / nested calls / binary / &&-|| / if-branches / index / field / struct-enum-map literals / cast / try-assume-assert / match scrutinee) that does NOT rewrite inside a nested `match` arm rebinding `name` (its inner binding is renamed by its own recursion; its scrutinee IS renamed). If the scrutinee is int-modelable, push `(= smt_var(fresh) <scrutinee>)` — the outer `anb_<name>` and every assumption over it are UNTOUCHED (no filtering), so a scrutinee-constraining `requires(p==a)` survives and `p==a>50` still proves `g(a)`, and identity `match a { a => .. }` becomes `fresh==a` which keeps a's own facts (no identity special-case). If the scrutinee is a non-modelable INT expr (user call / cast / `/`), `fresh` stays UNCONSTRAINED so an INT obligation over it FAILS CLOSED (rejects — the binding value is unknown), never fail-open (a float/string binding gets an int-lane fresh with no float/string membership → a float/string call over it stays fail-OPEN, the pre-existing residual). Restore = `truncate(snap)` + `solver_int_vars.remove(fresh)` (only added state). This is the REDESIGN of an earlier invalidate-and-reuse-symbol attempt a review NO-GO'd for two blockers, both fixed: (B1) a non-modelable scrutinee no longer drops the binding from the model → fail-CLOSED; (B2) the outer symbol's facts are never filtered → `p==a` survives, no over-rejection. Verified: CASE-3 shadow rejects; identity-safe accepts+runs-clean; identity-unsafe rejects; provable scrutinee accepts; non-modelable rejects (B1); `p==a` accepts (B2); wildcard-guard-composed rejects; nested rebind rejects; nested-call/binary/if-branch/&&-rhs/index all reject (rename covers every site); inner-different-binding still renames the outer name. 2-lens adversarial review + synthesis GO. Red baseline match_whole_value_binding_aliases_the_scrutinee (8 cases). Corpus verdict-diff 0/319 (corpus-inert). VM 547/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted. RESIDUAL (honest): a DESTRUCTURING binding (`Some(n)`/`[a]`/`Struct{x}`) is unchanged (the bound sub-value is not the whole scrutinee — needs a per-payload fresh symbol, a separate slice, hunt-confirmed pre-existing); a match-arm BLOCK body (`{ let b=0; recip(b) }`) stays deferred (fail-open, hunt-confirmed); the separate BUILTIN-MODEL-SHADOWING class (a user `max`/`min`/`abs`/`len` mismodeled by the solver builtin) is hunt-confirmed and queued.) **QF_S str.len STRING-LENGTH lane DONE** (`f30e78d` — a string-LENGTH contract `len(s) OP k` (OP ==/!=/</<=/>/>=) now DISCHARGES in Z3 QF_S over `str.len` instead of fail-opening: `requires(len(s)>=3)` seeds `str.len` as a premise, a body `assert(len(s)>=1)` proves from it, a call `h("ab")` vs `requires(len(s)>=3)` is caught (2<3), and `ensures(len(result)>=len(s))` discharges. Encoders is_strlen_term (gated by is_string_modelable)/is_bool_modelable_strlen/strlen_bool_to_smt wired as an `else if` into 5 sites (requires-seed, branch-guard, call-site discharge, assert, ensures), each strings:true → QF_S with fact_is_string-filtered assumptions. SOUNDNESS: runtime `len(String)`=`s.chars().count()` (Unicode scalar count); a string VAR's `str.len` is abstract → pure-var reasoning sound, BUT z3 materializes a LITERAL byte/unit-wise (`str.len("é")`=2, `str.len("😀")`=4) AND TRUNCATES a raw-NUL literal (`str.len("ab\0cd")`=2≠runtime 5) → mis-model. FIX: is_strlen_term gates the arg with `is_string_modelable` — the SIBLING string-eq gate already hardened to PRINTABLE-ASCII (reusing it, not a bespoke `s.is_ascii()` which admits NUL, is deliberate); a non-ASCII/NUL/control literal stays fail-open (=pre-lane). An int-VAR bound (`len(s)>=n`) is excluded (BitVec ≠ QF_S Int) → fail-open. A FOUR-PASS adversarial review drove it: caught (A) the NUL sub-case my first `s.is_ascii()` fix admitted, (B) a CONSTANT strlen fact `(= (str.len "abc") 3)` (no var) leaking into an int obligation → z3 sort error → over-reject, fixed by fact_is_string recognizing `str.len`/`"` markers, (C) a MIXED-lane `&&` requires (`len(s)>=3 && s=="abc"`) matching no single seed lane → over-reject, fixed by `seed_requires_fact` DECOMPOSING top-level `&&` to seed each conjunct in its own lane (sound completeness gain, also handles int&&string), and (D) UNCOVERED-VAR STRANDING — an obligation whose justifying precondition is UNSEEDABLE (`requires(s=="é")` non-ASCII, `requires(len(s)>=n)` int-var) carried ZERO string facts → z3's spurious `s=""` over-rejected a valid pre-lane-accepted program (3 reproducers incl. call-site forwarding), fixed by COVERAGE-GATING the two previously-fail-open sites (assert + call-site requires@) via `strlen_vars_covered` — uncovered → skip (fail-open = pre-lane, the assert stays runtime-enforced), var-less literal obligations stay modeled, the ensures site NOT gated (fails closed pre-lane; its tautology proves assumption-free); the gate cannot introduce a false accept (pre-lane caught nothing in this class). Verified firsthand: assert-unprovable rejects, provable/call-site/ensures correct, len(s1) OP len(s2), empty string, all 6 ops, string-let length, mixed-lane no-leak, non-ASCII+NUL fail-open, constant-seed no-clash, mixed-&& seeding, int-var fail-open, 3 uncovered-var reproducers accept + covered forwarding discharges both directions. 2-lens review×4-pass + synthesis GO. Red baseline phase3_qf_s_strlen_contracts_discharge_or_disprove (13 asserts). Corpus verdict-diff 0/319 (corpus-inert). VM 548/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted. RESIDUAL (fail-OPEN): a non-ASCII/NUL literal length (needs a codepoint-faithful encoder); an int-VAR length bound (needs a BitVec↔Int bridge); `str.++`/`str.substr`/`str.indexof` stay unmodeled.) **STRUCT-LITERAL FIELD READ modeling DONE** (`f2b6e89` — a hunt-confirmed false accept closed: `g(P{v: 0}.v)` against `requires(x > 0)` check-ACCEPTED and run-TRAPPED because a `FieldAccess` was never int-modelable so the arg's precondition was skipped fail-open. A struct-LITERAL field read is now modeled as the NAMED field's value expr — the struct analog of the `[a,b][0]` bounded-array read, mirroring its ALL rule (the accessed field must exist, ALL field values int-modelable; a call-bearing/nested-literal value declines → fail-open unchanged). FOUR coupled walker additions per the LESSON-4 rule: is_int_modelable arm, expr_to_smt_with_width named-field unfolding, expr_to_smt_value validation, and NEW substitute_vars FieldAccess+StructLiteral arms — previously clone-catch-all, safe while unmodelable, but once `requires(P{v: x}.v > 0)` is modelable an unsubstituted callee-param name would RE-BIND to a caller var of the same name (the precondition-bypass class; verified caught: caller `x=-5` rejects, `x=5` proves). collect_expr_vars already exhaustive → the ensures anti-launder holds (`ensures(result == P{v: x}.v)` over a reassigned x rejects). Verified firsthand: false-accept rejects+run-traps, safe dual accepts+runs-clean, multi-field selects the NAMED field, arithmetic-through-field proves, same-name rebinding caught, anti-launder rejects. 2-lens review + synthesis GO. Red baseline struct_literal_field_access_models_the_field_value. Corpus verdict-diff 0/319. VM 549/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted. RESIDUAL (fail-OPEN, documented): a field access on a struct VAR (`let p = P{v:0}; g(p.v)`) — per-field facts need own symbols + reassignment invalidation (the frame sweep keys on whole-var mangled names), a separate feature; call-bearing/nested-literal field values decline the ALL rule (consistent with the array sibling, pre-existing).) **DESTRUCTURED-SHADOW FRESH SYMBOLS + NESTED-ALIAS WIDTHS DONE** (`1afdbfd` — completes the match-binding fresh-symbol machinery (c8e31b9) on two flagged residuals. (1) DESTRUCTURED SHADOW CONFLATION (confirmed false accept, enum AND list patterns): a destructuring pattern's bound name SHADOWING a modeled outer var was discharged against the OUTER var's facts — `fn f(a, o) requires(a>50) { match o { Opt::Some(a) => g(a), .. } }` (g requires x>0) check-ACCEPTED though the binding is the PAYLOAD (0) and `g(0)` traps. Unlike whole-value there is NO value to alias (payload unmodeled), so each SHADOWING bound name (∈ int/float/string vars) is renamed to a fresh UNCONSTRAINED symbol in the SHADOWED VAR'S OWN LANE (float→solver_float_vars, string→solver_string_vars, else int+width) → the obligation fires with no facts → FAIL-CLOSED in every lane. The lane routing is LOAD-BEARING (review pass-1 NO-GO'd an int-only version: a float/string shadow's fresh symbol matched NO lane → the callee's float/string requires was SKIPPED → fail-OPEN, flipping the pre-diff fail-closed reject built over the OUTER var into a NEW false accept — 3 reproducers, all now reject+trap; restore removes from all three lanes+widths). Pass-2 caught the STRING sub-lane split: string-LENGTH is coverage-gated (strlen_vars_covered) so the unconstrained fresh symbol's obligation was SKIPPED (fail-open) while string-EQUALITY (ungated) failed closed — fixed by seeding a TAUTOLOGICAL `(>= (str.len anb_fresh) 0)` fact at mint (constrains nothing, marks the var covered → the obligation FIRES and fails closed; truncated on restore, verified leak-free post-match). Monotone-toward-reject on exactly the shadow class (wrong-premise accept→reject, wrong-premise reject→reject); a NON-shadowing bound name is UNTOUCHED (fail-open, documented residual) — no fail-open introduced anywhere. BONUS completeness: an arm GUARD over the shadowing binding is renamed too, so `Opt::Some(a) if a>0 => g(a)` now PROVES via the guard fact on the FRESH symbol (previously the guard bound the OUTER var — a wrong premise in both directions), while the weak dual (`if a>-100`) rejects and genuinely traps. (2) NESTED-ALIAS WIDTH (review-noted over-rejection): whole-value fresh symbols were in solver_int_vars but not symbolic_widths, so a valid NESTED alias chain (`match a { p => match p { d => g(d) } }` under a>0, d==p==a) couldn't encode its inner alias → rejected. Fresh symbols now carry width 64 (removed on restore); the chain proves, the weak dual rejects, a non-modelable inner scrutinee (`match ident(p)`) stays unconstrained→rejects. Verified firsthand: enum+list shadows reject (both trapped), non-shadow pin, enclosing-var both directions, guard-renamed prove + weak-guard reject, mixed bound names, sequential-match restore, nested chain both directions. 2-lens review ×3-pass + synthesis GO. Red baseline destructured_binding_shadows_get_fresh_symbols. Corpus verdict-diff 0/319. VM 550/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted. RESIDUAL (fail-OPEN, documented): a NON-shadowing payload binding feeding a contracted call — needs enum-payload/list-element value modeling or a fail-closed design decision (same class as the deferred builtin-shadowing task); the WHOLE-VALUE binding path keeps its int-lane-only fresh symbol (a float/string whole-value binding is fail-open there — pre-existing, review-noted out of scope).) **MIXED-LANE `&&` CALL-SITE REQUIRES DECOMPOSITION DONE** (`d2d4c1b` — a review-found fail-open closed: a callee `requires` that is a top-level `&&` MIXING solver lanes (`s == "ok" && len(s) >= 2`) was neither fully string-eq nor fully strlen modelable → discharge_call_requires matched NO lane atomically → emitted no obligation → a call `gs("no")` certified a runtime-trapping precondition (splitting into two `requires` clauses already rejected; only the single-clause `&&` leaked). FIX: a `flatten_and_exprs` helper splits a top-level `&&` into leaf conjuncts and the per-clause lane dispatch runs over each — `A && B` at a call site requires proving BOTH, so per-conjunct obligations are equivalent+SOUND (mirrors seed_requires_fact's requires-SEED decomposition). Each conjunct discharges in its own lane; an unmodelable conjunct still sets all_requires_checkable=false (ensures-assumption gating UNCHANGED, byte-identical); homogeneous int `&&` was one is_bool_modelable formula → now two obligations, same verdict. Verified firsthand: mixed violated rejects+run-traps, satisfied accepts, strlen-only-conjunct-violated rejects, int&&string catches the string violation, float&&float the float, deep `A&&(B&&C)` fires every conjunct, a `||`-conjunct discharged whole + caught, an UNMODELABLE conjunct beside a VIOLATED modelable one still rejects (`opaque(x)>0 && x>0`, x=-5 → x>0 rejects — the trapping `ff(-5)` case went ACCEPT→REJECT, strictly better than the pre-diff whole-requires fail-open), unmodelable-beside-satisfied does not over-reject. 2-lens review + synthesis GO. Red baseline mixed_lane_conjunction_requires_decomposes. Corpus verdict-diff 0/319. VM 551/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted. RESIDUAL: an unmodelable conjunct stays fail-open (pre-existing unmodelable-predicate residual, not this slice's — the decomposition only ADDS coverage, monotone-toward-reject).) **QF_S str.++ STRING-CONCATENATION lane DONE** (`7990cf0` — string concat `s + t` now models as `(str.++ anb_s anb_t)` in QF_S so `ensures/requires/assert(result == s + t)` DISCHARGES instead of the ensures site fail-CLOSING (`ensures(result==s+t)` for `return s+t` was a spurious reject) or the requires/assert site fail-OPENING (`g("x","y")` vs `requires(s+t=="ab")` certified a trapping precondition). TWO-point diff: is_string_modelable gains a Binary `+` arm (both operands string-modelable), string_expr_to_smt emits `(str.++ lhs rhs)`; everything downstream picks it up — the string-EQUALITY lane, the string-LET modeler (`let u = s + t` pushes `(= anb_u (str.++ …))`, symbolic_widths eviction still prevents a bvadd over String symbols), and is_strlen_term (`len(s + t)` composes: z3 str.len(str.++ a b) = len a + len b). SOUNDNESS: runtime string `+` = char-sequence concat = SMT str.++, exact for string VARS (abstract); a LITERAL operand rides the SAME printable-ASCII gate as str.len/string-eq so a non-ASCII/NUL operand DECLINES (fail-open, no byte-wise mis-model); the `\`→`\u{5c}` escape survives composition. z3-validated (reflexive unsat, `"a"++"b"=="ab"` unsat, not commutative). Verified firsthand: valid ensures discharges+runs-clean, false ensures disproved, call-site violated caught+traps, satisfied accepts, string-let both directions, associativity, empty-concat identity, two-literal concat, concat+str.len compose, non-ASCII fail-open, no int sort-clash, backslash escape survives; no regression (string→int re-let holds; a reassigned concat let stays fail-open exactly like a reassigned non-concat let — pre-existing reassigned_roots gate). The stale `phase3_qf_s_string_let_chaining` "concat must stay unmodeled" assertion is updated to pin the new discharge. 2-lens review + synthesis GO. Red baseline phase3_qf_s_string_concat_contracts_discharge_or_disprove. Corpus verdict-diff 0/319. VM 552/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted. RESIDUAL: a non-ASCII/NUL literal operand fail-opens (z3 byte-wise literal issue, same as str.len); a contract-less fn's params aren't modeled (has_contract gate); `str.substr`/`str.indexof`/`str.replace` unmodeled; `len(a)+len(b)` (a SUM of str.len terms) not yet a strlen operand.) **struct-PARAM field access in contracts DONE** (`adcdde0` — a `requires`/`ensures`/`assert` over a struct PARAMETER's scalar field `p.field` now MODELS the field in its declared lane (int/float/string) instead of fail-opening. Closes an adversarial-hunt FALSE ACCEPT (probe P5, from the POST-STRING-LANE whole-surface hunt): `requires(p.a=="x") { assert(p.a=="z") }` was ACCEPTED while `g(P{a:"x"})` traps — the field access was unmodeled so the string assert fail-opened; the prior slice `f2b6e89` modeled only struct-LITERAL reads `P{v:0}.v`, never a struct VAR's field. Each `(base,field)` gets a canonical `mangle_field` SMT symbol `{len}fld_{base}_{field}_e` — digit-led (user-collision-proof), length-prefixed (injective), sentinel-safe `_e` terminator (can't end in the `__arr` seq-Array marker even for a field named `_arr`) — routed to the field's lane via `ctx.struct_fields`; FieldAccess-over-Var arms added to is_int/float/string_modelable + the 4 encoders. COVERAGE folded into registration + a PRUNE (LESSON-12 stranded-obligation guard): only a requires-MENTIONED field is registered, then any field NO seeded assumption references is un-registered — so an unseedable requires (non-ASCII literal, user-predicate call) reverts the field to fail-open, no over-rejection. Registration GATED OFF a base param reassigned OR SHADOWED (`collect_assigned_roots` + `collect_let_bound`) — the 2-lens adversarial review caught (lens-1) that omitting `collect_let_bound` let a `let p=P{n:99}` shadow leave the entry `requires(p.n==5)` seed live to FALSELY PROVE `assert(p.n==5)` while runtime reads 99 (fixed + locked; the sibling nzdiv/ensures gates already did both); lens-2 confirmed the fix complete + every vector clean. Now behaves EXACTLY like a scalar param (a non-pinning `requires(p.n>3)` + a stronger `assert(p.n==10)` correctly REJECTS — fail-closed modular verification, consistent — verified the scalar analog already rejects on HEAD). SOUNDNESS: structs are value types (run.rs `AnubisValue::Struct` not Rc-shared) so `let q=p; q.n=..` can't alias-mutate `p.n`; distinct `(base,field)` → distinct symbols (no false alias `p.a`/`q.a`). Verified firsthand: string/int/float false asserts + false ensures REJECT, true accept, field-concat `p.a+p.b` + field-`len(p.a)` compose, field-as-call-arg discharges interproc, reassigned/nested/unmentioned fields fail-open, struct-LITERAL #13 sentinel intact. Red baseline struct_param_field_contracts_discharge_or_disprove (9 cases). Corpus verdict-diff 0/319. VM 491/0, language 217/217, turing 13/13, security 17/17, stdlib 11/11, selfhost seal 9/9 fixpoint dc680001 held, dogfood 3/3; Ammit 0-contradicted. RESIDUAL: a struct-VAR field via a LET (`let p=P{..}; assert(p.field..)`) stays fail-open (params only — the #13-documented residual); nested `p.a.b` fail-open; a non-ASCII field literal fail-open.) **struct-VAR field via a LET DONE** (`ba8e16e` — the body twin of the struct-PARAM path: a `let p = P{f: v, ...}` now seeds each SCALAR modelable field's `mangle_field` symbol from its CONSTRUCTION value, so `assert`/`ensures(p.field ..)` is proven/DISPROVED instead of fail-opening — closes the #13-documented "struct VAR's fields need their own per-field symbols" residual: `let p = P{v:5}; assert(p.v==6)` was accept+trap, now rejects. Reuses ALL of adcdde0's infra (mangle_field + FieldAccess-over-Var arms + encoders); the only new code is a struct-literal branch in the body `Stmt::Let` handler + a `shadowed_lets` set. THREE gates, mirroring the float/string let branches: (1) `!reassigned_roots(p)` — a field write `p.f=..`/reassignment roots to p (incl. embedded writes); (2) `!shadowed_lets(p)` — a NEW per-function set (names bound >1×, complete walk reusing expr_let_bound) fail-opens a re-bound base whose mangled per-field fact escapes the scalar shadow-clear AND an expression-position match/if-let shadow that never reaches the Stmt::Let clear (a false-proof vector the 2-lens review's lens-1 probed); (3) VALUE-STABILITY (per-field, review lens-2 Finding 1 — a stranded-field OVER-REJECTION `let p=P{v:x}; x=10; assert(p.v==5)` ran clean but was rejected because the fact `(= sym anb_x)` is dropped on the x-write while the symbol stays registered → free var): register a field ONLY when no var in its value expr is reassigned/shadowed (value semantics — the field copies at construction). A param+let same-name is mutually exclusive (slice-19's collect_let_bound gate skips a let-shadowed param → no contradictory facts). 2-lens adversarial review: lens-1 (soundness) CLEAR resting on struct value semantics (AnubisValue::Struct is a non-Rc Vec → no alias mutation) + the whole-body gate; lens-2 found + I fixed the value-stability over-reject. Verified firsthand: false int/string/float field asserts REJECT, true accept, a stable-param + arithmetic-over-stable-lets field is modeled, a shadowed base + a reassigned-var-value fail-open (no false proof — the discriminator: both true/false asserts accept), an expression-position match shadow rejects correctly via the existing match-binding+#13 path, #19 param path intact. Red baseline struct_let_field_contracts_discharge_or_disprove (11 cases). Corpus verdict-diff vs 48e07a3 = 0/319. VM 492/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted. RESIDUAL: a reassigned/shadowed base; a field value referencing a reassigned/shadowed var; a NON-modelable construction value (a user-call — most real corpus certificates); nested `p.a.b`; a non-ASCII field literal.) **QF_S string-PREDICATE lane DONE** (`eb5b8d9` — `contains(s,sub)`→`(str.contains s sub)`, `starts_with(s,p)`→`(str.prefixof p s)`, `ends_with(s,p)`→`(str.suffixof p s)` now DISCHARGE in QF_S so a `requires`/`ensures`/`assert` over them is proven/DISPROVED instead of fail-opening — `requires(s=="hello") { assert(starts_with(s,"xy")) }` was accept+trap, now rejects. Runtime Rust str::contains/starts_with/ends_with = z3 char-seq predicates over printable-ASCII (validated); string VAR abstract → sound. TWO-point core (is_bool_modelable_string + string_bool_to_smt Call arms), reusing the existing string-bool dispatch; predicates compose with ==, str.++, str.len (contains(s,"abc")⊨len(s)>=3) and reason abstractly (starts_with(s,"ab")⊨contains(s,"a")). 2-lens review caught THREE issues, all fixed+locked: (1) BUILTIN-SHADOW false accept — a user `fn starts_with` OR a LOCAL `let contains=|..|`/param shadows the builtin at runtime (run.rs resolves user-fn/local before builtin) → mis-certify; fixed via a per-function `shadowed_string_preds` set (all_fns ∪ params ∪ collect_let_bound → decline). The first cut guarded only all_fns, missed local shadows (lens-1 found the let-closure PoC). len/abs/min/max carry NO such guard (#12) — this lane is strictly safer. (2) pure-predicate OVER-REJECTION — a predicate assert with an UNSEEDABLE justification (non-ASCII `requires(s=="café")`) strands a free var → z3 `s=""` → reject a program that runs clean; fixed by coverage-gating a PURE-predicate assert (is_pure_string_predicate, no ==) via the string-var coverage; equality is NOT gated (its uncovered reject is correct, gating it = false accept). (3) strlen-INTERACTION over-rejection — a `requires(contains(s,…))` str.contains fact spuriously "covered" a `len(s)>=N` strlen obligation whose real justification is the unseeable pin; fixed by excluding predicate facts (is_predicate_fact) from the strlen obligation sites (strlen lane behaves as pre-predicate-lane). SOUNDNESS: printable-ASCII gate declines a non-ASCII literal arg; a list `contains(xs,5)` declines (args not string-modelable). Verified firsthand: false predicates reject (all 3, both dirs), true accept, var-var non-vacuous, ==/str.++/str.len compose, ensures both dirs, user+local shadow fail-open (pre-lane parity), both over-rejection classes accept the valid programs, non-ASCII fail-open, no sort-clash. Red baseline phase3_qf_s_string_predicates_discharge_or_disprove (13 cases). Corpus verdict-diff 0/319. VM 493/0, language 217/217, turing 13/13, security 17/17, stdlib 11/11, selfhost seal 9/9 fixpoint dc680001 held, dogfood 3/3; Ammit 0-contradicted. RESIDUAL: a user/local shadow of a predicate builtin (#12 class — fail-open); a non-ASCII/NUL literal arg; `str.substr`/`str.indexof`/`str.replace` unmodeled; a pure-predicate assert with an unseedable justification fail-opens.) **builtin-model-shadowing guard DONE** (`1d29f9b` — #12: the solver models `abs`/`min`/`max`/`len` by CALLEE NAME, but a user `fn max` / local `let max`/param SHADOWS the builtin at runtime → the builtin model mis-certifies (wrong-PROOF false accept AND over-rejection of a valid user-fn program). Declines a shadowed builtin via a `shadow_builtin_mark` `\u{1}` sentinel on solver_int_vars/solver_string_vars (zero threading, like nzdiv/seq marks), inserted per-fn for {abs,min,max,len} shadowed by all_fns∪params∪collect_let_bound. TWO review-caught fixes folded in: ORDERING — the shadow detection must run BEFORE seed_requires_fact (else a `requires(max(a,b)<=5)` over a user max is seeded with builtin semantics → poisons the body assert → false accept; moved ahead of seeding, also fixing a slice-21 shadowed_string_preds staleness); CROSS-CALL — discharge_call_requires drops caller-LOCAL-only shadow marks so a caller param named `max` no longer suppresses the callee's builtin discharge (closes the caller-param-leak for int + the pre-existing string lane). Genuine builtins still model. Red baseline builtin_int_modeling_declines_user_or_local_shadow. Corpus 0/319. VM 494/0 fixpoint dc680001. RESIDUAL (exotic, corpus-inert, task #22): a caller closure as a nested arg (over-reject) + a callee param shadowing a builtin in its own requires (false accept, pre-#12); + the design-wide fail-open on an unmodeled int assert.)  **QF_S str.indexof lane DONE** (`6e1120a` — `index_of(s,sub)` now DISCHARGES as `(str.indexof s sub 0)`, an INT-valued string term modeled as an EXTENSION of the str.len lane, so `requires`/`ensures`/`assert` over an index proves/DISPROVES instead of fail-opening — `assert(index_of(s,"ll")==5)` was accept+trap, now rejects. Runtime `s.find`→char index/-1 = z3 str.indexof over printable-ASCII (validated: empty-needle→0, absent→-1, needle==s→0, first-of-repeats; string VAR abstract→sound). TWO-point core: is_strlen_term also matches `index_of(s,sub)` (both args string-modelable), strlen_int_to_smt emits `(str.indexof s sub 0)`; the comparison/coverage/operand machinery inherits it — `index_of(s,"x") >= 0`, `== 5`, `< len(s)` (composes with str.len) all discharge. SOUNDNESS: printable-ASCII gate declines a non-ASCII literal; the LIST overload `index_of(xs,2)` declines (args not string-modelable → never mis-modeled); not in the int-lane Call arm (no bit-vector mis-model); SHADOW guard (#12) — a user/local `index_of` declines via a shadow_builtin_mark sentinel on solver_string_vars in the pre-seeding block, and discharge_call_requires drops the caller-local index_of shadow (parity with len). 1-lens review CLEAR (no false accept/over-reject; surfaced + fixed the cross-call parity gap). Verified firsthand: false index rejects, true proves, missing-needle `>=0` disproved, abstract contains⊨indexof>=0, str.len composition, user/local + cross-call shadow decline, list declines, non-ASCII fail-opens. Red baseline phase3_qf_s_string_indexof_discharge_or_disprove. Corpus 0/319. VM 495/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted. `str.substr` DEFERRED (Anubis clamps negative start to 0 vs z3 str.substr→"" — a divergence needing a special-case). RESIDUAL: a non-ASCII literal arg; str.substr/str.replace; a user/local index_of shadow; offset fixed at 0.) **QF_S str.substr lane DONE (nonneg-literal subset)** (`fc04631` — `substr(s,off,len)` with NONNEG INT LITERAL off/len now DISCHARGES as `(str.substr s off len)`, a STRING-valued term extending is_string_modelable (like str.++), so `requires`/`ensures`/`assert` over a substring proves/DISPROVES — `assert(substr(s,0,3)=="xyz")` was accept+trap, now rejects. Runtime `chars.skip(off).take(len)` = z3 str.substr for off>=0 over printable-ASCII (validated, code-point indexed incl. multi-byte tail, clamp/off>=|s|→""/len=0→""). The ONE divergence (negative start: Anubis→from-0 vs z3→"") is EXCLUDED by the nonneg-literal gate; an int-VAR off/len is excluded (BV-sorted vs str.substr Int). TWO-point core (is_string_modelable + string_expr_to_smt substr arms); composes with string-eq/str.++/str.len (`len(substr(s,0,3))<=3`) + nests. COVERAGE GATE (LESSON-12): a substr-equality is a WEAK consequence, so an uncovered one (non-ASCII/user-pred requires) fail-opens instead of stranding — is_pure_string_predicate now classifies a `==` containing a substr (via expr_contains_substr) as gateable; a plain PIN + a MIXED substr&&pin stay fail-closed (no false accept). SHADOW guard (#12) + cross-call drop (parity with len/index_of). 1-lens review CLEAR (no false accept/over-reject; z3=runtime over all gated corners; the encoder unwrap_or(0) is unreachable). Verified firsthand: false rejects, true (prefix/interior/clamped) proves, negative/var offset fail-opens, str.++/str.len/nested compose, covered-false rejects, over-rejection accepts (gated), mixed fail-closed, shadow declines. Red baseline phase3_qf_s_string_substr_discharge_or_disprove. Corpus 0/319. VM 496/0, language 217/217, fixpoint dc680001 held, Ammit 0-contradicted. RESIDUAL: a negative/VAR/arithmetic off/len (only nonneg literals — a var offset needs Int↔BV); a non-ASCII s literal; `str.replace`; a substr-eq over an unseeable-justified var (fail-open, sound).) **QF_FP float loop-invariant lane DONE + float-let unification** (`3ad704d` — a `while` loop over FLOAT vars now verifies its invariants inductively in Float64, the mirror of the integer B3 lane: `verify_while_invariants_float` emits QF_FP base + preservation obligations via `float_bool_to_smt`/`float_expr_to_smt`, dispatched from `verify_while_invariants` when the loop is float-eligible AND int-INeligible over the PRE-LOOP solver sets (disjoint — a symbol is in exactly one of solver_int_vars/solver_float_vars, judged with NO tracked-var injection). Unblocked by the FLOAT-LET UNIFICATION: the `reassigned_roots` gate on the float `let` seed was REMOVED so a genuinely-float `let` seeds its defining fact UNCONDITIONALLY (like the integer seed), making the loop counter's entry value available at the base case; soundness held by PARITY with the integer lane — the `Stmt::Assign` handler now also `solver_float_vars.remove`s (+ solver_string_vars) a reassigned binding (fail-closed, honest ANUBIS_FLOAT_CONTRACT_UNMODELED), and the lane-agnostic `invalidate_embedded_writes`/`havoc_loop_written` catch every embedded write the statement sweep misses (match-arm/if-expr/block/nested-loop/let-init) — the exact machinery the integer lane has always relied on (its existence proof that the gate was redundant caution). The `<=`/`>=` NaN disjunction in float_bool_to_smt is load-bearing in base AND step (matches run.rs `partial_cmp().unwrap_or(Equal)` at NaN). The While handler is lane-aware (a float post-fact carries an `fp.`/`to_fp` token → re-admit into solver_float_vars, and push post-facts to `assumptions` ONLY, never `ctx.constraints` — keeping the self-host IR projection byte-identical); the float `push_ob` filters obligation symbols to `anb_`-prefixed (else float operator tokens `to_fp`/`RNE`/`isNaN` are declared as Float64 consts and COLLIDE with z3 builtins → a spurious over-reject). `extract_loop_transition` parameterized with a `term_modelable` closure (is_int_modelable | is_float_modelable). FIRST-HAND adversarial review CLEAR across all 6 surfaces (28+ ground-truth oracle probes; the spawned Explore agent thrashed but converged on the same risk surface — the post-fact re-admission — which the probes cleared): an embedded-write reassign DROPS the fact (sound fail-open, deferred to runtime — NOT an unsound proof, confirmed by the discriminator that the TRUE post-value assert is not rejected), post-loop false assert rejects, in-body false assert rejects AND traps, a false invariant is rejected inductively (base or step FAILs), an aux var written-but-not-in-invariant is unmodeled-deferred (identical to the integer lane, before=after), `%` (fmod) modeled since `1328722` (was fail-closed here), NaN/division sound, a MIXED int+float loop rejected (no mis-sort), bare-int stays on the int lane, neither-set falls through. Red baseline phase3_qf_fp_float_loop_invariants_verify_inductively (13 cases). Corpus verdict-diff 0/319, VM 497/0 + language 217/217 + turing 13/13 + security 17/17 + stdlib 11/11 + shadow 0-unexpected + selfhost 9/9 + dogfood 3/3, fixpoint dc680001 HELD, Ammit 0-contradicted. RESIDUAL (all sound): a non-integer-step decreasing float loop over-rejects (the solver cannot know the value stays integer-valued); `while`-only (no `for`-range float invariant); a straight-line float reassign does not re-establish a fresh fact.) **REMAINING:** QF_S broadening (`str.substr` nonneg-literal subset DONE `fc04631` (negative/var offset still fail-open); `str.indexof` DONE `6e1120a`; `contains`/`starts_with`/`ends_with` predicates DONE `eb5b8d9`; var-vs-var equality DONE `0f9d903`, `str.len` DONE `f30e78d`, concat `str.++` DONE `7990cf0`; `str.len` SUMS DONE `9ad2410` (`len(a)+len(b)` discharges in pure SMT Int — pure length-term sums only; a LITERAL addend is EXCLUDED because `len(a)+<near-i64::MAX literal>` overflows the runtime i64 at a small realizable string while unbounded Int does not, a first-hand-caught false accept); struct-PARAM field `p.field` DONE `adcdde0`, struct-VAR-via-LET field DONE `ba8e16e`; struct field-WRITE `p.f = v` DONE `88e4728` — a top-level straight-line write to an INTEGER field registers `mangle_field(p,f)` with the written value (drop-old-add-new), so a later read `p.f` is CHECKED not fail-opened (`let mut p=P{x:1}; p.x=5; assert(p.x==5)` proves, a false `assert(p.x==6)` is disproved, a rewrite tracks the latest, multi-field composes, param writes model too); gated fail-closed on `struct_write_disqualified` (any base reassigned anywhere except as a top-level straight-line field write — whole/embedded/branch/loop reassign — is excluded, so no stale mangled fact survives an un-evicted reassign), sound via Rc-COW value semantics (`let q=p; q.x=9` leaves p.x); verified by 7 adversarial lenses (~330 probes) + a completeness critic, zero false accepts. FLOAT field-WRITE `p.f = v` DONE `7058a63` — the QF_FP dual (genuine floats prove/disprove, `%`/int-division defer), same disqualifier gate + COW; the hunt exposed and this closed a SYSTEMIC pre-existing float-lane false accept — an INT-KINDED value (a bare int literal `7`, or `1 / 2`) was modeled as float (`7.0`, `0.5`) via `is_float_modelable`'s f64-parseability, but the runtime stores it as `Int` with no coercion so `p.x / 2` is INTEGER division; fixed with a `!is_int_modelable` gate (matching the sound float-`let` `genuinely_float` gate) at BOTH struct-field float sites (write + struct-literal-let read twin), 3 hunts ~120 probes clean (NaN/inf/rounding faithful); the float-PARAM case is a distinct, DEEPER pre-existing gap queued separately (task #34) — a f64 param/location can hold `Int` at runtime (`f(7)` passes `Int(7)` to a f64 param with no boundary coercion, breaking even with a float-literal `requires(x==7.0)`), so the field write, though sound for genuinely-float values (a float literal/arithmetic/`let`, which the `genuinely_float` gate keeps float-kinded), is a VEHICLE for #34 when its value derives from a pinned float param (`p.x = q; assert(p.x/2==3.5)`); the correct fix is boundary coercion, which re-establishes the float-lane invariant and closes both the direct and field-through manifestations. FLOAT-PARAM boundary coercion DONE `1abd0fd` (task #34, operator-approved int→float policy) — `anubis_coerce_float_param`/`anubis_coerce_float_ret` (the float dual of the existing `anubis_require_int` guard) coerce an Int argument/return to a Float at the f64/f32 boundary (lossless for |n|<2^53; non-numeric fails closed), so `f(7)` binds x=7.0 and `x/2` is float division — the checker/runtime divergence is ELIMINATED (check and run now AGREE). SOUND: runtime + checker share the same `ty::is_float`; int→f64 rounds identically both sides; FIXPOINT-SAFE (self-host uses no float params — dc680001 unchanged). Hunt 52 probes clean, all run-gates + corpus green. STRING field-WRITE `p.s = v` DONE `0d5a477` — completes the field-write TRILOGY (int/float/string), the QF_S dual: a straight-line write to a string field registers `mangle_field(p,s)` in the string lane, so a later read is CHECKED (composes with the whole QF_S surface — len/contains/starts_with/ends_with/index_of/substr/concat). The CLEANEST of the three (no value-KIND coercion subtlety — a string field only holds a string); two guards carry from the shared string encoder: the `\`→`\u{5c}` escaping (defeats z3's `\u` decode) + the printable-ASCII `is_string_modelable` gate (defeats NUL-truncation). Same `struct_write_disqualified` gate + COW; an adversarial hunt (~90 probes) + ~24 manual, zero false accepts. The one shared `struct_write_disqualified` gate now underpins all three field-write duals — validated as a reusable primitive. NESTED field READ `p.a.b` DONE `49cd371` — a read off a `let p = P{a: A{b: 5}}` is now CHECKED at ARBITRARY depth (int/float/string): a new `field_access_symbol` folds `mangle_field` over the path and the struct-literal-let seed is refactored into a recursive `seed_struct_literal_fields` that seeds the same nested symbol. SOUND with ZERO new invalidation — the seed fires only for a base neither reassigned nor shadowed, and ANY write at any level folds to root `p` (assign_target_root + whole-function collect_assigned_roots) → disables the whole struct's seed → a mutated/copied/shadowed base fail-opens, so the LESSON-19 stale-symbol gap the WRITE lane must evict cannot arise on the read side (runtime value semantics — no mut params/references — make aliased mutation impossible). Hunt 47 probes + ~15 manual, zero false accepts, mangle injective (`p.ab`≠`p.a.b`). NESTED field WRITE `p.a.b = v` DONE `f3d60bc` — completes the full struct-field surface (read+write × single+nested × int/float/string). concrete_field_write generalizes to any field-access chain (field_place_path + place_struct_type + field_access_symbol). SOUNDNESS CORE = a new PREFIX-CONFLICT gate: disqualify a root if any two of its top-level field-write paths are prefix-related (`p.a` prefix-of `p.a.b`), since an intermediate `p.a = ..` would stale the leaf's subtree; under the gate all writes are PARALLEL non-prefix leaf writes each updating its own symbol — no subtree eviction (LESSON-19 sidestepped). Load-bearing: structs are stored BY VALUE (Vec, not Rc — deep-cloned on every bind/pass, so NO struct aliasing; only Str/List/Map are Rc-COW). Hunt 47 probes + ~14 prior + ~20 manual, zero false accepts (prefix gate holds at both orders + 4-level; branch/loop/whole disqualify; mangle injective). The hunt's taint lens flagged that a secret/tainted assign inside a `research`/`exploit`/`hybrid` block does not trigger ANUBIS_SECRET_EXFILTRATION on a later egress — INVESTIGATED (task #32) and found NOT a Safe-mode false accept: such a block promotes the whole function to Research mode (first_mode / whole-file research semantics), and the program REFUSES to run in Safe mode (`ANUBIS_RUN_RESEARCH_REQUIRES_ALLOW`), so the egress can only occur under an explicit `--allow-research` opt-in — there is no `check`-accept / `run`-trap pair (the deep-verify refuter classified it non-real for the same reason). Localizing research mode so non-research code in a research file stays Safe-enforced is an optional defense-in-depth DESIGN question, deferred pending intended semantics; not a soundness bug); bounded-array maturity (already-shipped QF_ABV; CONCRETE-index WRITE DONE `6f0b96e` (`a[k] = v` for a literal in-range `k` and a `v` not referencing `a` UPDATES cell k in place — drop the old `(select a__arr k)` fact, add the new — instead of de-modeling the whole seq, so a later read of the written cell + the untouched cells + the length all verify; sound because a seq copy `let b=a` is unmodeled so no two modeled seqs share an `__arr`; self-/cross-cell / symbolic-index / OOB writes fall through to de-model, hunt CLEAN); symbolic-index loop-invariant READ half DONE `8453ae8` — a body read `a[i]` under a strict `i<len(a)` guard + an `i>=0` invariant discharges as `(select a__arr i)`, quantifier-free, gated by an in-bounds sentinel + a modelability-delta base-case check; a hunt also found+fixed a PRE-EXISTING push/pop/insert/remove stale-length false proof (mutating builtins now de-model their seq) and an ordering gap (a loop mutating a modeled seq is rejected). The quantified array-WRITE-FILL half — `invariant(forall j<i: a[j]==0)` — stays DEFER_RESEARCH: it needs a real `forall` (leaves decidable QF) + a store transition + trips 3 false-accept landmines); COLLECTION loop invariants DONE `b57fced` (`for x in a invariant(P) { body }` over a bounded modeled seq VAR verifies by desugaring to `for i in 0..len(a) { let x = a[i]; body }` and reusing the for-range + array-read lanes; enabled by making extract_loop_transition PROPAGATE a loop-body `let` — `let x = a[i]; last = x` → `last ↦ a[i]`, a sound + more-precise 2-line core change that also sharpens while/for loops reading an element through a body let; accumulator invariants verify, an element-referencing invariant / iteration-count accumulator / unbounded collection fail closed, hunt CLEAN). FOR-RANGE loop invariants DONE `d210af8` (`for i in start..end invariant(P) { body }` verifies by desugaring to the exactly-equivalent `let i=start; while i<end invariant(P) { body; i=i+1 }` and reusing verify_while_invariants — composes with the symbolic-index array read so `for i in 0..len(a) { …a[i]… }` verifies; gated to a Range source with modelable bounds and a body that does not reassign the counter, adversarial-hunt CLEAN; the implicit `i >= start` is auto-added `84b50a3` so a `for i in 0..len(a) { …a[i]… }` array scan verifies WITHOUT an explicit `invariant(i >= 0)`); float loop-invariants DONE `3ad704d` (QF_FP lane + the float-let unification landed — the `reassigned_roots` gate was removed, straight-line-reassigned floats chain via `invalidate_embedded_writes`/parity remove); **float `%` MODELED as fmod DONE** `1328722`: float `%` is C/Rust fmod (round-toward-zero quotient), encoded EXACTLY in `float_expr_to_smt` via `fp.rem` + a sign(a)·|b| correction (NOT the divergent SMT `fp.rem`); `is_float_modelable` admits `%`, so float `%` verifies in contracts, loop invariants (`i = i % 2.0` proves), and struct-field writes (`p.x = 5.5 % 2.0` ⇒ 1.5). Verified: 5.5%2.0=1.5, -5.5%2.0=-1.5, 5.5%-2.0=1.5; the divergent fp.rem value (-0.5) correctly does NOT prove. Soundness hunt `a1250c79` caught an interim false accept — an f64-parse branch added to `divisor_is_proven_nonzero` LEAKED into the INTEGER QF_BV lane (a beyond-i64 literal `2^64+1` reduced mod 2^64 → false model); FIXED by reverting the gate to integer-literals-only (the FLOAT lane never consults it — `fp.div`/fmod are total, and a genuine float divisor makes `is_int_modelable` false via its operand check anyway). Verified: 511/511, VM 511/0 fixpoint dc680001 HELD, 2 hunts CLEAN (46 + earlier probes), a 4-lens z3 workflow (81 bit-exact differential points vs libm fmod + 24 special-values + 117 runtime discriminator probes across all 4 positions = ZERO false accepts). RESIDUAL (all fail-safe): no machine-checked UNIVERSAL z3 proof (z3 cannot bit-blast `fp.rem` over Float64 in budget — soundness rests on dense sampling + the analytic fp.rem→fmod correction argument); a symbolic `x % y` var-divisor defers (NaN when y==0 defeats reflexivity); a loop-BODY assert on a loop-carried modulo variable defers (generic loop-carried opacity); a non-integer-step float loop over-rejects; a `for`-loop RANGE-bound embedded write is the one un-swept residual (astronomically rare). **Cross-lane soundness AUDIT DONE** `da753c0`: a holistic 10-lens adversarial audit attacking lane INTERACTIONS (float×loop, string×struct, array×kind, match×lane, field-write invalidation, divisor×field) that per-slice hunts covered only in isolation — 663 probes, each candidate independently skeptic-verified — found 3 discriminator-confirmed false accepts (2 roots), 7 lenses CLEAN. FIXED: (A) a struct-literal field's declared NUMERIC kind was unenforced, so an int↔float smuggle diverged on `/` (int→f64 field: checker floats 3.5, runtime ints 3; float→i64 field: checker bvsdivs 3, runtime floats 3.5) — enforced at the RUNTIME construction boundary (the #34 param-coercion dual): int value into a float field COERCES to Float, float value into an int field FAIL-CLOSES (ANUBIS_TYPE_VIOLATION); GENTLE guards act on the value (only a Float in an int field traps — String/Bool/List coerce to int at runtime, matching the model; the parser stores `[int]` as `int`); the checker also rejects the resolvable float→int early (ty::synth_concrete resolves Call/FieldAccess/Index the flat inference cannot) — comprehensive across literal/var/Call/FieldAccess/Index; int→float now COERCES so `P{x:7}` is valid like `f(7)`. (B) a param-field WRITE `p.x=5` survived a param-shadowing `let p=P{x:99}` (proved the stale 5) — disqualify the param-write when the param is let-bound (mirrors the slice-19 collect_let_bound gate), so it defers while the precise let-value disproof survives. 513/513, corpus CHECK-diff 0/319, RUN-diff 0/42, VM fixpoint dc680001 HELD (the field guards are behaviorally identity for the self-host). **Integer-arithmetic-subexpr-in-float-term FALSE ACCEPT CLOSED** (task #39, `0f914a0` — the orthogonal PRE-EXISTING float-encoder gap the cross-lane audit surfaced): a `+ 0.0`/`* 1.0` coercion flipped a term's static type to float and engaged the QF_FP `fp.*` encoder on an INTEGER-arithmetic subexpression the runtime computes with INTEGER semantics — `(7/2)+0.0` modeled as `fp.div` (3.5) vs runtime int-div (3.0). FIVE successive adversarial hunts surfaced FIVE distinct divergence mechanisms between the two number systems, closed by two gates in `is_float_modelable`: (A) an all-integer `/` DEFERS unless one operand is GENUINELY float (`is_genuinely_float`) — integer `/` truncates; (B) an all-integer `+`/`-`/`*`/`%` subexpr is admitted ONLY if `int_chain_exact(e).is_some_and(|v| v != 0)` — it const-folds exactly in i64 to a NON-ZERO value with every partial (and literal) `|v| ≤ 2^53`, under which every `fp.*` step is exact and equals the runtime's exact-i64-then-coerce-once. That one predicate subsumes OPERAND ROUNDING (`>2^53` literal rounds under `to_fp`, `%2` flips 1→0), `*` PRODUCT OVERFLOW (`wrapping_mul` sign-flips vs `fp.mul`'s true positive), `+`/`-` CHAIN DOUBLE-ROUNDING (per-op `fp.add` rounds at every step; operands can all be 2^52-small), and SIGNED ZERO (runtime `Int(0)`→+0.0 always, but `fp.rem`/`fp.mul`/`fp.neg` yield -0.0, exposed by `1.0/((0-4)%2)` = -inf vs +inf). Manifestations: `ensures` REJECTS (fail-closed), body-assert/let/struct-field DEFER (fail-open) — all sound (discriminator: accept-all, no false model). Checker-only → fixpoint dc680001 HELD across all VM re-seals; 514/514, corpus verdict-diff 0/319. ([[anubis-float-int-arith-subexpr-2026-07-18]], LESSONS 56–57.) **CALL-SITE f64-PARAM COERCION-ROUNDING false accept CLOSED** (task #40, `db5b2ff`): `anubis_coerce_float_param` (run.rs) coerces an INTEGER argument into an f64 param via `n as f64`, which ROUNDS (round-to-even) when |n| > 2^53 — but `discharge_call_requires` substituted the RAW integer and discharged the callee's `requires` in the EXACT integer QF_BV lane, so `g(2^53+1)` certified `requires(x > 2^53)` while the runtime coerces the arg to 2^53, trapping a mirroring body `assert` (check-ACCEPT / run-TRAP). The fix models the coercion per f64 param (via `ctx.fn_params`): a genuine float arg (or float-returning call) is UNCHANGED; a const-foldable int arg (`const_fold_i64`, an ungated exact i64 fold) is replaced by its runtime-coerced value `n as f64` (lossless below 2^53, the rounded value above → reject); a SYMBOLIC int arg (`references_int_var`) is replaced by a FRESH UNCONSTRAINED FLOAT var (arbitrary-f64 model) → fail-closed reject. The havoc uses a FLOAT var so a fractional bound (`x > 2^53.5`) still routes to QF_FP and rejects (a fresh INT var deferred it → hunt-found false accept p10). Checker-only, fixpoint dc680001 held, 515/515, corpus verdict-diff 0/319, 16-probe adversarial hunt CLEAN. ([[anubis-callsite-float-param-coercion-2026-07-18]], LESSONS 58–60.) **RETURN-side f64 coercion + ROBUST coercion-value rewrite DONE** (task #41, `4eb0e88`): `anubis_coerce_float_ret` rounds a float-returning fn's return the SAME way, so `fn src() -> f64 ensures(result > 2^53) { return 2^53+1; }` was a check-accept/run-trap false accept (modeled `result` as the exact int). Fixed by running each returned value through the SHARED `coerce_int_into_float_slot` helper (extracted from the #40 call-site loop) at the tail + early return sites. An EXHAUSTIVE 51-agent parallel-workflow hunt then found **42 confirmed false accepts** exposing three holes in the coercion-VALUE model shared by #40+#41: (B) `const_fold_i64` handled only `+-*/%` so `abs/<</>>/&/|/^/~/cast` fell through unmodeled; (D) a rounded value > 2^53 rendered as an i64 literal was rejected by the #39 int-form gate → silently DEFERRED → false accept (**this hole was live in the committed #40 too**); (FA33/34) int struct-field/array-elem returns passed through. All closed by gating the helper on `is_int_modelable` + rendering the coerced double in FLOAT-FORM (`{:.1}`, dodging the #39 gate, preserving the exactly-representable-2^53+2 accept) + havoc fallback. Checker-only, fixpoint dc680001 held, 517/517, verdict-diff 0/319. ([[anubis-return-side-float-coercion-2026-07-18]], LESSONS 61-63.) RESIDUALS the hunt surfaced — RE-TRIAGED via the accept-everything discriminator to be FAIL-OPEN DEFERRALS (the documented "unmodelable → defer" residual class), NOT genuine false-PROOFS (#41 closed all the genuine coercion false-proofs): #42 a large float literal whose `{:?}` is scientific (>= ~1e16) is unmodelable → deferred (`assert(2^54+1 > 2^54)` AND `< 2^54` BOTH accept ⇒ deferred, not proven) — a completeness gap, the encoder already `to_fp`s literals that reach it; #43 an opaque int-returning-call arg defers a float-param `requires` (the pre-existing bare-unmodeled-arg fail-open) then certifies the body assert under the unenforced precondition; #44 a deferred `assert` over an unmodeled f64-call result (completeness — the runtime assert enforces it). **VALUE-POSITION branch-guard threading (completeness) DONE** (ziros Task B, `f60d5a7`): a value-position `if`/`match` whose arms feed the result discharged each `ensures` WITHOUT the arm's path condition, so `max(a,b) >= a && >= b` REJECTED. Fixed by `tail_values_guarded`/`expr_tail_values_guarded` collecting (tail_value, guards) + pushing each via `push_branch_path_condition` in the tail-value discharge loop — the value-position twin of the statement-position 3c7075c gate, reusing its vacuity exclusion. max AND min prove (bounded + unbounded); nested if-expr composes. SOUND (completeness — a true premise only turns REJECT→PASS): false branch contracts still get counterexamples, dead-branch guards don't leak vacuous passes. Fixpoint held, 520/520, verdict-diff 0/319. RESIDUAL: match binding-pattern+guard. ([[anubis-ifexpr-branch-guard-2026-07-18]].) **u32 PARAM boundary coercion DONE (ziros Task A / A1, `4ce6537`):** a `u8`/`u16`/`u32` PARAMETER is MASKED to [0,2^w) at the runtime boundary (`anubis_coerce_uint_param`), so the solver SOUNDLY injects `0<=x<2^w` for it — the `requires(x>=0)` tax is GONE (`fn nonneg(x:u32)->u32 ensures(result>=0){return x}` now PROVES). PATH Q (mask PARAMS only, NOT returns): `u32` is Anubis's canonical integer spelling (`normalize` maps int/i64/i32→u32), so masking returns silently broke 10 solver tests + 2 examples that use `-> u32` for signed i64 arithmetic, and over-rejected valid programs (z3 can't reason `(call-result & mask)>pc`). A u32-param ARG is coerced `(arg & (2^w−1))` when composed into the callee's `requires`+`ensures` — closing a CONFIRMED check-accept/run-trap (a contracted caller composing `g(3*x)`). Gate on the RAW spelling `ty::unsigned_mask_width` (u8/16/32; u64 excluded; a signed `int` is NEVER injected → still correctly taxed). Invariant: **range injected ⟺ runtime masked ⟺ u32 PARAM** (fields/locals/call-results are neither — a struct u32-field `ensures(result>=0)` correctly REJECTS, runtime returns −5). Runtime differential exact (`-1`→2^32−1); 521/521, verdict-diff **0/319**, adversarial hunt clean, VM battery green, **fixpoint dc680001 held** (self-host uses no u32 params → mask inert). Docs: `SPEC.md` "Integer semantics (normative)". RESIDUAL: u64 keeps the tax; a u32 return/local is unbounded-i64. ([[anubis-u32-boundary-coercion-2026-07-18]].) **Fail-closed `build` DONE (ziros Task C, `7b9e8d7`):** `anubis build` now runs the SAME Z3 contract solver as `check` and REFUSES (non-zero, `ANUBIS_ASSERTION_UNPROVEN` + counterexample) on any unproven `requires`/`ensures`/`assert` BEFORE emitting an artifact — closing the DX gap where a false contract slipped into a build silently. `--no-verify` is the in-progress escape; README Quick Start now leads with `check`. CLI-only (no compiler/solver change) → fixpoint dc680001 trivially unchanged; tool-test 62/62, package gate PASS, VM battery green. (Closes the ziros completeness pack B+A+C.) |
| **4 — Port checker into Anubis** | 🟡 **IN PROGRESS (all THREE semantic engines self-hosted — effect DIFFERENTIAL-COMPLETE, type at slice 2, taint at slice 1; interprocedural depth remains)** | Parse, basic check, and codegen were already dogfooded through Anubis (`SELFHOST_DOGFOOD_GATE` 3/3). Now ALL THREE enforcing lanes are Anubis-authored + differential-gated 0-disagreement: the **EFFECT engine** (transitive rows + capability set + closure-hidden egress + method-calls — PARSE+DIFFERENTIAL-COMPLETE over the SH subset; `run_effect_selfhost_gate.sh`, `run_capset_selfhost_gate.sh`), the **TYPE engine slice 1** (let-annotation assignability → byte-identical `ANUBIS_TYPE_MISMATCH`; `run_type_selfhost_gate.sh`, #109), and the **TAINT engine slice 1** (intraprocedural source→sink → byte-identical `ANUBIS_TAINTED_SINK_WITHOUT_DECLASSIFY`; `run_taint_selfhost_gate.sh`, #110) — each SH ⊆ Rust (0 spurious) over the whole corpus. Each slice deliberately reseals the self-host fixpoint (`dc680001→…→6c46a76c→…`, logged). REMAINING = the interprocedural DEPTH of each engine (slice-2s), not a missing engine — the TYPE engine's arg/return-position checks are now self-hosted too (#112: byte-identical argument + return `ANUBIS_TYPE_MISMATCH`/`ANUBIS_RETURN_TYPE_MISMATCH`, top-level+implicit-tail return parity with `check_return_types`, 20/20 gate): the remaining depth is taint's two interprocedural fixpoints (return-taint + param→sink summaries) + closure/container granularity. Precisely named, not hidden: each engine covers its intraprocedural-direct core; the interprocedural-summary + closure + container precision, and (for type) HM/generics/traits (STRUCTURALLY UNREACHABLE in the SH grammar — [NEEDS-HUMAN] parser growth), are deferred. **The taint slice-2 (interproc self-host) is deliberately GATED on a prerequisite:** a 2026-07-21 adversarial hunt (wf_8f5be96f + wf_fe819fdb) confirmed a PRE-EXISTING escaping-closure false-accept CLASS in the RUST interproc taint summary (~17 forms — a closure capturing a secret/tainted param bound via destructure / container-index / if-init / struct-field / reassignment / **a helper that RETURNS an arg-capturing closure** / HOF dispatch launders it to a sink with a green check; the `lambdas` app-env registers only `Stmt::Let{init:Lambda}` + descends only `Call{callee:name}`). Since the SH taint engine differentials SH ⊆ Rust, porting the interproc summary to SH must mirror a SOUND Rust target — so the class is being closed FIRST as a unified closure-value-flow fix (a partial per-form patch was written, shown to close only 6/17 + introduce an over-rejection, and REVERTED per the "one construct, all walkers" / "prefer decline over clever modeling" discipline); tracked separately, then the SH interproc port follows. |
| **5 — Mechanized semantics + soundness** | 🟢 **AT DoD** | **DoD MET (2026-07-20):** all THREE named theorems are machine-checked in Lean 4 core (no Mathlib, formal gate PASS): **(a)** Safe-mode non-interference (`NonInterference.lean::noninterference` + `DeclassifyWellFormed.lean` for the malformed-declassify refinement), **(b)** inferred-⊆-declared effect soundness (`EffectSoundness.lean::gate_sound` — a program that passes the `⊆` gate performs only declared effects in every world, with an executable `gate_sound_decide`), **(c)** SMT-encoding soundness (`Encoding.lean` — the encoder's `bvadd/bvsub/bvmul/bv{and,or,xor}` are definitionally the runtime ops). 14 modules total (the three DoD theorems + 11 more: capability linear-use, QF_ABV, loop-invariant Hoare, contract-composition, path-condition, string/compare/signed/mask encodings, bit-blaster). Honest boundary (unchanged): theorems are stated over the erased dynamic semantics. small-step semantics in Lean/Rocq; prove (a) Safe-mode non-interference, (b) inferred-⊆-declared effect soundness, (c) SMT-encoding soundness. **Slice 1 DONE (`f7e7e47`) — (c) integer SMT-encoding soundness bootstrapped in Lean 4 core (no Mathlib):** `formal/Anubis/Encoding.lean` machine-checks that the encoder's `bvadd/bvsub/bvmul/bvand/bvor/bvxor` ARE the runtime's wrapping-i64 arithmetic (a Rust `i64` and an SMT `(_ BitVec 64)` are the same object), and that the **A1 u32-param mask** (`x & 0xFFFFFFFF`, `4ce6537`) provably lands in `[0, 2^32)` (`u32_mask_lt`) AND is signed-non-negative (`u32_mask_nonneg`) — mechanizing the exact soundness of the injected `bvsge x 0`/`bvsle x (2^32-1)`. `scripts/run_formal_gate.sh` runs `lake build` + fails closed on any `sorry`/`admit`/`axiom`. This is the third-party-trust elimination: what a consumer would take on faith is now a Lean theorem. **Slice 2 DONE (`a0367fa`):** `enc_slt`/`enc_sle` — the encoder's `bvslt`/`bvsle` ARE the runtime's SIGNED i64 `<`/`<=` (the `toInt` order); `u32_mask_eq_mod` — the A1 mask `n & 0xFFFFFFFF` EQUALS `n mod 2^32` (so the boundary genuinely reduces mod 2^32). **Slice 3 — EXPANSION (`6588527`): 6 modules, 72 machine-checked theorems** (parallel multi-agent draft → independent re-verify → integrated `lake build`; all `#print axioms`-clean, only propext/Classical.choice/Quot.sound). `IntSigned` (bvsdiv/bvsrem = i64 wrapping_div/rem — truncating, sign-of-dividend, the `MIN/-1` wrap; bvashr arithmetic; shift-amount = `r mod 64`); `UnsignedMask` (uN mask ∈ [0,2^w), = `x mod 2^w`, signed-nonneg; truncating-cast ≠ identity — why the checker fails closed); `CompareUnary` (bvsgt/bvsge = signed >/≥, `==` exact, bvneg wrapping, `~x = -x-1`); **`NonInterference` — Phase-5 goal (a)**: mechanized Safe-mode NON-INTERFERENCE over a taint calculus (low-equivalent stores agree on every `Lo` result — a secret never flows to a public sink — with a `hi_can_leak_into_hi` non-vacuity witness); **`EffectSoundness` — Phase-5 goal (b)**: inferred effects OVER-APPROXIMATE actual, so passing `inferred ⊆ declared` performs only declared effects (`gate_sound_run`) + decidable ⊆; `StringEncoding` (QF_S `==` exact, `len` faithful + ≥ 0, `len(a++b)=len a+len b`). Gate `scripts/run_formal_gate.sh` hardened (comment-stripped no-stopgap scan + reject `native_decide`). **Goals (a)/(b) DEEPENED:** `NonInterference` now models **declassify** (`c5227a6`) — non-interference holds absolutely over the DECLASSIFY-FREE fragment, and `declassify_downgrades` proves a `declassify`d secret genuinely reaches a public value, pinning declassify as the SOLE sanctioned breach; `EffectSoundness` now models a **`call`** (`19121df`) with `infer_transitive` + `gate_sound_transitive` — a caller inherits all a callee's effects, so a green build cannot hide an undeclared effect behind a call (the transitive discipline of `da28de4`). **Capability pillar DONE — the Phase-2 security trilogy (taint · effects · capabilities) is now fully mechanized:** `formal/Anubis/Capability.lean` models the linear use-once capability tokens (`9824807`) as a `run` that threads the consumed-capability set and aborts on any reuse; `linear_never_double_spends` proves a `Linear` (Nodup-uses) program NEVER double-spends at runtime, `run_isSome_iff` proves operational success is EXACTLY the absence of a double-spend (linearity necessary AND sufficient — not a one-way over-approximation), and `run_none_of_not_linear` proves EVERY non-linear program genuinely aborts. So the checker's linearity gate is the precise runtime-success criterion. **QF_ABV array-lane correspondence DONE:** `formal/Anubis/ArrayEncoding.lean` machine-checks the theory-of-arrays encoding (`(select arr (_ bv i 64))` cell facts + `store` writes, `compiler/src/middle/mod.rs`) against the runtime bounded `Vec`: the McCarthy read-over-write axioms z3 uses (`sel_sto_eq`/`sel_sto_ne`), that the total-array model's `select` at an in-bounds index IS the runtime read (`model_sel_inbounds`), that a literal array's asserted cell facts are exactly the runtime elements (`sel_model_literal`, encoder honesty), and that an IN-BOUNDS write `a[k]=v` modeled equals the SMT `store` on the whole domain (`model_set_eq_store` — the total-vs-bounded gap: an out-of-bounds store writes while a runtime `set` no-ops, so the correspondence is scoped to the in-bounds write the encoder actually emits). **LOOP-INVARIANT (Hoare while-rule) soundness DONE:** `formal/Anubis/LoopInvariant.lean` machine-checks the partial-correctness while rule the checker relies on to ASSUME an invariant after a loop — a big-step `WhileBig` eval relation (non-termination = no derivation, exactly the checker's partial-correctness claim); `while_invariant` (a preserved invariant holds at loop EXIT and the guard is FALSE there — licensing the post-loop assumption), `while_establishes` (the discharge shape: `I` preserved ∧ `I∧¬guard ⇒ Q` ⟹ the loop establishes the post-loop obligation `Q`), and `for_range_bounds` (the desugared `for i in start..n` with the auto `i≥start` invariant of #27/#28 exits with `start≤i` ∧ `i≥n` — the counter fact assumed after a `for`). `while_invariant`/`while_establishes` need ZERO axioms. **CONTRACT-COMPOSITION / substitution soundness DONE** (the first down-payment on connecting to the real IR): `formal/Anubis/ContractComposition.lean` machine-checks the soundness of `substitute_vars` (the call-site discharge machinery, a recurring precondition-bypass bug source) over the modelable arithmetic core — `subst_sound` (a substituted contract evaluated in the caller env equals the callee contract on the ARG VALUES, so no param name leaks back to caller scope), `discharge_sound` (the caller-scope obligation `subst σ Q` holds iff `Q` holds of the arg values — the discharge neither strengthens nor bypasses the contract), and `dropping_a_var_is_unsound` (TIGHTNESS, zero axioms — a subst that clones a `Var` unsubstituted evaluates differently, the exact `g(150)`-checked-as-`abs(5)` bypass class). **99 machine-checked theorems across 11 modules**, all `#print axioms`-clean (propext / Classical.choice / Quot.sound; the array, capability, loop, and contract-composition modules use only propext or no axioms). NEXT: the QF_FP float-lane correspondence (harder in core Lean — Float has weak core support), and lifting the `ContractComposition` mini-Exp to the real `middle::Expr`/`substitute_vars`. |
| **6 — Proof-carrying packages** | 🟢 **AT DoD** | Signed evidence bundles + consumer verify + fail-closed tamper are LIVE and gated (`scripts/run_package_gate.sh` 9/9: sign_and_trust · package_lock · package_verify · tamper_hash · check_with_deps · run_with_deps · untrusted_signer). A sealed bundle carries a source **Merkle root** + per-`pub fn` summaries (`compiler/src/package/summary.rs`), which are **re-derived and byte-compared on verify** — a summary that lies about a package, or a swapped source, fails closed (`ANUBIS_DEP_PROOF_UNVERIFIED`). Effect/taint inheritance is enforced by mounting + re-typechecking the dependency sources. **Contract sealing DONE (schema `anubis.summaries.v2`):** each `pub fn`'s `requires`/`ensures` are now rendered to canonical source form and sealed into the summary (declaration order preserved, source-merkle covers the text), so a dependency's advertised pre/postconditions are tamper-evident (`seals_requires_and_ensures_contracts` proves a swapped `requires` fails the re-derive). **DoD MET (verified + regression-locked 2026-07-20):** the Phase-6 definition of done — a dependency's summaries hash-pinned AND enforced at the consumer's call sites, build fails closed on tamper — holds end-to-end. Building a consumer against a real `[dependencies]` package (lock + evidence-verified) DISCHARGES an imported fn's `requires` at the consumer's own call site (`ANUBIS_ASSERTION_UNPROVEN: requires@math__safe_div` with a counterexample on a violating arg), ASSUMES its `ensures`, and INHERITS its effects (`ANUBIS_EFFECT_FORBIDDEN_IN_MODE`) and taint (`ANUBIS_TAINTED_SINK…`) — all via mount + live re-typecheck of the hash-pinned source, which additionally RE-PROVES the dep body so an advertised-but-unmet contract is caught. Locked by the seal-gated unit test `phase6_cross_module_summary_enforced_at_call_sites` (all four dimensions, both verdict directions). RESIDUAL (named, optional — NOT a soundness gap): summary-ONLY discharge (enforce from the sealed summary WITHOUT mounting the source) — a leaner/efficiency variant; the mounted re-typecheck model is a strict superset guarantee. **Evidence-snapshot integrity fixed** (`7253626`): `anubis build --evidence` was concatenating the native artifact into the `source.anubis` snapshot (a native binary emitted under `src_root` entered the collected file tree, and the "no `source.anubis` leaf ⇒ concat all leaves" fallback appended its bytes) — a ~500 B source became hundreds of KB, so `anubis report` emitted thousands of parse errors and the verdict flipped to FAIL though `check` passed. Fixed in two layers: the build handler filters the collected tree to `.anb`/`.anubis` leaves, and the evidence lib's concat fallback includes only valid-UTF-8/NUL-free leaves. The merkle `source_hash` is still over all leaves, so bundle integrity is unchanged; the trust-anchor bundle no longer ships a corrupt artifact with a false FAIL. |
| **7 — Ma'at endgame (minimize TCB)** | 🟢 **ADVANCED (residual scoped)** | **Trust-minimization substantially achieved for the solver TCB:** the from-scratch native SMT solver (`solver/`, ZERO external dep) is AUTHORITATIVE-capable for the integer lane under `ANUBIS_NATIVE_AUTHORITATIVE=1` — `run_native_authoritative_gate.sh` PASS (verdict-equivalent on **441 files, 0 disagreements, native ALONE proves+rejects with z3 hidden**), SAT models re-checked by the independent `bv::eval` replay, and the ripple-carry ADDER machine-checked in Lean (`BitBlast.lean`, covering bvadd/bvsub via two's complement). So z3 is DROPPABLE for the int lane today. **Lean-proven bit-blaster ops (UNSAT = a machine-checked proof, not merely differentially validated):** the ripple-carry ADDER (bvadd/bvsub/bvneg), the COMPARATORS (`ult_correct`/`slt_correct` — unsigned + signed), the COMPLEMENT, and now the CONSTANT LEFT SHIFT (`shlConst_correct`: `⟦a<<k⟧ = (⟦a⟧·2^k) mod 2^w`, 2026-07-20) — the CONSTANT MULTIPLY (`mulConst_correct`: `⟦x·c⟧ = (⟦x⟧·c) mod 2^w`, via a `foldl` of `shlConst` partial products + `addBits_correct` + `mod_two_pow_succ`), and the VARIABLE (BARREL) LEFT SHIFT (`barrelShl_correct`: `⟦a<<b⟧ = (⟦a⟧·2^⟦b⟧) mod 2^w`, 2026-07-20 — the log-depth conditional-`shlConst` layers, invariant `⟦cur⟧=(⟦a⟧·2^⟦b.take m⟧)%2^w`; the per-bit mux with a constant selector reduces each layer to `if bit then shift else keep`). and the LOGICAL RIGHT shifts (`shrConstL_correct`/`barrelLshr_correct`: `⟦a>>b⟧ = ⟦a⟧ / 2^⟦b⟧`, via the `bitsToNat_drop`=`⟦a⟧/2^k` dual of `bitsToNat_take` + the `Nat.div_div_eq_div_mul` exponent law). So the ADDER, COMPARISON, COMPLEMENT, and the WHOLE logical-shift (left+right) + MULTIPLY family are machine-checked. CONCAT (`bitsToNat_append_list`: `⟦l₁++l₂⟧ = ⟦l₁⟧ + 2^|l₁|·⟦l₂⟧`), EXTRACT (`bitsToNat_extract`: `(⟦a⟧/2^lo) mod 2^len` via take∘drop), and ZERO-EXTEND (`bitsToNat_append_replicate_false`) are also value-checked. **Residual (precisely scoped, with the path CLARIFIED):** a SOUND default-flip does NOT require proving every op — only that native be AUTHORITATIVE exclusively for the ops it can PROVE, deferring the rest to z3 (as division already is). So the ~15 proven bit-blaster ops (all arithmetic + concat/extract/zero-extend) become the native-authoritative fragment, and `ashr` / `sign_extend` (the signed ops, needing `toIntW` + Int floor-division) simply JOIN division as z3-deferred — they are not flip blockers. The one genuine remaining prerequisite is (c) the CDCL SAT engine's `Unsat`-soundness: either mechanize it (a proof-emitting / resolution-certificate `Unsat` checker — a large effort) OR keep z3 as a mandatory `Unsat` cross-check even in default mode (cheap, sound, and it still drops z3 as the AUTHORITY for every SAT / model / bit-blast — the TCB shrinks to z3-as-Unsat-oracle). The latter is the pragmatic flip. Then the flip code + gate + seal. Also named future work: the audited minimal runtime seed + a second independently-authored parser/backend (author-diversity). shrink Rust runtime seed to an audited kernel; second independently-authored parser + independent second backend (closes author-diversity residual). **NATIVE SMT SOLVER — dropping the z3 third party for the integer lane, STARTED:** `solver/` is a from-scratch QF_BV decision procedure with ZERO external solver dependency (std only) — an SMT-LIB2 parser (`parse.rs`, any non-BV sort/op → decline), a Tseitin bit-blaster (`blast.rs`: const/var, and/or/xor/not, neg, ripple-carry add/sub, constant-multiplier mul (shift-and-add), all 8 signed+unsigned comparisons, eq, extract/concat/{zero,sign}_extend, ite, and shifts by any amount — constant (direct wiring) or variable (a log-depth barrel shifter of muxes) — semantics matching `run.rs`/`Encoding.lean`; only var×var mul and div/rem still deferred), and a bounded SAT engine (`sat.rs`, over-budget → defer). `native_check_sat(smt)` returns `Some(true/false)` only when fully parsed+blasted+decided, else `None` (defer to z3) — SOUND by construction. **Differential-validated vs z3 4.15.4: 2000 small + 600 WIDE (16/24/32-bit) + 7 hand-crafted 64-bit formulas, 100% agreement, ZERO disagreements, ZERO deferred** (`solver/tests/differential.rs`) — the wide 32-bit regime (what real `u32` obligations compile to) is fully DECIDED by the **CDCL SAT engine** (watched literals, 1-UIP clause learning, VSIDS, Luby restarts; `Unsat` only via a level-0 root refutation, sound by construction; public API byte-identical to the DPLL it replaced). **Shadowed on the real corpus** (`ANUBIS_NATIVE_SHADOW=1`, now covering the PRIMARY proof stream): z3 stays authoritative, native compared, fail-closed on any disagreement — **243/293 real obligations decided by native, 0 disagreements** (the 50 deferrals are the non-BV float/string/array obligations). The **adder AND both comparators are machine-checked in Lean** (`formal/Anubis/BitBlast.lean`: `rippleCarry_spec` = true integer addition, chaining to `Encoding.lean`'s bvadd=wrapping_add=runtime; `ult_correct` = the unsigned comparator IS `<`; `slt_correct` = the signed comparator IS two's-complement `<`; all 3-core-axioms-clean). **Z3-AUTHORITATIVE FLIP LANDED (opt-in, `ANUBIS_NATIVE_AUTHORITATIVE=1`):** native now DECIDES the integer lane and z3 is consulted only as a fail-closed cross-check while present — with z3 ABSENT, native alone carries the lane (the actual TCB drop). A native `sat` counterexample is re-verified by an INDEPENDENT concrete evaluator (`bv::Formula::eval`, the native replay) before it is returned. Proven safe by `scripts/run_native_authoritative_gate.sh`: **verdict-equivalent to z3 across the whole corpus (326 files, 0 mismatches, 0 disagreements)**, and native alone (z3 hidden) proves the passing int fixture + rejects the violating one while the DEFAULT mode without z3 fails (z3 was load-bearing → the flip is what drops it). Fixpoint dc680001 UNMOVED (opt-in, off-by-default, Rust-only, not projected). **NATIVE FLOAT (QF_FP) COMPARISON LANE DONE:** a `Float64` IS a `BitVec 64`, so `fp.lt/leq/gt/geq/eq` + `isNaN/isInfinite/isZero` + exact `fp.neg/abs` are LOWERED to the existing (machine-checked) BV pipeline via the monotonic-key transform (`solver/src/fp.rs`: `sign ? ~x : x^0x8000…0`, unsigned key order = IEEE order, with NaN-unordered and ±0-equal special-casing) — no rounding, no new gates. Float ARITHMETIC (`fp.add/mul/div/rem`, which rounds) is sound-declined → z3. **Differential vs z3's real FP theory: 2000 random formulas over NaN/±0/±∞/subnormals, 0 disagreements.** **NATIVE STRING (QF_S) EQUALITY LANE DONE:** string-equality is equality logic, so each DISTINCT string literal interns to a distinct 32-bit constant and each `String` var becomes a free bit-vector with `=` as BV equality (`solver/src/parse.rs`; `unquote_and_decode` canonicalizes `\u{..}`/`""` so equal strings get equal ids; a `MAX_STR_TERMS` guard keeps the 2^32 domain ≫ the term count so vars can always be forced distinct — no spurious UNSAT). Non-equality string ops (`str.len/++/contains/…`) decline → z3. **String differential vs z3: 2000 random equality formulas over vars + a literal pool (incl escaped backslash, empty string, doubled-quote), 0 disagreements.** With both non-BV lanes the corpus shadow reaches **280/293 decided (95.6%), 0 disagreements** — only the ~13 float-ARITHMETIC obligations (which round) still defer; the authoritative equivalence gate stays **326/0/0**, and per-lane soundness hunts confirm native stays EQUAL-OR-MORE-CONSERVATIVE than z3. **Verification status, stated honestly (external-audit item):** the bit-BLASTER is machine-checked (adder + all comparators in Lean), but the **CDCL SAT engine itself is not yet mechanized** — its `Unsat`-only-via-level-0-refutation soundness is an argued invariant, not a proof. This is contained: while native is opt-in, z3 stays the authority and cross-checks every verdict (fail-closed on disagreement), so there is no live exposure. Two PRECONDITIONS gate flipping the DEFAULT to native-authoritative: (a) either mechanize the CDCL engine (or a proof-emitting `Unsat` checker) OR keep z3 as a mandatory cross-check even in default mode; (b) native float-ARITHMETIC + non-equality string ops (the last ~13 deferrals, which need rounding / unbounded strings). **PROOF-BACKED FRAGMENT GATE LANDED (2026-07-20):** the clarified flip path is now ENFORCED, not aspirational — `solver/src/fragment.rs::is_proven_authoritative` is a TOTAL, no-wildcard walker (a new `Term`/`Pred` variant is a COMPILE error, not a silent admission) that recurses into EVERY child (both shift operands, the non-const multiplier, `And`/`Or` vector elements, extract/concat inner terms, both operands of every comparator), and the compiler's three authoritative sites (vacuity, primary, `z3_check_sat_raw`) call `native_check_sat{,_model}_authoritative`, so a native verdict is trusted as authoritative ONLY when every op in the obligation carries a machine-checked blast. Newly proven to make the fragment non-degenerate: `ule_correct`/`sle_correct` (ALL EIGHT comparators are now theorems — `>`/`≥` are operand swaps of the strict forms, `≤` = negated-swap; blast.rs::cmp is exactly that) and `eqBits_correct` via `bitsToNat_inj` (EQUALITY machine-checked — the string-equality lane rides on it). The SHADOW/differential path stays deliberately UNGATED: the full blaster keeps its z3 cross-check + differential coverage while only its AUTHORITY is fragment-bounded (the gate can only turn a `Some` into `None` — never change a verdict). `run_native_authoritative_gate.sh` extended + PASS: verdict-equivalent on **442 files (0 mismatches, 0 disagreements)**; native alone (z3 hidden) proves the u32-contract fixture (∀-input `ensures` over `{bvadd, bvsle, bvsge}` — all proven blasts) + rejects the violating one; **NEW fail-closed soundness demo: a TRUE property built on `bvashr` (unproven) PASSES with z3 present but FAILS CLOSED z3-free** — the gate declines rather than certify on an unproven blast, demonstrating native's z3-free authority is bounded to the machine-checked fragment; and a DRIFT check ties the Rust allow-list to the live Lean theorems both ways (an admitted op without its `*_correct` in `BitBlast.lean` fails the gate; a deferred op name appearing in `PROVEN_OP_TAGS` fails the gate). Post-change adversarial hunt (3 lenses: walker-completeness, wiring, encoder-carried false-accept): ALL HOLD, zero findings. **BITWISE WIDENING LANDED (same day):** `andBits`/`orBits`/`xorBits_correct` machine-checked via the `bitsToNat_testBit` bridge (`(⟦l⟧).testBit i = l.getD i false` — the parity/halving induction) + a generic pointwise `zipWith` value theorem + core `Nat.testBit_and/or/xor` and `Nat.eq_of_testBit_eq`; `And`/`Or`/`Xor` admitted to the allow-list, and the CONCRETE-CALL payoff verified: `int_contract_proves.anb` restored its literal-arg call (`clamp_add(700,300)`) whose u32 coercion checks lower to `bvand` — and the whole file, ∀-input contract AND concrete-call checks, now proves Z3-FREE on proven blasts only (gate re-PASS 442/0/0; the drift check itself CAUGHT the deny-list going stale on `Xor` before the update — the tripwire works). `bvsub`/`bvneg` LANDED same day (`subBits_correct` = wrapping subtraction via `rippleCarry_spec` + the complement identity `bitsToNat_not_add` — the SAME circuit the proven `ult` rests on; `negBits_correct` = wrapping two's complement from the all-zero word), admitted + drift-synced. `ite` LANDED same day (`iteBits_correct` — a common-selector per-bit mux IS the list-level if-then-else; the mux gate's four clauses are the TIER-0 Tseitin family), un-deferring the `abs`/`min`/`max` contracts: a `max(x,0)` floor contract now proves z3-free. **LEFT-SHIFT `<<` LANDED via an ENCODER IDENTITY (no new proof):** the shift-amount mask `r mod 64` IS the low 6 bits of `r`, so the encoder now emits it as `(zero_extend 58 (extract 5 0 r))` instead of `(bvurem r 64)` — z3-PROVEN identical (both `(not (= …))` obligations are `unsat`) but built entirely from ALREADY-PROVEN ops (`bitsToNat_extract` + `bitsToNat_append_replicate_false` + the proven `bvshl`). So `<<` — const-amount AND variable-amount (via the proven barrel shifter), INCLUDING wrapping amounts (`1<<67 == 8`) — now decides z3-free; `run`/`check`/z3 all agree on the edge cases (arithmetic-negative `>>`, wrapping `<<`), 442-file equivalence 0/0 held. **`sign_extend` DROPPED from the residual list — the encoder NEVER emits it (a widening cast just re-widths the expected width); it is not reachable, so proving it is zero-value.** **RIGHT-SHIFT `>>` ALSO LANDED via the same ENCODER-IDENTITY pattern (no `bvashr` proof needed):** arithmetic shift = logical shift, then — iff the value is negative — force the vacated top bits to 1. The encoder spells `>>` as `ite(v<0, (v>>ᵤamt) | ~(allones>>ᵤamt), v>>ᵤamt)` — z3-PROVEN identical to `bvashr` for all inputs (the `~(allones>>ᵤamt)` sign-mask is 0 when amt=0, so `x>>0==x` holds exactly), and built ENTIRELY from proven blasts (`bvlshr`, `bvor`, `bvnot`, `bvslt`, `ite`). So `>>` — including the arithmetic-negative case (`-8>>1 == -4`, not 4) and wrapping amounts — now decides z3-free; `run`/`check`/z3 agree at every edge. **TERMINAL DEFERRAL SET is now MINIMAL and exact:** the native-authoritative lane declines ONLY (1) `bvsdiv`/`bvsrem` — `/` and `%`, division, deferred BY DESIGN (a divisor-nonzero check gates it; sound), and (2) var×var `bvmul`, which the blaster never decides. **EVERY OTHER operation a real Anubis integer contract emits — arithmetic (+,-,*const), all 8 comparators, equality, bitwise (&,|,^,~), unary negate, abs/min/max (ite), and BOTH shifts (<<,>>) — is now in the machine-checked native-authoritative fragment.** The DEFAULT flip waits ONLY on the CDCL `Unsat` certificate (LRAT/DRAT emit + verified replay); `ANUBIS_NATIVE_AUTHORITATIVE` stays opt-in until then. `sign_extend` is unreachable (never emitted). This is the endgame shape the flip path called for: everything native decides as AUTHORITY is machine-checked; the only deferrals are division (by design) and the SAT-search certificate. The DEFAULT flip still waits on precondition (a): the CDCL `Unsat` core is not mechanized and emits no LRAT/DRAT certificate, so `ANUBIS_NATIVE_AUTHORITATIVE` stays opt-in — but within the flag, the TCB story is now precise: z3-free UNSAT = proven blast + CDCL search; z3-present = additionally cross-checked fail-closed. |
| **8 — Developer experience** | 🟢 **AT DoD** | **DoD MET (2026-07-20): `scripts/run_dx_gate.sh` PASS (15/15)** — every DX piece has an integration test exercising the REAL path, not a stub: doc-gen contracts, repl (eval / fail-closed / --exact), hello check+run, LSP JSON-RPC stdio roundtrip (diagnostics + contract hover, `scripts/test_lsp_roundtrip.py`), editor validity, tree-sitter assets + CLI parse test, tutorial+spec presence, phase5/phase6 regression, `fmt --check` idempotence, and the `anubis test` runner. Artifacts committed (`ed7d201`: tree-sitter grammar + VS Code ext source, `TUTORIAL.md`, LSP roundtrip test, gate fixtures; node_modules gitignored). LSP (hover shows contracts+effects+taint), formatter, test runner, doc-gen, REPL, tree-sitter, registry, spec, tutorial. Much of this is BUILT (LSP + JSON-RPC roundtrip, `anubis fmt`, tree-sitter grammar + VS Code ext, `TUTORIAL.md`, `SPEC.md`, local package registry) but not all pieces are individually gate-verified. **LSP hover taint DONE (`5420b94`):** `find_fn_hover` now renders an explicit **Taint & confidentiality** section (each `tainted<T>` param / `secret<T>` param+return), closing the "hover shows … taint" claim that was previously buried in the type string — gated by the `hover_shows_taint_and_secret` unit test (VM battery cargo-test) + the LSP JSON-RPC roundtrip. **`anubis fmt --check` + `anubis test` gate-wiring DONE** (verified 2026-07-19): `scripts/run_dx_gate.sh` already exercises both end-to-end — `fmt_check_idempotent` (drift is fail-closed, idempotent after `--write`) and `test_runner` (`anubis test` honors `// EXPECT: PASS|FAIL` over a directory) — and the whole DX gate is green **15/15** (unit_dx, doc contracts, repl ×3, hello check/run, lsp_roundtrip, editors_valid, tree_sitter ×2, docs_tutorial_spec, phase5/6 regress, fmt, test). NEXT: add `run_dx_gate.sh` to the VM seal battery (`scripts/vm/run-slice.sh`) so DX is sealed on every deliverable — precondition: the golden image must carry node + `editors/vscode-anubis/node_modules` + the tree-sitter CLI (or the committed `parser.c`), else `editors_valid`/`tree_sitter_cli_test` would fail closed in the VM. |
| **9 — Independent external reproduction** | 🔵 **[NEEDS-HUMAN] (bundle prepared)** | The self-contained proof bundle is prepared — `REPRODUCIBILITY.md`, `docs/CLAIMS.md`, the pinned-toolchain + hermetic-Docker reproducibility gate (`run_selfhost_repro_gate.sh` 6/6), and the diverse-double-compile DDC gate (34/34). What remains is intrinsically human: multiple INDEPENDENT strangers reproducing the sealed hashes + confirming the negative controls on their own hardware. I build the reproducible artifact; the operator looses it (no agent-created public repos, no agent-pressed sends). |
| **10 — Production hardening + 1.0 freeze** | 🔵 **[NEEDS-HUMAN] (partial; operator freeze)** | Real Anubis systems already exist across ≥2 domains WITH signed evidence bundles — a confidential proof-carrying value ledger (MA'AT: notes / nullifiers / Merkle / SMT-conservation / ZK-solvency, 13/13 green) and verified private settlement + a security-PoC corpus (`examples/showcase/`, `examples/security/`). What remains is human/operator: the semver + backwards-compatibility policy and the FROZEN 1.0 spec are a stability commitment for the operator to make, and hardening is proven by sustained real-world use, not asserted. |

### ➡ NEXT ACTION

The lethal trifecta is now a **Safe-mode compile error** (`ec69ab6` shadow-first → `ad86af2` enforcing)
— the arc's Phase-2 differentiator. A function holding all three legs (private read + a distinct
untrusted-input channel + a net/shell egress) with no well-formed declassify rejects by DEFAULT. With
the four interprocedural legs already enforced (return-taint, return-secret, param→sink, param→egress),
**Phase 2's distinctive claim — "the language where the structural separation prompt-injection defense
prescribes is enforced by the type checker, not by hope" — is now realized in the default lane.** And
the effect pass now walks every value-shape wrapper (`1dd3bd4`), so a privileged/sink call cannot be
laundered through an aggregate/index/cast/operand/application-arg either, and the `secret<T>` qualifier
(`e6fd51a`) lets a caller-supplied/`getenv` secret be labelled without a `secret_source(..)` call. The
remaining Phase-2 work is precision/completeness. Candidate next moves (operator steers; each a clean,
VM-validated slice):

1. **Classify `http_get`/`http_post` as a net.send effect** — ✅ **DONE (`e900d41` 2026-07-20).**
   They were in `is_egress_sink` (value-flow) but not in `builtin_effect_of`'s net.send mapping, so the
   trifecta leg-3 + the ⊆-declared effect check under-fired an http constant-beacon. Fixed across four
   coordinated sites: `effects::builtin_effect_of` net.send arm + its parity table (20→22), the enforcing
   Safe-mode net.send arm in `analyze_stmts`, and `run::is_non_run_builtin` (so http is a checker-recognized
   egress with no runtime lowering — like `shell`; `run` refuses it as `ANUBIS_UNSUPPORTED_NATIVE_LOWERING`).
   Now an http beacon without `uses(net.send)` rejects (`ANUBIS_EFFECT_FORBIDDEN_IN_MODE`), a secret→`http_post`
   flow is `ANUBIS_SECRET_EXFILTRATION`, and the 3-leg http coexistence is `ANUBIS_LETHAL_TRIFECTA`; a properly
   declared constant beacon still compiles. Three fixtures (`examples/security/http_*`), security gate 115/115,
   531 unit tests green, zero corpus verdict flips (no prior fixture called http). `sql` local-injection egress
   remains the one named leg-3 residual.
2. **Interproc `tainted<T>`/`secret<T>` param → return** — a param annotated `tainted<T>`/`secret<T>` is
   NOT propagated to a helper's return summary (`seed_one_let_secret`/`seed_one_let` are param-blind), so
   `fn fwd(x: secret<u64>){ return x; }` does not make `fwd` secret-returning. Propagate the qualifier
   through `compute_secret_fns`/`compute_tainting_fns` (do BOTH sides — same param-blindness). *(Both
   param→return AND the twin direct-walker nested-CF residuals are now closed — `719915b` + `d74c49b`. The
   accept-biasing `expr_param_flow` precision fix is now sound to build on top of the unified summary.)*
3. **Complete `param_return_taint`'s named residuals, THEN make `expr_param_flow` `param_return`-aware.**
   The param→SINK/param→EGRESS summaries over-flag a secret into a DISCARDING forwarding call
   (`send(ignore(x))` where `fn ignore(y){return 0}` rejects — a FALSE POSITIVE). The obvious fix — make
   `expr_param_flow` consult `param_return_taint` — is **still NOT sound**: `expr_param_flow` currently
   OVER-approximates (unions all arg flows), so param→egress catches every param→sink flow (fail-closed, no
   false negatives); consulting `param_return` would reduce it to `param_return`'s completeness, and
   `param_return` STILL under-approximates in its named residuals (loop-carried, value-block-local return,
   Lambda, non-`Var`) — so the fix trades a SAFE over-rejection for UNSAFE false negatives (a leak through a
   residual-path forwarder). For a fail-closed language the over-rejection is the correct direction. **Do
   this ONLY after `param_return_taint` is complete** — close those residuals first (a loop-body fixpoint +
   threading a `found` out-param into `expr_param_return_flow` for value-block-local returns). Qualifier
   params ARE already complete (`11935e5`); the non-qualifier residuals are the blocker.
4. **Remaining impl-method / destructuring flow gaps** *(security completeness; the `c151bdc`/`79705f1`
   follow-ons).* **(a) DONE** — method-RETURN secret/taint exfil (`let k = v.key(); send(h,p,k)`) closed
   by `79705f1` (`method_{secret,tainting}_fns` + the `_m` walker `CallExpr` arm). **(b) DONE** — interproc
   sink/egress ARGUMENT laundering through methods (free-fn→method + method→method) closed by `4f4433e`
   (the joint free-fn↔method `compute_sink_summaries_joint` fixpoint + `CallExpr` arms). **(c) DONE** —
   top-level destructuring `let` seeding hole closed by `d91497c` (`seed_effect_pattern` + span-patch).
   **(d) DONE** — method-RETURN chaining (`fn alias(self){ return self.key() }`) closed by `8faf9f0`
   (combined method-return fixpoints `compute_method_{secret,tainting}_fns` consulting the growing method
   set; fixture flipped to EXPECT:FAIL). **The entire impl-method / destructuring exfil cluster is now
   CLOSED (a/b/c/d).** **(e) DONE** — closure-hidden egress (#65) closed by `573f955`
   (`effects::higher_order_closure_args` recognizer incl. `times` + a lambda-body descent in effects.rs
   and analyze_expr_effect at higher-order-builtin call sites, guarded by `!all_fns` so user `fn
   apply`/`fn compose` don't over-charge; 2-binary verdict-diff 0 flips). **With #65, the ENTIRE Phase-2
   taint/secret/effect flow-analysis surface is hardened** — no known fail-open egress/laundering path
   remains across free-fn, impl-method, destructuring, or closure forms. Remaining Phase-2 work is
   precision/completeness on documented residuals (closure-by-variable, user-defined HO wrappers,
   value-block nested-CF summaries), not new leaks.
5. **Broaden (arc dependency order):** Phase 3 solver lanes (QF_FP / QF_S, parallel-able), or finish
   the remaining Phase-1 residual (generic-bound capture + trait bound checking — a fixpoint-reseal
   parser change; typed `?` is now closed at both variable and call-return positions by `3536581`).

Every slice: reproduce-before-certify where it's a soundness fix, shadow-first for a new check,
VM-validated via `scripts/vm/run-slice.sh`, fixpoint held at `dc680001`.

### Working discipline (unchanged, every slice)
Every heavy build runs in the tart macOS VM via `scripts/vm/run-slice.sh` (the host twice hit a
watchdog reset from all-core builds — see `scripts/vm/README.md`). New checks land **shadow-first**,
promoted only on real evidence (corpus shadow diff UNEXPECTED=0 — and note macOS needs coreutils for
`timeout` or that gate silently no-ops). No parser/AST/projection change without confirming the
fixpoint holds. Every guarantee is either proven or precisely scoped; residuals are named, not hidden.

---

## The arc, verbatim (operator-authored 2026-07-14)

Before the phases, the honest frame, because it determines the whole shape. Full maturity for this language does not mean catching up to Rust or Dafny on feature count — you'd lose, and it's the wrong race. It means completing the one thing no other language holds all of at once: push-button proof, counterexamples an agent can actually read and repair, capability-and-effect enforcement that makes whole classes of attack a compile error, evidence bundles that let one party verify another's work without trusting them, and a self-reproducing compiler witnessed by diverse toolchains. My research this session confirmed the gap is real — Dafny has the automation but no capabilities, no evidence bundles, no self-host witness; Austral has the capabilities but no SMT and no proofs; Koka has the effects but no verification; Lean has the depth but twenty-seven percent agent success and no systems story; CakeML has the verified minimal core but none of the rest. A class of its own means assembling that entire stack to production quality, and proving or precisely scoping every guarantee along the way. The maturity is the assembly, and the honesty about each boundary is what makes the assembly believable. Here is the arc, dependency-ordered, from the foundation you've already banked to completion.

Phase zero is the trust spine, and it is done — the byte-identical source and binary fixpoint, the ablation gate that proves the language's own features are load-bearing, the diverse double-compiling lane now closed at the source-derivation level by your C-native parser, and the hermetic reproducibility lane. This is the foundation everything downstream inherits, and it is the rarest thing you own. Its residual is now author-diversity, which Phase seven addresses. You built this first correctly, because a package manager shipping evidence bundles means far more when the compiler verifying them is itself witnessed by two toolchains.

Phase one is making the type system real, and you are inside it now. The bidirectional inference core landed, floats closed the unsound aliasing bug, and what remains is captured generics, real traits with coherence and bound checking, and typed question-mark propagation — each landing shadow-clean through the harness with the fixpoint holding on c640badd. This is first because everything after it needs a real type system beneath it: you cannot build a capability system, or port a checker, or prove soundness, over a stringly-typed substrate. Definition of done is the four workstreams enforced at zero shadow regressions, the frozen parity oracle untouched, the fixpoint unmoved. The honest boundary is that the checker is still Rust after this — porting it into Anubis is Phase four.

Phase two is the differentiator, the one that earns the agentic-engineering claim outright: the capability-and-effect system that makes the lethal trifecta a compile error. You fuse three proven ideas onto the real type system from Phase one. From Koka you take row-polymorphic effect types, so a function's side effects are visible in its signature and an expression typed without an effect provably never performs it — maturing your existing uses clause into real inference. From Austral you take capabilities as unforgeable linear tokens, so the only way to touch the filesystem, the network, a signing key, or a fund transfer is to hold a token that cannot be duplicated or conjured, which is exactly how you constrain a dependency or a sub-agent from doing what it was never granted. And from your own taint layer you make I/O reads into taint sources and I/O writes into sinks, so a program that reads untrusted content and reaches an exfiltration path without an explicit declassification simply will not compile in Safe mode. This phase also closes the one documented taint fail-open — the reassignment-insensitivity — with proper control-flow-merge dataflow, and adds higher-order summaries and field granularity, retiring three of the residuals the dissertation lists at once. This is the phase that matters most for your stated audience, because the entire security industry converged in 2026 on the conclusion that prompt injection is architectural and unpatchable, with OWASP mapping it to six of ten agentic risks — and Anubis can be the first language where the structural separation everyone prescribes is enforced by the type checker rather than by hope. Definition of done is a fixture suite where the trifecta is a type error with accept-bias guards proving it doesn't over-reject dynamic code, and the effect discipline's inferred-subset-of-declared property stated soundly enough to be proven in Phase five.

Phase three broadens the verified surface soundly, and it can run partly parallel to Phase two since it touches the solver rather than the effect system. You add the float and string solver lanes that are currently planned behind flags — QF_FP and QF_S — and mature the bounded-array lane, each under the same fail-closed discipline where anything outside the model is refused with a precise diagnostic rather than falsely discharged, and each guarded by extending the differential-testing harness beyond its current i64-only generation. This is a coverage gain inside an already-sound framework; the obstacle, as the dissertation names it, is widening what can be modeled without ever claiming a proof you don't have. Definition of done is the harness exercising floats and strings with zero false-accepts and every unmodeled construct still deferring.

Phase four completes the self-hosting story by porting the checker into Anubis. Right now only parse, basic check, and codegen are dogfooded; the type checker, the effect checker, and the taint engine are Rust. Porting them — the real inference from Phase one, the capability-and-effect discipline from Phase two — makes the checker itself Anubis, so the ablation gate extends to prove the type-checking constructs load-bearing too. You do this after the checker's shape has settled through Phases one through three, so you port it once rather than three times. The fixpoint may reseal to a new hash, which is fine as long as the gate still enforces it. The honest boundary is the one the dissertation already draws: this deepens self-hosting toward the irreducible runtime seed but does not eliminate it, because something must still execute the first interpreter.

Phase five takes the language into genuinely rare territory: mechanized formal semantics and soundness proofs. You give Anubis a small-step operational semantics in Lean or Rocq and prove three theorems — that the Safe-mode taint discipline enforces non-interference, meaning no tainted value reaches a sink without declassification, as a theorem rather than a set of enforced diagnostics; that the effect discipline's inferred-subset-of-declared property is sound with respect to that semantics; and that the SMT encoding is sound, so a discharged contract implies the runtime property. This is what puts the distinctive guarantees on a proof footing instead of an enforcement footing, and it is the company of CompCert and seL4 and CakeML, scoped narrowly to your specific disciplines. The obstacle the dissertation correctly identifies is that the erased dynamic runtime means these theorems are stated over dynamic semantics, which changes their shape from a conventional type-soundness result. You prove soundness before Phase six propagates these guarantees through a dependency closure, because you want the properties proven before you multiply them across an ecosystem.

Phase six is proof-carrying packages, the phase that multiplies the trust spine across the supply chain. The package manager produces and consumes evidence: every package ships a signed bundle carrying a source Merkle root, its effect and taint summaries, and its fixpoint, and the consumer verifies each dependency's bundle and inherits its summaries, so a dependency's declaration that it returns tainted data, or needs network access, or requires a nonzero divisor, is enforced at the consumer's own call sites, fail-closed on anything unverified or untrusted. This is the cryptographic answer to the supply-chain problem — the one that let a single viral agent accumulate five hundred vulnerabilities — and it pays off more precisely because the compiler verifying those bundles is witnessed by diverse toolchains from Phase zero. Definition of done is a multi-package project where a dependency's summaries are hash-pinned and enforced at call sites and the build fails closed on tamper.

Phase seven is the Ma'at endgame: minimize and independently verify the trusted base, and close the author-diversity residual. You shrink the hand-written Rust runtime seed toward a small, audited kernel with its own mechanized correctness argument — the CakeML north star, where the trust reduces to a tiny examined core rather than a thousand-line interpreter. Here also live the two deepest trust moves the dissertation flags as future work: a second, independently-authored parser from an independent reading of the spec, which is the actual closure of the author-diversity residual your C-parser work exposed, and a genuinely independent second backend that shares neither execution machinery nor code generator, so the diverse lanes agree at a deeper level than execution. The obstacle is the irreducible bootstrap floor — the seed cannot be zero — so the honest goal is minimize-and-verify, not eliminate.

Phase eight is full developer-experience and toolchain maturity, the adoption layer. The LSP grows from MVP to complete, and it can do the one thing no other language's tooling can — hover a function and show its contracts, its effects, and its taint status. The formatter, the test runner, a doc generator that surfaces requires and ensures as a contracts section, a REPL, a tree-sitter grammar, syntax highlighting, a package registry, a language spec, and a real tutorial. Each ships with an integration test exercising the real path, not a stub. This is the phase that decides whether anyone but you ever writes Anubis, and it is deliberately late because a rich toolchain over an unsound core would be polishing the wrong thing.

Phase nine is the one the dissertation names as most valuable and the one you cannot do alone: independent external reproduction and adversarial scrutiny. You publish the self-contained proof bundle — the fixpoint source, both manifests, the gate scripts, the pinned toolchain and container digest, the reproduction instructions — such that a stranger can reproduce the agreed hashes and confirm the negative controls on their own hardware. The entire Thompson frame is about not trusting a single party, and every hash in your dissertation was verified only by you; a trust artifact whose certificates have never been re-checked by anyone else has a self-referential tension it cannot resolve from the inside. This is less a research problem than an exposure task, and the C-parser closure makes what a stranger would reproduce — source-to-execution diversity, not just execution diversity — far more compelling than it was a week ago. Definition of done is multiple independent parties reproducing the sealed hashes and confirming the controls, documented.

Phase ten is production hardening and the 1.0 spec freeze — maturity proven by use rather than asserted. The language demonstrates itself by building real systems in its class behind proper host applications with authenticated data and human approval: a high-assurance financial engine matured from your ledger kernel, an agentic control plane where the trifecta-as-compile-error from Phase two is deployed in a real orchestrator, evidence-native security tooling where a bug-bounty finding ships with a receipt a skeptical triager can re-derive, and consensus and ledger cores where the proofs are load-bearing. Alongside it comes the stability contract: semantic versioning, a backwards-compatibility policy, and a frozen 1.0 specification for the stable surface, so the language becomes something others can build on for a decade. Definition of done is real systems shipped in at least two target domains with their evidence bundles, and a versioned spec freeze.

Two honest truths to close on. First, completion for a language is asymptotic — the incumbents you'd stand beside have decades and communities, and no roadmap reaches "one hundred percent done" as a discrete event; what these phases give you is the point at which the distinctive stack is complete, every guarantee is either proven or precisely scoped, and the residuals are named rather than hidden, which for this language is the only definition of done that would be honest. Second, if I had to name the single highest-leverage move in the whole arc, it is Phase two — the capability-and-effect system that makes the lethal trifecta a compile error — because it is the one capability that no incumbent can stack onto what they already have, and it is the one that turns "a verification-first language" into "the language you must use to build an agent that cannot be turned against you." Everything else deepens or proves or propagates; that phase is the thing that makes it a class of its own. The trust spine you already banked is what makes anyone believe the claim when you make it.
