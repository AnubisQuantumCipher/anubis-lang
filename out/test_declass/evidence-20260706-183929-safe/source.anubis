// EXPECT: PASS

@research(
  authorization: "bug-bounty",
  scope: "local",
  reason: "redact for report",
  non_destructive: true
)
fn main() {
    let secret = taint_source("token");
    let public = declassify(secret, policy: "hash-only", reason: "bounty proof redaction");
    sink(public);
}
