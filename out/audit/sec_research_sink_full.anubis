fn report() {
    research {
        let raw: tainted<u32> = symbolic();
        assume(raw < 1000);
        assume(raw > 0);
        sink(raw);
        let clean = declassify(raw);
        sink(clean);
    }
}
