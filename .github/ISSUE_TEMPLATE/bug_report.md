---
name: "🐛 Bug report"
about: "A crash, wrong output, or behavior that contradicts the docs"
title: "[bug] "
labels: ["bug"]
---

<!-- For a SOUNDNESS break (check accepts, run violates), use the "False accept"
     template instead — it is a different, higher-severity class. -->

## What happened

## What you expected

## Minimal reproduction

```rust
// smallest .anb that shows it
```

```
$ anubis <command> the.anb
# paste the actual output
```

## Environment

- `anubis --version`:
- `git rev-parse HEAD`:
- OS / arch (Anubis targets Apple Silicon):
