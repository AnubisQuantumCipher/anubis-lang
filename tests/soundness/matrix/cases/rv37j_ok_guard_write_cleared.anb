struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) { let x = 0; match q.pub_n { n if (if true { x = q.k; true } else { true }) => { x = n; } _ => { x = 1; } } return x; }
fn main() { let p = S { k: 42, pub_n: 3 }; println(f(p)); }
