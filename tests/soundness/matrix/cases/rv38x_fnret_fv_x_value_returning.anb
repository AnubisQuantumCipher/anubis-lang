struct S { k: secret<i64>, pub_n: i64 }
fn getn(q) { return q.pub_n; }
fn twice(f, x) { return f(f(x)); }
fn run(p) {
    let n = getn(p);
    let m = twice(|v| v + 1, n);
    print(m);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p);
    let r = getn(p);
    print(r);
}
