struct S { k: secret<i64>, pub_n: i64 }
fn g(v) {
    match 1 {
        _ if (if true { println(v); false } else { false }) => { }
        _ => { }
    }
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    g(p.k);
}
