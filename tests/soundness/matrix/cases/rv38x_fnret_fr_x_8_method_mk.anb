struct S { k: secret<i64>, pub_n: i64 }
struct B { n: i64 }
impl B { fn mk(self, q) { return || q; } }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let b = B { n: 1 };
    let g = if true { b.mk(p) } else { || 0 };
    print(g());
}
