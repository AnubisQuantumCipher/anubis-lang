struct S { k: secret<i64>, pub_n: i64 }
fn g(v) {
    let xs = [0];
    let ys = if true { push(xs, v); } else { [0] };
    println(len(ys) + ys[1]);
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    g(p.k);
}
