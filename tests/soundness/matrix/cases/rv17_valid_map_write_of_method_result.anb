fn f(x: i64) -> i64 requires(x > 0) { return x; }
struct User { id: i64 }
impl User { fn name(self) { return "u${self.id}"; } }
fn main() { let us = [User { id: 1 }]; let names = {}; for u in us { names[u.id] = u.name(); } print(names); print(map([1], f)); }
