struct S { k: secret<i64>, pub_n: i64 }
fn total(ys) { let g = |y| y.pub_n; let t = 0; for y in ys { t = t + g(y); } return t; }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; print(total(xs)); }
