struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let t = [1];
    if true {
        t = "ab";
        let t = [2];
        print(len(t));
    }
    print(len(t + p));
}
