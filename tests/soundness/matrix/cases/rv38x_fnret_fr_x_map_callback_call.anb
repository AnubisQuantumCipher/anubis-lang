struct S { k: secret<i64>, pub_n: i64 }
fn mkp() { return |q| { print(q); 0 }; }
fn run(p) {
    let xs = [p];
    map(xs, mkp());
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p);
}
