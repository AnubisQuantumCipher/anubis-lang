fn go(p: i64, c: bool) {
    let mut i = 0;
    while i < 10 {
        i = i + 1;
    }
    let mut b = 0;
    if c {
        b = i;
    } else {
        b = p;
    }
    assert(p > 0);
    println(b);
}
fn main() {
    go(-5, true);
}
