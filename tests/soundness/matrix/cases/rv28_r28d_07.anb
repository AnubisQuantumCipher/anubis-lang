struct S { k: secret<i64>, pub_n: i64 }
fn rend(acc, x) {
    return str(x);
}
fn rend1(x) {
    return str(x);
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let q = S { k: 7, pub_n: 1 };
    let ys = [q, p];
    let r = if true { str(p) } else { rend1 }; print(r);
}
