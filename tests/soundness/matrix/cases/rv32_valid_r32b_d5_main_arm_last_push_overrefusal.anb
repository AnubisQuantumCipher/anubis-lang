struct S { k: secret<i64>, pub_n: i64 }
fn push(a, b) {
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let ys = [0];
    if let Some(v) = Some(1) {
        push(ys, p);
    }
    print(ys);
}
