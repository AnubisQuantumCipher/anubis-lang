fn f(x: i64) -> i64 requires(x > 0) { print("f entered with ${x}"); return x; }
fn safe(x: i64) -> i64 { return x; }
fn getf(v) { return f; }
struct H { h: any }
struct O { n: i64 }
impl O { fn h(self, x) { return x; } }
fn main() { let m0 = { "h": 1 }; let mm = map_values(m0, getf); print(mm.h(-1)); }
