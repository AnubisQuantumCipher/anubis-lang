struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) { return 0; }
fn id1(x) { return x; }

fn main() {
    let p = S { k: 42, pub_n: 3 };
    let f = |x| 0;
    let i = 0;
    loop {
        if i == 0 { f = |x| x; break; }
        f = |x| 0;
        i = i + 1;
    }
    print(f(p));
}
