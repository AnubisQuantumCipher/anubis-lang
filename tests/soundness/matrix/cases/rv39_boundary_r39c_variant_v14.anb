struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) { return 0; }

fn main() {
    let p = S { k: 42, pub_n: 3 };
    let z = if p.pub_n > 0 {
        let q = 0;
        if p.pub_n > 1 { q = p; }
        print(q);
        0
    } else { 0 };
    print(z);
}
