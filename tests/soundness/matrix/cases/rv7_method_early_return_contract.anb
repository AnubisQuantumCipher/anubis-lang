fn f(x: i64) -> i64 requires(x > 0) { print("f entered with ${x}"); return x; }
fn safe(x: i64) -> i64 { return x; }
struct P { n: i64 }
impl P { fn pick(self, c) { if c < 5 { return f; } return safe; } }
fn main() { let p = P { n: 1 }; let h = p.pick(1); print(h(-1)); }
