// A record gains a field only when a secret threshold is not exceeded.
struct Rec {
    a: i64,
}

fn main() {
    let s: secret<i64> = 42;
    let mut r = Rec { a: 1 };
    for round in [1] {
        if s > 100 {
            continue;
        }
        r.b = 2;
    }
    print(len(r));
}
