struct S { k: secret<i64>, pub_n: i64 }
fn f(ys) {
  match ys {
    [g] => { print(g.f); }
    _ => { print(0); }
  }
}
fn main() {
  let p = S { k: 42, pub_n: 3 };
  let mut ys = [0];
  ys[0] = { "f": p };
  f(ys);
}