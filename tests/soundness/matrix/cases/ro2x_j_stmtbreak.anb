struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let c = p.pub_n > 1;
    let x = 0;
    loop {
        x = p.k;
        if c { break; }
        x = 0;
    }
    print(x);
}
