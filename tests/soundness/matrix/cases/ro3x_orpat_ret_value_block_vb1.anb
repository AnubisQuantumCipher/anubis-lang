struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    let x = q.k;
    let t = if true { let r = 0; match 1 { 1 | x => { r = x; } } r } else { 0 };
    return t;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p));
}
