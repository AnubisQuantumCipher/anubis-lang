struct S { k: secret<i64>, pub_n: i64 }
struct T { name: string }
impl S { fn tag(self) { return str(self); } }
impl T { fn tag(self) { return "T"; } }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    let c = sort(xs);
    println(c[0].tag());
}
