struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    let xs = [0];
    match q.pub_n {
        3 => { push(xs, q.k); }
        _ => { [5] }
    }
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p));
}
