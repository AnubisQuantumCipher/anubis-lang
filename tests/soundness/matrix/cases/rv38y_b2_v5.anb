struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) { return 0; }
fn zero2(x) { return 2; }
fn zero3(x) { return 3; }
fn id1(x) { return x; }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let g = zero;
    let fs = [id1];
    let h = zero;
    let o = Some(id1);
    let i = 0;
    if p.pub_n > 1 { let z = if true { for f in [id1] { g = f; } 0 } else { 0 }; }
    print(map([p], g));
}
