struct S { k: secret<i64>, pub_n: i64 }
fn show(q) { print(q); return 0; }
fn zero(q) { return 0; }
fn run(p, n) {
    let g = call(apply, identity, [show]);
    g(p);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p, 5);
}
