struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    let i = 0;
    loop {
        i = i + 1;
        match i > 2 {
            true => break,
            _ => {}
        }
    }
    return q.k;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p));
}
