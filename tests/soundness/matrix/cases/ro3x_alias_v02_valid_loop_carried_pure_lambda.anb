struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) {
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let f = zero;
    let i = 0;
    while i < 2 { let z = f(p.k);
    f = |x| 0;
    i = i + 1;
    }
}
