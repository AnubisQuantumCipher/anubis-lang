struct S { k: secret<i64>, pub_n: i64 }
fn call0(g) {
    return g();
}
fn shout0(g) {
    print(g());
}
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; let s = str(p); let f = || s; let s = 1; print(f()); }
