fn f(n) requires(n >= 3) {
    println(n);
}
fn main() {
    let x = 2.5;
    let mut i = 0;
    while i < 1 {
        let x = 3;
        println(x);
        i = i + 1;
    }
    if x > 2 {
        f(x);
    }
}
