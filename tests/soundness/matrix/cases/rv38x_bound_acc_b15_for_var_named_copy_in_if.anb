struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) { return 0; }
fn zero2(x) { return 2; }
fn zero3(x) { return 3; }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let g = zero;
    for h in [zero2, zero3] { if p.pub_n > 1 { g = h; } }
    print(g(p));
}
