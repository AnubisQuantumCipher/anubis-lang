struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) { return 0; }
fn pick(c) { if c { return abs; } return zero; }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let f = pick(p.pub_n > 1);
    print(f(p.k));
}
