// The finding's program, byte-identical: /home/sicarii/.cache/anubis-review-esc/r39fix/boundary/perf/q_c4_15.anb (15 nested `for eN in [1, 2]` loops, each assigning g0..g3 = if true { g(i+1) } else { |x| N } under `if eN == 1`). Companion cost cases in the same directory: q_choose12.anb (NEW 22.1 s, patched 4.9 s, PREV 1.07 s), pf9.anb (NEW 18.1 s, patched 4.6 s, PREV 0.98 s), q_c8_10.anb, sc4_17.anb, q_noif.anb, q_alias.anb.