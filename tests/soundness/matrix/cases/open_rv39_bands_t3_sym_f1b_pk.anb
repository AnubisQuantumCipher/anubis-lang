struct S { k: secret<i64>, pub_n: i64 }
fn show(q) { print(q); return 0; }
fn mk() { return [call, call]; }
fn run(p, n) {
    let w = mk();
    let r = match w[0] { call => call };
    r(show, p.k);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p, 5);
}
