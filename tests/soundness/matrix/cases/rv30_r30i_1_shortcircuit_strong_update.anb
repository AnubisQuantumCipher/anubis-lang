struct S { k: secret<i64>, pub_n: i64 }
fn pick(q) {
    let x = q;
    if false && if true { x = 0; true } else { x = 0; true } {
        return 1;
    }
    return x;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(pick(p));
}
