struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) { let y = if true { let x = 0; let v = if true { x = q.k; 0 } else { 0 }; x } else { 0 }; return y; }
fn main() { let p = S { k: 42, pub_n: 3 }; println(f(p)); }
