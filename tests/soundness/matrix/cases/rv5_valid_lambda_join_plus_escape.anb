fn f(x: i64) -> i64 requires(x > 0) { print("f entered with ${x}"); return x; }
fn mk(c) { if c > 0 { return |x| x + 1; } return |x| x + 2; }
fn main() { let h = mk(1); print(h(1)); print(map([1, 2], f)); }
