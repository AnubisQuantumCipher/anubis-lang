struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) {
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let c = p.pub_n > 1;
    let f = if c { abs } else { let abs = zero;
    abs };
    print(f(p.k));
}
