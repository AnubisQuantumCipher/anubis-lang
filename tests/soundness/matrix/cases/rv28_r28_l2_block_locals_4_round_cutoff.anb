struct S { k: secret<i64>, pub_n: i64 }
fn show(s) { let a = s; let b = a; let c = b; let d = c; let e = d; print(e); }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; show(xs[0]); }
