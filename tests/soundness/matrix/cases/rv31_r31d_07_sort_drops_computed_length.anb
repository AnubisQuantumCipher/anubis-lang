struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let q = S { k: 20, pub_n: 1 };
    let xs = filter([p, q], |x| str(x) > "S { k: 5");
    print(len(sort(xs)));
}
