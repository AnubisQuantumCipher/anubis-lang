struct S { k: secret<i64>, pub_n: i64 }
struct R { pub_n: i64 }
struct T { a: string, b: i64 }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; for pair in enumerate(xs) { let r = match pair { [0, _] => "first", _ => "other" }; print(r); } }
