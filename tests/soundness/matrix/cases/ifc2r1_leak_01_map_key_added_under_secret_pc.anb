// A feature map gains a key only when a secret threshold is not exceeded.
fn main() {
    let s: secret<i64> = 42;
    let mut m = {"a": 1};
    for round in [1] {
        if s > 100 {
            continue;
        }
        m["b"] = 2;
    }
    print(len(m));
}
