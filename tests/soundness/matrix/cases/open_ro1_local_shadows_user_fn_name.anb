struct S { k: secret<i64>, pub_n: i64 }
fn h2(x) {
    print(x);
    return 0;
}
fn h1(q) {
    let s: secret<i64> = 7;
    return s;
}
fn id1(x) { return x; }
fn zero(x) { return 0; }

fn main() {
    let p = S { k: 42, pub_n: 3 };
    let c = p.pub_n > 1;
    let d = p.pub_n > 100;
    let h2 = zero;
    let z = h2(p.k);
}
