struct S { k: secret<i64>, pub_n: i64 }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; let mut m3 = { "a": 1, "b": 2 }; let key = if str(p) == "S { k: 42, pub_n: 3 }" { "a" } else { "b" }; remove(m3, key); print(keys(m3)); }
