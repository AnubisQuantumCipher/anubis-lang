fn f(x: i64) -> i64 requires(x > 0) { print("f entered with ${x}"); return x; }
fn safe(x: i64) -> i64 { return x; }
fn pick(c) { let h = f; if c > 5 { return safe; } return h; }
fn main() { let h = safe; let q = pick(1); print(q(-1)); }
