enum Opt { Some(i64), None }
fn main() {
    let mut i = 0;
    while i < 10 {
        i = i + 1;
    }
    let mut o = Opt::None;
    while let Opt::Some(i) = o {
        o = Opt::None;
    }
    assert(i == 10);
    println(i);
}
