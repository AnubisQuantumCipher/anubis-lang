struct S { k: secret<i64>, pub_n: i64 }
struct W { n: i64 }
impl S { fn show(self, x) { print(x); return 0; } }
impl W { fn show(self, x) { return 0; } }
fn g(p) {
    let r = if p.pub_n > 100 { [str(p)] } else { p };
    let q = { "a": { "a": r }, "z": [[[[[[[[[[[[[str(p)]]]]]]]]]]]]] };
    let r2 = q["a"]["a"];
    return r2.show(p);
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let z = g(p);
}
