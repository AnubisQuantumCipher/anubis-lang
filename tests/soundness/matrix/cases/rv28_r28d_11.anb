struct S { k: secret<i64>, pub_n: i64 }
struct R { pub_n: i64 }
struct T2 { items: list, b: i64 }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; let kk = "pub_n"; let m2 = { kk: p }; print(m2["pub_n"]); }
