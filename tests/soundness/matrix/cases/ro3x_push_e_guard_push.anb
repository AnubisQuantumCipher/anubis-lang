struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [0];
    match p.pub_n {
        3 if len(push(xs, p.k)) > 100 => { println("a"); }
        _ => { println(xs); }
    }
}
