struct S { k: secret<i64>, pub_n: i64 }
fn helper(q) -> i64 {
    match [q] {
        [g] => { print(g.pub_n); }
        _ => { print(0); }
    }
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    helper(p);
}