fn say(x: i64) -> i64 {
    println("said");
    x
}
fn same(x: i64) -> i64 {
    x
}
fn main() {
    let s: secret<i64> = 42;
    let c = if s > 100 { compose(say, same) } else { compose(same, same) };
    let r = c(7);
    println("done");
}
