struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let mut m = [1];
    let y = if p.pub_n > 0 { m = { "z": 0 }; 0 } else { 1 };
    m["a"] = p;
    print(m.a);
}
