struct S { k: secret<i64>, pub_n: i64 }
fn show(s) { println(s); return 0; }
fn other(s) { println(1); return 0; }
struct T { n: i64 }
struct U { n: i64 }
struct W { t: T }
impl T { fn pick(self) { return other; } }
impl U { fn pick(self) { return show; } }
fn run(p: S) -> i64 {
    let w = W { t: T { n: 1 } };
    w.t.pick()(p);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p);
}
