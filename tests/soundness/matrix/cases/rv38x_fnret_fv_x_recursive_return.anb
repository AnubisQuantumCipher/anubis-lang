struct S { k: secret<i64>, pub_n: i64 }
fn showpub(q) { print(q.pub_n); return 0; }
fn getf(n) { if n <= 0 { return showpub; } return getf(n - 1); }
fn run(p) {
    let r = getf(3);
    r(p);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p);
}
