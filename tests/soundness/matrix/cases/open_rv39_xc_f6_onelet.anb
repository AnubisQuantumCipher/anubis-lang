struct S { k: secret<i64>, pub_n: i64 }
struct T { n: i64 }
struct U { n: i64 }
impl T { fn run(self, s) { return 0; } }
impl U { fn run(self, s) { println(s); return 0; } }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs: list<T> = [U { n: 1 }];
    let a = xs[0];
    a.run(p);
}
