struct S { k: secret<i64>, pub_n: i64 }
fn mk5(q) { let h = || q; return identity(h); }
fn run(p) {
    let g = mk5(p);
    print(g());
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p);
}
