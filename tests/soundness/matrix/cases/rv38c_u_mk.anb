struct S { k: secret<i64>, pub_n: i64 }
fn show(q) { print(q); return 0; }
fn mk(f) { return [f, f]; }
fn run(p, n) {
    let w = mk(show);
    let r = identity(if n > 0 { let show = w[0]; show } else { 0 });
    r(p);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p, 5);
}
