struct S { k: secret<i64>, pub_n: i64 }
struct T { a: S, b: i64 }
fn g(r) { return r[0]; }
fn main() { let p = S { k: 42, pub_n: 3 }; let t = T { a: p, b: 0 }; print(g(if true { t } else { [0, 0, p] })); }
