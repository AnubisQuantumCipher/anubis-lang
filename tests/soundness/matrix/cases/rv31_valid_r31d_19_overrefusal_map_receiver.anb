struct S { k: secret<i64>, pub_n: i64 }
struct U { v: i64 }
impl U {
    fn show(self) {
        return str(self);
    }
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let u = U { v: 1 };
    print(u.show());
    let obj = { "show": || "obj", "owner": p };
    print(obj.show());
}
