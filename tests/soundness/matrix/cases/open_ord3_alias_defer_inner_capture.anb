struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let c = p.pub_n > 1;
    let f = || 0;
    if c { let s = p.k;
    f = || { print(s);
    0 };
    } let z = f();
}
