struct S { k: secret<i64>, pub_n: i64 }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; let mut r = { "z": 0 }; for x in xs { r = { "pub_n": r, "q": x }; } print(r.pub_n); }
