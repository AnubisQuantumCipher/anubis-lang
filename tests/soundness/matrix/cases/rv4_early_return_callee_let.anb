fn f(x: i64) -> i64 requires(x > 0) { return x; }
fn safe(x: i64) -> i64 { return x; }
fn pick(g, c) { if c > 0 { return g; } return safe; }
fn main() { let h = pick(f, 1); let z = h(-1); print(z); }
