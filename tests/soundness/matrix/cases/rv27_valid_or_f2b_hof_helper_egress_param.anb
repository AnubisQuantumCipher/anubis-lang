struct S { k: secret<i64>, pub_n: i64 }
fn log_all(ys, key) { for y in ys { print(key(y)); } }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; log_all(xs, |s| s.pub_n); }
