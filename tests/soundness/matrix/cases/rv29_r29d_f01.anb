struct S { k: secret<i64>, pub_n: i64 }
enum W { Box { v: S }, Empty }
fn g(w) { match w { W::Box { v } => { return v; } _ => { return 0; } } }
fn main() { let p = S { k: 42, pub_n: 3 }; let w = W::Box { v: p }; print(g(w)); }
