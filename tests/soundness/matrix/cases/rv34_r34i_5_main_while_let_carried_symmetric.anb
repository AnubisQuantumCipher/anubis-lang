struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    let m = { "a": p };
    let a = 0;
    let i = 0;
    while let Some(v) = (if i < 2 { Some(a) } else { None }) { print(v); a = p; i = i + 1; }
}
