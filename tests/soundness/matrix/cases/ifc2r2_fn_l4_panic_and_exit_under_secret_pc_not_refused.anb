fn check(xs, pred) {
    println("checking");
    each(xs, |x| if pred(x) { panic("rejected entry") } else { 0 });
    return len(xs);
}
fn main() {
    let s: secret<i64> = 42;
    let n = check([1, 2, 3], |x| x == s);
}
