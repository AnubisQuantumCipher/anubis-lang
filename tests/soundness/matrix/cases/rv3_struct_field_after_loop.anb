struct P {
    a: i64,
}
fn f(n: i64) requires(n > 0) {
    println(n);
}
fn go() {
    let mut p = P { a: 5 };
    let mut i = 0;
    while i < 3 {
        p.a = -1;
        i = i + 1;
    }
    f(p.a);
}
fn main() {
    go();
}
