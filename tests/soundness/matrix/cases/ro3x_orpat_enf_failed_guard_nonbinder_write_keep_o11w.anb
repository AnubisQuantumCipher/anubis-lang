struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let x = 5;
    let r = 0;
    match p.pub_n {
        x if (if true { r = p.k; false } else { false }) => { }
        _ => { println(r); }
    }
}
