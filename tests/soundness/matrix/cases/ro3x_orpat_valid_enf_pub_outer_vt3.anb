struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let x = p.pub_n;
    let r = 0;
    match 1 {
        1 | x => { r = x; }
    }
    println(r);
    println(match 1 { 1 | x => x });
}
