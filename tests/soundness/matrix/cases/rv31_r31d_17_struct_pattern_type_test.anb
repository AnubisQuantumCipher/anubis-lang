struct S { k: secret<i64>, pub_n: i64 }
struct U { v: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let x = if str(p) > "S { k: 5" { p } else { U { v: 1 } };
    let b = match x {
        S { pub_n } => 1,
        _ => 0,
    };
    print(b);
}
