struct S { k: secret<i64>, pub_n: i64 }
fn f(g) {
    let x = 0;
    loop {
        x = g();
        match 0 {
            _ => break,
        }
        x = 0;
    }
    print(x);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(|| p));
}
