struct S { k: secret<i64>, pub_n: i64 }
struct T { n: i64 }
struct U { n: i64 }
struct W { t: T, m: i64 }
impl T {
    fn show(self, o) {
        print(self.n);
    }
}
impl U {
    fn show(self, o) {
        print(o);
    }
}
fn keep_m(w: W) -> W {
    w.m = w.m + 1;
    return w;
}
fn same(w: W) -> W {
    return w;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let r = same(keep_m(W { t: T { n: 1 }, m: 0 }));
    r.t.show(p);
}
