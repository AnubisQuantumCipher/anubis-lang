struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) { let x = q.k; if true { x = declassify(x, "demo-policy", "public sample"); return x; } return 0; }
fn main() { let p = S { k: 42, pub_n: 3 }; println(f(p)); }
