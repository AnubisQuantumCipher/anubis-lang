struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    let x = q.k;
    let r = 0;
    match [q.pub_n, 1] {
        [x, 1] | [1, x] => { r = x; }
        _ => { }
    }
    return r;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p));
}
