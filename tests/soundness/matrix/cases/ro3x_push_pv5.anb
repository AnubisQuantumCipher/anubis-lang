struct S { k: secret<i64>, pub_n: i64 }
fn f(v) {
    let xs = [0];
    let ys = match 1 { _ => { let t = 0; push(xs, v); } };
    return ys;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p.k));
}
