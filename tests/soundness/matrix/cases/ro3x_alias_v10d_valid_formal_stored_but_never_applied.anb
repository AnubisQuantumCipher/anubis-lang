struct S { k: secret<i64>, pub_n: i64 }
fn pr(x) {
    print(x);
    return 0;
}
fn store(g, x) {
    let fs = [g];
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let z = store(pr, p.k);
}
