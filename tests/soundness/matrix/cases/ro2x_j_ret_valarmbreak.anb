struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    let x = 0;
    loop {
        x = q.k;
        let y = match q.pub_n > 1 {
            true => break,
            _ => 0
        };
        x = y;
    }
    return x;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p));
}
