struct S { k: secret<i64>, pub_n: i64 }
fn rend(s) { return str(s); }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; print(each(xs, |s| s.pub_n)); }
