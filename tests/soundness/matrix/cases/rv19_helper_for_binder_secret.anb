fn getsec() -> secret<i64> { let s: secret<i64> = 42; return s; }
fn pub1() -> i64 { return 1; }
fn sec2(k) { for g in [getsec] { if k > 0 { return g; } } return pub1; }
fn r(c, g, h) { if c > 5 { return g; } let q = r(c + 1, h, sec2(1)); return q; }
fn main() { let q = r(1, pub1, pub1); print(q()); }
