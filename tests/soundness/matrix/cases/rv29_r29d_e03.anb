struct S { k: secret<i64>, pub_n: i64 }
fn r(x, n) { if n == 0 { return x; } return r({ "pub_n": x, "q": 0 }, n - 1); }
fn main() { let p = S { k: 42, pub_n: 3 }; let v = r({ "pub_n": 0, "q": p }, 3); print(v.pub_n); }
