struct S { k: secret<i64>, pub_n: i64 }
fn run2(f) { return f(); }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    let m = { "a": p };
    let a = p;
    let r = 0;
    r = run2(|| { if p.pub_n > 5 { a = 0; } a });
    print(r);
}
