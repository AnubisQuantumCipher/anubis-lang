fn f(x: i64) -> i64 requires(x > 0) { return x; }
fn safe(x: i64) -> i64 { return x; }
fn app(g, a: i64, y: i64) -> i64 { return g(y); }
fn main() { let mut x: i64 = 5; let r = app(f, if 1 == 1 { x = -1; 0 } else { 0 }, x); print(r); }
