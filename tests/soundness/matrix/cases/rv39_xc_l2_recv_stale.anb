struct S { k: secret<i64>, pub_n: i64 }
struct T { n: i64 }
struct U { n: i64 }
impl T { fn show(self, o) { print(self.n); } fn next(self) -> T { return self; } }
impl U { fn show(self, o) { print(o); } fn next(self) -> T { return self; } }
fn mk() -> T { return U { n: 1 }; }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let x = mk();
    let y = x.next();
    y.show(p);
}
