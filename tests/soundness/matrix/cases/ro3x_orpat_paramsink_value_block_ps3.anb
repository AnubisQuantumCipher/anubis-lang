struct S { k: secret<i64>, pub_n: i64 }
fn g(v) {
    let x = v;
    let t = if true { let r = 0; match 1 { 1 | x => { r = x; } } r } else { 0 };
    println(t);
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    g(p.k);
}
