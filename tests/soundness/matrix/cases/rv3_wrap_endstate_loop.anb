fn go(n: i64) requires(n > 0) {
    let mut x = n;
    let t = x * 2305843009213693952;
    println(t);
    while x > 1 invariant(x >= 1) {
        x = x - 1;
    }
    println(x);
}
fn main() {
    go(5);
}
