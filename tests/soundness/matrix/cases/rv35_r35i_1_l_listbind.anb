struct S { k: secret<i64>, pub_n: i64 }
impl S { fn me(self) -> S { return self; } }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    let m = { "a": p };
    let t = if true { let [g] = [p]; g.me() } else { 0 };
    print(t);
}