struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) { return 0; }
fn id2(x) { return 0; }

fn main() {
    let p = S { k: 42, pub_n: 3 };
    let g0 = zero;
    for e in [1, 2] {
        g0 = if true { let t = id2; t } else { g0 };
    }
    print(map([p], g0));
}
