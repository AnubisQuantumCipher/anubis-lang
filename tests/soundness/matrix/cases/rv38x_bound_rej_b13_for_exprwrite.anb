struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) { return 0; }
fn zero2(x) { return 2; }
fn id1(x) { return x; }

fn main() {
    let p = S { k: 42, pub_n: 3 };
    let g = zero;
    for e in [1] { let z = if true { g = id1; 0 } else { 0 }; }
    print(apply(g, [p]));
}
