struct S { k: secret<i64>, pub_n: i64 }
struct T { n: i64 }
enum E { A(i64), B(i64) }
impl T {
    fn show(self, o) {
        print(self.n);
    }
}
impl E {
    fn show(self, o) {
        print(o);
    }
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let x: T = E::A(1);
    x.show(p);
}
