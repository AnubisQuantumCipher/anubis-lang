struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    let mut x = 0;
    if q.pub_n > 1 { x = q.k; }
    if q.pub_n > 0 { x = 1; } else { x = 2; }
    return x;
}
fn main() { let p = S { k: 42, pub_n: 3 }; print(f(p)); }
