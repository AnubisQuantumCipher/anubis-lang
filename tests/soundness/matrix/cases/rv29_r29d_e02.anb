struct S { k: secret<i64>, pub_n: i64 }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; let r = reduce(xs, |acc, x| ({ "pub_n": acc, "q": x }), { "z": 0 }); print(r.pub_n); }
