struct S { k: secret<i64>, pub_n: i64 }
fn h(p: S) -> i64 {
    let t = 0;
    if false && (if true { t = p; true } else { false }) { }
    print(t);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let r = h(p);
}
