struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) {
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let f = zero;
    for i in 0..2 { let z = f(p.k);
    f = |x| { print(x);
    0 };
    }
}
