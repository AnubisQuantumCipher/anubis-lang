struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let x = 5;
    match p.pub_n {
        x if (if true { x = p.k; false } else { false }) => { }
        _ => { println(x); }
    }
}
