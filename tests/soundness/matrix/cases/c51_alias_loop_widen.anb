fn f(x: i64) -> i64 requires(x > 0) { return x; }
fn safe(x: i64) -> i64 { return x; }
fn app(g, y) { let mut h = safe; let mut i = 0; while i < 2 { h(-1); h = g; i = i + 1; } }
fn main() { app(f, 0); }
