struct S { k: secret<i64>, pub_n: i64 }
fn label(xs) {
    let head = get(xs, 5, "none:");
    return head + xs;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    print(join(flatten(label(xs)), ""));
}
