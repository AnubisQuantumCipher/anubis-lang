struct S { k: secret<i64>, pub_n: i64 }
fn run(p, n) {
    let f = |q| { print(q); 0 };
    let h = |q| 0;
    let r = identity(match h { g => { h = f; g } });
    r(p);
    h(p.pub_n);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p, 5);
}
