struct S { k: secret<i64>, pub_n: i64 }
struct W { n: i64 }
impl S { fn me(self) { return self; } }
impl W { fn me(self) { return 0; } }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    let z = max(xs).me();
    print(z);
}
