struct S { k: secret<i64>, pub_n: i64 }
fn rel(v) { return declassify(v > 1000000, "audit.big", "only the over-limit bit is public"); }
fn f(q: S) {
    let mut x = 0;
    if q.pub_n > 1 { x = rel(q.k); }
    return x;
}
fn main() { let p = S { k: 42, pub_n: 3 }; print(f(p)); }
