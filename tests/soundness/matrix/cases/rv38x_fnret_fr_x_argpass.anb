struct S { k: secret<i64>, pub_n: i64 }
fn myid(a) { return a; }
fn show(q) { print(q); return 0; }
fn app(f, x) { return f(x); }
fn run(p) {
    app(myid(show), p);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p);
}
