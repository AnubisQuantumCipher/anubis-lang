struct S { k: secret<i64>, pub_n: i64 }
fn showpub(q) { print(q.pub_n); return 0; }
fn pick(c, f, g) { if c > 0 { return f; } return g; }
fn run(p, n) {
    let r = pick(n, showpub, |q| 0);
    r(p);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p, 5);
}
