struct S { k: secret<i64>, pub_n: i64 }
fn mkc(q) { return || q; }
fn zero() { return 0; }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let g = if true { mkc(p) } else { zero };
    print(g());
}
