struct S { k: secret<i64>, pub_n: i64 }
struct T { v: i64 }
struct U { inner: S }
struct W { f: T }
impl T { fn show(self) -> str { return "t ${self.v}"; } }
impl U { fn show(self) -> str { return "u ${self.inner}"; } }
fn mkw(q) -> W { let mut w = W { f: T { v: 0 } }; w.f = U { inner: q }; return w; }
fn main() {
  let p = S { k: 42, pub_n: 3 };
  let w = mkw(p);
  println(w.f.show());
}
