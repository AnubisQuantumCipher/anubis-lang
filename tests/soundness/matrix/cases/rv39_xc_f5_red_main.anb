struct S { k: secret<i64>, pub_n: i64 }
fn mkw() { return [|acc, x| x]; }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let c = p.pub_n - 3;
    let a = if c > 0 { |acc, x| 0 } else { 5 };
    let w = mkw();
    let z = reduce([p], a, w[0]);
    println(z);
}
