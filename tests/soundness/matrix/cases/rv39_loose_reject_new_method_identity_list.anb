struct S { k: secret<i64>, pub_n: i64 }
struct T { n: i64 }
struct U { n: i64 }
struct H { n: i64 }
impl T {
    fn show(self, o) {
        print(self.n);
    }
}
impl U {
    fn show(self, o) {
        print(o);
    }
}
impl H {
    fn same(self, xs: list<T>) -> list<T> {
        return xs;
    }
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let ys = [T { n: 1 }];
    push(ys, U { n: 2 });
    let h = H { n: 0 };
    let zs = h.same(ys);
    zs[1].show(p);
}
