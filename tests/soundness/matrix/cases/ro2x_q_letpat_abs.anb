struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) { return 0; }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let c = p.pub_n > 1;
    let (g, h) = if c { (abs, zero) } else { (zero, zero) };
    print(g(p.k));
}
