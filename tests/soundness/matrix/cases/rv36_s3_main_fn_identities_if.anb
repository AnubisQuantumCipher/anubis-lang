struct S { k: secret<i64>, pub_n: i64 }
fn h1(x) { return 0; }
fn h2(x) { print(x); return 0; }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    let m = { "a": p };
    let c = p.pub_n > 5;
    let f = if c { h1 } else { h2 };
    let z = f(p);
}
