struct S { k: secret<i64>, pub_n: i64 }
impl S { fn n(self) { self.pub_n } }
fn main() { let p = S { k: 42, pub_n: 3 }; let ys = [p, S { k: 7000, pub_n: 1 }]; print(sort_by(ys, |s| len(str(s)))[0].n()); }
