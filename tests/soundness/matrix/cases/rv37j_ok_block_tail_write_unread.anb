struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) { let y = if true { let a = 0; (if q.pub_n > 1 { a = q.k; 0 } else { 0 }) + 1 } else { 0 }; return y; }
fn main() { let p = S { k: 42, pub_n: 3 }; println(f(p)); }
