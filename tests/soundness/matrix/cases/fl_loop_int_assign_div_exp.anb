fn f(s: f64) -> f64 requires(s > 1e-10 && s < 1e-9) ensures(result == 3.5) {
    while s < 5.0 invariant((s > 0.0 && s < 1e-9) || s == 7.0) {
        s = 7;
    }
    let r = s / 2;
    return r;
}
fn main() { println(f(5e-10)); }
