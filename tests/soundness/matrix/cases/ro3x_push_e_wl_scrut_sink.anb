struct S { k: secret<i64>, pub_n: i64 }
fn showo(v) { println(v); return Some(1); }
fn g(v) {
    let i = 0;
    while let Some(z) = showo(v) {
        if i == 1 { break; }
        i = i + 1;
    }
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    g(p.k);
}
