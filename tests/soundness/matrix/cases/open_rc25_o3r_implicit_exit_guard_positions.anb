struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    match q.k {
        42 if Err(0)? == 0 => { print("a"); }
        _ => { print("b"); }
    }
    return Ok(1);
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p));
}