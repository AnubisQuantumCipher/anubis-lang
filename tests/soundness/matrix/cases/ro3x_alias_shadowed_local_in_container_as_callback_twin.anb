struct S { k: secret<i64>, pub_n: i64 }
struct B { g: i64 }
fn pr(x) {
    print(x);
    return 0;
}
fn app(g, x) {
    return g(x);
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let fs = [pr];
    let z = each([p.k], fs[0]);
}
