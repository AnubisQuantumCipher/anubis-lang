struct S { k: secret<i64>, pub_n: i64 }
fn h(q) {
    let f = || q;
    let g = if true { if true { f } else { f } } else { if true { f } else { f } };
    return g();
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(h(p));
}
