struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let y = if true {
        let v = |x| 0;
        if true { v = p; }
        str(v)
    } else { "" };
    print(y);
}
