struct S { k: secret<i64>, pub_n: i64 }
fn dump(i, s) { print("${i}: ${s.pub_n}"); }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; for pair in enumerate(xs) { dump(pair[0], pair[1]); } }
