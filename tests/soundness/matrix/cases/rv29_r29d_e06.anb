struct S { k: secret<i64>, pub_n: i64 }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; let t = [p.pub_n, p]; print(get(t, "x1", 0)); print(get(t, " 1", 0)); }
