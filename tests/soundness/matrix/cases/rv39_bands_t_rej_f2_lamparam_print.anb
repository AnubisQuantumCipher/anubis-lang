struct S { k: secret<i64>, pub_n: i64 }
fn mk() { return [print, print]; }
fn run(p, n) {
    let w = mk();
    let pick = |print| print;
    let r = pick(w[0]);
    r(p);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p, 5);
}
