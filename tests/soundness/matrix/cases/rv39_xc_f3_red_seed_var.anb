struct S { k: secret<i64>, pub_n: i64 }
fn mkw() { return [|acc, x| acc]; }
fn run(p, c) {
    let a = if c > 0 { |acc, x| 0 } else { p };
    let w = mkw();
    let z = reduce([1], a, w[0]);
    println(z);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p, 0);
}
