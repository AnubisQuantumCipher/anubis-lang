struct S { k: secret<i64>, pub_n: i64 }
fn pr(x) {
    print(x);
    return 0;
}
fn zero(x) {
    return 0;
}
fn apps(g, x) {
    let r = g(x);
    g = zero;
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let z = apps(pr, p.k);
}
