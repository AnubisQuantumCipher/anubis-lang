struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let x = p.k;
    let r = 0;
    match 1 {
        1 | x if (if true { r = r + x; false } else { false }) => { }
        _ => { }
    }
    println(r);
}
