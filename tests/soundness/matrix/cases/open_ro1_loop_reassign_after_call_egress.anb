struct S { k: secret<i64>, pub_n: i64 }
struct Box { g: u64 }
struct T { n: i64 }
fn h2(x) {
    print(x);
    return 0;
}
fn h1(q) {
    let s: secret<i64> = 7;
    return s;
}
fn id1(x) { return x; }
fn zero(x) { return 0; }

fn main() {
    let p = S { k: 42, pub_n: 3 };
    let c = p.pub_n > 1;
    let d = p.pub_n > 100;
    let i = 1;
    let f = h1;
    let n = 0;
    while n < 2 { let z = f(p.k); f = h2; n = n + 1; }
}
