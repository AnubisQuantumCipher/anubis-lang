struct S { k: secret<i64>, pub_n: i64 }
fn showl(v) { println(v); return [1]; }
fn g(v) {
    for i in 0..len(showl(1)) { println("i"); }
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    g(p.k);
}
