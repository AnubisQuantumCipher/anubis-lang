fn f(x: i64) -> i64 requires(x > 0) { print("f entered with ${x}"); return x; }
fn safe(x: i64) -> i64 { return x; }
fn pick(g, c) { if c > 5 { return safe; } let h = g; return h; }
fn main() { let h = safe; let q = pick(f, 1); print(q(-1)); }
