struct S { k: secret<i64>, pub_n: i64 }
impl S { fn me(self) { return self; } }
fn f(ys) {
  match ys {
    [g] => { print(g.pub_n); }
    _ => { print(0); }
  }
}
fn main() {
  let p = S { k: 42, pub_n: 3 };
  f([p]);
}