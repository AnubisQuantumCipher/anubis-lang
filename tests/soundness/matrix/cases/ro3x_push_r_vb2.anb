struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    let xs = [0];
    let ys = match 1 { _ => { push(xs, q.k); } };
    return ys;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p));
}
