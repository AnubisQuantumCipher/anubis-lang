struct S { k: secret<i64>, pub_n: i64 }
fn showpub(q) { print(q.pub_n); return 0; }
fn wrap(n, f) { if n <= 0 { return f; } return wrap(n - 1, |x| f(x)); }
fn run(p) {
    let r = wrap(3, showpub);
    r(p);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p);
}
