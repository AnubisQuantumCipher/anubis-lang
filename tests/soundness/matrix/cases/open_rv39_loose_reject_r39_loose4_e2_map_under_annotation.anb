struct S { k: secret<i64>, pub_n: i64 }
struct T { n: i64 }
impl T {
    fn show(self, o) {
        print(self.n);
    }
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let x: T = { "show": |o| print(o) };
    x.show(p);
}
