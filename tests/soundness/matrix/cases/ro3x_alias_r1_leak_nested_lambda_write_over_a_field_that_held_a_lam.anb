struct S { k: secret<i64>, pub_n: i64 }
struct B { g: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let c = p.pub_n > 1;
    let b = B { g: |x| 0 };
    if c { b.g = |x| { print(x);
    0 };
    } let z = b.g(p.k);
}
