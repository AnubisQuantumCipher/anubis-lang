struct S { k: secret<i64>, pub_n: i64 }
fn pr(x) {
    print(x);
    return 0;
}
fn appk(g, x) {
    let h = |y| g(y);
    return h(x);
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let z = appk(pr, p.k);
}
