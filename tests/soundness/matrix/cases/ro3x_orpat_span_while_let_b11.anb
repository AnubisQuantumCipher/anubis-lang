struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let o = Some(1);
    while let Some(x) = o {
        x = p.k;
        match p.pub_n {
            x => { }
        }
        println(x);
        o = None;
    }
}
