struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) { let x = 0; if true { x = declassify(q.k, "demo-policy", "public sample"); } return x; }
fn main() { let p = S { k: 42, pub_n: 3 }; println(f(p)); }
