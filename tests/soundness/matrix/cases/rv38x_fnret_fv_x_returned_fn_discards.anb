struct S { k: secret<i64>, pub_n: i64 }
fn zero(x) { return 0; }
fn myid(a) { return a; }
fn run(p) {
    let r = myid(zero);
    let v = r(p);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p);
    let g = myid(zero);
    g(p);
}
