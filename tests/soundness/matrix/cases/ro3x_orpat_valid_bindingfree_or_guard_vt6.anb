struct S { k: secret<i64>, pub_n: i64 }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let r = 0;
    match p.pub_n {
        1 | 2 | 3 if (if true { r = r + 1; false } else { false }) => { }
        1 | 2 | 3 => { r = 7; }
        _ => { }
    }
    println(r);
}
