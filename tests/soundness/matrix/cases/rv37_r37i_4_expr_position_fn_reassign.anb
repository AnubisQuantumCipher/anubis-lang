struct S { k: secret<i64>, pub_n: i64 }
fn id1(x) { return x; }
fn zero(x) { return 0; }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    let m = { "a": p };
    let g = zero;
    let z = match p.pub_n { 3 => { g = id1; 0 }, _ => 0 };
    print(g(p));
}