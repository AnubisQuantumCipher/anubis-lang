struct S { k: secret<i64>, pub_n: i64 }
struct B { g: i64 }
fn pr(x) {
    print(x);
    return 0;
}
fn zero(x) {
    return 0;
}
fn h2(x) {
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let fs = [pr];
    let g = fs[0];
    let z = g(p.k);
}
