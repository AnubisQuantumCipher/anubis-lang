struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) { let o = Some(q.k); let x = 0; match o { Some(v) if (if true { x = v; true } else { true }) => { } _ => { } } return x; }
fn main() { let p = S { k: 42, pub_n: 3 }; println(f(p)); }
