struct S { k: secret<i64>, pub_n: i64 }
fn mk() { return {}; }
fn build(p) {
    let m = mk();
    m["user"] = p;
    m["title"] = "T";
    return m["title"];
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    println(build(p));
}
