struct S { k: secret<i64>, pub_n: i64 }
fn h(p: S) -> i64 {
    let f = || p;
    let r = match f { g => g() };
    print(r);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    let m = { "a": p };
    let r = h(p);
}
