struct S { k: secret<i64>, pub_n: i64 }
fn show(x) -> bool { print(x); return true; }
fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p, S { k: 7, pub_n: 1 }];
    let m = { "a": p };
    let a = 0;
    let i = 0;
    while show(a) && i < 2 {
        a = p;
        i = i + 1;
    }
}