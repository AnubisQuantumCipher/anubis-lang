struct Acct { id: i64 }
impl Acct {
    fn log(self) -> i64 {
        print("logged");
        0
    }
}
fn main() {
    let k: secret<i64> = 42;
    let a = if k > 100 { Acct { id: 1 } } else { Acct { id: 2 } };
    a.log();
    print("done");
}