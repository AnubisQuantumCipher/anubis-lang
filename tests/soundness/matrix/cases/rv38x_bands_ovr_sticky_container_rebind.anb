struct S { k: secret<i64>, pub_n: i64 }
fn run(p) {
    let f = |q| { print(q); 0 };
    let fs = [|q| 0];
    f = fs[0];
    f(p);
    return 0;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    run(p);
}
