struct S { k: secret<i64>, pub_n: i64 }
fn main() { let p = S { k: 42, pub_n: 3 }; let xs = [p, S { k: 7, pub_n: 1 }]; let m = { "a": p }; let c = len(xs) > 5; let r = if c { enumerate(xs) } else { { "pub_n": p } }; print(r.pub_n); }
