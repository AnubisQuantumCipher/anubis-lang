struct S { k: secret<i64>, pub_n: i64 }

fn render(xs) {
    let line = "";
    for x in xs {
        line = line + str(x);
    }
    for x in [1, 2] {
        line = line + ".";
    }
    return line;
}

fn main() {
    let p = S { k: 42, pub_n: 3 };
    let xs = [p];
    print(render(xs));
}
