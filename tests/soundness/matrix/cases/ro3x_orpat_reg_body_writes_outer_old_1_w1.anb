struct S { k: secret<i64>, pub_n: i64 }
fn f(q: S) {
    let x = 0;
    match 1 {
        1 | x => { x = q.k; }
    }
    return x;
}
fn main() {
    let p = S { k: 42, pub_n: 3 };
    print(f(p));
}
