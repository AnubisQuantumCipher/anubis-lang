// CHECK_EXPECT: FAIL (confidentiality boundary)
// CHECK_ERROR_CONTAINS: ANUBIS_SECRET_TO_PUBLIC
fn expose_seal(value: secret<i64>) -> i64 {
    return value;
}

fn main() {
    let seal: secret<i64> = 730031;
    print(expose_seal(seal));
}
