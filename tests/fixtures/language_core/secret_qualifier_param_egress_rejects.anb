// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// The secret<T> qualifier auto-labels a param as secret (the confidentiality dual of tainted<T>), so
// egressing it is exfiltration — no secret_source() call required.
fn f(x: secret<u64>) uses(net.send) {
    send("host", 80, x);
}
fn main() { }
