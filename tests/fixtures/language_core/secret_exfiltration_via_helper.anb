// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Interprocedural confidentiality flow: the secret is minted INSIDE a helper and returned. The
// compute_secret_fns fixpoint marks `get_key` as secret-returning, so expr_secret_source flags
// `send(get_key())` at the egress even though no `secret_source(..)` appears in `main` — the dual of
// the interprocedural return-taint summary that already catches `sink(get_secret())`.
fn get_key() { return secret_source("api_key"); }
fn main() uses(net.send) {
    send("host", 80, get_key());
}
