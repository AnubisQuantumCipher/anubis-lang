// EXPECT: PASS
// Egress-only scope: a secret into a param that reaches a LOCAL write (write_file) is NOT exfiltration
// — the summary uses is_egress_sink (net/shell), not is_sink, matching the direct egress check's scope.
fn store(x) uses(fs.write) { write_file("log", x); }
fn main() uses(fs.write) { store(secret_source("api_key")); }
