// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// DIRECT (intraprocedural) confidentiality leak through a value-position block with NESTED control-flow:
// a secret assigned to a local inside `if c { ... }` nested in an if-branch block, then yielded and sent.
// The value-position secret walker's Block arm formerly dropped the nested `if` (`_ => {}`), so `out`
// read clean. Now `walk_block_secret` may-merges the branch (mirroring the enforcing pass) → out secret.
fn main(c: bool) uses(net.send) {
    let out = if true {
        let r = 0;
        if c { r = secret_source("api_key"); }
        r
    } else {
        0
    };
    send("host", 80, out);
}
