// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_LETHAL_TRIFECTA
// A secret<T> param is leg-1 private data: with a distinct untrusted-input channel and an egress and
// NO value flow, the function forms the lethal trifecta (the no-flow steering case).
fn agent(k: secret<u64>) uses(net.send) {
    let steer = input();
    send("host", 80, "beacon");
}
fn main() { }
