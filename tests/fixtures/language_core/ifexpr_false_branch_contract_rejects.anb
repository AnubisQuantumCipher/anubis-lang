// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_ASSERTION_DISPROVED
// Task B soundness guard: threading the branch guard must NOT mask a genuinely-false branch contract. This
// function returns `a` on BOTH arms but claims `result >= b`; with a=3,b=5 the result is 3 < 5, so the
// contract is false and the solver must still produce a counterexample. The added path-condition premise
// (`a > b` / `!(a > b)`) is a TRUE fact and cannot turn this false contract into a PASS.
fn bad(a: u32, b: u32) -> u32
    requires(a < 1000)
    requires(b < 1000)
    ensures(result >= b)
{
    if a > b { a } else { a }
}
fn main() { let m = bad(3, 5); }
