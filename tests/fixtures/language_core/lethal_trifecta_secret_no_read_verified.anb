// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_LETHAL_TRIFECTA
// Phase-2 final slice, leg-1 confidentiality label: leg 1 (private-data access) is no longer only
// the coarse "a file was read" proxy — an explicit secret_source(..) value counts. `agent` reads NO
// file, but holds a secret_source value (leg 1), takes untrusted input from a distinct channel
// (leg 2), and communicates externally (leg 3) → the trifecta, caught via the confidentiality label.
@verified
fn agent() uses(net.send) {
    let sc = cap_acquire("net.send");
    let key = secret_source("api_key");
    let steer = input();
    cap_use(sc);
    send("host", 80, "beacon");
}

fn main() { }
