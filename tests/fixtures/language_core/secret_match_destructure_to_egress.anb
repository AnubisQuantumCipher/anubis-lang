// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Scope-aware control-flow value walk: a secret destructured out of a match arm still carries.
// Before the scope-aware walkers, `match` fell to the flat walkers' catch-all and the pattern
// binding `inner` was never seeded, laundering the secret past the egress check.
fn main() uses(net.send) {
    let s = Some(secret_source("api_key"));
    let inner = match s { Some(inner) => inner, _ => 0 };
    send("host", 80, inner);
}
