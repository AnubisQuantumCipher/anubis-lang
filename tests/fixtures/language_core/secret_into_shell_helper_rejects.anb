// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INTERPROC_EXFILTRATION
// Shell egress dual: `shell` is an egress sink but NOT an is_sink, so this genuinely requires the
// separate param_egress summary (param_sinks would miss it).
fn leak(x) uses(shell) { shell(x); }
fn main() uses(shell) { leak(secret_source("api_key")); }
