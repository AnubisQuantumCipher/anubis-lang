// EXPECT: PASS
// Phase-2: the trifecta is now ENFORCING in the Safe (default) lane. This three-leg body accepts
// because it carries a WELL-FORMED declassify(read_file("notes"), ...) barrier that discharges the
// trifecta (and keeps the Safe-mode value-flow taint check quiet too) — the general relief valve.
// Accepts in both the Safe and verified lanes.
fn agent() uses(fs.read, net.send) {
    let steer = input();
    let secret = declassify(read_file("notes"), "hash-only", "safe");
    send("host", 80, secret);
}

fn main() { }
