// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Interprocedural scope-aware walk: a helper that returns its parameter through a match arm
// summarizes {0} (param 0 reaches the return), so the secret is caught at the call boundary.
// Before, `expr_param_return_flow` treated the match as opaque and `pick` summarized {} — the
// secret was laundered across the call.
fn pick(x) { return match x { _ => x }; }
fn main() uses(net.send) {
    send("host", 80, pick(secret_source("api_key")));
}
