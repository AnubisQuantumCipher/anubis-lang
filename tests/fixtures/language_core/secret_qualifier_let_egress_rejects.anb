// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// A secret<T> let annotation auto-labels the binding: `let k: secret<u64> = 42` is secret without a
// secret_source() wrapper, so egressing it is exfiltration.
fn main() uses(net.send) {
    let k: secret<u64> = 42;
    send("host", 80, k);
}
