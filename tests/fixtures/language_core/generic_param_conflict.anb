// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_GENERIC_CONFLICT
// A type parameter used in two argument positions, bound to two incompatible
// concrete arguments at one call, is a monomorphization conflict the captured
// generics let the checker see. Dynamic `T` alone would accept this.
fn same<T>(a: T, b: T) -> T {
    return a;
}

fn main() {
    let z = same(1, "x");
    print(z);
}
