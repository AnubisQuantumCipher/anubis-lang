// EXPECT: PASS
// Accept-bias guard (clean value): the same value-position nested-control-flow shape carrying a constant
// accepts — the block never yields the caller's or a secret value.
fn main(c: bool) uses(net.send) {
    let out = if true {
        let r = 0;
        if c { r = 7; }
        r
    } else {
        0
    };
    send("host", 80, out);
}
