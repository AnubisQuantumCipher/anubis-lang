// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Egress is not only net.send: a shell-out (exec/system/shell/target_run) is the canonical agent
// exfiltration channel (`curl -d @secret ...`) and reaches the network just as directly. A secret
// passed to a shell egress without declassify() is exfiltration on the leg-3 shell surface.
fn main() uses(shell) {
    let s = secret_source("private_key");
    exec(s);
}
