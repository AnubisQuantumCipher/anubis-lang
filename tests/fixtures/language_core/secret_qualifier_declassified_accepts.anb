// EXPECT: PASS
// A well-formed declassify releases a secret<T> value before egress (the release hatch composes with
// the qualifier).
fn f(x: secret<u64>) uses(net.send) {
    send("host", 80, declassify(x, "hash-only", "reviewed"));
}
fn main() { }
