// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_ANALYSIS_LIMIT
// Regression: closure analysis must terminate without aborting.
fn main() { let g = |s| 0; g = |s| g(s); print(declassify(g(0), "test", "explicit release")); }
