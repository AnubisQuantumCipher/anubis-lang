// EXPECT: PASS
// Precision: a helper that egresses a literal (its param never reaches the egress) is not summarized,
// so passing a secret to it accepts.
fn ignore(x) uses(net.send) { send("host", 80, "literal"); }
fn main() uses(net.send) { ignore(secret_source("api_key")); }
