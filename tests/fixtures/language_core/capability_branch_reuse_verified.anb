// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_CAPABILITY_REUSE
// Phase-2 slice 2, verified-lane fail-closed: `c` is consumed on one branch only. Uncertainty
// resolves toward CONSUMED in verified mode (may-consume), so the later `cap_use(c)` is a reuse.
@verified
fn f(cond) {
    let c = cap_acquire("x");
    if cond {
        cap_use(c);
    }
    cap_use(c);
}

fn main() { }
