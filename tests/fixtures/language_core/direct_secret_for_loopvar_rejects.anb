// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// The `for` loop VARIABLE carries the collection's secrecy: iterating a secret binds a secret element,
// which is written to an outer local then yielded and sent. walk_block_secret seeds the loop var with the
// source label (mirroring the enforcing For arm), so this laundering-through-a-loop-var is caught.
fn main() uses(net.send) {
    let out = if true {
        let r = 0;
        for x in secret_source("api_key") { r = x; }
        r
    } else {
        0
    };
    send("host", 80, out);
}
