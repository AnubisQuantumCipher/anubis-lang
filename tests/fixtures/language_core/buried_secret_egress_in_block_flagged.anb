// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// A secret egress buried in a NON-tail block statement inside an if branch is enforced.
fn main() uses(net.send) {
    let k = secret_source("api_key");
    let f = true;
    let r = if f { let _z = send("h", 80, k); 0 } else { 0 };
}
