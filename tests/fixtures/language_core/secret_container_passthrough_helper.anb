// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Interprocedural composite laundering: a pass-through helper that wraps its argument in a container
// before returning it. The shared param_return_taint summary now walks the container (via
// expr_param_return_flow's aggregate arms), so `wrap` summarizes {0} — param 0 reaches the return —
// and the secret passed through it is caught at the egress. Before the fix the wrapped return
// summarized as {} (empty) and the secret was laundered across the call boundary.
fn wrap(x) { return [x]; }
fn main() uses(net.send) {
    send("host", 80, wrap(secret_source("api_key")));
}
