// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INTERPROC_EXFILTRATION
// Transitive: the param→egress summary propagates through the monotone fixpoint (a → b → egress).
fn b(y) uses(net.send) { send("host", 80, y); }
fn a(x) uses(net.send) { b(x); }
fn main() uses(net.send) { a(secret_source("api_key")); }
