// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_SECRET_EXFILTRATION
// Interprocedural secret<T> param→return: a helper that RETURNS its `secret<u64>` parameter is
// UNCONDITIONALLY secret-returning (the qualifier declares the param secret regardless of the argument),
// so `send(get(5))` exfiltrates a secret even though the argument `5` is clean. Closed by seeding
// secret<T> params in the return-secret summary (compute_secret_fns / fn_returns_secret).
fn get(x: secret<u64>) -> u64 { return x; }
fn main() uses(net.send) { send("host", 80, get(5)); }
