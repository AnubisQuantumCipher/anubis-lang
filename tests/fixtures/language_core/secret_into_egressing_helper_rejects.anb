// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INTERPROC_EXFILTRATION
// Confidentiality param→egress-sink dual (the interproc twin of ANUBIS_SECRET_EXFILTRATION): a helper
// whose formal reaches a network egress, called with a secret, leaks at the call boundary — even
// though the send is inside the helper. `compute_param_egress` summarizes `leak`'s param 0 as egressing.
fn leak(x) uses(net.send) { send("host", 80, x); }
fn main() uses(net.send) { leak(secret_source("api_key")); }
