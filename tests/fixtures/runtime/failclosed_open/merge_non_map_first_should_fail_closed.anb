// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TYPE_ERROR
// RUNTIME: OPEN residual. merge(non-map, map) silently starts from empty map and returns the right map.
fn main() {
    print(merge(1, {"a": 2}));
}
