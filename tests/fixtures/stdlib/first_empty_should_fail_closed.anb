// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_EMPTY_COLLECTION
// RUNTIME: exercise via `anubis run` (not `anubis check` — this is a runtime panic;
// `anubis check` on this file passes clean today, verified 2026-07-26).
// PENDING (fails today): first([]) returns Int(0) instead of failing closed
// (compiler/src/backends/run.rs anubis_first, ~line 2849). `0` is indistinguishable
// from a real element — a caller cannot tell "empty" from "first element is 0".
// See scratchpad/fleet_20260726/sonnet5c_stdlib_failclosed.md for the full audit
// and the proposed patch spec.
fn main() {
    let xs = [];
    print(first(xs));
}
