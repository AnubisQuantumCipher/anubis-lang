// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TYPE_ERROR
// RUNTIME: exercise via `anubis run` (not `anubis check`).
// Phase-5 M-Z SILENT_WRONG: pin anubis-106d335d0f64, sin("x") -> 0.0
fn main() {
    print(sin("x"));
}
