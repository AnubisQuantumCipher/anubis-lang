// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TYPE_ERROR
// RUNTIME: exercise via `anubis run` (not `anubis check`).
// PENDING (fails today): values([1,2]) returns [] (run.rs:anubis_values).
// Adjacent same-class as keys_non_map.
fn main() {
    print(values([1, 2]));
}
