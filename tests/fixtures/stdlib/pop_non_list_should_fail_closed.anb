// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TYPE_ERROR
// RUNTIME: exercise via `anubis run` (not `anubis check`).
// (run.rs:anubis_pop else-arm). Twin of pop_empty (wrong-type case).
fn main() {
    let mut s = "hi";
    print(pop(s));
    print(s);
}
