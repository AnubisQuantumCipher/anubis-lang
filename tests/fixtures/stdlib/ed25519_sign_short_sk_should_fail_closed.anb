// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_CRYPTO_ED25519_SK_LEN
// RUNTIME: exercise via `anubis run` (runtime panic, not a compile-time error).
// SEALED 2026-07-27: ed25519 secret key must be exactly 32 bytes.
fn main() {
    print(ed25519_sign("short-key", "msg"));
}
