// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TYPE_ERROR
// RUNTIME: exercise via `anubis run` (not `anubis check`).
// Phase-5 A–L SILENT_WRONG: on pin anubis-4909d6953c4c, `floor("x")` printed `0`
// via soft `as_f64` coerce. Distinct from DOC_OK IEEE NaN/inf on real floats.
fn main() {
    print(floor("x"));
}
