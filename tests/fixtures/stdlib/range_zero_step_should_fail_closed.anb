// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INVALID_ARGUMENT
// RUNTIME: exercise via `anubis run`.
// Phase-5 M-Z SILENT_WRONG: pin anubis-106d335d0f64, range(0, 5, 0) -> []
fn main() {
    print(range(0, 5, 0));
}
