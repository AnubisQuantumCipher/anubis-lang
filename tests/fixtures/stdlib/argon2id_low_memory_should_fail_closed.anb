// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_CRYPTO_ARGON2_M
// RUNTIME: exercise via `anubis run` (runtime panic, not a compile-time error).
// SEALED 2026-07-27: Argon2id m_kib must be >= 8.
fn main() {
    print(argon2id_hash("password", "saltsaltsalt1234", 4, 3, 1, 32));
}
