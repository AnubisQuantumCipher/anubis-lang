// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_CRYPTO_X25519_SK_LEN
// RUNTIME: exercise via `anubis run` (runtime panic, not a compile-time error).
// SEALED 2026-07-27: X25519 secret key must be exactly 32 bytes.
fn main() {
    print(x25519_public_key("short"));
}
