// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_INVALID_ARGUMENT
// RUNTIME: exercise via `anubis run`.
// Phase-5 M-Z SILENT_WRONG: pin anubis-106d335d0f64, times(-1, |i| i) -> []
fn main() {
    print(times(-1, |i| i));
}
