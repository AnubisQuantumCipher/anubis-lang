// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TYPE_ERROR
// RUNTIME: exercise via `anubis run` (not `anubis check` — this is a runtime panic;
// `anubis check` on this file still passes — this is a *runtime* fail-closed gate).
// SEALED 2026-07-27: runtime fails closed (stdlib fail-closed gate 32/32).
// patch, but never previously identified: `sort` is not routed through `anubis_iter`,
// so the `anubis_iter` fix does NOT close this one). Found independently by SONNET5-B
// during the stdlib patch reconciliation, 2026-07-26 — present in NEITHER the SONNET5-C
// nor the GROK-PTAH package; verified real (`sort(42)` -> `42`) against the current
// binary before being added here. See scratchpad/fleet_20260726/stdlib_merged_package.md.
fn main() {
    let r = sort(42);
    print(r);
}
