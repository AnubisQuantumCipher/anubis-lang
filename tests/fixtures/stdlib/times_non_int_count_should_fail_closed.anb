// EXPECT: FAIL
// ERROR_CONTAINS: ANUBIS_TYPE_ERROR
// RUNTIME: exercise via `anubis run`.
// Phase-5 M-Z SILENT_WRONG: pin anubis-106d335d0f64, times("2", |i| i) -> [0, 1]
fn main() {
    print(times("2", |i| i));
}
