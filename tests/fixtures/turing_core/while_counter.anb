// EXPECT: PASS
// FEATURE: turing-core (while loop + mutation)
fn main() {
    let i = 1;
    let total = 0;
    while i <= 10 {
        total = total + i;
        i = i + 1;
    }
    print(total);
}
