// EXPECT: PASS
// FEATURE: turing-core (loop / break / continue)
fn main() {
    let i = 0;
    let sum = 0;
    loop {
        if i >= 20 { break; }
        i = i + 1;
        if i % 2 != 0 { continue; }
        sum = sum + i;
    }
    print(sum);
}
