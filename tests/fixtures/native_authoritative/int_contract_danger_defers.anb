// EXPECT: PASS
// Phase-7 fragment-gate soundness demo (FAIL-CLOSED direction).
// Non-power-of-two division (`/ 3`) is NOT in the machine-checked native fragment, so under
// native-authoritative with z3 removed the lane DECLINES rather than certify on unproven blast.
// (Contrast int_contract_half_nonneg_proves.anb: `/ 2` under requires(x>=0) rewrites to proven bvlshr.)
pub fn third_nonneg(x: int) -> int
    requires(x >= 0)
    ensures(result >= 0)
{
    return x / 3;
}

fn main() {
    print(0);
}
